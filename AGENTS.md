# AGENTS.md — DmkWords 工作区规则

> 本文件随工作区自动注入每个会话。项目宪法是 `CLAUDE.md`（最高法律）；本文件只管"开工流程 + 协作纪律 + 记忆衔接"。

## 开工必读（每次会话第一步，不可跳过）

1. 读 `docs/项目交接-20260824.md`（最新交接卡：当前状态 / 待办 / 血泪教训）——**恢复口令即指向此文件**；`docs/项目交接-20260823.md` 已归档为历史参考
2. 读 `docs/LEDGER.md`（模块台账，唯一事实源：谁在什么状态）
3. 涉及资金/借阅/状态机的任务：先读 `CLAUDE.md` 对应铁律 + `docs/billing-circulation模式手册.md`
4. 用户报 bug 时：先读 `error_list/错误记忆库-全项目-*.md` §二根因模式库，对照防复发动作

## 协作纪律（用户铁律）

- 中文回复；代码/标识符/提交信息英文；直接给结论不奉承
- **诚实第一**：不编造、不美化、未验证就明说；测试失败如实报，禁止弱化断言/删测绕过
- **模块交付制**：每模块 = 后端 + 前端 + `bash scripts/gate.sh` exit 0（贴原始输出）+ 手册步骤（`docs/04`）+ LEDGER 更新；用户手动验收通过才算交付
- 完整性排查：先枚举全量再逐项比对，不准遗漏
- 最小改动；不顺便重构；需求模糊先问
- 不主动 commit/push；破坏性操作先说明影响等确认
- 环境：macOS/zsh，Node 用 pnpm，Python 用 uv（.venv 已就绪），MySQL 走 OrbStack 容器 dmkwords-mysql:3306
- 改完代码跑 `bash scripts/gate.sh` 验证；跑不了明说"未验证"
- 用脚本批量改文件后必须 grep 验证落盘（历史事故：脚本中途断言失败导致端点丢失两天）

## 记忆衔接

- 跨会话记忆 = `docs/项目交接-20260824.md` + `docs/LEDGER.md` + git log
- 重大节点（模块交付/事故修复/方向变更）要更新交接卡或 LEDGER，让下一个会话零成本恢复
- 手动验收状态在 LEDGER：MANUAL_TEST = 等用户测；ACCEPTED = 用户已确认
