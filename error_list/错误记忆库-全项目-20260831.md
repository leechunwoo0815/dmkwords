# 错误记忆库（全项目）· 增量版 2026-08-31（WM13 运营审核工作台 架构关整改 + 门禁验证）

> 承接：20260830（E-01~E-18）。本轮来源：WM13 五个批次开发收尾、架构关整改
> （commit 7574f37）、全量门禁验证（gate-runs/2026-08-31/gate-wm13.log）。
> 五要素：事实 / 被抓方式 / 根因 / 修复 / 是否复发。

---

## 一、本轮新增错误

### E-20260831-01 ruff ≥0.16 把 Markdown 内 python 代码块纳入 `format --check`（门禁级）
- 事实：WM13 架构关整改后重跑全量门禁，第 [1] 步 `ruff format --check .` 报
  `✗ 门禁失败`，未格式化对象是 **文档** `docs/任务包-20260831-WM13运营审核工作台.md:67:5`。
  该 .md 内嵌 1 个 ` ```python ` 块（`class AdminNotification(Base)` 字段表），
  ruff 要把 `id, scene` 单行拆成多行括号形式。
- 被抓方式：门禁日志第 [1] 步直接报 `✗ 门禁失败: ruff format --check .`；
  复验 `ruff format --check .` 得 `1 file would be reformatted, 249 files already formatted`，
  确认**未格式化范围仅此 1 个文件**，无其他连带。
- 根因：ruff **0.16.4** 起支持格式化 Markdown / docstring 内嵌的 Python 代码块。
  项目 `pyproject.toml` 的 `[tool.ruff]` 并**没有**任何 `.md` 相关配置——
  这是工具版本新能力带来的覆盖面扩张，**不是项目配置问题**，靠改配置规避属于掩耳盗铃。
- 修复（待执行）：`ruff format "docs/任务包-20260831-WM13运营审核工作台.md"` 后补 commit。
- **复发风险：高**（任务包/设计文档里嵌 python 代码块是本项目惯例，
  每个含代码块的 .md 都会炸门禁）。防复发：
  1. 写含 ` ```python ` 的文档后，**提交前必跑 `ruff format <该 .md>`**；
  2. 开发模型易犯的错：只跑 `ruff format <自己改的 .py>` 就宣称"ruff 全绿"，
     但门禁跑的是**全量 `ruff format --check .`**——**两者覆盖面不同，不得互相替代**；
  3. commit message 里写"ruff 全绿"前，必须确认跑过的是全量 check 而非指定文件。

### E-20260831-02 gate.sh 的 `run()` 失败不中断（读日志易误判）
- 事实：门禁日志同时出现 `✗ 门禁失败: ruff format --check .`（第 [1] 步）
  与后续 `[2] 单测 [3] BDD [4] 架构关` 正常执行。
- 根因：`scripts/gate.sh` 的 `run()` 实现为
  `if ! "$@"; then echo "✗ 门禁失败: $*"; FAILED=1; fi`——
  **只置位 `FAILED=1`，不 `exit`**，是有意设计（收集全部失败项后统一判退出码）。
- 影响：读日志时**看到后面步骤在跑 ≠ 前面步骤通过**，最终是否放行只看退出码。
- 防复发：**判门禁过没过，看退出码；看日志时要专门 grep `✗ 门禁失败`**，
  不能因为日志末尾在跑第 [7][8] 步就认为前面绿了。

### E-20260831-03 纯 docs 改动跑全量门禁（违宪法分级）+ 为迎合格式化改坏文档可读性
- 事实：commit 33cf5c5 为让任务包 .md 内嵌 python 块通过 `ruff format --check`，
  把专家的清晰字段清单拆成难读的多行括号形式，**并为此跑了 6 分钟全量门禁**
  （gate.sh full 含 stop/start dev，可能打断用户浏览器验收）。
- 被抓方式：用户质问"为什么改个 md 文档就要跑全量门禁？？？？违背了宪法分级"。
- 根因（两层）：
  ① **分级意识缺失**：宪法分级 A（前端 tsc）/B（后端 pytest）/C（models/migrations full gate）
  按"改动影响"定验证等级——纯 markdown 改动连 A 级都不是，历史裁定有"docs 可直推"先例；
  ② **优先级倒置**：面对"文档被工具扫描"的事实（ruff 0.16 确实扫 md，见 E-01），
  选择"迎合格式化改坏文档可读性"而不是"让标注匹配文档语义"。
- 修复（已执行）：① 恢复专家一行式（revert 33cf5c5 的格式改动）；
  ② 代码块 fence 从 ```python 改 ```text——伪代码示意本就不是 python，
  ruff 0.16 只扫 python 标注块，text 标注后一行式永久安全（251 files formatted 验证）；
  ③ 本 commit 自身**零门禁**（git diff 核对落盘即完成，践行分级）。
- **复发风险：中**。防复发：
  1. **先问"这次改动的分级是什么"再动手验证**——docs/注释/记忆库改动不做任何门禁动作；
  2. 设计文档里的伪代码示意一律 ```text 标注，不用 ```python（语义诚实 + 免被扫描）；
  3. 文档可读性 > 工具洁癖：formatter 输出与文档意图冲突时，改标注/配置，不改文档内容。

### E-20260831-04 useSyncExternalStore getSnapshot 返回新对象字面量 → 登录后全站白屏（P0，门禁全绿）
- 事实：WM13 批次三 `useTodoCounts.ts` 的 `getSnapshot()` 每次调用返回
  `{ counts: cache, failed: loadFailed }` **新对象字面量**，违反 useSyncExternalStore
  快照引用稳定合同 → React 用 Object.is 比较快照永"不相等" → 无限重渲染 →
  `Maximum update depth exceeded` → **React 卸载整棵树**。该 hook 挂在 Layout（徽标），
  Layout 包裹全部受保护路由 → **登录后全站白屏**（登录页正常，未挂 Layout）。
- 被抓方式：用户手动验收打开 :5173 一片空白（恰好是验收流程本要抓的环节）。
  专家用浏览器子代理登录复现：仪表盘/通知中心/会员管理全部 0 节点白屏。
- 根因（三层漏检，每层都"绿"）：
  ① tsc 类型层全绿——这是运行时语义 bug，类型完全正确；
  ② 开发模型无浏览器工具（简报如实声明"目视验收未做"）；
  ③ **专家深审漏检**：用 curl 验了后端 API 全通，却没把"前端能渲染"当独立验证项——
  专家明明有浏览器子代理能力，审后端不等于验前端。
- 修复（已执行，用户授权紧急破例直修）：模块级 `snapshot` 稳定引用，
  `fetchOnce` 数据落地时才换引用；tsc 0 错 + 浏览器端到端复测
  （登录/仪表盘待办卡/通知中心双 tab/会员管理全渲染，报错清零，徽标 2 与计数一致）。
- **复发风险：高**（React 18 全项目在用 useSyncExternalStore）。防复发：
  1. **前端批次交付的验证清单必须含"浏览器端到端目视"**——curl 验后端 ≠ 前端已验证，
  专家审查改前端的前端批次时必须派浏览器子代理走登录-渲染链（本次白屏从 WM13-3
  提交起一直存在，两轮审查（简报核验+专家深审）都没发现，直到用户打开浏览器）；
  2. useSyncExternalStore 使用三查：getSnapshot 返回缓存引用 / subscribe 幂等 /
  数据落地才 notify；
  3. 全站无 ErrorBoundary——任何组件崩溃即整树卸载白屏。改进项挂 docs/09
  （App 根部加 ErrorBoundary 显示"页面崩溃请联系管理员"而非空白）。

### E-20260901-01 资金面"读→校验→写"无行锁：ORM 覆盖写穿透应用层守卫（P1 批次根因）
- 事实：10 张 P1 卡共同根因——资金状态机入口主体行全是普通快照读，
  应用层 if 校验用旧值，双事务并发时后到者 UPDATE 按主键无条件覆盖（双台账/
  账实漂移/覆盖核销态/物理双借）。
- 被抓方式：外部审查全库并发 sweep（13 项抽验 13 实锤）；session_pair 并发测试实证。
- 根因：borrow 的 Child 行锁模板（circulation/service.py:102）从未推广到资金面
  （宪法铁律 5"资金四件套=状态机矩阵+行锁+幂等+Decimal"未落实行锁一环）。
- 修复：10 卡统一 with_for_update 锁定读 + 锁内查重/守卫；并发测试基建 session_pair。
- **复发风险：中**。防复发：
  1. 新资金路径入口主体行必带 with_for_update，校验在锁内（模板见
     circulation borrow / P1 各卡）；
  2. **锁定读必须警惕 identity map**：同事务先普通读过的对象，锁定读返回的
     行数据不刷新已加载实例——需要 `.populate_existing()`（P1-F4 探针实证，
     due 扫描→循环锁定读的守卫因此失效）；
  3. 同事务内子查询（聚合推进类）在 autoflush=False 下看不到未 flush 的
     本事务修改——**推进/复查前显式 `self.db.flush()`**（P1-F9 实证）。

### E-20260901-02 模式级 bug 只修单点未做同族 sweep：WM13-F4 修复漏掉会员页同款
- 事实：WM13-F4（tab 标题计数懒加载不一致）在 P0 批只修了通知中心一处；
  WM3 手动验收（2026-09-01）发现会员管理页订单 tab 同款 bug——
  `MemberManage.tsx:297-302` `if (tab !== "orders") return` 守卫导致默认 tab 下
  计数永远不拉，tab 标题显示「订单（0 · 待确认 0）」，点进去才正确。
- 被抓方式：用户手动验收（22 步动线第 15 步反馈）——三层自动化（tsc/单测/专家
  浏览器目视）全没拦住，因为目视只走了通知中心没走会员页 tab 标题。
- 根因：修"模式级"bug（同一错误模式散布多处的类族）时按单点出卡、按单点验收，
  没有全库 grep 同族；审查端复核修复时也未要求同族清单。
- 修复：WM3-A1（插修批次 1）去掉 tab 守卫；本条入库。
- **复发风险：中**。防复发：
  1. 任何"模式级"bug 修复卡必须附**同族 sweep 清单**（全库 rg 该模式的所有
     出现点，逐点标注 修/不修+理由）；
  2. 审查端验收此类卡时先核 sweep 清单完整性再核单点修法；
  3. 浏览器目视清单必须覆盖**所有承载该模式的页面**，不只报障页面。

## 一·补、P0/P1 修复闭环回写（2026-09-01，hash 由 git log 实取）

- **P0-F3 硬约束闭环**：项目记忆"Media endpoints must verify token type is 'admin'"
  已落实（commit `30ea058`：type+AdminUser active+token_generation 三道校验；
  `08b8936` 校验逻辑入 middleware 不污染域层；`64aab81` 抽 _load_active_admin 收敛）。
  红测试 test_wm2_media_auth.py 3 例（parent token 401 双通道/admin 200/gen bump 401）。
  **附注**：改密 API（reset_password/修改密码）当前不 bump token_generation（全库零
  bump 点）——gen 撤销"有校验无触发"，bump 接线属后续任务。
- **P0-F1/F2/F4/F5 全闭环**：commit dc58701/47ac550/d727381/d271355；红测试
  wm6_parent_guard 3 / wm6_login_guard 4 / wm4 +3；浏览器目视 ✅（专家补位）。
- **E-20260830 族 func.or_ 追加闭环**：billing 域最后一处 func.or_ 残留已修
  （commit `e099b33`，list_deposits keyword 过滤）；全 backend/domain 零代码残留。

---

## 二、工具链坑（审查/验证动作自身，防误判）

### T-20260831-01 zsh 会吞掉 `grep "A\|B"` 的 `\|`（致假阴性）
- 事实：`grep -n "WM13\|WM12" docs/LEDGER.md` 返回**空**，一度据此怀疑
  "开发模型声称更新了 LEDGER 但没更新"。单独 `grep -c "WM12"` 复验得 2、
  `grep -c "WM13"` 得 1——台账**是更新过的**，首次结论是**假阴性**。
- 根因：zsh 对未加引号/引号内的 `\|` 处理与预期不符（macOS BSD grep 亦不支持 `\|`，
  需用 `-E` 或 `grep -e A -e B`）。
- **复发风险：高**。防复发：
  1. 多关键词搜索用 `rg -e "A" -e "B"` 或分次单关键词，不用 `\|`；
  2. **"没搜到"不等于"不存在"——异常的空结果必须先复验再下结论**，
     否则会基于假阴性去指控开发模型，损害协作信任。

### T-20260831-02 rg 的 `-E` 是 `--encoding`，不是 grep 的扩展正则
- 事实：`rg -E "^(a|b)"` 报 `unknown encoding`。
- 防复发：rg 多模式用 `-e` 多次传入，或直接写裸正则（rg 默认即正则）。

### T-20260831-03 门禁进行中查库会读到中间态
- 事实：门禁 [2] 单测跑 pytest 时想查 `dictionary_words` 行数验证 340 万，
  但 conftest 正在操作数据库，此时查到的行数不可信。
- 防复发：**门禁/测试跑期间不做数据声明核验**，等跑完再查；
  代码层结论（有无 `.all()`、有无索引）不受影响，可先下。

---

## 三、本轮验证结论（供下任复用，避免重复劳动）

- **340 万词库不会被全量读取**（用户关切点，已闭环）：
  1. 代码层：`DictionaryWord` 全项目仅 2 个查询点，均安全——
     `reading/service.py:615` 查词为 `.filter(word == w, is_deleted == 0).first()`（精确+LIMIT 1）；
     `seeds/seed_dictionary.py:327` 导入去重为 `.filter(word.in_(names)).all()` 且仅 select 单列。
     **无任何裸 `.all()` 全表拉取**（对照 E-20260830-03 事故写法已不存在）。
  2. 索引层：`DictionaryWord.word` 声明 `index=True`，340 万行下精确查询走 B-tree（O(log n)）；
     `Vocabulary` 另有 `(child_id, word)` 联合唯一索引兜底生词本查重。
  3. 流程层：E-20260830-03 已入库且写明"大表去重必须用 EXISTS/IN，禁止 .all()"；
     E-20260829-09 更前置——conftest 清库时**排除 `dictionary_words`**（清了要重导 8 分钟）。

- **WM13 架构关整改（commit 7574f37）声明全部属实**（逐条核验）：
  `backend/domain/identity/wm10_withdrawal_service.py` 已拆出（14KB，20:33）；
  `wm10_service.py` 实测 **571 行**（commit 称 876→569，差 2 行属后续微调，非虚报）；
  `backend/domain/admin/todo_service.py` 已建（16KB）；
  `backend/common/admin_notifications.py` 仅 import 标准库 + 自家
  `admin_notification_models`，**零 `backend.domain.*` 依赖，"common 留纯写"成立**。

- **全量门禁实测（gate-runs/2026-08-31/gate-wm13.log）**：
  [2] 单测 **262 passed**（8m02s，真实 MySQL；较基线 226 增 36 个，即 WM13 专项）；
  [3] BDD 5 features / 12 scenarios / 28 steps 全过；
  [4] **架构关 PASS**（四件套齐全 / Router 零违规 / 行数达标 / 无 sqlite / import 白名单通过）；
  [6] 迁移一致性 PASS（`No new upgrade operations detected`）；
  **[1] lint 为唯一失败点**，修复 E-20260831-01 后即可全绿。
  注：开发模型自报的"51 passed"是其指定 8 个文件的专项子集，与全量 262 不矛盾，**不构成虚报**。
