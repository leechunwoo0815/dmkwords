# language: zh-CN
# FEAT-004 系统配置中心（完整开发流程 Demo 样板）
# 锚点: 宪法铁律6 数值全配置化
功能: 系统配置管理
  作为超级管理员
  我在后台调整价格与规则参数
  以便运营策略变化不需要发版

  背景:
    假如 超管已登录后台 且存在配置项 "borrow_limit" 当前值 30

  场景: 查看配置列表
    当 超管打开配置管理页面
    那么 全部业务配置按分类展示 当前值与默认值并列

  场景: 修改数值配置
    当 超管把 "borrow_limit" 从 30 改为 20 并填写变更原因
    那么 修改保存成功 新借阅校验立即按 20 执行
    而且 变更记录操作人、时间、旧值、新值、原因

  场景: 配置类型校验
    当 超管把数值型配置 "borrow_limit" 提交为字符串 "abc"
    那么 系统拒绝保存并提示类型错误

  场景: 非超管禁止修改配置
    假如 运营专员尝试修改任一配置
    那么 系统返回权限不足

  场景: 配置读取走缓存并失效更新
    当 配置被修改后
    那么 服务端缓存失效 下次读取返回新值

  场景: 已按旧规则进行的业务不受影响
    假如 小明已借 25 本书且尚未归还
    那么 借阅上限调整为 20 后 小明的在借书不受影响仅不能再新借
