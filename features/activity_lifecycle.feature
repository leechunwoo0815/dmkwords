# language: zh-CN
# T40（第四批·结构性根治）：活动域全景回归——报名三路径×状态机全组合
# 五轮插修（12→14）教训沉淀：单路径遗留代码打补丁的欠账，用场景集一次锁死。
# 此后活动域任何改动，跑本集即知回归——防"六轮七轮返工"。
# 不挂 @draft（新场景入正式集；BDD 67→~100 口径变更 LEDGER 留痕）。
功能: 活动域全景回归（报名三路径×状态机）
  作为系统
  我要把活动域全路径与状态机锁定为可执行规格
  以便任何改动破坏不变式时当场红

  场景: 路径 1——家长端报名付费活动全链
    假如 管理端发布付费活动 名额 5 费用 60 元 3 天后开始
    并且 已建档家长"全链家长"带孩子"全链孩"
    当 家长在小程序报名该活动
    那么 报名为待收款 生成入场券 且 名额占用 1
    当 馆员确认收款
    那么 报名转为已报名 且 订单状态为已支付

  场景: 路径 2——管理端造单代客报名全链（R7 通知同发）
    假如 管理端发布付费活动 名额 5 费用 60 元 3 天后开始
    并且 已建档家长"造单家长"带孩子"造单孩"
    当 馆员为孩子创建活动费用订单
    那么 报名同步生成且为待收款 生成入场券 且 名额占用 1
    并且 管理待办出现活动报名待确认通知
    当 馆员确认收款
    那么 报名转为已报名 且 待确认计数归零

  场景: 路径 3——退款后可重新报名（S4 口径）
    假如 已建档家长"重报家长"带孩子"重报孩"完成了付费活动报名与收款
    当 家长申请退款并走完审核执行链
    那么 报名状态为已退款 且 名额释放 且 订单状态为已退款
    当 家长再次报名
    那么 报名成功

  场景: 路径交叉——已有有效报名时防重复拦截（R8）
    假如 已建档家长"防重家长"带孩子"防重孩"已在某付费活动有有效报名
    当 馆员再为孩子创建同活动订单
    那么 系统拒绝并提示不可重复创建

  场景: 满员边界（家长报名与管理端造单双路径）
    假如 管理端发布付费活动 名额 1 费用 60 元 3 天后开始
    并且 已建档家长"占位家长"带孩子"占位孩"已报名占满名额
    并且 已建档家长"满员家长"带孩子"满员孩"
    当 满员孩的家长报名该活动
    那么 系统提示名额已满
    当 馆员为满员孩创建活动费用订单
    那么 系统拒绝且提示名额已满

  场景: 活动取消——付费已报名转退款待审
    假如 付费活动已有两名完成收款的已报名孩子
    当 馆员取消该活动
    那么 两名孩子报名均转为退款待审

  场景: 联动退会单灰态不进待处理（T20d 口径）
    假如 存在退款驱动的联动退会单和直接发起的退会单
    那么 联动单显示联动处理中 且 待处理计数只含直接退会单

  场景: 活动状态中文映射完备（防英文裸输出回归）
    当 扫描小程序活动域页面全部 status 输出点
    那么 每个输出点都有中文映射
    并且 映射覆盖后端六态全集
