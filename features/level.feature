# language: zh-CN
# FEAT-047/048 阅读等级 | 锚点: R-306, R-307
功能: 阅读等级
  作为孩子
  我每读完一百本书升一级直到 Z 级
  以便阅读生涯有长期目标

  背景:
    假如 等级阈值配置为 100 本 且孩子 "小明" 当前 B 级进度 99 本

  场景: 新学生初始等级
    那么 等级 "A" 进度 0 累计 0 本

  场景: 第 100 本书触发升级
    当 小明再通过一本新书的测验
    那么 等级升为 "C" 进度归零
    而且 升级通知发送给家长

  场景: 触发升级的书只计入旧等级
    那么 该书计入 B 级第 100 本 不重复计入 C 级

  场景: 同一本书不重复计级
    假如 小明已借过 3 次的某书首次通过测验
    那么 等级进度仅加 1

  场景: 等级只升不降
    假如 小明已达 D 级
    那么 任何情况都不会降到 C 级

  场景: Z 级封顶继续累计
    假如 小明已达 Z 级
    当 小明再通过 50 本书
    那么 等级保持 Z 展示累计有效阅读本数持续增长

  场景: 配置调低触发全量重算
    假如 管理员把阈值从 100 调为 50 且小明累计 300 本
    当 重算任务执行
    那么 小明等级按新阈值重算且只升不降
    而且 重算幂等可重复执行 结果可查询 操作留痕

  场景: 配置调高不降级
    假如 阈值从 100 调为 120 且小明 C 级进度 105
    那么 小明保持 C 级 进度封顶为 119

  场景: 禁止人工直接改等级
    当 管理员尝试直接修改小明的等级值
    那么 系统拒绝 仅允许基于流水的重算
