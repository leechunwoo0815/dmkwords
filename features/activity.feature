# language: zh-CN
@draft
# FEAT-057~061 线下活动 | 锚点: V1.1 §9, R-322, R-321
功能: 线下活动
  作为馆员
  我发布活动并管理报名签到退款
  以便每场活动组织有序

  背景:
    假如 馆员发布一场读书会 名额 20 费用 50 元 将于 3 天后开始

  场景: 每个孩子单独报名
    当 张女士为小明和小红同时报名
    那么 占用 2 个名额 生成 2 张电子入场券

  场景: 名额并发扣减不超卖
    假如 名额仅剩 1 个 且两位家长同时提交报名
    当 两笔报名并发处理
    那么 仅一笔成功 另一笔提示名额已满

  场景: 名额满即关闭报名
    假如 名额已报满
    当 家长尝试报名
    那么 系统提示已满不做候补排队

  场景: 待支付报名先占名额超时释放
    当 报名单创建进入待支付状态
    那么 名额被占用
    当 超时未支付
    那么 报名单过期 名额自动释放

  场景: 扫码签到
    当 馆员扫描张女士出示的入场券二维码
    那么 签到成功 记录时间与操作人

  场景: 已签到不退款
    假如 小明已签到该活动
    当 张女士申请活动退款
    那么 系统拒绝退款

  场景: 未签到未开始全额退
    当 张女士在活动开始前 3 小时申请退款
    那么 退款申请创建 全额 50 元

  场景: 开始前两小时关闭线上退款
    当 张女士在活动开始前 90 分钟尝试线上退款
    那么 系统提示请联系馆员线下处理

  场景: 门店取消活动逐单审核退款
    假如 门店取消该活动 且已有 15 个已支付未签到报名
    那么 15 笔订单进入 "refund_pending"
    而且 系统为每笔创建退款申请由超管逐单审核
    而且 拒绝的订单恢复 "registered"

  场景: 99 元每账号仅一次
    假如 张女士已支付过 99 元首场活动费且未全额退款
    当 尝试再次购买 99 元活动
    那么 系统拒绝

  场景: 99 元退款中不恢复资格
    假如 张女士的 99 元订单退款申请处理中
    那么 无法再次购买 99 元活动

  场景: 99 元全额退且未参加才恢复资格
    假如 99 元订单已全额退款 且张女士未实际参加活动
    那么 购买资格恢复

  # ---- 2026-09-20 增补（fix44 R7 / 专家 N3）：门店扫码枪连扫口径 ----
  # 来源：FEAT-059「2026-09-15 增补门店扫码枪口径」当时只有 PRD §9.2.1 描述、无场景。

  场景: 扫码枪连扫三张券各自签到成功
    假如 3 个孩子已报名该活动并持有入场券
    当 馆员用扫码枪连续扫描 3 张券（每张回车即提交）
    那么 3 张券状态依次变为 "checked_in"
    而且 每次成功提示都带孩子的姓名与活动名

  场景: 同一张券重复扫不重复签到
    假如 某孩子的入场券已经签到成功
    当 馆员再次扫描同一张券
    那么 系统提示"该券已签到"而不新增签到记录
    而且 该孩子的签到时间保持第一次的值

  场景: 扫码失败必须显式提示且不静默
    假如 馆员扫描一张不属于本活动的券
    当 提交扫描结果
    那么 系统给出明确的失败原因（越场券）
    而且 不产生签到记录

  场景: 连续扫描时输入框始终保持聚焦
    假如 馆员在管理端报名抽屉打开连扫面板
    当 依次扫描成功与失败的券各一张
    那么 无论成功还是失败，光标都回到扫描输入框
    而且 面板保留最近 20 条扫描记录

  场景: 出示入场券时屏幕常亮
    假如 家长从小程序「我的报名」点「出示签到码」进入入场券页
    那么 屏幕亮度拉至最高且保持常亮
    而且 离开该页后恢复原亮度设置

  # ---- 2026-09-20 客户口径变更：码从详情页移出（见 PRD §9.2.2 修订） ----

  场景: 活动详情页不再出现签到二维码
    假如 家长已报名某活动且未签到
    当 家长打开该活动详情页
    那么 详情页只显示报名状态，不显示二维码或券码文本
    而且 详情页给出「出示签到码」入口

  场景: 出示签到码入口只在已报名未签到时给出
    假如 家长对某活动的报名状态是待收款确认
    那么 「我的报名」列表不显示该活动的「出示签到码」按钮
    而且 直接进入入场券页会提示「入场券尚未生效」

  场景: 已签到的报名不再需要出示码
    假如 家长对某活动的报名状态是已签到
    那么 列表显示「已于 HH:MM 签到」且不显示「出示签到码」按钮

  场景: 馆员扫码成功后入场券页自动反馈并返回活动详情
    假如 家长正停在入场券页出示二维码 且 报名状态为已报名
    当 馆员扫码签到成功
    那么 入场券页自动弹出「签到成功」提示并震动
    而且 约 1.4 秒后自动返回该活动详情页 且 详情页状态条显示「已签到」
    而且 屏幕亮度还原、不再继续轮询

  场景: 事后回来查看已签到状态不打扰
    假如 家长在已签到之后重新打开入场券页
    那么 直接显示已签到信息 不弹成功提示、不自动跳转、不调亮屏幕

  场景: 报名被取消后旧入场券入口失效
    假如 家长已取消某活动的报名
    当 家长从历史入口进入该入场券页
    那么 提示「入场券已失效」并引导回活动列表

  # ---- 2026-09-20 客户需求：图文详情 + 往期活动回顾（B/C 批）----

  场景: 管理端编辑活动图文详情
    假如 管理端有 1 场未开始的活动
    当 运营添加两段文字和一张现场照片 并保存图文详情
    那么 图文按「段落/图片」顺序保存到该活动
    而且 配图落到独立的 activity_detail 目录（不与书封混放）

  场景: 活动开始后仍可补图文但核心字段不可改
    假如 某活动已开始 且 报名名额与费用已确定
    当 运营修改图文详情
    那么 图文保存成功（纯展示字段）
    当 运营修改名额或费用
    那么 系统拒绝（活动已开始，不可编辑）

  场景: 配图必须属于本活动且文件真实存在
    假如 运营在图文里引用另一个活动的配图
    那么 保存被拒并提示「不属于本活动」
    假如 引用的文件不存在
    那么 保存被拒并提示重新上传

  场景: 小程序详情页按顺序渲染图文
    假如 某活动的图文是「段落 → 图片 → 段落」
    当 家长打开该活动详情
    那么 页面按同样顺序渲染 且 点图片可全屏预览
    而且 图片 URL 带版本 token（换图后不显示旧图）

  场景: 往期活动出现在「往期回顾」
    假如 某活动开始时间已过去超过 1 天
    当 家长打开活动页的「往期回顾」
    那么 该活动出现在列表 且 显示参与人数
    而且 点进详情是只读回顾形态（不显示名额、无报名入口）

  场景: 过期但未结束的活动也进往期回顾
    假如 某活动已过期 但仍有人报名未签到（状态停在已发布）
    那么 它同样出现在「往期回顾」里
    而且 不会因为状态不是「已结束」而消失

  场景: 已取消的活动不进往期回顾
    假如 某活动已被门店取消
    那么 它不出现在「往期回顾」列表

  # ---- 2026-09-20 文档全量同步补录：口径细节 + 本轮两个报障的回归锁 ----
  # 说明：本文件仍是文件级 @draft（无 step 实现，不进 behave 计数）。
  # 要让某条进正式集，须新写 step 实现并去掉 @draft——届时 BDD 基线会从 8-30-103 变动，需在 LEDGER 留痕。

  场景: 入场券页同屏给出二维码、条形码与券码文本
    假如 家长进入「我的入场券」页 且 报名状态为已报名
    那么 页面同时呈现二维码（主）与 Code128 条形码（备）以及券码文本
    而且 券码文本可复制（扫码枪读不出来时手工兜底）

  场景: 待收款的报名既要占名额又不给任何入口
    假如 家长对某活动的报名状态是待收款确认
    那么 活动详情页状态条不出现「出示签到码」按钮
    而且 该报名仍占用 1 个名额（超时未确认收款才释放）

  场景: 入场券页对四种异常态各有兜底
    假如 家长进入某活动的入场券页 但 该报名已被取消
    那么 提示「入场券已失效」并引导回活动列表
    假如 该报名已签到
    那么 显示签到时间且不再提示"状态更新中"

  场景: 图文详情有规模上限
    假如 运营给一场活动添加第 61 个内容块
    那么 保存被拒（单场上限 60 块）
    假如 运营添加第 31 张配图
    那么 保存被拒（单场配图上限 30 张）
    假如 单段文字超过 1000 字 或 图注超过 60 字
    那么 保存被拒并提示字数上限

  场景: 图文块的顺序可以调整
    假如 某活动的图文顺序是「段落A → 图片B → 段落C」
    当 运营把段落C 上移一位
    那么 保存后的顺序是「段落A → 段落C → 图片B」
    而且 小程序详情页按调整后的顺序渲染

  场景: 活动取消后图文一并锁定
    假如 某活动已被门店取消
    当 运营尝试修改图文详情
    那么 保存被拒（已取消的活动不再对外展示，不再给改）

  场景: 配图文件名必须属于本活动且不许穿越目录
    假如 配图请求里的文件名带路径分隔符或上跳（../ 或子目录）
    那么 请求被拒（只接受 basename）
    假如 配图文件名的活动 id 前缀与 URL 里的活动不一致
    那么 请求被拒（跨活动取图封死）

  场景: 管理端预览配图走管理端自己的端点
    假如 管理端编辑器要预览图文配图
    那么 预览 URL 指向 admin 的 detail-image 端点并带 query token
    而且 用家长端端点会 401（两类 token 不通用，2026-09-20 实际踩过）

  场景: 关闭图片预览后不跳回页面顶部
    假如 家长在活动详情页滑到图文区并点开一张图
    当 家长关闭全屏预览
    那么 页面仍停在原来的滚动位置（不回到页首）
    而且 该页刷新是静默的（不出现骨架屏导致的整页重挂）

  场景: 往期取数以 24 小时为界
    假如 某活动的开始时点距现在 23 小时
    那么 它不出现在「往期回顾」
    假如 某活动的开始时点距现在超过 24 小时
    那么 它出现在「往期回顾」

  场景: 往期回顾是活动页的第三个页签
    假如 家长打开小程序活动页
    那么 三个页签依次是「可报名活动」「我的报名」「往期回顾」

  场景: 往期列表只给活动信息、不给参与孩子
    假如 家长打开「往期回顾」列表
    那么 返回只有活动的标题/封面/时间与参与人数聚合
    而且 不含任何孩子姓名、头像或可辨识信息

  场景: 往期列表的参与人数口径
    假如 某往期活动有 N 个已签到报名
    那么 列表显示的参与人数是 N
    假如 该活动没有任何签到记录
    那么 参与人数退回显示报名人数（口径待用户拍板）

  场景: 管理端活动列表可按「已结束」筛选
    假如 管理端在活动列表按状态筛选「已结束」
    那么 只列出已结束（finished）的活动

  场景: 活动结束后仍可补图文但核心字段不可改
    假如 某活动已结束
    当 运营修改图文详情
    那么 图文保存成功（活动后补回顾图文）
    当 运营修改开始时间、报名截止、名额或费用
    那么 系统拒绝（守卫只放行纯展示字段）
