# language: zh-CN
@draft
# FEAT-028 退会申请 | 锚点: R-311
功能: 退会申请
  作为家长
  我可以为孩子申请退会并按规则结算退款
  以便好聚好散且账目清楚

  背景:
    假如 孩子 "小明" 是正式会员 押金已缴 且家长 "张女士" 发起退会

  场景: 提交退会申请
    假如 小明无在借图书、无逾期、无未处理遗失损坏、无待结清赔偿
    当 张女士在小程序提交退会申请
    那么 申请创建 状态为 "applying"

  场景: 有在借图书时禁止提交
    假如 小明有 2 本在借图书
    当 张女士提交退会申请
    那么 系统拒绝并提示先归还图书

  场景: 有待结清赔偿时禁止提交
    假如 小明有待结清赔偿 70 元
    当 张女士提交退会申请
    那么 系统拒绝并提示先结清赔偿

  场景: 退会期间的操作锁定
    假如 退会申请处于进行中
    那么 小明禁止借书、预约、续借、开始新测验、发起转让、创建新会员订单、删除档案
    而且 允许归还图书与查看历史数据

  场景: 审核通过进入结算
    假如 退会申请通过审核
    那么 系统计算观察期费、年费、押金各自可退金额
    而且 生成退款单进入退款流程

  场景: 全部退款完成后退会完成
    假如 结算退款全部成功
    那么 小明会员状态变为 "withdrawn" 退会状态 "completed"
    而且 小明不可借书、听音频、做测验
    而且 历史词数、积分、等级保留

  场景: 用户撤销退会申请
    假如 退会申请处于 "applying"
    当 张女士撤销
    那么 申请状态变为 "cancelled" 锁定全部解除
