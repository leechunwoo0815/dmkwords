# language: zh-CN
# FEAT-031/032/033/034/035/037 到店借阅 | 锚点: V1.1 §5, 模式手册 P10/P11
功能: 到店借阅
  作为馆员
  我在操作台快速扫码完成借还
  以便孩子和家长的等待时间最短

  背景:
    假如 孩子 "小明" 是有效会员 押金已缴 当前在借 5 本

  场景: 正常借书
    当 馆员识别小明并扫图书 ISBN 条码借出
    那么 借阅记录创建 在借变为 6 本
    而且 系统自动创建该书的阅读记录

  场景: 借阅上限校验
    假如 小明已借满 30 本
    当 馆员尝试再借出 1 本
    那么 系统提示超过可借上限

  场景: 并发借书不超借
    假如 两个馆员同时为小明借同一本书的最后 1 个副本
    当 两笔借书操作并发提交
    那么 仅一笔成功 另一笔提示副本已被借出
    而且 在借数与副本状态保持一致

  场景: 逾期扣减可借上限
    假如 小明有 3 本逾期未还 已借 30 本
    那么 小明可借上限为 27 本 当前已不可再借
    当 小明归还 2 本逾期书
    那么 逾期剩 1 本 在借 28 本 可借 29 本 又可借 1 本

  场景: AR 超范围提示不拦截
    假如 小明当前 AR 3.2 某书 AR 4.8
    当 馆员借出该书
    那么 系统提示超出推荐范围由馆员确认继续
    而且 借书正常完成

  场景: 异常情况馆员人工放行
    假如 小明押金未缴 系统提示押金未缴
    当 馆员选择人工放行并填写原因"家长明日补缴"
    那么 借书完成 且放行操作人、时间、原因被记录

  场景: 未入会线下借书默认硬拦截
    假如 孩子 "小红" 未入会且系统开关未开启
    当 馆员尝试为小红借书
    那么 系统硬拦截 无法放行

  场景: 未入会临时借书开关开启后限借一本
    假如 管理员已开启未入会临时借书开关
    当 超管放行为小红借 1 本并留痕
    那么 借出成功 且系统生成入会跟进任务
    而且 72 小时内未归还或入会将提醒

  场景: 还书确认图书状态
    当 馆员扫条码还书
    那么 系统显示借阅人与是否逾期
    当 馆员确认图书"转维护"
    那么 副本状态变为维护中 不再可借

  场景: 续借一次七天
    假如 小明某书到期日为 7 月 1 日 未逾期
    当 张女士在小程序续借
    那么 新到期日为 7 月 8 日
    而且 该书续借机会已用尽 不可再次续借

  场景: 逾期图书不可续借
    假如 小明某书已逾期
    当 尝试自助续借
    那么 系统拒绝续借
    而且 馆员人工办理也不可突破一次续借上限
