# language: zh-CN
# FEAT-015/019 订单模型 | 锚点: R-312(订单类型), R-322
功能: 订单模型与状态机
  作为系统
  我需要统一的订单模型支撑五类收费
  以便资金流水清晰可审计

  场景大纲: 五类订单创建
    当 家长为孩子创建 "first_activity_fee" 订单 金额 99 元
    那么 订单创建成功 状态为 "pending_payment"
    而且 订单关联正确的孩子与家长

  例子:
    | 订单类型 | 金额 | 说明 |
    | first_activity_fee | 99 | 首场亲子活动 |
    | observation_fee | 500 | 观察期 |
    | formal_fee | 6000 | 年费 |
    | formal_fee | 5400 | 二孩年费 |
    | activity_fee | 任意 | 日常活动 |
    | deposit_supplement | 差额 | 押金补缴 |

  场景: 线上支付成功订单转已支付
    假如 存在状态为 "pending_payment" 的订单
    当 微信支付回调通知支付成功
    那么 订单状态变为 "paid"
    而且 支付时间与流水号被记录

  场景: 重复支付回调不重复入账
    假如 订单已因回调转为 "paid"
    当 同一订单的支付成功回调再次到达
    那么 订单状态保持 "paid"
    而且 不产生第二笔入账流水

  场景: 订单取消
    假如 存在待支付订单
    当 用户取消或系统判定超时
    那么 订单状态变为 "cancelled"
    而且 该订单占用的资源被释放

  场景: 僵尸单定时清理
    假如 存在超过配置时长仍未支付的订单
    当 清理任务运行
    那么 订单被标记取消
    而且 关联占用（如活动名额）被释放
    而且 任务可重复执行而不产生副作用

  场景: 非法状态流转被拦截
    假如 订单状态为 "cancelled"
    当 尝试将其置为 "paid"
    那么 状态变更被拒绝
