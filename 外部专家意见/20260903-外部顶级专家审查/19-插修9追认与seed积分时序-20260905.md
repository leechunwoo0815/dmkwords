# 插修 9 追认 + 19 号小批 — 正名归位 + seed 积分时序修复（2026-09-05）

> 定位：插修 9 目视返工族的专家追认归位（dev 挂账等本包）+ 复审新抓 seed bug
> 基线：HEAD=ac1511b（origin 同步，CI 33895770486 绿）；gate 375 passed / 82.53%；BDD 67
> 纪律：commit 前缀 `fix19`；本批三卡全小改，一轮 gate 收口

---

## T-A【追认归位】插修 9 目视返工族正名

**背景**：金卡口径（best_percent）+ 兜底真实到账两刀（da5b1ff/ae79db1）系 dev 越权自号"插修 10"直接施工，LEDGER 已自我正名（9bfcae5）待本包追认。

**动作（纯 docs）**：
1. LEDGER 行终态规范化：并入插修 9 行尾注"目视返工追认（专家 19 号 T-A）：R10 金卡百分制口径 + 兜底真实到账，da5b1ff/ae79db1/test_fix10_ 红测试 2 例追认入册"
2. 测试文件名 `test_fix10_quiz_badge_fields.py` → `test_fix9_r10_quiz_badge_fields.py`（git mv，import 同步；gate 375 基线只增不减）
3. **纪律留痕**（不另开教训，并入 E-20260905-01 附近）：返工也走任务包——目视反馈 → 专家定性出卡 → dev 施工。本次越权施工修复质量合格属幸运，流程缺口由本追认闭合

## T-B【复审实锤·bug】seed 积分 related_id 单次运行必扑空

**根因（专家 HTTP 活体复验 + 三查实锤）**：
- [seed_wm11_demo.py:297-326]：`db.add()` 三行积分（无 related_id）→ `db.execute(UPDATE ... WHERE related_id IS NULL)` 补挂
- SessionLocal `autoflush=False`：execute 时 pending add 未落库 → **UPDATE 单次运行必匹配 0 行** → commit 后三行 related_id=None
- 昨日活体验证=10 系 seed 双跑（第二轮 UPDATE 命中已有行）——单次运行的真实现状：get_quiz `points_added=0` → 兜底成绩单又显示 +0 积分（用户目视必撞）
- 库表现状实证：三行 `related_id=None`（2026-09-05 00:30 自动恢复 seed 创建）

**修法（一行级 + 删反模式段）**：
```python
# 创建三行时直接带 related_id=books[0].id（真链路入账即带，E-20260904-01 口径）
db.add(PointLedger(child_id=child.id, points=5, reason_type="quiz_first_pass",
                   related_id=books[0].id, detail="演示：首次通过测验"))
# （三行同款）——然后整段删除 db.execute(update(...)) 补丁段（反模式：先插后补
# 在 autoflush=False 下必扑空，教训 41 同族第三犯）
```

**红测试**：清 PointLedger → 单次跑 `_ensure_demo_growth` → 断言三行 `related_id == 演示书 id`（当前 None = RED）→ `get_quiz points_added == 10`。

**顺带**：现存三行 None 数据修复——seed 幂等段内加"存量 None 行补挂"一次性 UPDATE（此时行已在库，execute 能命中；跑一次后全体有值，此后该段天然空转 0 行，保留无害）或直接删演示积分三行让重建——dev 择一，简报声明。

## T-C【微瑕清偿】book1/2 QuizAttempt 同秒

**现状**：R7 seed 三书在循环内创建，book1/book2 的 submitted_at 同秒（11:52:04）——"禁同秒"条款只做了跨表错开，同表循环未递增。

**修法**：循环内 `offset_hours` 逐书递增（book1=11:52 / book2=14:37 / book3 无 attempt），一行级。

---

## 交付要求

1. `GATE_LOG_NAME=fix19 bash scripts/gate.sh full` → 375 基线只增 / BDD 67 不降 / 9 步全绿
2. 简报：T-A 追认 diff / T-B 红测试 RED→GREEN + 存量数据处置声明 / T-C 时间戳对账贴文
3. LEDGER：插修 9 行终态（含 T-A 追认）+ 19 号行（MANUAL_TEST）
4. 用户目视（1 项）：Brown Bear 金卡成绩单 **+120 词 / +10 积分**（上次 +0 的闭环）
5. push：与后续第四批同推或本批即推（用户定）

## 明确不做

- 教训 41 第三犯单独入库（T-B 卡内留痕即可，不扩条目）
- 其余一切（第四批合成等 dev 挂账照旧）
