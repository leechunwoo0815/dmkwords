# 插修 15 任务包 — 第四批目视返工（2026-09-08）

> 来源：第四批目视 4 项（专家代码级+浏览器自动化诊断实锤——白屏为浏览器复现所得）
> 基线：本地 16+1 刀攒批；gate 407 passed / 82.88%；BDD 103
> commit 前缀 `fix25`；验收 `GATE_LOG_NAME=fix25 bash scripts/gate.sh full` + docs/19 收尾

---

## 诊断摘要（4 项）

| # | 根因（实锤） |
|---|---|
| 1 | **测试数据缺口**：seed 双演示活动无逾期书/无押金孩——T41 六项复核与押金动线无法目视。非代码 bug，数据问题 |
| 2 | **C-13 提前命中**：音频端点（[reading/miniapp_router.py:175](file:///Users/litianyu/cc-projects/dmkwords/backend/domain/reading/miniapp_router.py)）只验家长 token 无会员守卫——已退会孩子可听。原挂 WM12，用户实测撞上→提前修 |
| 3 | **白屏（浏览器自动化实锤）**：`form.setFieldsValue({ start_at: new Date(...) })` 传 JS Date 给 antd5 DatePicker（需 dayjs）→ Form 渲染校验路径 `date4.isValid is not a function` → **React 整树崩**（无 ErrorBoundary）。轮播空非 bug：`cover_path.isnot(None)` 过滤正确，但 seed 活动无封面 → 轮播空。**Seed 缺封面图** |
| 4 | 澄清：F-L19 改传 id 是内部实现变更（books→detail 页跳转不再塞整书对象），外部行为不变——"详情能打开"就是回归通过的意思，无需测 |

---

## 施工顺序：R1 → R2 → R3 → R4 → R5

### R1【P0·#3 白屏】dayjs 修复 + ErrorBoundary

**修法**：
1. `openEdit`：`new Date(...)` → `dayjs(...)`（start_at/enroll_deadline 两处，import dayjs——项目已有依赖）
2. `onCreate`/`onEditSave`：`v.start_at.toISOString()` 若依赖 Date 方法，换 `dayjs` 同名 API（dayjs 兼容）
3. **ErrorBoundary**：admin-web 根部（App.tsx/Layout 外层）加一层 class 组件 ErrorBoundary——捕获渲染异常显示"页面出错了，请刷新"兜底 UI + console.error 原始堆栈（**E-20260831-04 教训的欠账**：白屏事件三层漏检后裁定"前端批次必须目视"，但兜底层从未做——本卡补上，从此单点渲染错误≠全站白屏）

**验证**：浏览器实测编辑弹窗正常弹出+预填+保存；tsc 0 错。

### R2【P0·#2 C-13 提前】音频会员门禁

**修法**（T46 核对表里已列方向，提前实施）：
1. `backend/domain/identity/guards.py` 加 `AUDIO` 动作常量 + 矩阵条目：未缴费**禁**/在册全馆**允**/过期**仅手头在借书允**（复用 report_progress 现有的在借查询逻辑）/退会**禁**——与 R-313 矩阵的"在线音频"行严格一致
2. 两个端点接守卫：`GET /books/{id}/audio`（L175）+ `report_progress`（L94）——后者已有部分逻辑（is_active_member/过期查在借），**统一收口到 guards**（删散落判定，防两处漂移）
3. 音频 URL 生成侧（book_audio_url 返回处）不拦（显示层），拦截在流端点（真正防直链）
4. 401/403 语义：未授权返回 403（ForbiddenError），miniapp request.js 已有 403 toast 分流

**红测试**：退会孩请求 audio → 403（当前 200 = RED）；未缴费 → 403；在册 → 200；过期+在借该书 → 200；过期+非在借 → 403。report_progress 同款五断言。

### R3【#1 测试数据】seed 补逾期书 + 押金孩演示数据

**修法**（seed_wm11_demo 幂等追加，走真实链路——E-20260904-01 铁律）：
1. **逾期书**：给演示孩（formal 那位）造 1 本真借阅→已逾期（BorrowRecord status=overdue，due_at 过去 3 天；BookCopy borrowed）——注意用 title 定位演示书，不占 id 前位（19 号 T-B 教训）
2. **押金孩**：造 1 个已缴押金演示孩（Deposit paid，走 R3 押金单链路或 DepositService 直造+ledger 流水同源——**dev 择真实链路实现**，简报声明）
3. 这两份数据专门服务 T41 目视（六项复核拦截弹窗）与押金动线目视

**验证**：seed 重跑 → 逾期书 1（演示孩名下）+ 押金孩 1；四表/五表对账贴简报。

### R4【#3 后半】seed 双活动补封面

**修法**：seed 的两个演示活动各生成一张 Pillow 绘本风封面（seed_demo_library 同款
PALETTES 逻辑，标题上字）→ 轮播 `cover_path.isnot(None)` 命中 → 首页轮播有真数据。
**封面生成幂等**（存在跳过）。

**验证**：seed 重跑 → `GET /api/miniapp/activities/carousel` 返回 2 条；小程序首页轮播真机目视。

### R5【#3 顺带】编辑弹窗与新增一致性核对

用户原话"我要的是像新增一样弹窗编辑"——R1 修完后编辑与新增**共用同一 Modal**（代码已如此：editTarget 三元切换 title/onOk）——R1 修复后自然达成。本卡仅目视确认，无代码改动。若目视发现弹窗内差异（字段缺失/布局不一致）按反馈出小卡。

---

## 交付要求

1. `GATE_LOG_NAME=fix25 bash scripts/gate.sh full` → 407 基线只增 / BDD 103 不降 / docs/19 收尾
2. 简报：逐卡四件套 + R2 矩阵断言清单 + R3 数据对账 + dayjs 修法说明
3. **目视清单（4 项）**：①管理端点编辑 → 弹窗正常弹出预填保存（不再白屏）②小程序首页轮播有演示活动封面自动播放 ③已退会孩点播放 → 403 提示拦截（不播）④用 R3 造的逾期书孩走退款执行 → 六项复核拦截弹窗
