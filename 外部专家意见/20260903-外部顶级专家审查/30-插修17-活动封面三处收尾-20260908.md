# 插修 17 任务包 — 活动封面三处收尾（2026-09-08）

> 来源：插修 16 目视 3 项新反馈（专家代码级实锤）
> 基线：本地攒批 25 刀；gate 410 passed / 82.95%
> commit 前缀 `fix27`；前端为主，A 级 tsc + 目视即可（无后端改动）

---

## 诊断（3 项）

| # | 根因（实锤） |
|---|---|
| A | [ActivityManage.tsx:188](file:///Users/litianyu/cc-projects/dmkwords/admin-web/src/pages/ActivityManage.tsx) 发布按钮直接 `setCreateOpen(true)`——**不 reset form/editTarget**：编辑过一次后点"发布活动"，Modal 是编辑模式（title"编辑活动"）+ 表单残留上个活动的值 |
| B | activity-detail 页 **hero 是纯色渐变头，根本没渲染 cover_url**——用户看到的"统一封面"就是这个纯色块。轮播/列表有图、详情页无图=断链第三处 |
| C | 轮播 `.activity-carousel` margin 20rpx 24rpx + 页面 padding 24rpx = 双重留白；首页其他组件贴 padding 24rpx 边——轮播应与其他组件同宽对齐 |

---

## 施工卡

### RA【#A】发布按钮 reset

```tsx
<Button type="primary" onClick={() => { form.resetFields(); setEditTarget(null); setEditCover(null); setCreateOpen(true); }}>发布活动</Button>
```
（Modal 的 title/onOk 由 editTarget 三元切换——置 null 即回创建模式；表单 initialValues 兜底生效）

### RB【#B】详情页 hero 接封面

hero 区改：`cover_url` 有值 → 背景图（image 铺满 hero + 深色渐变遮罩保文字可读，或 img 标签 + absolute 蒙层——与轮播图同款 aspectFill）；无值 → 现状纯色渐变（降级自然）。cover_url 走 media.fullUrl 拼 token（R2 同款）。**sweep**：活动域所有 cover_url 消费点（轮播/列表/详情）三处全走 fullUrl——本轮收口后封面链完整。

### RC【#C】轮播对齐

`.activity-carousel` 的 `margin: 20rpx 24rpx` → `margin: 20rpx 0`（页面已有 padding 24rpx，组件不再自加）——与首页其他卡片组件同宽。真机目视对齐。

---

## 交付要求

1. tsc 0 错（纯前端零后端，A 级即可）+ **浏览器实测发布按钮**（编辑后再发布=空表单创建模式）
2. 目视 3 项：①编辑过活动后点"发布活动"→ 弹窗是空白创建模式（title"发布活动"）②活动详情页 hero 显示上传的封面图（无封面活动仍纯色）③轮播与首页组件左右对齐无多余留白
