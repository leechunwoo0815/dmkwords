# DmkWords 修复闭环审查报告 — 2026-08-24

> ⚠️ **历史归档**：本文件为带日期的时间快照，仅作过程留痕；当前状态一律以 `docs/项目交接-20260824.md` + `docs/LEDGER.md` 为准。

> 审查依据：`docs/10-外部专家复审报告-20260823.md` + `docs/09-补充任务清单.md`  
> 审查对象：开发模型在 `6e84fc8` 推送的 P0/P1 修复包及证据附件 `docs/12`  
> 审查方式：静态代码复核 + 本地证据校验 + 独立复跑关键测试分组  
> 审查结论：**修复主体完整，门禁通过，未发现重大回归；仍有 1 处测试泄漏模式建议本轮收尾。**

---

## 1. 验证摘要

| 检查项 | 开发简报声称 | 独立验证结果 |
|---|---|---|
| 全量门禁 | 131 passed / 76.96% / exit 0 | ✅ 复核 `gate-runs/2026-08-24/gate-full-p1.log`：内容一致 |
| P0 四文件回归 | 26/26 绿 | ✅ 独立复跑：29 passed（含 P1 三文件） |
| P1 三文件 | 3/3 绿 | ✅ 同上 |
| 前端 tsc | exit 0 | ✅ 独立执行 `pnpm exec tsc --noEmit`：0 errors |
| MDL 挂起修复 | faulthandler 定位 + `_db()` 改 `with` | ✅ 逻辑正确：`test_wm10_state_machines.py` 7 处已改 `with _db() as db:` |
| 提交记录 | 5 个 commit 已推送 | ✅ `git log --oneline` 核对到 `6e84fc8` |

**关键测试独立复跑命令与结果：**

```bash
.venv/bin/python -m pytest \
  tests/unit/test_r313_guards.py \
  tests/unit/test_wm10_state_machines.py \
  tests/unit/test_wm10_transfer_refund.py \
  tests/unit/test_d1_member_gates.py \
  tests/unit/test_p1_crud.py -v --tb=short
# 结果：29 passed, 1 warning in 36.46s
```

原始输出：`gate-runs/2026-08-24/review-regression-45.log`

---

## 2. 逐项修复审查

### 2.1 P0 业务正确性缺陷（docs/09 / docs/10）

| 编号 | 原审查结论 | 修复证据 | 状态 |
|---|---|---|---|
| **D1** | 会员过期零拦截 | `Child.is_active_member` 读时判定 formal 到期；`is_expired_member` 兜底；涟漪修复借/约/续/音频/榜单/二孩折扣 | ✅ 已修复 |
| **C13** | 观察期→待评估链缺失 | `ChildService.mark_pending_evaluation` / `evaluate_approve` + 管理端端点 + `MemberManage.tsx` 按钮 | ✅ 已修复 |
| **C15** | 未入会临时借书未限 1 本/72h | `CirculationService.borrow` 在借计数硬拒 + due_at=72h | ✅ 已修复 |
| **C16** | AR 超范围提示缺失 | `borrow` 返回 warnings + `ar_warning_range` 配置 | ✅ 已修复 |
| **C17** | 过期会员借书与未入会共用硬拦截 | `borrow` 三路分流：退会禁 / 未入会开关+放行 / 过期软提示可放行 | ✅ 已修复 |
| **R-313** | 小程序状态守卫批量缺失 | 新增 `backend/domain/identity/guards.py` 统一守卫；接入 reading/growth/billing miniapp 路由；`test_r313_guards.py` 4 用例 | ✅ 已修复 |
| **WM10-01** | 退会状态机严重缩水 | `WithdrawalRequest` 6 态 + source 列；`WithdrawalService.review` 结算生成退款单 | ✅ 已修复 |
| **WM10-02** | 退款状态机缩水 + 缺 `Order.refund_status` | `RefundRequest` 7 态；`Order.refund_status`；迁移 `e1f2a8c4b6d3` | ✅ 已修复 |
| **WM10-04** | 权益转让 12 步事务缩水 | `transfer_service.py` 审核通过生成 `WithdrawalRequest` + 年费不退审计 + 到期日继承 | ✅ 已修复 |
| **WM10-06** | 会员费退款未联动退会 | `RefundService.apply` 创建联动 `WithdrawalRequest`；`execute` 成功后 `_complete_refund_withdrawal` | ✅ 已修复 |
| **WM10-07** | 退会前置条件仅 4 项 | `_preconditions` 已补：进行中转让 / 进行中会员费退款，共 7 项 | ✅ 已修复 |
| **WM3-03** | 99 元资格未按退款状态判定 | `OrderService.create` 已按 `refund_status != refunded` 拦截 | ✅ 已修复 |
| **C20** | 退会孩子可见会员专属活动 | `ActivityService.list_upcoming` 对 withdrawn 过滤 `member_only` | ✅ 已修复 |

### 2.2 P1 手册步骤/入口缺失

| 编号 | 原审查结论 | 修复证据 | 状态 |
|---|---|---|---|
| **C1** | 家长端周报月报页面与入口缺失 | `miniapp/pages/member-pkg/report/` 四件套 + `app.json` 注册 + 「我的」入口 `goReport` | ✅ 已修复 |
| **C7** | 管理端详情页无音频试听 | `BookDetail.tsx:136-139` 增加 `<audio controls>` + `/api/admin/uploads/{path}` 流 | ✅ 已修复 |
| **C8** | Excel 导入模板下载缺失 | `GET /api/admin/books/import-template` + 图书管理页下载按钮 + 防脏数据设计（示例放说明 sheet） | ✅ 已修复 |
| **C12** | 员工管理页缺失 | `StaffService` CRUD + `Staff.tsx` + `Layout.tsx` 菜单 + `App.tsx` 路由 + `test_p1_crud.py` | ✅ 已修复 |
| **C19** | 孩子 AR 值维护入口缺失 | `PUT /api/admin/members/children/{id}` + `MemberManage.tsx` 编辑按钮 + 只升不降校验 | ✅ 已修复 |

### 2.3 原报告 R-313 矩阵收口确认

| 功能 | 修复前实然 | 修复后实然 | 守卫位置 |
|---|---|---|---|
| 查词 | 无守卫 | 未缴费禁 / 过期仅音频场景 / 退会禁 | `reading/miniapp_router.py:310` |
| 生词本看 | 无守卫 | 未缴费禁 / 过期/退会只读 | `reading/miniapp_router.py:318` |
| 生词本写 | 无守卫 | 未缴费禁 / 过期/退会禁 | `reading/miniapp_router.py:326` |
| 收藏夹写 | 无守卫 | 退会禁（R-314 未缴费/过期可收藏） | `reading/miniapp_router.py:345,353` |
| Quiz | 无守卫 | 未缴费/退会禁 / 过期已解锁可完成 | `growth/miniapp_router.py:43,51` |
| 护照/成长汇总 | 无守卫 | 未缴费禁 / 过期/退会只读 | `growth/miniapp_router.py:59,85` |
| 积分明细 | 无守卫 | 未缴费禁 / 过期/退会只读 | `growth/miniapp_router.py:67` |
| 周报/月报 | 无守卫 | 未缴费禁 / 过期/退会只读 | `growth/miniapp_router.py:94` |
| 押金补缴 | 无入口 | 在册/过期可用 / 退会禁 | `billing/miniapp_router.py:76` |
| 活动列表会员专属 | 退会可见 | 退会不可见 | `activity/service.py:53-54` |
| 在线音频 | 已正确 | 已正确 | `reading/service.py:74-91` |
| 借阅操作台 | 已正确 | 已正确 | `circulation/service.py:142-180` |

---

## 3. 新增回归与风险排查

### 3.1 确认无重大业务回归

- **全量门禁通过**：lint / 单测 / BDD / 架构 / 反假绿 / 迁移一致 / 覆盖率均 exit 0。
- **架构关合规**：`transfer_service.py` 拆分后 `wm10_service.py` 缩至 786 行，无 god file。
- **迁移一致**：`alembic/versions/e1f2a8c4b6d3_wm10_state_machines.py` 正确添加 `refund_status`/`withdraw_reason`/`source`/`withdrawal_id`，并处理历史状态迁移。
- **前端类型同步**：`admin-web/src/api/schema.d.ts` 已包含 `StaffCreateRequest` 等新类型；`LoginRequest` 键名已适配。
- **Decimal/配置化**：金额字段仍用 `Numeric(10,2)`/`Decimal`；新增 `ar_warning_range` 配置已种子入库。

### 3.2 遗留风险与建议（本轮建议收尾）

#### ⚠️ 风险 1：测试 session 泄漏模式未完全根除（建议本轮修）

**位置**：`tests/unit/test_wm10_transfer_refund.py` 共 7 处：
- `L138-142`
- `L250-266`
- `L280-291`
- `L370-378`
- `L444-448`
- `L455-459`
- `L490-493`

**问题**：仍使用裸 `db = get_session()` + `db.close()`。与 `test_wm10_state_machines.py` 修复前同款。当前断言均落在 happy path，所以能关闭；一旦未来有人加入失败断言，会复现 MDL 挂起。

**建议**：统一改为 `with _db() as db:`（同已修复文件）。改动小、风险低、收益确定。

#### ℹ️ 建议 2：前端 tsc 未纳入 gate.sh

当前 `scripts/gate.sh` 仅覆盖后端。P1 修复涉及大量 admin-web 改动，建议增加一步 `cd admin-web && pnpm exec tsc --noEmit`，避免未来前端类型漂移。

#### ℹ️ 建议 3：RefundCenter 退会 tab 的 `pending_settle/refunding` 状态操作列提示

当前显示"—"，语义正确（退款操作在退款 tab 驱动），但可优化：在状态列或审核备注区增加提示"待关联退款单执行"，减少馆员困惑。

---

## 4. 结论与建议

### 4.1 总体结论

**修复闭环审查通过。** 开发模型已按 `docs/10` 和 `docs/09` 完成全部 P0/P1 修复，证据链完整，门禁通过，未发现重大业务回归。建议在修复风险 1（测试 session 泄漏）后，即可进入用户手动验收阶段。

### 4.2 下一步建议

1. **本轮收尾**：修复 `test_wm10_transfer_refund.py` 7 处 `_db()` 泄漏，重跑全量门禁。
2. **可选增强**：将 `pnpm exec tsc --noEmit` 加入 `scripts/gate.sh`。
3. **手动验收**：按 `docs/04-模块交付顺序与手动验收手册.md` 逐模块 WM1→WM10 验收，重点抽查：
   - WM3：过期会员借书软提示、未入会限 1 本 72h
   - WM10：退款中心 7 态 / 退会中心 6 态 / 权益转让审核
   - WM1：员工管理 CRUD 与权限边界
   - WM2：音频试听与 Excel 模板下载
   - WM8：小程序周报月报入口与图片加载

---

*审查人：GLM（外部专家/只审不改）*  
*日期：2026-08-24*
