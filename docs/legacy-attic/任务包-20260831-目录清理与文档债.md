# 任务包 20260831：目录清理与文档债（用户已批准，交开发模型执行）

> 出包人：外部审查专家（继任）。用户裁决记录（2026-08-31）：清理批准、专家书已 commit、
> 文档债合并本任务包。
> **状态：DONE（2026-08-31 执行完成，单刀 commit，push 前请示用户）**
> 验证等级：**C 级（含 git 操作，逐项贴命令输出）**。本包全部为文档/目录操作，
> **不触碰业务代码，无需跑 pytest**；git 操作后逐项 `git status` 核对。

## 任务 1：删除无引用旧截图（止血遗留）

- **位置**：`外部专家意见/acbdefd3-d938-4796-8bed-bb432e15474e.png`（242KB）
- **根因**：v6 批次止血任务包遗留空文件，全库 grep 仅专家交接书提及（提及其本身应被删）
- **修法**：`git rm "外部专家意见/acbdefd3-d938-4796-8bed-bb432e15474e.png"`
- **验证**：`git status` 显示 D 该文件；`ls 外部专家意见/` 仅剩 DmkWords-当前阶段风险分析报告.md

## 任务 2：作废方案截图归档（不删除，保决策痕迹）

- **位置**：`docs/miniapp-paint-v3/`（full-report.html + 23 截图）、`docs/miniapp-paint-v4/`（部分截图）
- **根因**：v3/v4 已被 v5 方案取代；专家裁定走保守路线移 legacy-attic 而非删除
  （与"打新不移动旧"tag 纪律同理：历史痕迹只封存不销毁）
- **修法**：
  ```bash
  git mv docs/miniapp-paint-v3 docs/legacy-attic/miniapp-paint-v3
  git mv docs/miniapp-paint-v4 docs/legacy-attic/miniapp-paint-v4
  ```
- **注意引用链**：`docs/miniapp-paint-handover-20260830.md` L348-350 引用旧路径——
  本包任务 3 该文档自身也归档，随任务 3 一并处理，不单独改
- **验证**：`ls docs/ | grep paint` 无 v3/v4；`ls docs/legacy-attic/` 出现两个新目录；
  截图文件数不变（`find docs/legacy-attic/miniapp-paint-v3 -type f | wc -l`，移前移后一致）

## 任务 3：两个过程报告归档（redesign 报告保留！）

- **归档对象**（移 legacy-attic）：
  1. `docs/miniapp-paint-handover-20260830.md`（美化模型交接，v3/v4 时代产物，与任务 2 互引，一起入 attic 上下文完整）
  2. `docs/backend-change-report-20260830.md`（后端改动过程报告，内容已沉淀入交接卡 §2 与错误记忆库）
- **保留对象（禁止移动）**：`docs/miniapp-redesign-20260830.md`——被交接卡 v13 引用
  （"详版改造报告"）、含 v5 全部验收截图索引，是 WM6 手动验收的活证据
- **修法**：
  ```bash
  git mv docs/miniapp-paint-handover-20260830.md docs/legacy-attic/
  git mv docs/backend-change-report-20260830.md docs/legacy-attic/
  ```
  两个文件头部各加一行归档横幅：`> 【已归档 20260831】历史过程文档，现行方案见 docs/miniapp-redesign-20260830.md`
- **验证**：grep 全库 `backend-change-report-20260830` 确认无活跃文档引用残留
  （legacy-attic 内部互引可留）；redesign 报告原路径原样存在

## 任务 4：docs/04 补 miniapp v5/v6 验收表（文档债主体）

- **位置**：`docs/04-模块交付顺序与手动验收手册.md`（WM6 小程序章节，格式对照 WM2 验收表）
- **根因**：v5/v6 批次开发模型报了结果但未回写手册，违反"手册步骤是模块交付制组成部分"
- **内容要求**（从 `docs/miniapp-redesign-20260830.md` §一/§二 + 交接卡 §2 提取）：
  1. v5 八页验收表：首页任务台/图书馆筛选（年级/主题/AR/排序/音频开关+chips+真分页）/
     书架三 tab/详情页借阅状态卡/听书页查词闭环/消息中心卡片化/测验 passed/locked 态/tabBar 图标
  2. v6 批次专项四项：quiz 确认弹层可见性、打卡日历 today+checked 白字、
     音频时长 MPEG2 解析（4-6s 显示正确）、听书页完播进度 100%（onEnded 兜底）
  3. 每行含"测试步骤 → 预期 → 截图证据路径"（截图在 docs/miniapp-redesign-20260830/screenshots/）
  4. 验收前置条件注明：`python -m scripts.seed_wm11_demo && python -m scripts.seed_demo_library`
     + 模拟器退出重登（parent_id 变旧 token 失效——症状：接口数据为 0）
- **验证**：手册新增小节标题含"miniapp v5/v6"；表行数 ≥ 12；tsc/pytest 均不需要（纯文档）

## 任务 5：收尾 commit

单刀 commit（纯 docs/目录操作，不混业务代码）：
```
docs: 目录清理与文档债清偿（任务包 20260831）

- 删除无引用旧截图 acbdefd3-*.png
- miniapp-paint-v3/v4 及两个过程报告归档 legacy-attic
- docs/04 补 miniapp v5/v6 验收表
```
**push 前请示用户**（裁定库纪律）。commit 后 `git status` 应仅剩 .workbuddy/ 未跟踪项
（工作记忆目录，不处理）。

## 红线提醒

- 本包**只动文档与目录**，任何 `*.py/*.ts/*.js` 出现在 diff 里 = 越界，立即回滚
- git mv 前后文件数必须一致（防"脚本中途断言失败导致端点丢失"同族事故——历史上
  批量操作后必须 grep/ls 验证落盘）
- 完成后回写本文件头部状态 + LEDGER 备注栏追加一行"20260831 目录清理与文档债 DONE"
