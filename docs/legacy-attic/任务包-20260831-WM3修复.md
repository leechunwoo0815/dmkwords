# 任务包 20260831-WM3：会员与订单 UX 基线修复（12 项）

> 出包人：外部审查专家。依据：docs/WM3-UX差距清单-20260831.md（专家深审）+ 用户 2026-08-31 四点裁决。
> 交付标准：docs/07 施工手册（七步闭环 + 简报模板 + UX 基线对齐表随简报提交）。
> 验证等级：A/B 级（前端为主）；涉及 seed 脚本改动跑 B 级。
> **状态：DONE（2026-08-31，W1-W12 全落地，门禁 229 passed / 80.02%，两刀 commit 未 push）**

## 用户裁决口径（2026-08-31，必须遵守）

1. **W1 家长选择**：建档改为搜索选择家长（手机号/姓名），运营人员不允许接触数据库 ID
2. **订单取消语义**：已支付订单只能走退款申请（现有退款中心链路），不能取消——UI 维持现状（取消按钮仅出现在"待人工确认"状态），后端 422 防护不动
3. W3 的完整"运营审核工作台"已另立 WM13 模块规划（docs/WM13-运营审核工作台规划提案-20260831.md），**本包只做轻量计数**，不做通知
4. **W11 收款方式**："微信线上"选项本批前端隐藏（后端枚举保留），WM12 接入后再放出

## 修复清单

### P1（3 项）

**W1 建档家长选择器**
- 位置：MemberManage.tsx L428-429"所属家长ID"Input
- 修法：改 Select 远程搜索（showSearch + onSearch 防抖调后端家长搜索接口；输入手机号/姓名模糊匹配，选项展示"姓名（手机号）"）。若后端无家长搜索端点，在 /api/admin/members/parents 加 `keyword` 查询参数（GET，B 级验证）。新建家长成功提示带出家长姓名+手机号，弹窗关闭后表单预选该家长
- 测试：远程搜索返回正确选项；选中后创建孩子挂到正确 parent_id（pytest 补 1 例：keyword 搜索家长）

**W2 取消订单错误处理**
- 位置：MemberManage.tsx L362
- 修法：`await apiCancelOrder` 包 try/catch + message.error(e.message) + 失败不刷新列表。竞态场景（他人刚确认收款）后端 422 detail 必须可见
- 测试：无需新 pytest（后端已有 test_cancel_pending_order 覆盖 422）；前端 tsc

**W3 订单 Tab 待确认计数**
- 位置：MemberManage.tsx L260-263 Tabs
- 修法：订单 Tab label 增加待确认数（如 `订单（12 · 待确认 3）`）。实现走后端：新增轻量 counts 查询（一次返回订单各状态计数，参照 WM11 counts 模式，禁 N 次请求）。**注意**：该 counts API 的返回结构预留扩展性（WM13 待办聚合要用，键名用 `pending_manual_confirm` 等语义化命名）
- 测试：pytest 补 counts 端点 1 例

### P2（7 项）

**W4 Modal 脏数据保护**：6 个 Modal（家长/孩子/订单/收款/评估上传/编辑）取消时若表单有值 → Modal.confirm 挽留（"已填写的内容将丢失"）。Form 用 isFieldsTouched，受控 state（评估上传的 fileList/remark）用非空快照判断。参照 WM2 P2-11 实现模式
**W5 URL 状态镜像**：tab/keyword/page 进 URL（window.history.replaceState，不堆栈），刷新还原。参照 WM2 P2-6
**W6 订单表 loading**：loadOrders 加 setLoading，订单 Table 加 loading prop（对照孩子表写法 L115-119）
**W7 排序**：订单表金额/创建时间列加受控后端排序（Table sorter + 后端 order_by 白名单校验）；孩子表暂不加（无强需求，避免扩散）
**W8 取消订单确认文案**：Popconfirm 改为"确认取消订单 {order_no}（￥{amount}）？取消后需重新下单"——写明影响
**W9 会员到期列增强**：到期列展示"日期 + 剩 N 天"（≤7 天橙色，已过期红字"已过期 N 天"）；已过期行状态 Tag 已有红色，保持
**W10 演示数据补 WM3 异常态**：seed_wm11_demo.py 增加演示覆盖：1 个 observation 孩、1 个 pending_evaluation 孩、1 个 expired 孩（member_expire 改昨天，D1 即时判定验证）、1 笔待人工确认订单。注意幂等（重复跑 seed 不重复造）

### P3（2 项）

**W11 隐藏"微信线上"**：PAY_METHOD_OPTIONS 删除 wechat 项（L67-74），后端枚举不动
**W12 创建订单 Modal 展示优化**：child_id disabled Input 改为纯文本展示"孩子名（家长手机号）"——行内创建订单入口本身已带上下文，去掉裸 ID

## 红线

- 后端 identity 域只允许两类改动：家长 keyword 搜索参数 + 订单 counts 端点。**状态机/折扣/收款联动零接触**（已验收放行，回归红线第 2 条）
- 既有测试禁弱化；新增测试先 RED 后 GREEN
- 完成后 `bash scripts/dev.sh stop` → gate.sh → start + 双 seed；UX 基线对齐表（U1-U12）随简报提交，逐项附文件+行号证据
- LEDGER WM3 行追加修复记录；本文件头部回写状态
