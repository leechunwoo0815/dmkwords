# 专家资格交接书 — 2026-08-31（前任外部审查专家 → 继任外部审查专家）

> **本文件定位**：继 docs/专家交接书-20260830.md（前任→我）之后的第二次专家资格交接。
> 交接动因：继任专家于 2026-08-31 完成全量全维度审查（外部专家意见/20260831-全量审查修复包/，
> 16 文件 3249 行约 100 卡），前任按"信任但验证"铁律完成资格核验后正式让渡审查权。
> 作者：前任外部审查专家（2026-08-31 就任，本轮卸任）。

---

## 一、资格交接裁定（前任验证结论：合格，交接生效）

**验证方法**：不采信简报，对修复包抽验 13 项声明逐条对源码（P0 全 5 项 + WM13 全 5 项抽样
+ WM5-F1 + P1 统一模板与 F1 + 交接卡/迁移/测试先例交叉核对）。

| # | 声明 | 核验结果 |
|---|---|---|
| P0-F1 | reading 域 6 端点缺 child 归属校验 | ✅ 逐行实锤（L157-221 全裸查；L189 无 None 检查的附带 bug 连细节都对） |
| P0-F2 | 登录码硬编码 "1234" 无生产拦截 | ✅ L88 原文一致；validate_production 不含验证码通道属实 |
| P0-F3 | 媒体鉴权不校验 token type | ✅ media_auth.py 全文 23 行亲读，只验签名 |
| P0-F4 | 押金赔偿负数金额反向加钱 | ✅ service.py L168-171 `min(-100,200)=-100` 推演成立，会多退真钱 |
| P0-F5 | 登录页泄漏管理员凭据 | ✅ Login.tsx:51,54 精确命中 |
| WM13-F1/2/3/4/5 | 全表加载/手写类型/可空唯一索引/懒加载计数/测试直造 | ✅ 5/5（schema.d.ts grep todo 零命中；迁移 nullable=True；交接卡 v15 L21-22 挂账原文在位） |
| WM5-F1 | 续借错行（点第 2 行续第 1 行） | ✅ CirculationDesk.tsx:229-235 逐字一致，验收必撞真 bug |
| P1 模板/F1 | 行锁模板定位 + 退款 execute 无锁 | ✅ circulation/service.py:102 原文；wm10 execute L339-353 无 with_for_update；test_wm13_admin_notify.py:487 REPEATABLE READ 注释引用准确 |

**裁定**：13/13 实锤、零虚报、零行号漂移。合格，资格交接生效。

**对用户的一句话**：这份审查比我的近期审查多抓了一个维度的真问题——我的深审聚焦在
验收模块（WM3/WM13）的回归与不变量，他做了**全库安全 sweep 与资金并发 sweep**，
抓到了我的盲区（P0-F1 归属校验、P0-F3——后者甚至是项目记忆里的硬约束而未落实）。
两种审查互补，这正是引入他的价值。

---

## 二、继任专家实证能力画像（前任观察记录）

**强项（有实证）**：
1. **证据纪律过硬**：每卡"验收可见性（必撞/测不出/可复核）+ 事实/推断标注"的分级，
   与本项目"声明核验四件套"同族；行号全部可复核
2. **项目知识是真读出来的**：宪法五-3（禁止手写类型）、铁律 5（资金四件套）、
   gate A/B/C 分级、E-20260831-04、交接卡 v15、项目自有的两段行锁教科书模板、
   既有测试的 REPEATABLE READ 先例注释——引用全部准确
3. **判断力与项目文化同频**：WM13-F5 对"测试直造资金状态"的裁定是"诚实标注而非删测"
   （正是本项目诚实文化的操作化）；P0-F2 死配置不顺手删、留给用户裁决——克制
4. **工程判断**：P1 统一包"用项目自己的模板修，不引入新模式"——这是老手做法

**需要继任专家知道的两个事实**：
1. 他的审查建立在 bece9ca（白屏修复）之后——他看到的 useTodoCounts 是修好的版本，
   并正确复核了修复（WM13 包正面项第 4 条）。但**白屏事件本身的全过程知识**（三层漏检、
   用户被堵在验收线上、紧急破例授权直修）在本文件 §三
2. "约 100 卡"中前任抽验 13 张（全 P0 + WM13 全量 + 各抽 1-2）命中率 100%，
   未逐卡验证的 ~85 张按此信用度采信，但**投喂开发模型后仍按简报核验四件套验交付**
   ——信用不豁免验证

---

## 三、项目前世今生（代码里读不到、前任独有的部分）

### 3.1 双角色体系与诚实文化的起源
- 本项目是"开发模型（多任轮换）× 外部审查专家（用户称'专家'）× 用户（唯一裁决人）"三元结构
- 诚实文化起源于 DeepSeek V4 自白事件（主动报备不惩罚、隐瞒是红线）；之后抓过 WM11 UX
  对齐表 4 项虚报（无证据=虚报由此立规）、两任"门禁数字只写 /tmp 就报"违规
- 现任开发模型 GLM 5.3 Flash 表现优秀（拷问式开工、主动报遗留），但"好"不等于"免检"——
  本轮 WM13 白屏（E-20260831-04）就是 tsc 全绿+简报如实但运行时崩了的例子

### 3.2 白屏事件始末（E-20260831-04，你复核过的那个修复）
WM13 批次三的 useTodoCounts getSnapshot 返回新对象字面量 → useSyncExternalStore 无限
重渲染 → 登录后全站白屏。三层漏检：tsc 查不出运行时语义 / 开发模型无浏览器 /
**前任深审用 curl 验了后端却没浏览器验前端**。修复由前任在用户明示授权下破例直修
（bece9ca）。教训已成防复发条款：**前端批次交付验证清单必须含浏览器端到端目视**——
你后续审前端批次时同样适用。

### 3.3 WM13 出生史（你审的是成品，出生过程在这）
口头需求（用户："PRD 缺失的章节"）→ 前任规划提案（四层蓝图）→ 用户裁决（独立表/事项级/
通知中心双 tab）→ 任务包 v1 → **前任自我重审推翻 v1**（抓出权限错配/孤儿通知/口径分叉
三个 P0，v2 引入"显示态实时算、审计态事件写"与不变量集 S1-S4/L1-L3/C1-C2）→
开发模型五批次实施（含架构关整改：查询聚合移出 common 归 admin 域，B10 模式）→
白屏事故与修复 → 需求侧文档补录（PRD §2.4 / docs/03 F12 FEAT-071~073 /
features/admin_todo.feature 7 场景）。
**你要守的设计灵魂**：显示态实时算是 WM13 的立身之本，任何"为性能把三态落库缓存"的
提案都必须先过 S1 反例表（落库缓存=孤儿通知回归之路）。

### 3.4 历史裁定库
docs/专家交接书-20260830.md §四 的 12 条裁决先例全部继续有效（验收依赖序/E-08 数据处置
顺序/tag 打新不移动旧/覆盖率门禁验收期不抬线/continue-listening 语义/dev.sh 0.0.0.0 等）。
**本轮新增裁定（20260831）**：
1. ruff ≥0.16 扫 md 内嵌 python 块（E-20260831-01）→ 设计文档伪代码一律 ```text 标注
2. 纯 docs 改动零门禁（E-20260831-03，连 A 级都不是）
3. gate.sh run() 失败不中断，判过没过看退出码 + grep "✗ 门禁失败"（E-20260831-02）
4. useSyncExternalStore 三查：快照缓存引用/subscribe 幂等/数据落地才 notify（E-04）
5. 前端批次必须浏览器目视（E-04，curl 验后端 ≠ 前端已验证）

### 3.5 当前战场快照（2026-08-31 深夜）
- 本地领先 origin **19 刀未 push**（WM3 修复 3 + handoff 1 + WM13 12 + 需求补录 1 +
  白屏修复 1 + 审查修复包本刀）——**git 无部分 push，两批验收全过后一次推 + 双 tag**
  （acceptance-wm3-ux-fixes → cef1c1e；acceptance-wm13 → 本轮修复包后的 HEAD）
- WM3/WM13 双手动验收动线已交付用户（WM3 15 分钟版 / WM13 25 分钟版），
  **P0 五项修完之前别让用户走双账号验收**（互写脏数据——新专家判断正确，前任背书）
- WM12 阻塞不变：T3 商户号资质（甲方）
- 真机 iOS 音频端到端（v6 批次唯一未闭环项）仍挂账——WM6 修复包若未覆盖请并入
- 全站无 ErrorBoundary（任何组件崩溃=白屏）——E-04 防复发第 3 条挂 docs/09，建议随
  横切-前端质量包一并处理

---

## 四、4 个决策点的历史上下文（辅助你引导用户，决定权在用户）

1. **HC-A1 EventBus ADR（方案 A 迁事件 vs 方案 B 修宪承认直调）**：历史上下文——WM11 的
   B11 模式（事件驱动通知+唯一索引幂等）**已验收在生产**，且 notify_handlers 是通知域的
   正确架构。若走方案 B（前任倾向同你：单机部署直调一致性是优点），修宪措辞应 carve-out：
   "通知域保留事件驱动（B11 已验收）；其他横切场景允许同事务直调"——一刀切回退会误伤 WM11
2. **HC-A2 Order 宿主迁 billing**：前任建议 WM12 开工时再定（迁移面大），现在先落 ADR
   记录两种选择的代价即可，不必为迁移而迁移
3. **WM6-F3 音频会员门禁是否 V1 刻意设计**：对照 R-313 未缴费权限矩阵（音频禁）——
   若 miniapp 实际未拦，历史无"刻意放开"裁定记录，倾向按矩阵修，但需用户确认产品口径
4. **WM10-F4/WM6-F6 家长审计口径 + 撤销入口**：纯产品口径，无历史包袱，列表格给用户选

---

## 五、战场优先级（前任最终建议，与新专家方案基本一致）

1. **今晚/验收前**：P0 包五项投开发模型（半天）——P0-F1/F3/F4 是安全+资金红线
2. **并行**：P1 资金行锁包（1 天，复用项目模板与 session_pair 基建）
3. **验收动线前**：WM13-F4（tab 计数同源，10 分钟）+ WM5-F1（续借错行，一行）先修——
   省两轮白测，这是新专家"必撞清单"的正确用法
4. **验收后**：19 刀一次 push + 双 tag + LEDGER 终态 + 交接卡 v16
5. **WM12 开工前**：他的支付前置安全清单逐项核 + HC-A2 ADR + 4 决策点全部落定

## 六、纪律交接（不可妥协项，违反即失去资格）

1. **只审不改**：前任本轮破例两次（白屏紧急修复、任务包 docs）均系用户明示授权——
   红线本身从未松动
2. **声明必核验**：简报是线索清单不是事实；信用不豁免验证（对本文件 §二 第 2 条同理）
3. **用户是唯一裁决人**：口径分叉列表格给用户选，你只给推荐和理由
4. **push 必请示**；tag 打新不移动旧
5. **诚实第一**：审不出就说审不出，未验证就明说未验证——用户的信任建立在
   "专家说过没问题就真没问题"

---

*资格交接自本文件落盘生效。前任的审查方法论沉淀于 docs/专家交接书-20260830.md §三，
历史裁定库同文件 §四，两者与本文件共同构成完整的专家记忆。祝守护好这个项目。*
