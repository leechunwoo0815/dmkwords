# 专家交接书 — 2026-09-01 在任专家会话交接（上下文压缩前完整快照）

> **本文件定位**：现任正式审查专家（就是我）在上下文压缩前的完整工作交接。
> 继任者（可能是压缩后的我自己，或全新模型）读本文件 + 两份前任交接书即可零成本接手。
> **恢复口令**：读本文件 → docs/专家交接书-20260831-资格交接.md → docs/专家交接书-20260830.md。
> 你是 DmkWords 项目的现任唯一正式审查专家。
> 写作时间：2026-09-01（用户手动验收 WM3/WM13 即将开始前的间隙）。

---

## 一、你是谁（身份与权力边界）

- **角色**：DmkWords 外部审查专家（2026-08-31 经前任 13/13 抽验合格正式接任，
  资格交接文件 docs/专家交接书-20260831-资格交接.md）。
- **三元结构**：开发模型（GLM 5.3 Flash，表现优秀但"好≠免检"）× 你（专家）×
  用户（唯一裁决人）。
- **红线**：只审不改（例外仅限用户明示授权）；声明必核验（简报是线索不是事实）；
  口径分叉列表格给用户选；push 必请示；tag 打新不移动旧；诚实第一。
- **你的核心资产**：`外部专家意见/20260831-全量审查修复包/` 16 文件 3249 行约 100 张
  可施工任务卡（前任抽验 13/13 实锤，行号可信但施工时以当前代码为准复核）。

## 二、本会话前生今世（时间线，压缩后看这段即可）

```text
2026-08-31 深夜~2026-09-01：
1. 用户投喂审查任务包（docs/审查任务包-全量深度审查PROMPT-20260831.md——此文件
   已按用户指示删除，是 workbuddy 过程产物）
2. 我执行全量深度审查：Phase 0 上下文加载 → 4 路并行深审子代理（域A+B模块边界/
   域C+D契约数据层/域E+F并发安全/域G测试质量+前端）→ 我亲验 8 处最高危发现
   → 输出完整审查报告（98 项：阻断5/高15/中39/低39；健康度 65/100）
3. 用户要求落盘成分类修复包 → 我提 16 文件方案（总览+P0+P1+11个WM包+3横切包）
   → 用户裁决：16 文件细分 + P0 先修再验收 → 全部落盘
4. 前任专家验证我 13/13 实锤 → 资格交接生效（commit a377034+a14eafb）→ 用户把
   交接内容发给我 → 我正式就任
5. 就任核验：发现交接书 21 刀误计数（实测 22——33cf5c5+ac1c5d0 ruff事故对漏计）；
   吸收关键增量：白屏事件全过程、HC-A1 方案B须 carve-out 通知域B11、WM6-F3
   对照 R-313 矩阵
6. 文件清理（用户授权）：删作废 SOP+审查PROMPT（workbuddy 产物）→ 手动验收清单
   xlsx 入库 + .workbuddy/ 入 gitignore（commit 0344822）
7. 我出 P0 投喂指令（P0五项+WM13-F4+WM5-F1 两个"验收必撞"小修）→ 用户发给开发模型
8. 开发模型交付 P0 批次（7卡+架构整改=9刀）→ 我严格审查：
   git 逐刀+门禁日志+代码亲读+测试假绿扫描+浏览器端到端目视（agent-browser：
   登录页 placeholder 干净/登录无白屏/通知中心首屏计数2与徽标同源/借阅台渲染）
   → 裁定通过 + 2 返工项（Notifications.tsx:474 "待处理（null）" U10 违例；
   test_wm6_login_guard.py:37-44 死代码）
9. 开发模型返工（cf80094+eb77e6e）→ 我核验 diff + 亲跑 tsc exit 0 → 闭环（34刀）
10. 用户问 P1 投喂时机 → 我裁定：等 P1 修完再验收（dev.sh stop 会打断验收现场），
    投喂指令追加"现场恢复"条款
11. 开发模型交付 P1 资金并发行锁批次（12项=14刀，含两个深层发现：populate_existing
    与 autoflush）→ 我执行最严审查：14刀逐刀+10处锁代码亲读+611行并发测试全文
    假绿扫描+session_pair基建+gate日志+现场恢复亲测（todo-counts admin_total=2）
    → 裁定通过 + 1 顺带修复项（checkout 锁读缺 populate_existing）+ 2 记录项
    （简报 hash 全错；pytest 未亲跑以保现场）
12. 【当前时刻】P0+P1 全落地（48刀未推），现场已恢复，验收放行指令已发给用户
```

## 三、当前战场状态板（数字全部 git 实测，2026-09-01）

| 项 | 值 | 备注 |
|---|---|---|
| 未推 commit | **51 刀** | `git log --oneline origin/main..HEAD \| wc -l`；构成：P1 审查后 48 刀 + 开发模型并行会话文档收尾 2 刀（d796791 LEDGER 终态回写/交接卡v16/记忆库闭环；7a315c8 dev 侧 v17 交接书）+ 本专家交接书 1 刀（4e55392） |
| tag 计划 | acceptance-wm3-ux-fixes → cef1c1e；acceptance-wm13 → 验收通过时 HEAD | 打新不移动旧；**两批验收全过后一次 push 全部 51+插修N 刀**（git 无部分 push） |
| gate 基线 | P0 批 275 passed/81.18%；P1 批 **287 passed/81.33%** | gate-runs/2026-09-01/gate-p0-fixes.log 与 gate-p1-locks.log（均已验尾：PASS 退出码 0、"✗ 门禁失败" 0 处） |
| BDD 现状 | 仍 844 steps skipped（12 scenarios 执行） | HC-T1 已知债不变，勿当新问题报 |
| dev 现场 | :8002/:5173/:8443 运行中，8443 已禁调度（P1-F10） | 双 seed 完成：管理待办 2（退款1+转让1）/待确认收款 2——我亲测 todo-counts 一致 |
| 小程序 | 需退出重登（E-20260829-09 三步恢复） | 用户验收小程序侧前必做 |
| WM 状态 | WM1/WM2 ACCEPTED；WM3~WM11+WM13 MANUAL_TEST 等验收；WM12 阻塞于 T3 资质（甲方） | |

## 四、已交付物与关键裁定（本会话产出清单）

### 4.1 审查修复包（16 文件，全部在 `外部专家意见/20260831-全量审查修复包/`）

00-总览与使用指南（投喂顺序+施工契约+两个施工顺序陷阱）｜P0-安全漏洞五项｜
P1-资金并发行锁｜WM3/WM4/WM5/WM6/WM7-WM8/WM9/WM10/WM11/WM13 各模块包｜
WM12-支付前置-安全清单｜横切-门禁与测试体系｜横切-架构债｜横切-前端质量

**每张卡八要素**：验收可见性（必撞/测不出/可复核）+证据+根因+代码级修复方案+
红测试要求+gate分级+施工顺序+事实/推断标注。

### 4.2 两次严格审查裁定（对开发模型交付）

**P0 批（11 刀）**：通过。7 主卡+2 返工+1 架构整改全实锤；测试零假绿；
浏览器目视补位（dev 如实声明"目视未做"，专家补上——E-04 纪律）。
返工 2 项已闭环（cf80094 null 标签、eb77e6e 死代码）。

**P1 批（14 刀）**：通过。F1~F10+追加A/B+架构整改全实锤；611 行并发测试
全文审读零假绿（B 旧快照→A 提交→expire_all 分流→B 走 service→终态实质断言）；
两个深层发现是真金：①锁定读必须 `.populate_existing()`（identity map 旧快照使
守卫失效，E-20260901-01 已入库）②autoflush=False 下聚合推进前须显式 flush。

### 4.3 遗留的待办原子（下批顺带，勿丢失）

1. **顺带修复项【低】**：`backend/domain/reading/service.py:596-606` checkout 的
   Reservation 锁定读缺 `.populate_existing()`（dev 自己发现的模式在自家新代码
   复现；风险窗口毫秒级，一行修）——**随下一批投喂顺带**（dev 已挂入 P1 包状态块）
2. **顺带修复项【低~中·WM1 遗留】**：token_generation 全库零 bump 点（grep 实证：
   仅 models.py:31 列定义，无任何递增代码）——"改密后旧 token 失效"是文档化属性
   （security.py docstring+列注释）但从未生效；admin token 8h 过期兜底窗口有界。
   修法：admin 改密端点 bump token_generation + 回归测试（改密后旧 token 401）。
   dev 在 P0 追加 B 时发现并如实报告（测试保留直改库+注释），我已独立 grep 复核坐实
3. **简报质量规则（下批投喂指令须写明）**：hash 用 `git log --oneline` 现场复制
   禁止凭记忆（P1 简报 hash 全错——实际 F1=2f30aeb/F2=b4975b5/F3=23898cc/
   F5=fd396fb/F4F8=99a22d9/F6=f857e4e/F7=f4ea8f3/F9=ccc5b37/F10=dd725b3/
   追加A=64aab81/架构=28f8176/docs=0cc2e51）；领先计数必须 git 实测
4. **HC-A1 方案 B 措辞修正（未写入架构债包文件）**：修宪须 carve-out——
   "通知域保留事件驱动（B11 已验收）；其他横切允许同事务直调"（一刀切回退
   会误伤 WM11）。随验收反馈批次一并改修复包文件
5. **dev 并行收尾核验结论**：d796791 对我 4 份修复包（P0/P1/WM5/WM13）加的
   状态块**纯增量、hash 全部正确、含裁定与返工记录**——质量合格无篡改；
   7a315c8 为 dev 侧 v17 交接书（其自身会话压缩用）

## 五、下一步作战指令（压缩后照此执行，不需要重新思考）

### 5.1 用户手动验收支持（当前主战场）

用户正在/即将走验收。**验收放行条件已满足**（P0+P1 落地+现场恢复+双 seed）。
已发给用户的动线：
1. P0 复核四步（10 分钟）：A token 查 B 孩子预约被拒 / parent token 直访 admin
   媒体 401 / 押金赔偿输 -100 得 422 / 登录页无凭据字样
2. WM3 动线：Excel 清单 22 步（`手动验收清单-WM3-WM13-20260831.xlsx` 已入库）
3. WM13 动线：四链 15 步，首屏应即显"管理待办（2 待处理）"
4. WM5 续借目视：借 2 本点第 2 行续借 → +7 天必须落第 2 行（WM5-F1 修复验证）
5. WM9 第一步时区实测（若验到 WM9）：发布 15:00 活动看列表显示——15:00 正常/
   07:00 坐实差 8 小时（WM9-F4，修复方案在 WM9 包）

**验收结果处理协议**：
- 用户发来失败编号 → 逐条定性（新问题 / 修复包既有卡 / 验收环境问题如未重登/
  seed 未恢复）→ 出插修批次投喂指令（新问题+该模块包既有卡合批）
- 全绿 → 出**收尾指令**（见 5.2）

### 5.2 收尾指令（验收全过后，原文照此）

```text
1. push 前核对：git log --oneline origin/main..HEAD | wc -l（应为 51+插修N刀，
   与最后简报一致；不一致先查清差异再动）
2. git push（一次推全部，无部分 push）
3. 打双 tag（打新不移动旧）：
   git tag acceptance-wm3-ux-fixes cef1c1e && git push origin acceptance-wm3-ux-fixes
   git tag acceptance-wm13 && git push origin acceptance-wm13
   （第二个 tag 指向验收通过时的 HEAD）
4. LEDGER 终态：WM3/WM13 行 → ACCEPTED（附验收证据日期）——注意 dev 已做
   P0-fix3/P1 修复批次的终态回写（d796791），验收后只需补 WM3/WM13 主行终态
5. docs/项目交接-20260824.md：dev 侧已自行写到 v17（7a315c8，含其五幕工作史/
   49刀快照/陷阱教训 20 条）——收尾时核对其 v17 与实际状态一致即可，无需专家代写
6. 若验收期有新教训 → 错误记忆库新条目（五要素）
7. 全部完成后向用户报"收尾完成"，等下一战场指令
```

### 5.3 四个决策点（等用户拍板，拍完更新修复包文件再投下一大批）

| # | 决策 | 我的推荐 | 关键上下文 |
|---|---|---|---|
| HC-A1 | EventBus ADR：A 迁事件 vs B 修宪承认直调 | **B**（单机部署直调一致性是优点） | **必须 carve-out 通知域 B11（已验收的事件驱动+唯一索引幂等是正确架构）** |
| HC-A2 | Order 宿主迁 billing | WM12 开工时再定，先落 ADR 记录代价 | identity/service.py 已 701 行，自述"WM5 后拆"条件已到 |
| WM6-F3 | 音频会员门禁是否 V1 刻意放开 | 按 R-313 矩阵修（音频禁） | 历史无"刻意放开"裁定记录，需用户确认产品口径 |
| WM10-F4/WM6-F6 | 家长审计口径 + 撤销入口 | 纯产品口径列表格给用户选 | 无历史包袱 |

### 5.4 下一大批投喂顺序（决策点落定后）

1. **WM9-F1 退款双轨 + WM10-F2/F3 拒绝审计族**（与 P1 已有的退款单出口统一衔接，
   C 级大批；WM9-F4 时区若坐实一并修）
2. **横切-门禁与测试体系包**（BDD 解封流水线——**注意两个施工顺序陷阱**：
   ①gate.sh 加 skip 阈值必须最后做（当前 844 skipped，先加立刻爆红阻塞一切）；
   ②verify_architecture 扩 glob 到 miniapp_router 前必须先下沉 31 处存量违规）
3. 各 WM 模块包随验收反馈逐个投（WM4 押金死join/WM5 上限公式两套/WM6 预约通知
   死订阅+唯一约束迁移等——各包内有完整卡片与顺序）
4. 横切-前端质量包（HC-F1 防双击 15 弹窗应**已含在 P1 后端锁的前端半**——注意
   P1 批只做了后端半，HC-F1 仍待做）+ 横切-架构债包
5. WM12 开工前：WM12-支付前置-安全清单逐项核（P0-F2 已完成其中验证码项）

### 5.5 投喂指令模板（给开发模型的标准骨架，历次已验证有效）

```text
【任务包投喂】<批次名>
先读三份文件：docs/专家交接书-20260831-资格交接.md §六纪律｜<修复包文件>｜
00-总览与使用指南.md §四通用纪律
施工顺序：<按包内卡片依赖>；追加项：<顺带修复项+简报质量规则>
纪律红线：TDD 先红后绿禁弱化断言｜ruff 目录模式禁单文件｜B/C级验证分级｜
跑 pytest/gate 前 dev.sh stop 跑完 start+双 seed+告知用户重登｜commit 每卡一刀
fix(scope) 格式｜禁止 push｜修不了如实报卡号
交付简报：卡片号|改动文件:行号|红测试名与 RED 证据|GREEN 断言|实际命令与退出码
+全量 gate 尾部+新增 commit 清单（hash 用 git log 现场复制+领先数 git 实测）
```

## 六、审查方法论增量（本会话新沉淀，压缩后必须带着）

1. **计数纪律**：commit 领先数一律 `git log --oneline origin/main..HEAD | wc -l`
   实测——本会话抓到两次手工计数错（前任 21→实 22；dev 30→实 32）
2. **hash 纪律**：简报 hash 必须现场复制（P1 简报 hash 全错但 commit 真实——
   按内容+顺序对卡，不按 hash 对）
3. **浏览器目视补位**：dev 声明"目视未做"时专家用 agent-browser 补（本会话
   命令链：open 5173/login → eval 验 placeholder → fill 登录 → open
   /notifications → get text 验计数 → 网络注入验失败态——注意共享 store 缓存
   会让 abort 注入失效，失败态问题以代码读判定为准）
4. **并发测试审读法**：验证"B 建旧快照→A 提交→expire_all 分流→B 走被测
   service→**终态实质断言**（计数/余额/状态不被覆盖）"结构；警惕：A 立即
   rollback 未构造冲突=假绿；同名函数后定义覆盖先定义=假绿（dev 本会话自曝
   两次，重写后干净）
5. **不动现场原则**：需要保 dev 运行时（用户即将验收），专家不亲跑 pytest——
   以 gate 日志实物+代码审读+测试全文审读替代
6. **深层知识**：①SQLAlchemy 锁定读遇 identity map 已载行须 `.populate_existing()`
   ②autoflush=False 下同事务子查询看不到未 flush 修改，聚合推进前显式 flush
   （均已入 E-20260901-01，dev 写的，我验证过落点）
7. **纯 docs 零门禁**（E-20260831-03）；md 内代码块 ```text 标注（E-20260831-01）

## 七、关键文件地图

| 文件 | 用途 |
|---|---|
| `外部专家意见/20260831-全量审查修复包/`（16文件） | 我的任务卡库——投喂开发模型的弹药库 |
| `docs/专家交接书-20260831-资格交接.md` | 我的资格来源+前任方法论+历史裁定库+白屏事件始末 |
| `docs/专家交接书-20260830.md` | 前任的审查方法论（声明核验四件套/回归红线检查表）与 12 条历史裁定 |
| `docs/项目交接-20260824.md` | 开发侧交接卡（滚动更新，**dev 已写至 v17**（7a315c8）：五幕工作史+陷阱教训 20 条；v16=d796791） |
| `docs/LEDGER.md` | 台账（T7=审查修复包；P1 行 0cc2e51 由 dev 增） |
| `手动验收清单-WM3-WM13-20260831.xlsx` | 用户验收清单（WM3 22步+WM13 15步，已入库） |
| `gate-runs/2026-09-01/` | gate-p0-fixes.log + gate-p1-locks.log |
| `error_list/错误记忆库-全项目-20260831.md` + 新增 E-20260901-01 | 错误记忆库（开工必读 §二根因模式库） |
| CLAUDE.md / AGENTS.md / docs/billing-circulation模式手册.md | 宪法与协作纪律（最高法） |

## 八、压缩后第一动作清单（照做）

```text
1. 确认身份：你是现任正式审查专家（恢复口令文件链已读）
2. 若用户发来验收失败编号 → 按 §5.1 协议定性+出插修指令（含 §4.3 顺带项）
3. 若用户说全绿 → 按 §5.2 出收尾指令
4. 若用户问下一步 → 按 §5.3/§5.4 引导决策点与下一批投喂
5. 任何开发模型简报到达 → 按 §六方法论严格审查（四件套：代码对源码/
   测试查假绿/门禁验尾/数字 git 实测；前端批次补浏览器目视）
```

---

*本文件由现任专家于上下文压缩前落盘。所有数字 git 实测、所有裁定有 commit 与
日志实物支撑。祝压缩后的我（或继任者）守护好这个项目。*
