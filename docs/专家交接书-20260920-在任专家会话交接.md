# 专家交接书 2026-09-20（在任专家模型 · 上下文压缩前完整交接）

> **恢复口令**：压缩后第一动作 = 通读本文件全文（自包含，读完即可继续履职）。
> 需要深挖历史时按 §八 恢复链按序读。本文件由专家本人于 2026-09-20 写入并单独提交（专家资产，勿并入 dev 批次）。

## 一、角色与协作模式（铁律，不可遗忘）

- **你是"在任专家模型"**：只负责审查（实证核验，不采信未验证声明）、裁决、下发任务包、收口（翻牌/commit/push/tag 执行）。
- **用户 = 唯一裁决人**；**dev 开发大模型（用户另找的独立会话，靠用户中转消息）= 全部施工**。
- **subagent 红线**：你的 Agent 工具只可用于**只读审计**或**纯文档任务**（如 §十 落盘先例），**禁止用 subagent 做开发**（用户明令：不消耗专家积分）。
- 协作流：用户贴 dev 汇报 → 你实证审查（git log/gate 日志/代码亲读/DB 查询/curl）→ 裁决+下发任务包 → 用户转 dev → 循环。
- 语言：中文回复；文档中文；commit 英文标识符。

## 二、会话全史（2026-09-09 → 09-20，本专家任期）

1. **09-09 验收修复批 1**（`4271cc6`/`a7b0afa`）：WM4-10 验收补数（seed 扩展 `_ensure_wm4_10_acceptance_data`：小红/活动报名三态/过期孩在借/四榜数据/生词本/退款演示孩押金）+ E-20260909-02 音频 child_id 断链修复 + E-20260909-03 补缴"扣多少补多少"真金漏洞（用户裁决口径）
2. **09-10 阅读圈链**：FEAT-084 需求登记五件套（`5ade21b`）→ WM14-A 一期 MVP（dev A 首轮+我 18 问裁决、dev B 接手修正 `f90b5a4`）→ fix29 三修（菜单溢出/中文名/审计500）→ WM14-B 二期（排名卡快照/周报/突破/横幅/display_name，`8921e1f`）→ fix31 收口（v25 对齐+push 11 刀+双 tag）
3. **09-11 WM15-A 社交化升级**（fix32，我 17 问裁决：样图门/头像三端/名片页/点赞深链/红点同源）→ fix33（用户四反馈：缩略图同构图/**社交主体切孩子** `(post_id,child_id)` 唯一索引/头像墙/管理端头像列）→ push + tag `acceptance-wm15a`/`wm15b`
4. **09-12 fix34 系列**（WM15-B：割裂感裁切方案/海报压缩/头像框四档/播报/生日彩蛋/红点 pageLifetimes）8 轮返工（fix34b-e5 含金光馆长头像/id 撞车）→ 全量 gate 457 → push + tag `acceptance-wm15b-fix34` → **WM4-10 验收前置盘点**（dev 独立做：E-20260912-03 成长态与词账脱钩修复，我审查收编 `88561a4`）→ **§十 批量验收总清单落盘**（`1abd9fd`，换模型可自包含执行）
5. **09-12→09-20 dev 独立推进 v29→v43**（35 刀，全部已推）：小程序全页面审查四轮/WM8-10 零散功能（生词本闪卡/退款退会分家/书架已通过）/门店扫码签到（ACCEPTED 两批）/图片体积纪律（8920b64）/演示数据按规则重做（7fc3860：演示孩 5 本 102,320 词积分 1,063，真实判定链）/Safari RDM 误判复盘（03fd714）
6. **09-20（今天）v28→v43 全面审查**：18 问 → dev 全部答完 → 我逐条裁定 + 挖出 N1-N4 新问题 → **fix44 任务包已下发** → 用户四项拍板已齐 → 等 dev 交付 fix44

## 三、当前精确状态快照（2026-09-20）

| 项 | 事实 |
|---|---|
| git | HEAD=`03fd714`，local=origin/main 同步；最新 tag=`acceptance-wm15b-fix34`（09-12 后零新 tag，fix44 R8 补） |
| gate 基线 | **477 passed / 84% / BDD 103 steps / 9 步**（gate-runs/2026-09-17/gate-115422.log；CI 35240752962 success） |
| 演示基线 | 书 35 / 家长 3 / 孩子 11 / 活动 2 / 阅读圈帖 10（`check_demo_baseline` 可复算） |
| 演示新口径 | 演示孩 **5 本 / 102,320 词 / 积分 1,063 / 勋章[100000] 真实判定链补发**；可借 28；§十 10.2 已同步新口径 |
| 工作区 | dev 有 3 文件已改未提交（gate.sh 注释 R1-R13 / check_miniapp_style docstring 补 R13 / 交接卡 §五 标题 24~28）——我已裁定 = fix44 **第 0 刀先单独提交**；另有本交接书（专家资产，已单独提交） |
| 交接卡 | dev 维护 `docs/项目交接-20260824.md` 现 v43（3928 行/25 版块——fix44 R9 滚动窗口瘦身） |

## 四、18 问审查裁定摘要（09-20，dev 已全答）

**三处前提更正（我接受）**：①Q11"五层修复系误判产物"不成立（Safari 212px vs Chromium 549px 同视口引擎差是真差异；RDM 结论属 09-18 另一条报障）②Q5 §十已被 09-17 轮同步 ③Q9 原版清理工具实跑于 08-24，危险目录当时不存在——"未上报 P0"撤回。

**关键裁定**：Q1 WM4-10 停滞=dev 自认失职（报障优先+未回抛决策项），74+5=**79 步待走表**（用户已定走表模式）；Q2 5775f1b 口头放行可追溯但零 FEAT 登记=N2 流程债；Q3 红线 28 条仅 3 条机制化→批准机化三条；Q4 BDD 8 天零增长=缺口；Q7 **自赞守卫缺失=真缺陷（N1）**（dev 证实 like() 无拦截、seed 自赞行系真实点击）；Q8 凭证未动（mtime 证据接受）但原始清单已失→立新规：**破坏性批量操作必须留清单**；三陷阱进 fix44；Q10 T27 对 schema.d.ts 系职责空白→治根批准；Q11⑤ 预览禁滚动取舍→用户已定稿接受；Q12 通知文案 emoji→docs/15 豁免口径（不加检查器）；Q14 补打 tag 批准；Q16 无翻我在任期裁决（三处甲方可推翻项无覆盖）。

## 五、fix44 任务包全文（已下发，dev 施工中——交付后按此审查）

**第 0 刀（立即）**：工作区 3 文件先单独提交（已验证：ruff/style/alignment ✓）。

**代码/门禁批**：
- **R1｜N1 自赞守卫**：`like()` 加 `post.parent_id == parent.id → 422「不能给自己的帖子点赞」`（同家长任意孩子都拦，与通知排除口径同源）+ 前端自家帖点赞按钮置灰/隐藏（`is_mine` 现成）+ 测试 1 例
- **R2｜Q10 治根**：`pnpm gen:api --check` 接入 gate 第[8]步 + docs/18 §五 错误口径修正（原文"漏 gen:api → gate[8] diff 红"是错的）；欠账 6 项清单已在 dev 答复留档
- **R3｜Q8 三陷阱**：`"-samples/"` 保护前缀匹配修正（现匹配不到 `wm15-samples/` 名存实亡）；`cover/activity/` 与书封范围分离（现靠 DB 引用保命）；回压口径统一 activity_cover 1200（现误用 cover 1080）
- **R4｜机化三条**：媒体目录禁裸 `.save()`/`open(wb)`（落盘唯一出口）、媒体 URL 禁 `?v=`、cleanup 系脚本默认 dry-run——入 gate 或独立检查器
- **R5｜报告图验证**：Pillow 小字 2x/3x 清晰度采样 + 生成耗时实测 + 打印尺寸推算，数据写 docs/15（dev 自认三项全无数据）

**文档/规格批**：
- **R6｜N2+N4**：补 3 条 FEAT 登记（生词本统计闪卡/书架已通过/门店扫码签到——给既有 FEAT 增补行）+ `refundable_types` 白名单留痕（现仅在 wm10_withdrawal_service.py:367-382 代码注释，补模式手册 P9b 旁 + FEAT-028 行）
- **R7｜N3 BDD @draft 场景**：扫码签到（连扫/防重/亮度）、生词本统计口径、自赞守卫——按 deposit.feature @draft 先例
- **R8｜Q14**：补打 `acceptance-scan-checkin` / `-2` 两枚 tag（指向 09-15 commits）+ CLAUDE.md §六 补"验收通过打 tag"条款
- **R9｜交接卡滚动窗口**：3928 行→留最近 3 版 + 归档索引
- **R10｜Q12**：通知文案 emoji 豁免口径 → docs/15

**增补（四项拍板落痕）**：积分 1,063 定稿 / 预览取舍定稿 / 走表模式确认（79 步待用户）/ 扫码真机复看两项（亮度+连扫手感）挂 docs/20 上线前检查清单——写进 LEDGER + v43 未决项就地标注。

**纪律**：fixNN 前缀；R1/R2/R3/R4 触后端与门禁→C 级全量 gate（基线 477/84%/BDD103 只增不降）；gate 绿后本地 commit、push 等指令；amend 禁用。

**交付审查重点**：R1 守卫语义（同家长跨孩拦截）、R2 gen:api 接线后 gate 首跑是否红（存量 diff）、R4 三条实际拦截力抽验。

## 六、用户四项拍板（09-20 终局，不可重问）

1. **79 步走表**：等用户有空执行；分批方案已背书：**WM7+WM8（17 步）先→WM5/WM6/WM6-F（41 步）→WM10+WM9 余步（16+5 步）**——先测 WM7/8 理由：新口径动线全在此
2. **积分 1,063 定稿**（words_per_point=100 不动）
3. **预览只缩放不滚动定稿**（Q11⑤ 闭环，不改代码）
4. **扫码签到真机复看 → 上线后再看**（挂 docs/20 清单）

## 七、待办全景与下一步动作

| 项 | 状态 | 下一步 |
|---|---|---|
| fix44 | dev 施工中 | 交付汇报到达→按 §五 审查→翻牌 |
| 79 步走表 | 等用户 | 用户开测后按 docs/04 §十 跟踪打勾，测完模块翻 LEDGER |
| WM12 微信支付 | 等甲方 T3 商户号 | 催办；到位后专家出任务包（支付回调验签/金额校验/幂等三件套+HTTPS 部署） |
| docs/20 生产部署手册 | 待**专家**复核（WM12 前置项 2，我的活） | 用户一句话即审 |
| FEAT-083 会员购买动线 | 等 WM12 | book-detail onPlay TODO 占位一行替换 |
| WM14-C/远期 | 按需 | 活动纪念卡/家长称呼偏好增强 |
| push/tag 纪律 | 惯例 | 模块交付点打 tag；push 必请示；一次全推禁部分 |

## 八、恢复链（按需深挖）

1. 本文件（当前工作状态 100%）
2. `docs/项目交接-20260824.md` **v43 区块**（dev 侧全史+环境命令+防复发红线 28 条）
3. `docs/LEDGER.md` L60-120（全部批次留痕，唯一事实源）
4. `CLAUDE.md` v1.5（宪法+gate 9 步）/ `docs/19`（门禁后恢复）/ `error_list/错误记忆库-全项目-20260831.md`（§六十二起含我任内 E-20260909/0912 系列）
5. `docs/04` §十（走表清单）+ 各模块详表

## 九、专家任内纪律清单（不可遗忘）

1. **实证核验**：一切 dev 声明亲验（git/gate 日志/代码/DB/curl），不采信未验证内容——"trust but verify"
2. **破坏性批量操作必须留原始清单**（Q8 教训：mtime 只能证明未动，不能逐字节对账）
3. 任务包五要素：开工前必读/施工范围/纪律红线/施工顺序（视觉资产设**样图门**：先 3 样过用户目视再批量）/汇报要求
4. TDD 红先绿（WM14-B"后补测试"偏差已记档，下不为例）
5. 计数同源（已 7+ 案前科：一切聚合复用既有 service 口径）；媒体消费点清单化（§六十二族）
6. 并发 gate 禁令（教训 66：两轮 gate 同库→Duplicate admin 假失败）；单 dev 同工作树
7. 改共享函数返回类型/端点必填参数 = 枚举全部消费端（教训 65，E-20260909-02 同族）
8. 交接卡/文档数字一律 `git log` 实取，禁写死刀数/hash（v28 教训，dev 已改）
9. 专家积分纪律：subagent 只读审计/纯文档，禁开发
10. 用户是唯一裁决人：甲方可推翻项（Q3 tab 第 4 位/Q5 弹窗两键/Q11 家长真名署名——均无翻案）单独列出交终审；不替用户宣布验收通过
