# DmkWords 开发工作简报 二轮 — 2026-08-24（流程整改 + C21-C23）

> 前置：专家修复闭环审查（docs/13）通过 → 用户裁定四项流程整改（测试先行若后补需锚点复核 / 批量提交 push 前询问 / 选型简报待追认 / 操作严谨性）→ 本文件记录本轮执行与待追认项。

---

## 1. 流程整改执行

| 裁定 | 执行 |
|---|---|
| 测试先行 | C21 按 RED→GREEN（先写 test_dashboard_business_cells 见红 `KeyError: copy_total` → 实现 → 绿）；C22/C23 按新的"验收步骤表先于代码"（表在图示回复，落于本文件 §4/§5） |
| commit 授权 | 任务包内 commit 自由；**push 前已暂停**，本简报仅为最后一次询问的引子 |
| 选型简报待追认 | 见 §6（5 项待追认） |
| 操作严谨性 | 门禁抓出的两处 F841/F401 均一次修复通过，未再出重复错误 |

### P1 测试锚点复核（裁定：找不到锚点的断言=无效）

| 测试/断言 | 锚点 | 类型 |
|---|---|---|
| test_import_template_download：200 + spreadsheatml + 表头列序 | 手册 WM2 步骤 8 + `import_books` 列序解析契约 | 直接 |
| …：模板原样导入 0/0（防脏数据） | **无直接 PRD 锚点**（自定工程防护） | ⚠️ 待追认 |
| test_staff_crud_permissions：staff 403 | PRD §11.1（员工管理=超管职责）+ 手册 WM1 步骤 2 | 直接 |
| …：创建 200 / 新员工登录 200 / 改名 200 / 禁用 200 / 禁用登录 403 / 重置密码 200 / 重设后登录 200 | PRD §11.1 账号管理（增删改/停用/密码）+ 手册 WM1 步骤 2 | 直接 |
| …：重复用户名 409 | `AdminUser.username` unique 约束（模型）+ §11.1 账号互斥 | 间接 |
| …：防自杀 422（不能禁自己/改自己角色） | **无直接 PRD 锚点**（运维安全防护） | ⚠️ 待追认 |
| test_child_update_ar_only_up：首设/上调/下调 422"只升不降" | docs/09 C19 + PRD §4.2/§3.3（老师评估、只升不降） | 直接 |
| …：非法值 422（"abc"） | AR 值格式校验（自定：非数字不可比较） | ⚠️ 待追认 |
| …：审计 ≥2 条 | 宪法审计铁律（child.update_profile → publish_audit） | 直接 |

**结论：无"想当然断言"（未发现照着实现写死接口的假规格）。4 项"自定防护"标签待追认，见 §6。**

## 2. C21 仪表盘经营格子（方案 A：本轮点亮）

- 后端：`DashboardService.overview()` 补 8 项只读统计（admin 域 import activity/catalog/circulation/identity models，verify_architecture 白名单允许）；口径：
  - copy_total/available/borrowed：`BookCopy`（is_deleted/status）
  - today_borrowed/today_returned：`borrowed_at/returned_at ≥ 今日 0 点`
  - overdue_active：**与 circulation.overdue_list 同口径** `status in(active,overdue) and due_at<now`
  - member_total：`member_status in (observation,pending_evaluation,formal,expired)`（R-313 在册，不含 none/withdrawn）
  - member_new_week：`create_time ≥ 本周一 0 点`（注意字段是 BaseModel 混入的 **create_time**，非自定义 created_at——本坑已踩并修复）
  - activity_enroll_recent：`created_at ≥ now-7d`
- Schema：DashboardOverviewResponse 加 9 字段（含 copy_available_borrowed 组合前端拼接）
- 前端：Dashboard.tsx BUSINESS_CELLS 由"置灰待模块"改为真实数据（去掉 moduleTag/opacity），'在馆/借出'为组合列
- 测试：test_wm1_dashboard.py 新增 `test_dashboard_business_cells`（真实链路：年费+押金+借-还-再借+家长端报名活动，断言 9 字段精确值）
- 验证：RED（copy_total KeyError）→ 实现 → **5/5 绿**；tsc 0 错
- 过程记录：借书契约经测试暴露为 isbn（非 book_id）——契约对齐非灰盒：修复测试而非实现（该契约与 test_wm5 一致）

## 3. C22 小程序「我的」页会员到期日

- member.js：`refresh()` 新增 `_expireLine(child)` + `expireWarn` 计算（**WXML 不放函数调用**——血泪教训 5，warn 标志在 JS 算出）
- 逻辑：none/withdrawn 不显示；expired→"已过期（请联系店内处理）"；observation 无 expire 字段→"会员到期(观察期 30 天) 推算日"；formal ≤7 天→高亮"即将到期，请联系馆员续费"
- wxml/wxss：`<view class="expire-line{ {expireWarn?' warn'}}">` **注意**：wxml 已修正为 `expireWarn ? ' warn'` 三元——WXML 允许三元/逻辑运算 ✓
- 验收表：§4

## 4. C22 验收步骤表（供手动验收）

| 步 | 操作 | 预期 |
|---|---|---|
| 1 | 登录有 formal 孩子的家长 → 我的 | 孩子卡显示「会员到期 YYYY-MM-DD」且与会员管理页一致 |
| 2 | 切换 observation 孩子 | 「会员到期(观察期 30 天) 日期」（若后端已给 expire 字段则显示其值） |
| 3 | formal 且到期 ≤7 天 | 追加高亮「即将到期，请联系馆员续费」 |
| 4 | none/withdrawn/expired | 前两者行不显示；expired 显示「已过期（请联系店内处理）」 |

## 5. C23 小程序押金页（端点备、页面缺）

- 后端：`GET /api/miniapp/deposits` 增加 `ledger` 数组（DepositLedger 最近 20 条：entry_type/amount/balance_after/reason/created_at）；`POST /deposits/supplement-orders` 已有（R-313 守卫）
- 测试：test_wm4_deposit.py 新增 `test_miniapp_deposit_with_ledger`（paid+流水 ≥1 条；unpaid+空流水）
- 页面：order-pkg/deposit/ 四件套重写（对齐当前 page-bg/section-card 风格，旧 wxss 为 librio 残留工件已覆盖）；state 中文/金额区/流水列表/补缴按钮（unpaid 置灰文案"未缴纳，请到店办理"；withdrawn/none 前端置灰镜像提示，后端守卫为最终判据）
- 注册+入口：app.json order-pkg 增加；member 功能网格加「押金」；api.js add `myDeposit`/`createSupplementOrder`
- 分段：**验收表 §7**

## 6. 待追认项（选型/无锚点断言，默认已执行）

| # | 项 | 我的选择 | 追认或推翻 |
|---|---|---|---|
| 1 | 模板原样导入 0 脏数据断言 | 保留（防脏），不删 | ？ |
| 2 | 员工防自杀锁（不能禁自己/改自己角色） | 保留 422 | ？ |
| 3 | C22 observation 到期日显示（无字段时按 now+30d 推算） | 推算显示 | ？ |
| 4 | C23 流水仅最近 20 条、无分页 | 截断 | ？ |
| 5 | AR 非法值（非数字）422 | 校验 | ？ |

## 7. C23 验收步骤表（供手动验收）

| 步 | 操作 | 预期 |
|---|---|---|
| 1 | 家长进入「我的」→「押金」（孩子未缴押金） | 状态「还未缴纳」；金额区标准 1200；按钮置灰「未缴纳，请到店办理」 |
| 2 | 管理端缴 1200 后刷新 | 「已缴足」；流水首条「缴纳 ￥1200.00」；按钮「已缴足」（disabled） |
| 3 | 借阅台对同孩子造赔偿扣款 → 刷新 | 「部分扣除」或「已扣完」；「已扣除」金额>0；流水现「扣除 负号？」（direction 由类型表达，金额正数、红色） |
| 4 | 点「申请补缴」（need_supplement 场景） | 弹窗「订单号 …请到店完成缴费」；流水新增「补缴」 |
| 5 | 切换 withdrawn 孩子进页面 | 按钮置灰提示「已退会，押金随退会流程处理」 |
| 6 | 切换 none 孩子进页面 | 按钮置灰「入会后可补缴押金」（服务端守卫为准） |
| 7 | 刷新/重进 | 流水时间倒序、金额两位小数、类型中文（缴纳/扣除/补缴/退款） |
| 8 | 造 21+ 条流水验证截断 | 仅显示最近 20 条，无报错 |

## 8. 门禁与改动清单

- 全量门禁 **exit 0：133 passed（+2 新用例）/ 覆盖率 77.08%（76.96→77.08）/ step8 tsc PASS**（原始输出：`gate-runs/2026-08-24/gate-full-c21.log`）
- 改动文件：admin schemas/service、circulation？不 → 实际：`backend/domain/admin/service.py`、`backend/domain/admin/schemas.py`、`backend/domain/billing/miniapp_router.py`、`admin-web/src/pages/Dashboard.tsx`、`miniapp/**(deposit 四件套/member.js/wxml/wxss/app.json/api.js)`、`tests/unit/test_wm1_dashboard.py`、`tests/unit/test_wm4_deposit.py`
- 过程中两次 ruff 门禁拦截（book/c2 未用变量）均修复后重跑全量通过——属操作严谨性项（见 §1），非代码缺陷。

---

*简报结束。待用户确认 §6 追认与 push 授权后执行。*
