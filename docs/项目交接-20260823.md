# DmkWords 项目交接 — 2026-08-23 深夜

> **恢复口令：读 `docs/项目交接-20260823.md`，继续。**
> 本文件是跨会话完整记忆载体。读完后按 §7 待办推进，按 §5/§6 纪律执行。

---

## 1. 项目身份（一句话）

DmkWords 少儿英语分级阅读系统（3-15 岁，线下借阅 + 在线听读 + 测验激励），**从旧项目 librio 推倒重建**——旧代码一行不进新库，只允许参考 `docs/billing-circulation模式手册.md`、`error_list/错误记忆库-*.md`、`docs/08-风险登记册.md` 等知识资产。

- 仓库：`github.com/leechunwoo0815/dmkwords`（SSH），主分支 `main`，**每次模块交付后必须推送**
- 宪法：`CLAUDE.md`（最高法律，冲突时以其为准）
- 需求基线：`PRD/业务需求文档-定稿V1.1.md`（甲方签字版）+ `PRD/需求决策详版V0.2-专家复审.md`（R-300 系规则）
- 规划：`docs/01-顶层规划.md` / `02-架构蓝图.md` / `03-Feature全量清单.md`
- 台账（唯一事实源）：`docs/LEDGER.md`

## 2. 当前状态（截至 2026-08-23 23:59）

| 项 | 状态 |
|---|---|
| WM1-WM10 | **代码全部交付**：门禁 exit 0（109 pytest + 12 BDD 场景 + 架构关 + 反假绿 + 迁移一致 + 覆盖率 74.87%），最新提交 `1c1d2e5`，已推送 |
| **手动验收** | **尚未开始**——用户即将按 `docs/04-模块交付顺序与手动验收手册.md` 逐模块浏览器/开发者工具测试（WM1→WM10 顺序） |
| 自审轮 | 已完成一轮全量代码审查（提交 `a3ebfc7`，修 11 处缺陷，含音频端点丢失等 P0） |
| 环境 | dev.sh 已 restart，8002/5173 运行新代码（有端口防呆） |
| 遗留清理 | librio 残留已清（提交 `1c1d2e5`），全库仅 `error_list/README.md` 一处历史陈述性 "librio" 字样（有意保留） |

模块速查（手册 §四有逐条测试步骤）：

| WM | 内容 | 管理端入口 | 小程序入口 |
|---|---|---|---|
| 1 | 登录/RBAC/配置/审计 | 系统配置、审计日志 | — |
| 2 | 图书资产 | 图书管理 | 图书馆 |
| 3 | 会员订单 | 会员管理 | — |
| 4 | 押金赔偿 | 押金与赔偿 | — |
| 5 | 借阅操作台 | 借阅操作台 | — |
| 6 | 阅读链+预约 | 预约管理 | 播放器/打卡/我的预约 |
| 7 | 测验成长 | 成长与测验 | 去测验/等级勋章 |
| 8 | 榜单护照报告 | 成长档案→生成周报 | 排行榜/护照/生词本/书架 |
| 9 | 线下活动 | 线下活动（券码签到框） | 首页→线下活动 |
| 10 | 退款退会转让 | 退款中心（三审 tab）、会员管理→评估报告 | 我的→退款申请/权益转让/评估报告 |

## 3. 环境操作（用户已会，AI 也要会用）

```bash
bash scripts/dev.sh start|stop|restart|status
```

- 管理端 `http://localhost:5173`：admin/dmkwords123（超管）、staff01/dmkwords123（馆员，验权限边界用）
- 后端 `http://localhost:8002`（/health 探活）
- 小程序：微信开发者工具导入 `miniapp/` 目录；登录 = 已建档家长手机号 + 验证码 **1234**（开发期固定，上线前接微信 code2session）
- MySQL：OrbStack 容器 `dmkwords-mysql`，端口 3306 独占
- **测试数据会被清**：pytest 用 TRUNCATE 隔离；用户手动造的数据在跑测试后可能丢失，提醒用户先验收再跑全量测试（或告知会清库）

## 4. 技术架构（30 秒版）

- 后端：FastAPI + SQLAlchemy 2 + MySQL-only（禁 SQLite）+ Alembic；域目录 `backend/domain/{admin,catalog,identity,billing,circulation,reading,growth,activity}` 四件套（models/repository/service/router）+ `miniapp_router`（家长端）
- 架构铁律（门禁 `scripts/verify_architecture.py` 机械执行）：Router 零 ORM、单文件 ≤800 行、import 白名单、四件套齐全
- 事件：进程内同步 EventBus（同事务回滚）；审计经 `catalog/audit_events.publish_audit` → admin 域落库
- 前端管理端：React+TS+antd（admin-web/，pnpm）；小程序原生（miniapp/，custom-tab-bar）
- 门禁：`bash scripts/gate.sh` 七关全过 exit 0 才算完成（lint/单测/BDD/架构/反假绿/迁移一致/覆盖率）
- 关键设计：词数/积分/等级汇总以流水表为唯一事实源（growth/summary 聚合查询，不信缓存字段）；播放防刷 = 服务端墙上时钟差（R-151）；转让/退会审核期 `Child.operation_locked` 冻结

## 5. 协作纪律（用户铁律，必须继承）

1. **中文回复**；代码/标识符/提交信息英文
2. **诚实第一**：不编造、不美化、未验证就明说"未验证"；测试失败如实报告，禁止弱化断言/删测试绕过
3. **模块交付制**：每模块 = 后端 + 前端 + 门禁 exit 0（贴原始输出）+ 手册测试步骤 + LEDGER 更新 + commit/push；**用户手动验收通过才算交付**
4. 完整性排查类任务：先枚举全量再逐项比对（如路由↔调用点 diff），不准有遗漏
5. 最小改动：不顺便重构；需求模糊先问
6. 不主动 commit（用户说"可以提交"才提交）；破坏性操作先说明影响
7. 金额 Decimal、数值进 SystemConfig、LIKE 用 escape_like、时区应用侧 datetime.now

## 6. 本项目血泪教训（新会话开工前必读，防复发）

1. **僵尸进程假绿**：手动起的 uvicorn 占住 8002，dev.sh 健康检查打到旧进程假装 OK → 新代码全 404。已加端口防呆；再遇"接口 not found"先 `lsof -ti :8002` 对 pid 文件
2. **补丁脚本部分写入**：用 python 脚本改文件时断言失败会中途退出，前几处已写入后几处丢失（WM6 音频端点因此丢失两天才在自审中发现）。**教训：脚本改完必须 grep 验证落盘 + 跑导入**
3. **ruff format 会改锚点**：先跑过 format 的文件，补丁脚本的字符串锚点可能不匹配——匹配失败时先 `sed -n` 看实际格式，不要盲试
4. **bash 变量紧邻全角字符**（如 `$pid（`）会被吞进变量名，`set -u` 下崩溃；用 `${pid}` 花括号
5. **WXML 不支持 Math/复杂表达式**：计算放 JS，WXML 只渲染
6. **alembic 非 MySQL 事务 DDL**：downgrade 中途失败留残余表，重升前手工 DROP + 改 version_num
7. **工作区陷阱**：若会话不在 dmkwords 目录打开，Edit 工具会被拒（"restricted to working directory"）被迫走脚本写文件（教训 2 的温床）。新会话必须直接在 `/Users/litianyu/cc-projects/dmkwords` 打开
8. **测试夹具**：哑 MP3 解析不出时长，需改库设 `audio_duration_seconds`；防刷测试用 `_backdate` 把 `last_report_at` 拨回过去模拟时间流逝

## 7. 待办（新会话从这里继续）

1. **陪伴用户手动验收 WM1-WM10**（用户报 bug → 修复 → 重测；手册在 docs/04）
2. **WM11 通知任务看板**（FEAT-067/068 + 数据看板 + Excel 导出）：
   - 通知双通道（站内必达表 + 微信订阅尽力；`integrations/wechat/subscribe.py` 已有占位）
   - 定时任务集：转让超时（现在靠列表惰性过期，要接定时）、预约释放、逾期标记、待支付超时、周月报生成、提醒
   - 死信兜底正式形态（events.py 现在只打日志）
3. **WM12 微信线上支付**（阻塞：甲方微信资质/商户号未定；`integrations/wechat/pay_v3.py` 占位）
4. 小项：schema.d.ts 重新生成；词典全量导入（现 309 词演示集，表结构已对齐 ECDICT）

## 8. 用户画像（协作方式）

- 偏好：先记待办再执行；按模块开发验收；每模块要"点哪里→填什么→看到什么算对"的操作表
- 会话习惯：长时间离开让 AI 连续开发多个模块，回来集中手动测试
- 重视：诚实 > 速度；完整性 > 效率；曾明确要求"不准瞎编、不准欺骗、不准有遗漏"
