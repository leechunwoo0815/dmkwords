# LEDGER 任务台账（唯一进度事实源）


> **跨会话交接**：读 `docs/项目交接-20260824.md`（当前状态 / 待办 / 血泪教训 / 恢复口令）。
> `docs/legacy-attic/项目交接-20260823.md` 已归档为历史参考。

> 规则（宪法第六节）：每轮开工读本文件取第一个未闭环项；状态流转必须先落证据（gate-runs/ 输出）再改本表；禁止凭记忆报进度。

> ✅ **2026-09-12 批次告警已闭环**：fix34 主体（09-11 `gate-fix34a.log`：453 passed / 83.46% / BDD 103）之后，用户 5 轮实测反馈产生的 fix34b~fix34e5 共 20 刀（含后端 identity/reading_circle、seed、gate.sh 新增第[5]步检查器）已于 09-12 补跑全量门禁 **PASS**：`gate-runs/2026-09-12/gate-fix34b.log`（**457 passed / 83% / BDD 103 steps / 9 步全绿**，exit 0，一次过；基线 453→457 = 净增 4 例）。gate 尾部自动 restart+双 seed；清场（跑 test_p0_t8_rate_limit 清 pytest 残留后 restart）三项核验 = **书目 35 / 家长 3 / 孩子 11 / 活动 2（干净基线）** / 后端 health ok（进程 00:51:55 新起）/ admin-web 200。

## 模块交付主线（详册：docs/04-模块交付顺序与手动验收手册.md）

| # | 任务 | 状态 | 证据 | 备注 |
|---|---|---|---|---|
| M0 | 项目骨架 + 门禁 + 宪法 | DONE | commit 820631f; gate.sh full exit 0 | 2026-08-22 |
| M0.5 | Feature 全量清单 + Gherkin 初稿 | DONE | commit 78388a1; 230 场景解析通过 | 2026-08-22 |
| WM1 | 平台基座（登录/RBAC/配置/审计 + admin-web 骨架 + dev.sh） | ACCEPTED | 2026-08-24 用户手动验收通过（11 步全过，含 C21 仪表盘 + C12 员工菜单核对） | 登录/RBAC/配置/审计/仪表盘/员工管理 |
| admin-web-ui | admin-web 绘本风视觉系统改造 | ACCEPTED | tsc 0 errors；gate.sh 147 passed / 78.11%；专家复验通过；commit dee28e5 | 不含 miniapp；含弹窗点击修复、P0/P1/P2 清理 |
| F1-F4 | 图书管理运营增强（分页/批量/进度条/滚动条） | ACCEPTED | 用户在 WM2 验收过程中逐项确认；commits `e4da393`/`4a43752`/`0bfe411`/`7e4a38f`/`c369b4b` | 已随 WM2 验收闭环 |
| admin-web-fix | 侧边栏固定 + 媒体重传缓存失效修复 | DONE | gate full PASS（148 passed / 77.94%）；tsc 0 错；commit `5980516` | 侧边栏不再随内容滚动；封面/音频重传后 URL 带 v 版本参数强制刷新；用户已验收 |
| WM2 | 图书资产 | ACCEPTED | **2026-08-28 用户手动验收通过**；外部终审 R1-R9 + P2 十二项 + C1-C3 + D1-D3 全部修复；gate PASS 187 passed/78.72%（gate-runs/2026-08-28/ 下 gate-wm2-p1.log、gate-wm2-p2.log、gate-wm2-d1d3.log 三份日志）；tag `acceptance-baseline-wm3-wm10`（固定于 ed60377） | 书目/副本/导入/封面音频/题目 + 运营增强 + 上架强校验（新书下架入库 + 五项完整性拦截 + book_onboarding_check 开关）|
| WM3 | 会员与订单（人工收款路径） | MANUAL_TEST | gate.sh full exit 0（提交 710d56d/543593a/448ede2）| 家长孩子/订单/收款/会员状态机/二孩折扣；等手动验收。**20260831 UX 基线修复 DONE**：W1-W12（家长搜索建档/待确认计数/脏数据保护/URL 镜像/排序/到期天数/演示异常态等），门禁 229 passed / 80.02%（gate-runs/2026-08-31/gate-wm3-ux.log）；两刀 commit 未 push |
| WM4 | 押金与赔偿 | MANUAL_TEST | gate.sh full exit 0（提交 74efe86）| 押金状态机/扣除/补缴/待结清；等手动验收 |
| WM5 | 借阅操作台 | MANUAL_TEST | gate.sh full exit 0（提交 7b5a8ee）| 借/还/续/逾期扣减/人工放行/未入会开关；等手动验收 |
| WM6 | 小程序与阅读链 + 预约 | MANUAL_TEST | gate.sh full exit 0（73 pytest + 架构关 + 覆盖率 60.73%）| 防刷按 R-151 墙上时钟重写；miniapp 9 页 + admin 预约管理/核销/阅读档案；等手动验收 |
| WM7 | 测验与成长 | MANUAL_TEST | gate.sh full exit 0（提交 4f7a985；81 pytest）| Quiz/词数/积分/等级/里程碑；等手动验收 |
| WM8 | 榜单报告护照 | MANUAL_TEST | gate.sh full exit 0（提交 077a2eb；90 pytest）| 五榜单/护照/周月报图片/生词本/收藏夹；等手动验收 |
| WM9 | 线下活动 | MANUAL_TEST | gate.sh full exit 0（提交 73e76d6；97 pytest）| 发布/报名/入场券/签到/退款矩阵/99 元链；等手动验收 |
| WM10 | 退款退会转让 | MANUAL_TEST | gate.sh full exit 0（提交 e6dadd3；104 pytest，覆盖率 69.6%）| 退款中心/退会/权益转让/评估报告；等手动验收 |
| P0-fix | docs/09 P0 四项（D1/C13/C15/C16 + C17 随修） | DONE | gate.sh full exit 0（119 pytest，覆盖率 75.19%）；测试 tests/unit/test_d1_member_gates.py 10 用例 | 会员过期即时判定（读时不等定时任务）+ 涟漪修复（音频仅在手/周期榜/转让/二孩折扣）；观察期→待评估馆员按钮+评估通过转正链；未入会放行限 1 本+72h 借期；AR 超范围软提示（ar_warning_range 配置）。**已提交**（commit `af2eac2`） |
| P0-fix2 | docs/10 P0 五项（R-313 守卫 / 退会 6 态 / 退款 7 态+refund_status / 会员费退款联动退会 / 转让 12 步）+ C20/WM10-07/WM3-03/WM4-01 | DONE | gate.sh full exit 0（2026-08-24，128 pytest / 覆盖率 76.50%）；test_wm10_state_machines 5/5、test_wm10_transfer_refund 7/7、test_r313_guards 4/4、test_d1_member_gates 10/10；架构关 PASS（transfer_service.py 拆分）；前端 RefundCenter.tsx 已适配（执行退款按钮+7/6 态中文标签，tsc 0 错） | MDL 挂起根因：①RefundService.review 未推进联动退会 applying→refunding（execute 失败分支失效）②_advance_withdrawal 补 pending_settle ③测试 _db() 改 with 防断言泄漏。**已提交**（commit `af2eac2` 代码 + `28f54eb` docs 回写） |
| WM11 | 通知任务看板 | GATE_PASS | 2026-08-29 最终全量门禁 PASS（216 passed / 79.51%，gate-runs/2026-08-29/gate-wm11-ux.log；同日演进 gate-wm11.log→fix.log→ux.log 三份全 PASS）；WM11 专项测试 25（tests/unit/test_wm11_*.py，含审查 P0 并发隔离 + P1 转让超时） | 通知中心（站内必达+微信尽力+发送记录）+ 12 项定时任务（D1 第 3 层过期落库/C13 待评估转换/预约释放/订单超时/转让超时/借阅逾期/活动提醒等）+ 任务看板/手动触发/运行日志 + 死信落库（D6）+ 数据看板补全（D5）+ Excel 导出（C18 审计/看板/通知）+ miniapp 消息页。**手动验收主体已通过**（管理端七步 + miniapp 消息中心 + 自动调度取证闭环；验收期修复 F1-F5/C41-C52 见 docs/17 §八）；遗留：miniapp 查词 UI 复测、看板七字段抽查。详册 docs/17 |
| WM12 | 微信支付与收尾 | PENDING | — | |
| P0-fix3 | 安全漏洞五项 + 验收必撞两卡（20260831 外部审查） | DONE | 2026-09-01 全量门禁 PASS（gate-runs/2026-09-01/gate-p0-fixes.log，275 passed / 81.18%，首轮 FAIL×3 整改后重跑）；并发/安全红测试 10（wm6_parent_guard 3 / wm6_login_guard 4 / wm2_media_auth 3）；专家裁定 ✅ 通过（浏览器目视补位 + 2 返工项闭环）| F1 reading 6 端点归属校验 / F2 验证码 fail-closed（LOGIN_DEV_CODE 配置化）/ F3 媒体 token type+gen（middleware validate_admin_payload 抽取）/ F4 押金 Decimal 防负数 + billing func.or_ 同族 / F5 登录页 placeholder；WM5-F1 续借错行 / WM13-F4 tab 计数同源+null 返工；commit dc58701/30ea058/08b8936/47ac550/d727381/e099b33/d271355/adeacc3/900dd48/cf80094/eb77e6e/64aab81；不 push 等验收 |
| WM10-13 插修 4 | 退会审核断链 P0+资金可见性+我的订单（X1-X7 七卡） | DONE | 2026-09-02：X1 退会 tab 操作列 pending→applying（WithdrawalRequest 无 pending 态，复制粘贴事故；同族 sweep 四处核对零误伤）+链路测试；X2 settle-preview 端点+审核弹窗明细（**同源 _settle_items**，一致性灵魂断言 preview==实际结算）；X5 seed paid_at 错开（333/417/4356 比例退可见，exists 幂等归一）；X6 可退卡片三形态（proportional 带折算过程/full/zero+calc 字段，观察期首笔业务规则测试坑）；X7 我的订单幽灵页落地（骨架起只有 wxss）+or_ 家长级单（child_id NULL schema 预留）+member 入口+状态映射对齐；X3 toast 带金额；X4 轮询挂载隐藏边界；单元 9 passed（test_wm10_state_machines）；X3/X4/X6 前端+tsc 0 | hash 414d81c/845a693/194d8ac/701c72a/653867b/39bf3f0/53eadbb；小程序目视受限（MCP 连接失败）只验编译页，功能页待用户/专家真机目视；**专家裁定有条件通过→返工已清**：R1 0 元退款禁提交三层守卫（apply 422+按钮置灰+onSubmit 校验，红测试实锤 0 元单曾 200 创建，hash 见 git log r1）；N1 全量 gate PASS（gate-runs/2026-09-02/gate-wm1013-fix4.log，**315 passed / 81.81%**，基线 310→315 只增；耗时 11:51——词库裁剪后仍 500s 级，覆盖率计算本身耗时）；**词库裁剪（用户裁定 2026-09-02）**：dictionary_words 335.7 万行裁至 100+209 演示词，全量备份 ~/dmkwords-backups/dictionary_words-full-20260902.sql.gz 上线导回；gate 禁全表读写词库 |
| 文档对齐批 | WM3/WM13 初验后全量文档对齐（用户指令 2026-09-02） | DONE | 专家执行（docs 例外职权）：docs/03 登记 FEAT-076（我的订单）/077（退会预估结算，同源断言）/078（可退三形态+0 元拦截）/079（撤销入口）+ 插修 1-4 随修项四段+基建随修段；docs/01 阶段表补插修批行；docs/04 新增 §六插修后补充验收动线（管理端 10 步+小程序 5 步+已知边界 3 条）；**PRD/业务需求文档-定稿V1.1.md 新增 §3.5.1 资金可见性与家长自主权增补**（四项：我的订单/三形态+0 元禁提交/撤销权/预估结算——增量模式对齐 §2.4 先例，不动定稿正文；首版盘点曾误判"PRD 已清理"，用户纠正后补齐）；error_list 补 E-20260901-04（gate.sh 注释曾引用但未落盘的欠账——防呆原则）+E-20260902-01（新 BDD 场景入 @draft 不执行，X1 场景 skipped 844→849 实证；pytest 兜底，解封仍归横切批）+E-20260902-02（add -A 误带入，0bd5aac revert）；docs/09 新增 §九工程裁定三则（词库裁剪/门禁双跑合并 -42%+durations 铺路/日志自动归档首战）。纯 docs 零门禁，git diff 验证 | 专家工作区未提交；随下批入库 |
| WM3 插修 2 | 首轮目视反馈 8 卡（F1-F8） | DONE | 2026-09-02 C 级全量 gate PASS（gate-runs/2026-09-02/gate-wm3-fix2.log，310 passed / 81.52%，基线 305→310 只增不减；首轮 FAIL×2：mkdir 老坑+format 残留→收敛后 PASS）；红测试 5（F2 1+F7 4）；BDD 6 场景全绿（+environment 业务表清理补齐）；E-03 反模式全站对照（F5 同族 obsUpload 一并修）| F6 client.ts 基建双修（FormData Content-Type+422 detail 数组）；F1 同族第三次（家长 tab 计数，sweep 清单附简报）；F7 守卫细化（身份锁/学籍放，用户拍板）；不 push 等目视复测 |
| WM3 插修 1 | 验收反馈 13 卡（20260901 任务包） | DONE | 2026-09-01 C 级全量 gate PASS（gate-runs/2026-09-01/gate-wm3-fix1.log，305 passed / 81.41%，基线 287→305 只增不减；首轮 FAIL×3 如实整改：F841/架构关四违规→OrderService 拆分 876→469+B2 端点下沉+voucher_auth middleware）；红测试 17（B1 10+B2 6+顺带 2）；BDD 4 场景独立 feature（membership @draft 存量勿动）；A1 同族 sweep 全站零同款 | A1-A5/D1-D3/B4 小修 8 刀；B1 编辑删除守卫双保险（FEAT-074/R-315 解禁口径）；B2 凭证上传查看（FEAT-075，迁移 a3b5c7d9e1f3 串链修双 head，member.manage 独立权限）；顺带 1-3 全清；不 push 等验收；目视未做（如实声明）|
| P1 | 资金并发行锁统一包（20260831 审查） | DONE | 2026-09-01：10 卡 + 2 追加项全落地（F1 退款 execute/review 行锁/F2 confirm_payment+押金幂等/F3 押金 deduct/F5 还书+overdue_mark 守卫/F4 预约释放锁序/F8 锁序 Child 最先/F6 99 元锁内复查/F7 apply 查重加锁/F9 双 commit 合一+F8 拆分/F10 dev 8443 禁调度/get_current_admin 收敛）；全量门禁 287 passed / 81.33%（gate-runs/2026-09-01/gate-p1-locks.log）；并发测试 11（session_pair 基建）；专家裁定 ✅ 通过（记录项：简报 hash 全错已纠——以后 hash 一律 git log 实取）；两个深层发现入记忆库 E-20260901-01（populate_existing/autoflush）；god file 整改（reading 拆 vocabulary_service.py 201 行）顺带；**顺带待修挂账：checkout res 锁定读补 populate_existing（一行，随下一批）** | commit 2f30aeb/b4975b5/23898cc/d0cb4bd/fd396fb/99a22d9/f857e4e/f4ea8f3/ccc5b37/dd725b3/64aab81/28f8176；不 push 等验收 |
| WM13 | 运营审核工作台（管理端通知/待办聚合/双 tab 通知中心） | MANUAL_TEST | 任务包 docs/任务包-20260831-WM13运营审核工作台.md；五批次 DONE 2026-08-31：底座（admin_notifications+StatusResolver 实时算显示态+5 触发点）/收件箱双 tab/徽标+待办卡/转让超时预警任务/9 处 L2 审计回写+RefundCenter 定位高亮；专项测试 32 | 等手动验收（docs/04 WM13 验收表 10 步）；显示态实时算/审计态事件写（S1 结构性歼灭） |
| P0 第一批 | 外部顶级专家审计 P0 修复包 10 卡 T1-T10（任务包：外部专家意见/20260903-外部顶级专家审查/11-P0第一批任务包-20260903.md） | DONE | 2026-09-03 C 级全量 gate PASS（gate-runs/2026-09-03/gate-p0batch1-fix1.log，**330 passed / 82.22%**，基线 328→330 只增；首轮 FAIL×3 如实整改：ruff lint 10 错+format 21 文件+behave 11 场景 429）；BDD 51 steps/849 skipped 与基线一致；架构关含 T6 新规则（miniapp_router glob+check_lock_populate）全绿 | 逐卡 hash：T1 RC 隔离 8854371（前置检查 binlog=ROW✓+锁兜底盘点）/T2 populate_existing 20 处 6516e3a/T3 预约 create Child 锁 16f444b/T4 退会+转让 review 行锁 9c17e12/T5 退会死锁出口 cab5a47/T6 门禁扩 glob+identity/auth.py 下沉 78accfc/T7 refund_order 走 R-308 审核链 c4b8c14/T8 限流接线+seed 弱口令拦截 b93d392/T9 借书额度单次扣减 51f1ce5/T10 无逾期费 docs 451a180；接手会话补刀 t8b behave RateLimiter 重置 ca57dfe（T8 只修 pytest 侧漏 BDD）+门禁收敛 7776c75；**专家严审通过（A-）2026-09-03，两项裁定留痕**：①转让受让方须无有效会籍=正确设计（转让=剩余权益转移给无会籍孩子，如大孩转二孩；有效会员受让会产生双重会籍语义）——维持现状不改，若产品要改属需求变更走登记流程；②无押金孩退会 approve 500 实锤真 bug（wm10_withdrawal_service.py:389 dep 仅在 settle 含押款项时赋值→UnboundLocalError，存量非本批引入，种子主路径不撞）→ **第二批首卡**（dep=None 初始化+红测试）；2026-09-03 专家放行 push（实测领先 17 刀 + 本裁定留痕 commit = 18 刀一次推；v20 交接书"144"为误记已勘误，reflog 实证 origin/main=c35a334 无新 push） |
| P0 第二批 | 外部顶级专家审计 P0 第二批 16 卡 T11-T26（任务包：外部专家意见/20260903-外部顶级专家审查/12-P0第二批任务包-20260903.md） | MANUAL_TEST | 2026-09-03 C 级全量 gate PASS（gate-runs/2026-09-03/gate-p0batch2-fix1.log，**352 passed / 82.42%**，基线 330→352 只增；首轮 FAIL×2 如实整改：T25 ORM 下沉+WM13 回写测试按 T16 新语义改写）；BDD 51 steps/849 skipped 与基线一致；tsc 0 错 | 逐卡 hash：T11 dep=None ce66442/T12 押金扣光拦截 b31caac/T13 预约占额度双向 0b4d9d6/T14 付费 cancel 守卫 03290be/T15 fee Decimal e4cf901/T16 活动退款统一台账 1199698+bb0493c/T17 活动域三锁 ac8883f/T18 僵尸单条件 UPDATE 623b9ef/T19 过期任务行锁 253bd7c/T20 逾期名单守卫 c0ccf35/T21 renew 锁 02d62bc/T22 解锁时序统一 4c5753b/T23 alembic import 87fd6e8/T24 台账先扣后记+review 锁 3b68446/T25 报告图归属校验 f64aec4/T26 前端专项 71067a9/收敛 d78cb1d；**T23 误报证伪**（drop 地雷不成立——from-import 加载全模块三表一直在 Base.metadata，autogenerate 空迁移实证，补 import 保留为防御性）；**T16 行为变更声明**：approve≠翻状态（rr→approved+e 保持 refund_pending，execute 联动后 refunded；REF_ACTIVITY 回写收口移 execute 侧，踩教训 41 同族 autoflush COUNT 坑已修）；T14/T17 两条回退 stash 实证 RED，T15/T23 守卫性质未出 RED（如实声明）；三口径默认值：T16 方案 A/C-13 音频门禁归 WM12 前置/B-10 lost 不做（B-12 已兜底）；**等用户目视**（E-04）：RefundCenter 断网假 0、活动退款新链路文案+拒绝必填原因、小程序 error-view 三页、首页预约徽标；不 push 等请示 |

## 待办（非模块主线）

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| T1 | OrbStack 安装 → MySQL 3306 → 真实库冒烟 | DONE | 2026-08-22 完成，旧服务已清 | |
| T2 | 按图施工手册（docs/07） | DONE | 2026-08-28 产出并入库（413 行；含第十二章 UX 增强基线，commit d757511） |
| T3 | 微信资质确认（小程序主体/类目/商户号/订阅模板） | PENDING | 甲方跟进，阻塞 WM12 |
| T4 | 需求签字状态跟踪 | PENDING | P3+ 业务域以签字为前提 |
| T5 | PRD 复审补充任务（P0×4/P1×6/P2×10/债×4） | IN_DEV | 详单 docs/09-补充任务清单.md；P0/P1 已全部修完（门禁证据见 gate-runs/ 最新日志）；D3 已修（CI 重写，首绿 run）；剩余 P2×8（C2/C3/C5/C6/C9/C10/C11/C18）与 WM11 统筹 |
| T6 | 目录清理与文档债（任务包 20260831） | DONE | 2026-08-31 完成：删无引用 png、paint-v3/v4 与两过程报告入 legacy-attic、docs/04 补 miniapp v5/v6 验收表（14 行）；纯文档 commit |
| T7 | 外部专家全量审查 + 专家资格交接 | DONE | 2026-08-31：外部专家全库审查产出 16 文件修复包（外部专家意见/20260831-全量审查修复包/，~100 卡）；前任专家抽验 13 项声明 13/13 实锤后完成资格交接（docs/专家交接书-20260831-资格交接.md）；P0 安全五项 + P1 资金行锁十项待投开发模型 |
| T8 | 第三批合成 + 第二批专家挂账（2026-09-03 严审 A 裁定留痕） | PENDING | **第三批任务源**：B-3 WithdrawalRequest ALLOWED_TRANSITIONS 声明 / F 域其余 LOW / 域 G/H 分流 / C-13 音频门禁归 WM12 前置 / B-10 lost 不做不变；**专家三项挂账**：①cancel_activity 半程加固——批量循环内逐单 refund_svc.apply()（内有 commit），第 K 单失败前 K-1 单已提交且重跑被"活动状态不可取消"挡住，修复方向 apply 加 skip_commit 或 commit 移循环外；②教训 41 防呆（E-20260901-04 铁律）——"flush 后 COUNT"进 verify_architecture 或测试基建自动检测（该族第二犯，首次靠提示词失败）；③新教训入库：复合声明（事实+推断）必须逐项拆验——T23/D-1 误报根因（外部专家验了"缺 import"事实未拆"DROP TABLE"推断，在任专家采信时同犯） |
| P0 第三批 | 第三批 9 卡 T27-T35（Spec 回填+契约加固+挂账清偿；任务包：外部专家意见/20260903-外部顶级专家审查/13-第三批任务包-20260903.md） | ACCEPTED | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-p0batch3-fix1.log，**363 passed / 82.00%**，基线 352→363 只增；首轮 FAIL×1 lint/format 收敛 120cc85）；BDD 51 steps/849 skipped；架构关含 T32 新扫描；[8] 契约快照检查生效 | 逐卡 hash：T27 契约快照 0cf99af/T28 docs-18 规范 94ca03d/T29 sweep+域H 22 项处置 9783395/T30 H-4 矩阵双向 3b20eb8+t30b/T31 cancel 原子性 8d27836/T32 防呆脚本 147e85f/T33 状态机声明 6dac8e7+t33b+c/T34 F-LOW+域G 75bce0a+714c03c/T35 教训入库 02b813b/收敛 120cc85+4a9ac7c；T27 拦截验证实证（改字段→红→恢复→绿）；T30 预埋坑位（skip_lock_check 豁免）按卡处理+REFUND_PENDING 委托链冲突首轮回归抓出修复；T32 告警 2 处自查均误报（跨表/commit 后快照）留痕；**演示救火副产物**：seed_demo 幂等演示数据入库+dev.sh 集成（"图书馆空"根因=pytest 与 dev 共库 gate TRUNCATE 清业务表，dev.sh seed 从不重建业务数据）；F-LOW 10 修/1 证伪/7 登记；域G 2 修/1 关/1 转 WM12/8 挂账；域H 22 项处置表 docs/09 §十（需决策 10 项待用户/专家裁决）；**2026-09-04 用户验收通过（第三批通过）**：纯基建批，验收口径=T27 快照机制知悉+T30 矩阵双向拦截+T31 断点续跑；严审 A-（13 号任务包补刀 46a326b+覆盖率口径 seed_demo 0% 代录）；gate [9] 交付完整性防呆 7e69926 落地；E-20260903-03 入库（6063d86，含双 seed 重复造轮子案——用户指正）；CI run 33828538946 绿 |
| 插修 5 | 客户演示反馈 bug 批 9 卡 B1-B7/D8/D9（任务包：外部专家意见/20260903-外部顶级专家审查/14-插修5-演示反馈bug批-20260904.md） | 部分通过（B2-B7 用户实测通过 2026-09-04；B1 两处返工挂插修 6——R1 跳转缺 child_id、R2 弹窗改测验状态门控） | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-fix5.log，**369 passed / 82.48%**，基线 363→369 只增）；BDD 51 steps/849 skipped；[8] 契约快照一致（B7 两步显形首战）；[9] 交付完整性 PASS；tsc 0 | 逐卡 hash：B6 守卫值比对 21a7e4d/B5 收藏软删复活 28ab025/B4 确认收款刷计数（B1+B2+B3 合刀 3862441）/B7 预约搜索+全部 e742ce8+契约刀 58f0414+b7b/D8 需求登记+D9 裁定落痕 5b65b0a+d9b 1c5f45d；B2 同族 sweep 5 处（2 修/3 兜底式合法留用）；B1 数据流选择=消费 loadProgress 后 finished 不消费 just_finished（覆盖全路径）；func.or_ SQL 语法错红测试当场抓改 sqlalchemy.or_；D8 Gherkin 存 docs/03 附录（behave 无步骤定义报 error，任务卡预警条款）；**目视清单 7 项待用户真机/浏览器**；不 push 等请示 |
| 插修 6 | B1 返工两卡 + FEAT-080 提前实现（任务包：外部专家意见/20260903-外部顶级专家审查/15-插修6-B1返工与订单类型扩展-20260904.md） | ACCEPTED（2026-09-04 用户目视基本通过 + 专家严审 A 放行；R2 弹窗逻辑正确但触发链断裂、R4 URL 残渣——两项由插修 7-R5/R4 收口） | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-fix6.log，**373 passed / 82.53%**，基线 369→373 只增）；**BDD 67 steps（基线 51→67，order_types 四场景 16 steps 正式接线）**；[8] 契约快照两步（contract-change 9498510）；[9] 交付完整性 PASS；tsc 0 | 逐卡 hash：R1 跳转补 child_id+sweep（ccd819e，quiz 入口 3 处 2 漏）/R2 弹窗测验状态门控（后端语义实锁：failed=次数尽，仅 available 弹，文案带剩余次数）/R3 FEAT-080 六类订单 e09ad39+契约 9498510（押金 confirm 走 billing 既有分支激活 Deposit+Ledger/活动单不联动报名边界默认值/自定义单不参与会员计算灵魂断言；func.or_ SQL 误用红测试当场抓改 sqlalchemy.or_；BDD 移入 features/ 16 steps）+FEAT-080 REGISTERED→IMPLEMENTED+docs/03 附录移除；插修 5 状态改部分通过（B2-B7 过/B1 返工挂本批）；**目视 3 项待用户真机**：①R1 读完→去测验→出题 ②R2 已读完再读完→弹（剩余次数）③R3 三种单创建全链；2026-09-04 用户目视基本通过 + 专家严审 A 放行，状态转 ACCEPTED |
| 插修 7 | 再读弹窗与续播伪音三卡 R4/R5/R6（任务包：外部专家意见/20260903-外部顶级专家审查/16-插修7-再读弹窗与续播伪音-20260904.md） | MANUAL_TEST | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-fix7.log，**373 passed / 82.53% 基线持平**——miniapp-only 改动不触后端覆盖率/admin tsc）；BDD 67 steps/849 skipped 不降；[4] 架构关 PASS（教训 41 告警 2 处存量自查留痕）；[8] 契约快照一致；[9] 交付完整性 PASS（16 号随批入库） | 逐卡 hash：R4 严审返工**已在插修 6 刀 8fc8e48 修净**（本批实读核验 book-detail.js:158-159 与任务包修法一字不差，零新改动）/R5 onPlay 无条件重设会话起点 4448ac3（16 号任务包随刀入库；根因=首次读完 onEnded 尾部推进 _sessionStart=总长，ended 后再 play 微信从头播 currentTime=0 而 null 守卫不重置 → 再播完 endPos>sessionStart 恒 false → 上报+测验引导弹窗链整体跳过；四推演逐条自查：①首次播放 currentTime=0 行为不变②ended 再 play 起点 0 播完 300>0 弹窗恢复③暂停续播 onPause 已推进至暂停位置 onPlay 重设同值区间连续④seek 后 onSeeked 已重设 onPlay 同值幂等）/R6 已读完的书不续播 2459abb（_applyResume 续播条件加 !finished；时序安全：loadProgress 先 setData(finished/lastPosition) 再调 _applyResume，onCanplay 早到时两值均初始态不误 seek，_resumeApplied 幂等标志保留）；过程偏差如实报：R5 首刀 git add 整文件误带同文件 R6 改动致 R6 刀落空——soft reset 拆刀重做（R5/R6 各自独立 hash）；明确不做：slider 末帧 ~99.7% 物理现象/F-LOW/H-1/M-2 归第四批；**目视 3 项待用户真机**：①已读完未通过的书→进度从 0→从头听完→弹窗带剩余次数（R5+R6）+详情页"去测验"出题（R4）②已通过的书（演示孩 book 1/2/3）再读完不弹③未读完中途退出的书再进仍续播（R6 回归）；2026-09-04 用户目视基本通过 + 专家严审 A（12 刀快照）放行——LEDGER 转刀随批 push 共 13 刀 + CI 盯测 |
| 插修 8 | 儿童测验成就徽章 FEAT-081（任务包：外部专家意见/20260903-外部顶级专家审查/17-插修8-儿童成就感徽章-20260904.md） | MANUAL_TEST | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-fix8.log，**373 passed / 82.53% 基线持平**——纯 miniapp 前端）；BDD 67 steps/849 skipped 不降；[8] 契约快照一致；[9] 交付完整性 PASS；gate PASS 分支双 seed 恢复提醒行首跑生效（E-20260903-04 防呆落地） | 逐卡 hash：U1 三态徽章 c7ad5b8（17 号任务包随刀入库；金卡 🏆+金橙渐变 #FCD34D→#FF8A5C+星级档位 ≥90 五星/≥80 四星/≥70 三星/≥60 两星/其余一星+鼓励语随机 3 条+点击→quiz-result 成绩单；蓝卡 #60A5FA+剩余次数+最佳分+pulse 脉冲按钮；灰态锁定引导；loadQuizStatus 用 getQuiz 现成字段零后端改动，onLoad/onShow 双接线测完返回徽章即时切换；原 quiz-tag 退役）/U3 完播弹窗文案统一 6093555/U2 书架角标**移出本批**（需后端批量接口 GET /quiz/status-batch 挂第四批——防 N+1 防扩散，详情页才是成就感主舞台）；边界决策声明：①任务包三态字面 available&&used>0 会把"读完未测"（used=0）落灰态'听完解锁'语义错误——实读后归蓝卡文案自适应（轮到你挑战啦/开始挑战！+共 N 次机会）②failed 态任务包未显式提——归灰态文案分支（机会用完了请老师重置）③wxml 三态互斥链首轮自抓 bug：金卡误用 progress.finished 做条件会让读完未测的书落 hidden 金卡致三态全灭——改纯 quizStatus 判定；验证：node --check 双文件+C 级 gate+docs/03 FEAT-081 登记；**目视 5 项待用户真机**：①已通过的书（book 1/2/3）金卡 🏆+分数+星级 ②未通过有机会蓝卡+再挑战按钮+脉冲 ③未读完的书灰态引导 ④测完返回详情徽章即时切换 ⑤完播弹窗新文案；**部分通过（2/3/4 用户实测通过；1/5 返工挂插修 9——1 金卡 0 分假数据根因/5 成绩单跨书污染）** |
| 插修 9 | 假数据清剿+PASSED 印章+成绩单跨书污染 R7/R8/R9/D10（任务包：外部专家意见/20260903-外部顶级专家审查/18-插修9-假数据清剿与PASSED印章-20260904.md） | MANUAL_TEST | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-fix9.log，373 passed / 82.53% 基线持平——seed 脚本+miniapp 前端）；BDD 67 steps 不降；[8] 契约快照一致；[9] 交付完整性 PASS | 逐卡 hash：R7 假数据清剿+真链路三态 bf75c79（18 号随刀入库；dev 库 SELECT 实锤假 WordsLedger 恰 3 行 16:02:17 同秒→DELETE 3，book 6 用户真实全链分毫未动；seed_wm11_demo 新增 _ensure_demo_quiz_journey——title 定位+三表一致+时间戳错开禁同秒，Brown Bear 5/5 通过+120 词→金卡 100 分五星/Chicka 3/5 未过→蓝卡还剩 2 次/Corduroy 读 60%→灰态；_ensure_demo_growth 删单表直插段+words_total 真实口径；E-20260904-01 入库 e145e0b——用户方法论'我们要造数据，就要造有真实链路的'）/R8 PASSED 印章 7764258（64rpx/900/白描边/rotate(-6deg) 歪斜感+深棕 #6B4A12 副字+星级 40rpx；徽章区外提独立 block 首进即显示——passed 不再依赖 progress 存在，灰态无 progress 文案降级消'已听 undefined%'隐患）/R9 跨书污染 d545df7（缓存键 per-book quiz_result:childId:bookId+金卡跳转带书参+quiz-result 无缓存 getQuiz 服务端兜底构造只读成绩单带'本机无本次作答记录'提示；旧单键自然废弃）；四表对账实贴（gate 后双 seed 重跑，2026-09-04 16:52）：words_ledgers 仅 book1(120, seed 真链)；quiz_attempts book1 5/5 通过+book2 3/5 未过（时间戳 11:52 与 words 13:52 错开）；reading_progress book1/2 finished=1、book3 finished=0 cov 3/6（灰态显示"已听 50%"——6 秒演示音频整数秒无法精确落 60%，50% 为最接近稳定值，目视③口径以此为准）；**book 6 用户真实记录已被 gate 清库抹掉（pytest/behave TRUNCATE 与 dev 共库固有代价）——目视④的 book6 半边改为"现场真测任意一书"验证各归各**；**目视 4 项待用户真机**：①Brown Bear 首进即 PASSED 印章金卡+100 分五星 ②Chicka 蓝卡'还剩 2 次' ③Corduroy 灰态→完整听完→弹窗（假 passed 清除后恢复）④点 Brown Bear 金卡 120 词/book 6 金卡 480 词各归各；插修 8 三刀（c7ad5b8/6093555/9c24c10）随本批一起 push；**目视返工追认（专家 19 号 T-A）：R10 金卡百分制口径 + 兜底真实到账，da5b1ff/ae79db1/红测试 2 例（改名 test_fix9_r10_quiz_badge_fields.py）追认入册，'返工也走任务包'纪律入册（494b9ca）** |
| 19 号小批 | 插修 9 追认收口 + seed 积分时序修复 T-A/T-B/T-C（任务包：外部专家意见/20260903-外部顶级专家审查/19-插修9追认与seed积分时序-20260905.md） | **ACCEPTED**（2026-09-06 用户目视 Brown Bear 成绩单 +120 词/+10 积分通过——+0 闭环收口） | 2026-09-06 C 级全量 gate PASS（gate-runs/2026-09-06/gate-fix19.log，**376 passed / 82.53%**，基线 375→376 只增——T-B 红测试 1 例）；BDD 67 steps 不降；[8] 契约快照一致；[9] 交付完整性 PASS；**gate 自动恢复机制首战成功**（PASS 后检测本地 dev→自动 restart+双 seed，"现场已自动恢复 ✓"）| 逐卡 hash：T-A 追认归位 494b9ca（测试改名 test_fix9_r10_quiz_badge_fields.py/'返工也走任务包'纪律入册/LEDGER 插修 9 行追认尾注）/T-B seed 积分直挂 fbe4d14（教训 41 同族第三犯：先插后补在 autoflush=False 下单次必扑空、昨日 10 系双跑巧合——INSERT 直挂 related_id+UPDATE 段转存量自愈+红测试 test_fix19_seed_points_related_id.py 先红后绿；**debug 加抓脏库错位**：pytest 清库后测试残留书占前位致 books[0] 锚点与 journey title 定位错位→积分挂空——统一锚点 Brown Bear title 定位；dev 真库终验 POST-AUTO-RESTORE：passed/100/120词/10积分 单跑成立）/T-C journey 逐书递增 3h（fbe4d14 同刀，book1/2 同秒清偿，跨表+同表全错开）；**T-B 过程如实报**：debug 期间多次误读（测试残留行当演示数据/utf8mb4 缺失致 DELETE 0 行）——共库清表+双 seed 完整性认知升级：跑过 pytest 后必须 dev.sh 完整 restart 而非单跑 seed_wm11；**用户目视 1 项**：Brown Bear 金卡成绩单 +120 词/+10 积分（+0 闭环）；**2026-09-06 目视通过转 ACCEPTED**，push 3 刀+CI 盯测 |
| 插修 9 目视返工（**19 号 T-A 追认归位 2026-09-06**——曾误自号"插修 10"，dev 无权自开专家编号体系，纠错留痕 9bfcae5） | 用户目视插修 9 实锤两 bug：①金卡'最佳成绩 5 分'+一星②兜底成绩单 +0/+0 与护照片 +120 词矛盾；另专家严审小瑕疵留痕：book1/2 QuizAttempt 同秒批量插入痕迹（T-C 已清偿） | ACCEPTED（2026-09-06 用户目视 +120 词/+10 积分闭环 + 专家 19 号 T-A 追认） | 2026-09-04 C 级全量 gate PASS（gate-runs/2026-09-04/gate-fix10.log，**375 passed / 82.53%**，基线 373→375 只增——红测试 2 例）；BDD 67 steps 不降；[8] 契约快照一致（**get_quiz 为 dict 返回端点无 response_model，字段级变化不进 OpenAPI schema——T27 机制边界留痕，无 contract-change 刀**）；[9] PASS；gate 后双 seed 恢复 | 返工刀：后端 da5b1ff（get_quiz 响应补 best_percent（按最佳 attempt total_questions 百分制换算——best_score 题数口径是'5 分一星'根因）/words_added（WordsLedger 真实入账）/points_added（PointLedger related_id=book_id 三类求和——真实链路入账全挂 related_id 实锤 growth/service L133/L159/L168）；红测试 test_fix10_quiz_badge_fields.py 2 例先红后绿（5/5→100 分档+3/5→60 分档零入账）；seed 演示积分补 related_id=book_id UPDATE 幂等收敛）/前端 ae79db1（book-detail loadQuizStatus 消费 best_percent 旧响应容错换算；quiz-result 兜底对象 words_added/points_detail 服务端真实值）/LEDGER 首刀 aebd9a2（冒号原刀，本刀正名）；dev 活体验证实贴：book1 passed/best_percent=100/words_added=120/points_added=10（=5+3+2 related_id 求和命中）、book2 available/60/0/0、book3 locked/0/0/0；**待专家任务包**：①修法是否认可/需返工 ②同秒瑕疵是否要求处理 ③红测试文件名 test_fix10_ 前缀是否随正名调整；复测 2 项（金卡 100 分五星/兜底 +120+10）待用户在专家正名后进行；**2026-09-05 补记**：首轮复测成绩单 0/0 根因=dev 后端旧进程未重启（start 幂等跳过，E-20260905-01 入库）——dev.sh restart 后 HTTP 活体验证 best_percent=100/words_added=120/points_added=10 全中，用户可直接复测（小程序无须重编译）；gate PASS 提醒行已改 restart；**2026-09-06 终态 ACCEPTED**：19 号 T-A 追认（测试改名 test_fix9_r10_*）+ T-B seed 直挂/锚点统一 + T-C 同秒清偿（fbe4d14），用户目视 +120 词/+10 积分闭环收口 |
| 插修 10 | WM13 复测反馈批 7 卡（任务包：外部专家意见/20260903-外部顶级专家审查/20-插修10-WM13复测反馈批-20260906.md） | ACCEPTED（2026-09-07 用户目视实测通过+专家严审判 A·push 收口） | 2026-09-06 C 级全量 gate PASS（gate-runs/2026-09-06/gate-fix20.log，**382 passed / 82.63%**，基线 376→382 只增——红测试 6 例：T20a aware 时区 2/T20b 回归锁定 2/T20c 手动标记 1/T20d 联动灰态 1/T20g read_only 改写 1）；BDD 67 steps 不降；[8] 契约快照一致；[9] 交付完整性 PASS（20 号任务包随刀入库）；gate 自动恢复现场 ✓ | 逐卡 hash：T20a `e1ea0f4`（ActivityCreateRequest field_validator 剥时区 start_at/enroll_deadline——aware TypeError 500 实锤；全库 sweep：toISOString 仅 ActivityManage 两处、aware 比较风险仅 activity create 入参 L147/150）/T20b `44ea174`（前端 hasPermission(permissions,'audit.view') 条件请求退款列表+staff 无退款 tab；签到装饰器 member.manage→borrow.operate **诊断偏差如实声明：任务包'仅超管'有误——签到端点实际 member.manage，staff 从未受限，本子项语义对齐行为零变化，红测试降级现状回归锁定（grill Q1 专家裁 A 认账）**；退款审核保持仅超管 Q10）/T20c `5017e3a`（resolve_many 人工标记覆盖机器推导——**实现偏差声明：条件覆盖仅限 pending 推导**，因 mark_handled 回写（家长撤销/终态）也写 handled_at，无条件前置会把'已失效·家长已撤销'错误覆盖（test_s1_orphan_* 实锤）；注释推翻专家批准：admin_notification_models 'handled_at 不参与显示态'口径改写+alembic 迁移 10d43873633c）/T20d `cfd9dbd`（_decide_withdrawal 判 source——联动单 ST_DONE+'联动处理中·随退款自动推进'+linkage=True 灰态不入计数，**linkage 字段方案专家批准**；前端灰 Tag+灰行+无按钮由 done 态自动满足；直接退会单保持待处理）/T20e `03ed120`（box 默认 admin URL 显式 parent 尊重+管理待办红底白字胶囊 D1 同款+会员管理 order_pending_manual 徽标）/T20f `2b38a87`（REF_META 退款中心三类补 tab+highlight——两入口 URL 一致）/T20g `6dfe7a1`（list_inbox staff 分支全量+read_only=true，handle 权限矩阵不动；前端 read_only 驱动提示条+去处理→查看+标记已处理隐藏；**test_s2 行为变更改写声明：'staff 空数据'→'staff 可见只读'**）；#4 澄清非缺陷跳过；**目视清单待用户双视角**：超管 6 项（发布/胶囊/默认 tab/联动灰态/查看关联高亮/手动标记）+ staff 3 项（无 403 能发布签到/只读视图/侧边栏徽标） |
| 插修 11 | 目视返工批 6 卡（任务包：外部专家意见/20260903-外部顶级专家审查/21-插修11-目视返工批-20260907.md，随刀入库） | ACCEPTED（2026-09-07 用户目视实测通过+专家严审判 A·push 收口） | 2026-09-07 C 级全量 gate PASS（gate-runs/2026-09-07/gate-fix21.log，**385 passed / 82.64%**，基线 382→385 只增——R2 红测试 3 例：管理端无报名 confirm 404→200 实锤/免费单/家长链回归）；BDD 67 steps 不降；[8] 契约快照一致；[9] 交付完整性 PASS（首轮 FAIL 即 [9] 防呆命中 21 号任务包 untracked——入库后 PASS，防呆机制有效实证）；gate 二轮 FAIL=ruff 测试文件格式（如实记录）；**docs/19 标准收尾首战**：gate 自动恢复 ✓ + 三项核验通过（书目 35/家长 4/孩子 9——WM13 演示链为预期态非残留，后端进程 11:12 新启/health ok/admin 200） | 逐卡 hash：R1 `90814df`（mirrorUrl box 显式写——T20e 默认反转后'省略=parent'旧假设失效致家长 tab 弹回，#6 无法点击根因）/R2 `90814df`（confirm_payment 活动单按有无报名分流——R3 设计矛盾专家认账：管理端直建单无报名纯入账 FEAT-080，家长报名链联动转正照旧；红测试 404→200 实锤）/R3 `47e14d3`（staff 待办'查看'=行展开详情不跨页——不跳退款中心无 audit.view 即 6 连 403；**RefundCenter 权限门**：无 audit.view 渲染无权访问页不发请求，直输 URL 也根治）/R4 `3688de5`（联动退会单超管'查看详情'——admin_list 补 source 字段+settle-preview 只读弹窗复用 X2 同源，无通过/拒绝灰态不变）/R5 `c3dc92d`（高亮 3 秒真修**双修策略**：时序起算修正为主嫌疑[挂载即起算烧窗口]+CSS 深层选择器对抗加固[内描边兜底]——真凶定性待目视，简报声明）/R6 `fa77488`（退款中心徽标双挂：侧边栏=refund_pending+transfer_pending 退会不计[用户口径]+内部退款/转让 tab 红底白字胶囊 D1 同款）；**专家'格式化补刀未提交'口径纠偏：e92995d 上轮已提交**（8 文件含迁移）；**目视 6 项待用户**：①家长通知 tab 可点可停留 ②管理端活动单确认收款成功 ③staff 待办查看→行展开无 toast ④联动单超管查看详情按钮 ⑤查看关联→高亮 3 秒 ⑥退款中心侧边栏徽标+双 tab 胶囊 |
| 需求登记批·阅读圈（FEAT-084，2026-09-10） | 阅读圈需求登记（类朋友圈成就分享，P1 增长激励）：一期 MVP 6 类卡片（A 类终身卡：里程碑/读本数/等级/满分/连击 + 完读卡）全部后端生成**零 UGC**；双署名「家长显示名 · 孩子英文名」（孩子侧 R-317/318 口径）；发布=家长主动授权晒（双入口：事件触发「晒成就」+护照「我的成就」补晒）；仅点赞无评论（微信审核红线）；防滥用：同成就终身一晒+每日限晒 2 帖+card_data 快照冻结+删除权仅家长/超管；通知 circle.liked（馆长赞特殊文案可关闭）；管理端阅读圈模块（未赞徽标/行内馆长赞/置顶/删除填原因/运营概览）；审核受阻降级预案=荣誉墙模式 | REGISTERED（等 WM14 开发） | 纯文档+@draft 草稿零门禁（分级纪律）；behave features/reading_circle.feature --dry-run 通过（不动 DB）；git diff 逐文件核对 | 五件：①docs/03 新增 F14 小节+FEAT-084 行（含技术概要：reading_circle 域/Post+Like 表/Pillow 模板引擎复用 gen_cover/约 5 API+管理端端点，新卡片图片字段须媒体消费点清单化）②docs/04 新增 §七 WM14 验收章节（A 期 20 步：用户侧 13+管理侧 7）③features/reading_circle.feature @draft 六组场景（behave.ini ~@draft 排除，D8 先例零门禁）④PRD 新增 §7.5（变更记录声明第十三章第 1 项"阅读成果分享社区"解禁落地；不引入新 R 编号、不刷附录 A——复用 R-317/318 口径）⑤本行留痕；二期挂账 WM14-B：排名/周报/突破卡+社区横幅（依赖每周一排行榜快照，APScheduler 已有基建） |
| WM14-A 阅读圈一期 MVP（FEAT-084，2026-09-10） | 阅读圈一期全量：6 类成就卡（后端 Pillow 生成零 UGC）+ 双入口晒卡（成绩单页按钮 / 阅读护照「我的成就」+ 阅读圈页顶）+ 信息流（时间倒序真分页 + 下拉刷新 + 全屏预览长按保存）+ 一心一赞（原子计数 + 软删复活）+ 被赞通知 scene=circle.liked（馆长赞特殊文案）+ 管理端模块（帖子管理 / 行内馆长赞 / 置顶 / 删除必填原因 / 未赞徽标 + 侧边栏徽标 / 运营概览） | ACCEPTED（2026-09-10 用户实测通过；验收反馈 3 项缺陷——侧边栏菜单溢出不可滚 / 帖子搜索署名丢弃孩子中文名 / 审计日志 500——均由 fix29 三修收口） | 2026-09-10 全量 gate PASS（gate-runs/2026-09-10/gate-fix28b.log（终轮，与提交树一致；首轮 fix28.log 同值），**420 passed / 82.77% 覆盖率 / BDD 103 steps 不降**；9 步全绿含架构关 PASS＋[8] 契约快照一致＋[9] 交付完整性 PASS；基线 410→420 只增，覆盖率 82.93%→82.77%（-0.16pt 系新域稀释，gate 阈值 25% 通过）；pytest 由 1440s 降至 1017s 系测试 7 改 1 家长 4 孩子复用单次登录） | 接手施工（dev B，批号 fix28）——dev A 首轮实现经 18 问专家裁决修正：Q9 唯一索引降级 ix_circle_post_achievement（普通索引 + 服务层 is_deleted=0 查重，删除恢复晒权，BorrowRecord 先例）/ Q10 完读卡 ref_id=book_id / Q8 置顶 FOR UPDATE 行锁 + list_posts 多条置顶自愈（不静默丢帖）/ Q15 小程序 DELETE 去 body / Q7 时区口径注释（Asia/Shanghai，禁 utcnow）/ Q5 完读弹窗还原「去测验/先不」+ 晒卡入口移至成绩单页按钮（显示条件=有可晒未晒卡）+ share 页 focus 定位 / Q3 tab 位口径校准（阅读圈第 4 位、我的最右）；测试 410→420（wm14 由 7 例扩至 10 例：删后重晒 / 多条置顶自愈 / finish_book ref_id 语义）；文档校准 docs/03 FEAT-084→IMPLEMENTED + docs/04 §七 去占位并校准 + PRD §7.5；活体全链 7 步（my-cards→晒卡→列表置顶置首→卡图 200/54844B→家长赞→馆长赞[双文案通知]→DELETE 无 body→删后重晒同成就 200） | **三处甲方可推翻项**：Q3 tab 第 4 位 / Q5 弹窗保两键 / Q11 家长显示名直接用 Parent.name（无隐私开关）；A 期挂账两项（家长称呼 display_name / 孤儿卡片图清理）均已在 WM14-B 交付 |
| WM14-B 阅读圈二期（FEAT-084，2026-09-10） | 六件主线：①周榜快照基建（新表 circle_rank_snapshots + 任务 circle_rank_snapshot，口径复用 LeaderboardService.period_entries——计数同源）②上榜卡 rank_top（周榜 TOP10）/ 上升卡 rank_up（较上周名次上升，新上榜无 baseline 不算）③周报卡 weekly_report（上一完整自然周，区间口径复用 ReportService.range_summary）④突破卡 breakthrough（单日词数新高；新配置 circle_breakthrough_min_words 默认 1000）⑤社区横幅（list_posts 加 banner，口径同周榜 active_only）⑥display_name 家长展示称呼（Parent 新列 + PUT /api/miniapp/parent/profile + 三消费端统一 _parent_display）；三件清偿：C1 docs/03 旧 tab 口径 / C2 补测（assemble 6 类字段 + 运营概览五指标）/ C3 孤儿卡片图清理任务 circle_image_cleanup（30 天阈值 + 路径穿越防御） | GATE_PASS → MANUAL_TEST（待用户按 docs/04 §七 7.3 共 7 步验收，步骤 21-27）→ **ACCEPTED（2026-09-11 用户实测 7 步全过：横幅数字/四类新卡/晒卡/称呼编辑三消费端/管理端搜索）** | 2026-09-10 全量 gate PASS（gate-runs/2026-09-10/gate-fix30b.log，**430 passed / 83.15% 覆盖率 / BDD 103 steps 不降**；覆盖率较 A 期 82.77% 回升 +0.38pt 并超过 WM14 前基线 82.93%；9 步全绿含架构关 PASS＋[8] 契约快照一致＋[9] 交付完整性 PASS）；三项核验：书目 35 / 家长 4 / 孩子 12 / 活动 2 + 快照 5 条；活体：手动触发 circle_rank_snapshot→success（processed=0 幂等）+ circle_image_cleanup→success；banner {words:750, kids:3}；演示孩四类新卡枚举 + 晒上升卡 200 + 卡图 200/48350B | 新增配置键 circle_breakthrough_min_words（dashboard 断言 39→40 同步）+ 任务数 13→15（wm11_tasks 断言同步）；测试 421→430（wm14 由 10→19 例）；**施工期偏差如实**：B 期测试为「完成后补写」而非红先绿（A 期与 fix29 均红先绿）；scheduler 是 interval 触发器故新任务无固定 cron 时刻（周任务靠唯一索引幂等、清理任务 30 天阈值下多跑无害）；seed 演示数据一律取书目真实 word_count（真链路，禁自定值） | **三处甲方可推翻项**（同 A 期）：Q3 tab 第 4 位 / Q5 弹窗保两键 / Q11 家长显示名默认用真实姓名；遗留：WM14-C（活动纪念卡等）远期按需 |
| fix33 四项修复（专家 2026-09-11 下发，**已交付待复验**） | 用户实测 WM15-A 反馈四项：**R1** 缩略图与完整版同构图（`_render_thumb` 复用 `_render_full` 管线去文字层 + seed 重渲回填）；**R2（P0 数据模型）** 社交主体切孩子——唯一索引改 `(post_id, child_id)` + `child_id` 列新增 + like 必传 child_id（可选→必填 breaking）+ liked_by_me 按当前孩子 + 历史迁移（liker_child_id 回填、NULL 行软删）；**R3** 取消赞后头像墙不消失（circle.js 取消分支也刷新）；**R4** 管理端会员列表加头像列 | GATE_PASS → MANUAL_TEST（待用户按 docs/04 §8.3 第 17-23 步复验）→ **ACCEPTED（2026-09-11 用户复验通过；口头反馈"缩略图与大图仍有割裂感，先这样"——视觉连贯性优化挂 WM15-B）** | 三刀未 push：`be046b6`(R1+R2 后端) → `953a65c`(前端 R2/R3/R4) → `2be5418`(T27 契约快照第二步)；全量 gate **一次过** `gate-runs/2026-09-11/gate-fix33a.log`（**448 passed / 83.38% / BDD 103 steps / 9 步全绿**，基线 443→448＝fix33 新增 5 例）；三项核验：35/4/12/2 + 帖 3（**三帖 thumb 全为 v2 规格**，证明 seed 重渲到位）+ 快照 5 + 头像 6；活体：无 child_id 点赞 → 422「请先选择孩子」（PydanticCustomError 无 `Value error, ` 前缀）、带 child_id → 计数/头像墙/liked_by_me 同步、**同家长两孩各赞同一帖 → 计数 2 且墙上两人**（旧 (post,parent) 唯一索引下不可能）、切孩 liked_by_me 各自独立、B 取消只掉 B（A 的赞不动）、浏览态（不传 child_id）liked_by_me 恒 false；迁移 `c3a9e5b1d7f2` 已做 **downgrade↔upgrade 往返验证**（9 行数据两向无损）；R1 对比图三卡型（旧/新/完整版）judge **4/4 PASS** | commit 前缀 `fix33-`；T27 两步显形已走（代码刀 + 快照单独刀）；**禁 push**；测试增量 5 例见 `tests/unit/test_fix33_circle.py`（兄弟同赞两行/切孩隔离/缺 child_id 422/迁移回填+软删+扣减幂等/缩略图规格重渲幂等）；**口径变更提示**：老版本小程序端（不带 child_id 的点赞）从此 422，属专家认可的 breaking；**已知技术债**：`card_engine.py` 795 行贴 god-file 800 上限（仅 5 行余量，下次改动须先拆渲染段出去） |
| fix34 / WM15-B 割裂感根治 + 头像框 + 播报 + 生日彩蛋 + 朋友圈式红点（2026-09-11） | 七件：**R0** tab 红点跨实例失效修复（微信自定义 tabBar 每页各一实例，原只在 attached 拉一次→红点只活在首页）+ 改朋友圈式「谁赞了你」（进阅读圈顶部通知条 → 点开看到谁赞了 → **看完即消**）；**R1-C** 卡片改版式（上部方形插画区含主数字+吉祥物零文字 + 下部文字条；缩略图 = **插画区裁切**，点开=同一画面放大）；**R2** 海报 PNG 723KB→JPEG 57KB；**R3** card_engine 795 行 god-file 拆分（渲染段→card_render.py）；**R4** 等级头像框四档（A-B 星芒/C-E 银环/F-H 金冠/I+ 彩虹）+ 五端叠层（信息流/名片页/点赞墙/榜单/我的页）；**R5** 全馆达成播报（里程碑副标题「全馆第 N 位达成」，晒卡时冻结）；**R6** 生日彩蛋（is_birthday 布尔，不泄露日期）；**R7** 文档 + 三项核验基线订正 | **ACCEPTED**（用户 2026-09-11 按 docs/04 §9 实测后回「差不多都可以了」；返工项见下方 fix34b/c/d 行，`fix34c` 用户明言「复验成功」） | gate `gate-runs/2026-09-11/gate-fix34a.log`（**453 passed / 83.46% / BDD 103 steps / 9 步全绿**，一次过；基线 448→453 = fix34 新增 5 例）；三道刀未 push：4090079(R1-C+R4 样图脚本) → 3654b81(BE 数据面) → 1c6d74a(FE 五端+通知条) → f1b416d(契约快照) → 08a8996(架构关返工)；样图门①：judge 卡片 **1 轮 PASS**、头像框 **4 轮返工 PASS**（首版环被头像盖住/装饰只换色/银环档位倒挂/小尺寸糊掉全修）；三项核验：书目 35 / 活动 2 / 帖 3（**三帖缩略图全 v3**）/ 快照 5 / 头像 6（家长 4 孩子 12 系 pytest 残留 +seed，**干净基线=3/11**，已订正 docs/19）；活体：登录 children 带 level、信息流 item.level、点赞墙 likers[].level、榜单 entries[].level、通知 scene 过滤 total=1 且 actor_avatar/actor_level 有值、is_birthday 布尔无日期泄露、海报 image/jpeg **57KB** 且旧 poster_2.png 被自动清掉、里程碑副标题「累计有效阅读词数 · 全馆第 1 位达成」 | commit 前缀 `fix34-`；**禁 push**；开工前 dev 证伪 3 条任务包前提（R1 裁切区几何必然含文字→用户改选 C 案；R4 level 在 5 消费端里 4 端不存在→补后端数据面+契约刀；R5 计数口径由 dev 提案专家确认）；**施工期自纠 2 处如实**：① 起 gate 前跑架构关抓到 common/notifications.py 依赖业务域（真违规）→ 行为主体装饰搬去 identity 域（08a8996）；② `git add` 因含已删除路径整体失败被 `2>/dev/null` 吞掉 → R1 刀只提交了删文件，已 amend 修正（**教训：add 的退出码要看，别吞 stderr**）；**遗留**：tab 红点跨页行为只能在真机/开发者工具复验（已加 console.warn + 保留旧值便于诊断）；卡片缩略图 618×618 方形（信息流 60% 宽显示） |
| fix34b 用户实测三条反馈返工（2026-09-11） | ① **红点仍「切回首页又出现」**（真根因：自定义 tabBar 每页各一实例 + `pageLifetimes.show` 对该注入组件不生效 → 首页实例自 App 启动后再不刷新）→ 改为**五个 tab 页 onShow 各刷一次**；② **五处头像框看不到**（`size="84"` 是字符串 + 只挂 observers 初值不触发 → 框宽 NaN）→ `attached`+`observers` 双挂、`Number(size)` 兜底、wxml 改数字绑定；③ **生日彩蛋没看到**（上批只做后端 is_birthday、**名片页 UI 漏做**，如实认领）→ 补齐 🎂 横幅（看自己/看别人两套文案）+ seed 把演示孩生日对齐「今天」 | **ACCEPTED**（用户重编译后实测通过；随后又提出「收到的赞」逻辑问题 → fix34c） | commit `6e59674`（miniapp 五页 + 组件 + 名片页 + seed；**含一处自纠**：首次提交漏列 member.js，已 amend 补入）；验证：JS 全部 node --check 通过、活体 `is_birthday=True` 且响应无 birthday 字段、五页 refreshBadge 各一处 | 本批为**纯小程序端 + seed** 改动，未跑全量 gate（无 Python/TS 变更）；①属运行时行为，只能真机/开发者工具复验 |
| fix34c 「收到的赞」通知条状态机修正（2026-09-11） | 用户实测：「点开看到 2 个孩子、红点消失，之后条上只剩『谁赞了你』，再点一下整条消失、什么都不显示」。真因是**状态机残留**两条并存：①`likesOpen` 在数据刷新后不复位（离开再回时列表已空、面板不渲染，但条因 likesOpen 仍显示 → 空条）；②列表只保留「未读」→ 读过的赞再也回看不了。修法：列表拿**全量**（含已读），未读数只决定**高亮态**；已读降级为**低调条**「收到的赞 · 共 N 条」→ **永远可回看**；空列表自动收起面板 + 面板空态文案兜底 | **ACCEPTED**（用户复验成功） | commit `154821b`；活体验证：2 孩赞同一帖 → 未读 1/列表全量 → 标记已读后 unread=0 且列表仍可回看 | 纯小程序端改动，未跑全量 gate |
| fix34d 馆长赞排面（2026-09-11，用户委制） | 用户实测「馆长点赞后小程序不显示任何小头像」——真因：馆长的赞只落 `admin_liked=1`+计数，**不建 CircleLike 行**，而点赞墙按 CircleLike⋈Child 查 → 墙上恒空。修法：①新资产 `miniapp/icons/special/{gm_avatar,gm_frame}.png`（金光馆长头像 + 最高档「鎏金冠冕框」，几何与四档等级框一致，**不进** AVATAR_IDS 白名单、仅小程序端消费）；②`_likers_map` 按 admin_liked **合成馆长条目排第一**（child_id=0 哨兵 + is_admin，无迁移）；③前端 avatar-ring 增 `gm` 开关 + 点赞墙馆长条更大/金色光晕/不叠压 + 金字「馆长亲赞 ✨」；④seed 修 `admin_liked=1` 必须计入 like_count（原演示帖「墙 1 人/计数 0」自相矛盾） | **ACCEPTED**（用户 2026-09-11 回「ok 了」） | commit `7101ee3`（+ 呼吸式微缩放动效 `116eb16` 之后一刀）；judge 视觉门 **3 轮至 5/5 PASS**（首轮样图排版破损、次轮框中心实心灰盘——真因 `art.glow` 内部 `paste(mask)` 会给透明底铺色块，已改画环带外同心圆环并实测中心透明）；测试 +1 例（馆长排第一/取消不残留/计数口径） | 本批含后端 1 处（service）+ seed + 小程序端；未跑全量 gate（无迁移/无契约变更），已跑 test_fix34_circle 6 passed |
| fix34e3 播报互锁 + 馆长条目标记（2026-09-12） | 用户实测两条：① 播报仍不弹——真因不是代码断链（上一条已修），而是**互锁缺失**：点一次「收到的赞」通知条会把整个场景标已读，把未读的馆长赞**顺手吃掉**（用户实测中被吃掉的就是这条）；② 馆长条目右侧显示「看名片 ›」但馆长无名片可去。修法：`onOpenLikes` 只标记**孩子赞**（馆长赞必须由播报消费）；关播报时**一次清干净所有未读馆长赞**（馆长赞多帖不连弹）；馆长条目改显金色「亲赞 ✨」徽标；seed 增：馆长赞通知每次 seed **重置为未读**（演示可复现）+ 清掉指向已删帖/已取消馆长赞的陈旧通知 | **ACCEPTED**（2026-09-12 用户真机实测通过：金光播报正常弹出 / 通知条与展开列表的馆长头像与点赞墙一致 / 红点可消） | commit 见本刀（fix34e3，`dfd01c4` 前后同批）；三段链复核（算式 160 → setData 177 → 模板 120/127）+ 活体（未读 1 → 会弹 → 关闭后未读 0 → 已复原初态） | 纯小程序端 + seed；未跑全量 gate |
| fix34e 两个「羡慕杀」（2026-09-11，用户委制） | ①**馆长亲赞金光播报**：未读的馆长赞 → 进阅读圈弹一次居中金卡（GM 头像/呼吸光晕/弹入动效），关掉即标已读、红点归零；数据面把馆长赞通知独立为 `ref_type=circle_admin`（不再靠文案猜）+ 消息中心文案「馆长亲赞！」。②**被馆长赞的帖子缩略图金边 + 循环流光**（只动 transform）。顺带修：孩子赞去重键 `child:{id}` → `child:{id}:post:{post_id}`（原键导致**同一孩子赞你两帖只通知一条**） | **ACCEPTED**（2026-09-12 用户真机实测通过：金光播报正常弹出 / 通知条与展开列表的馆长头像与点赞墙一致 / 红点可消） | commit `0c54421`（**首版漏写 setData → 播报不弹，已修 `9773964`**，教训入错误记忆库 E-20260912-01）；测试 +1 例（馆长赞逐帖各一条/连赞两帖各一条）→ test_fix34_circle **7 passed**；活体：帖1 墙上[馆长 GM] + 1 条未读 circle_admin 通知（seed 按真链路补，幂等） | 含后端 1 处 + seed + 小程序端；无迁移/契约变更，未跑全量 gate |
| fix34e4 播报真因（装饰层漏字段）+ 红点死锁 + 文案错乱（2026-09-12） | 用户第三次实测三条：①播报仍不弹 ②横幅文案变成「馆长赞了 Demo 的成就 赞了你的成就」③点开后红点消不掉。逐条定位：①**装饰层漏映射 `content`** → `adminHit.content` 取到 undefined → celebrate 恒空（**同一会话里「值没到手」在两层各犯一次**：上一刀修 setData，这一刀修映射层）；②前端用 `content.split(' 赞了')` 取展示名，而馆长文案是「馆长赞了 X 的成就」（**无空格**）切不出来 → 改由后端给 `actor_name`；③上一刀的「互锁」（通知条不吃馆长赞）在播报异常时让红点**永远清不掉** → 回退为「点通知条标记全部未读」，播报在进页瞬间弹、不会被抢。另把播报动效的 opacity 淡入去掉（万一动画不跑，卡片也必须可见） | **ACCEPTED**（2026-09-12 用户真机实测通过：金光播报正常弹出 / 通知条与展开列表的馆长头像与点赞墙一致 / 红点可消） | commit `a6a5d81`；**新增机制化防复发**：`scripts/check_miniapp_bindings.py`（wxml 读的顶层变量必须真的进过 data）已接进 gate 第[5]步，首跑抓出 1 处残留引用并修复；测试 +2 例（actor_name=馆长 / 孩子赞=英文名）→ test_fix34_circle **8 passed**；活体**四段链**：后端项(actor_name=馆长,content,unread) → 装饰后条目 → celebrate 非空(会弹) → 点通知条未读归零 | 小程序端 + 后端 1 处（identity 服务补 actor_name）；未跑全量 gate |
| fix34e5 馆长条目头像与点赞墙不一致 + 潜在 id 撞车（2026-09-12） | 用户实测：「上面的通知，馆长赞了我的成就，头像应该跟下面点赞的馆长头像一致啊，用我自己的不合适啊」。①馆长条目 actor_avatar=null → 前端回落默认 `cat_sun.png`，而那正是演示孩的头像（看起来像用了自己的）→ 通知条头像堆叠(52rpx)与展开列表条目(60rpx)在 `refType=circle_admin` 时改用**金光馆长头像 + 鎏金冠冕框**（与点赞墙同源）；②**更危险**：`attach_actor_profiles` 原本对所有数字 ref_id 都查孩子表，而馆长赞的 ref_id 是**帖子 id** → 帖/孩同号（演示现场 帖1/孩1）时会给馆长条目配上**别人的孩子头像** → 改为孩子维度字段只对 `ref_type=child` 生效、非孩子主体显式 None（形状统一） | **ACCEPTED**（2026-09-12 用户真机实测通过：金光播报正常弹出 / 通知条与展开列表的馆长头像与点赞墙一致 / 红点可消） | commit `839dc65`；测试 +1 例（馆长条目永不带孩子维度字段）→ test_fix34_circle **9 passed**（+wm15 共 19 passed）；活体：馆长条目 ref_type=circle_admin/actor_avatar=None → 前端渲染金光馆长 | 小程序端 + 后端 1 处；未跑全量 gate |
| fix34 收口 · 全量门禁复核（2026-09-12） | 上表 fix34b/c/d/d2/e/e2/e3/e4/e5 七行均标注「未跑全量 gate」——本行统一闭环：`GATE_LOG_NAME=fix34b bash scripts/gate.sh full` **PASS**（`gate-runs/2026-09-12/gate-fix34b.log`：**457 passed / 83% / BDD 30 场景 103 步 0 failed / 9 步全绿 / exit 0**，一次过；架构关含 miniapp_router 零违规、第[5]步 `check_miniapp_bindings` 首战「小程序数据面一致 ✓」、T27 快照一致、交付完整性零 untracked）。三层防呆链：先 `dev.sh stop` → gate（TRUNCATE 演示库）→ 尾部自动 restart+双 seed；再跑 `test_p0_t8_rate_limit`（不造业务数据）清 pytest 残留 + restart，得**干净基线 35/3/11/2**。活体复核两条待验项的数据面（⓪环真值）：登录 children 带 level、通知 `/api/miniapp/notifications?scene=circle.liked` → `{content:"馆长赞了 Demo 的成就", ref_type:"circle_admin", actor_name:"馆长", read:false}`（unread_by_scene.circle_liked=1）、信息流帖 1 `thumb_url` 非空 + `likers=[{child_id:0,name:"馆长",level:"GM",is_admin:true}]` + `liked_by_me=false` → **取数侧已证通，余下只剩端上渲染**。 | **ACCEPTED**（2026-09-12 用户真机实测通过；见上方 fix34e/e3/e4/e5 各行） | 无新增 commit（纯复核） | **已 push**（2026-09-12 用户授权）：`origin/main` 同步，tag **`acceptance-wm15b-fix34`**（旧 `acceptance-wm15b` 名被 09-11 的 WM15-A+fix33 占用，未强改） |
| 全量文档对齐批 · WM15-A/B 验收回写（2026-09-12） | 用户真机验收通过后，把「待验收」状态与 B 期内容传播到全部事实源：docs/01 新增 WM15 A/B 期行（ACCEPTED）；docs/03 FEAT-085 状态 IMPLEMENTED→**ACCEPTED**，并**纠正验收锚点错字**（原写 `docs/04 §8.4 第 24-31 步`——§8.4 不存在且步数到 36 → 改为 `§9 第 24-36 步`）；docs/04 §八/§九 标题去掉「待用户验收」、§九 补验收结论与 5 轮返工史；PRD §7.5.1 增第 8 条「馆长亲赞排面」；docs/15 增 §2.7（头像与等级头像框规范：四档/组件/馆方专属资产白名单纪律/同心环画法约束）；error_list 新增 §六十四 E-20260912-02（外部输入符号先验真族）；交接书 v28 §八 存本次外部输入核验结论 | DONE | 本次为 docs-only（无可执行代码变更 → **不重跑 gate**；gate-fix34b 结果对当前代码仍有效，已用 `git diff --name-only d6ab34a..HEAD` 证实仅在 docs 变动） | 与 push + tag 同批 |
| 小程序全页面审查任务移交（2026-09-13 夜） | 用户委制下一任任务：**小程序全部页面深度审查自检 + 统一风格基准**（逐页截图/向下滚动/不漏元素 → 零遮挡·零覆盖·零风格不统一·零 bug；并按 PRD 补齐缺失页面）。dev 交付**自包含任务包** `docs/任务包-20260913-小程序全页面深度审查与统一风格基准.md`：① 29 页清单与**直达参数表**（截图协议用）② 元素对齐契约（骨架/状态四件套/卡片/按钮/列表/浮层/长文本/合规）③ 统一风格基准（49 个令牌 + 禁项）④ 截图协议（微信开发者工具 + computer-use + 每页 query）⑤ **已知偏差量化证据**（裸色值 circle 44 / book-detail 24 / deposit 22；内联 style refund-apply 18、observation-report 11；`.empty-text` 等 8~10 页同义类各自实现；`loading-skeleton`/`pay-button` 零采用；tabBar 0 令牌；member-pkg **9 个孤儿 wxss**；`agreement` 两合规页只有 wxss 无页面且 sitemap 已声明）⑥ 逐页审查结果表（新模型填写）| 待新任执行 | 交接卡升级 **v30 置顶**（v29/v28 归档；并纠正上一轮遗留的 v29 正文标题被误标 `v28·` 的结构错位）；本行数据均为 2026-09-13 静态扫描实测 | 未 push 待用户指令 |
| 小程序全页面审查与统一风格基准·执行（2026-09-13 夜～09-14 凌晨） | 30 页（27 注册 + 合规 2 页 + FEAT-083 purchase）逐页整改：**风格七规则静态归零**（`check_miniapp_style.py` exit 0：R1 裸色 0 / R2 内联 0 / R3 同义类 0 / R4 孤儿 0 / R5 JSON 值域 0 / R7 禁项 0，白名单 1 条=login 微信品牌绿，入 gate 第[5]步）；10 页完成 before+after 双截图证据（books/index/shelf/member/login/circle×3/activity×2，用户点名活动两页含逐元素清单附录 A1/A2）；机械清理 18 死文件 + error-view/platform.js 剥死端点 + pay-button iOS 合规去重；四件套全站对齐（loading-skeleton 增 grid 变体）；到底文案统一「— 到底啦 —」；触控区 88rpx（MP-018）；seed 封面生成器三修（词级换行+CJK 标签+CJK 标题作者）重生成 35 书封+2 活动封；合规两页正文=事实性占位待法务；purchase 页零硬编码金额（配置端点欠账挂账） | **静态层全零 + 10 页截图双证；15 页整改后截图待补**（后续轮次已补拍并获用户 2026-09-15"整体 ok"验收）（IDE 项目窗口僵尸态+屏录权限被吊销，E-20260914-01/02，用户明早打开项目窗口即可补拍）；gate full 与现场恢复见 gate-runs/ | 证据目录 `uploads/miniapp-audit-20260913/`（INDEX.md 为证据本体） | 专家裁定书 19 问全闭合；consent.js 去留与法务文本两件待用户拍板；不 commit/push |
| 交接卡 v40（2026-09-15，**DONE**） | 用户压缩上下文前的完整交接。v40 置顶（七节：**用户长期目标=用我自己的视觉能力主动找并修视觉/UX 问题，不再让用户提** / 当前交付状态 / 本轮做了什么 / 下一轮该做什么（含 23 页·608 死类、读者页、测验页、quiz-result、emoji 图标等优先清单）/ **防复发红线十条表**（canvas 原生层、`?v=` 破缓存、删除类脚本、状态枚举兜底、单源、改文案改跳转、吸顶 top、网格 calc、React 输入框清空、门禁后现场恢复）/ 环境与命令（gate 9 步 · 双 seed · automator 重登 · 清理脚本）/ 待用户决定六项），v39 归档。**并在文首直接回答"压缩完读哪个文档"**（本文 + CLAUDE v1.5 + LEDGER + 错误库 §七十~§七十五）。 | **DONE**（已推送） | 门禁与检查器全绿（见上方两行）；文档引用检查 ✓（23 文档/162 接口/40 配置键/38 表）|
| uploads 与仓库垃圾清理批（2026-09-15，**DONE**） | 用户指令："清理项目中无用过时的文件，特别是 uploads 里…只要保留双 seed 的测试数据及真正的项目资源即可，还有其他文件夹也都巡查一遍"。**先踩坑后动手**：现成工具 `scripts/cleanup_uploads.py` 的保留集只来自 `Book.cover_path/audio_path` 两列，照它跑会删掉活动封面、阅读圈卡片图、收款凭证、评估报告图、以及 docs/04 与交接卡引用的视觉证据目录（`miniapp-audit-*`/`*-samples`）——**重写为保守版**：引用集补齐 6 个列（含软删行）、只清理白名单内可再生目录、证据与人工上传目录进保护名单、默认 dry-run、`--trash` 先移动留回滚。**执行结果**：孤儿 8743 个 / 668MB（其中 2269 个音频是字节完全相同的占位 mp3）→ **uploads 726MB → 39MB**；被引用的 49 个文件**零缺失**；两个 seed 重跑后**零新增孤儿**（幂等）。另清：`__pycache__`×24 / `.pytest_cache` / `.ruff_cache` / `.coverage` / `admin-web/dist`（可再生构建产物）/ 根 `project.config.json`（.gitignore 自注"devtools 误生成"）/ `.DS_Store`×3 —— 全部移到废纸篓而非硬删。**保留未动**：证据目录、`voucher/`（人工上传凭证）、`observation/`（评估报告图）、`posters/`、以及 11 个"疑似未被引用但属源码/规划中能力"的模块（pdf_service / sql_utils / consent_texts / SMS 三家等，外部审查已登记，删它属设计决策不属清垃圾）。【全仓巡查（第二批，用户追问"其他文件夹呢"）】①三个**在本项目跑不通/无人引用**的工具移入 `docs/legacy-attic/`（沿用既有惯例，同 check_model_consistency 先例）：`verify_action_wiring.py`（校验 HTML data-action，本项目管理端是 React，扫不到任何东西）、`loop_check.py` + `loop_gate.sh`（旧项目 LOOP 协议，此处直接报错"台账不存在"，仅 legacy-attic 提及）；②删空目录残留 `backend/common/domain-reference/`；③清根 `project.private.config.json`（devtools 误生成）、vite 预打包缓存（在 admin-web 的 node_modules 内，18MB）、`.ruff_cache`/`.pytest_cache`/`.coverage`/`__pycache__`×24（门禁会重建，均已在 .gitignore）；④`git gc --prune=now`：6376 个松散对象 → 打包 **9.6MB（.git 43MB→10MB）**；⑤修死链：`billing-circulation` 手册称旧代码在 `backend/domain-deposit|domain-reference`，**本仓并不存在**，已如实标注。**核实后判定"不是垃圾"故保留**：`data/ecdict.db` 812MB——docs/20 §六「词库导回」的导入源（335 万词），删了就没法重导；`gate-runs/` 17MB（15 批次 77 份门禁证据日志，纪律要求留档）；`miniapp/icons` 49 个素材**零未引用**；6 个 `gen_*` 一次性生成器保留（产物是入库资产或本地证据，脚本是唯一复现路径）。 | **DONE**（现场已复验） | 证据：清理前后引用校验（49/49 在盘）、seed 幂等复跑、`cleanup_uploads --dry-run` 归零；错误库新增 **E-20260915-27**（清理工具引用集不全的数据丢失风险） |
| 全库文档基线对齐批（2026-09-15，**DONE**） | 用户指令："轮询整个项目的全部文档 md 及 prd 等核心重要文档，让当前的代码与全部文档基线全部对齐后可 commit + push"。做法：4 路并行**只读审计**（分区覆盖 docs/01/02/03/04/07/08/09/15/18/19/20 + LEDGER + billing 手册 + PRD + CLAUDE/AGENTS/README）→ 逐条按代码复核 → 分区落笔（互不重叠文件）。**主要修正**：①`docs/03` 状态按代码**逐行重算**（71 行 DRAFTED 全错 → 实测 ACCEPTED 20 / MANUAL_TEST 62 / IMPLEMENTED 1 / REGISTERED 3 / DRAFTED 1），总数 12 包 70 项 → **15 包 87 项**，补登 FEAT-086（书架已通过）/FEAT-087（合规静态页）并给 7 行补验收标准；②`docs/02` 状态机清单 6 处与代码不符（活动报名 9 态→6 态、订单把退款状态混进订单态、预约 locked→active、转让 pending_review→pending、阅读进度无 voided、Quiz 是派生值）+ staff 八码全错（按 `admin/service.py` 重列，audit.view 属超管）+ "全部状态机已接入"→部分（5 个模型）；③`CLAUDE.md` 升 **v1.5**：门禁 7 组命令（含两条已归档死命令）→ **实际 9 步表**、覆盖率阈值 85%→实测 25% 下限/84% 现值、`theme.ts`→`theme-paint.ts`、`需求决策链`/`docs/ADR/` 死链如实标注；④`docs/04` 三条**指导错误操作**的过期步骤（书架三 tab→四 tab、管理端签到入口顶部输入框→报名名单抽屉、退会入口"退款页内"→独立退会页）+ 补退会结算验收 2b/4b；⑤PRD 按"增量增补"体例加 5 节（§3.5.3 退款退会分家 / §8.1.1 生词本统计与闪卡 / §8.2.1 书架已通过 / §9.2.2 券码置顶与亮度 / §9.5 活动只读详情）+ 修 §7.5.1 标题错位；⑥`billing-circulation` 手册补新项目值域对照 + P9 如实标注"365 天拦截/罚款抵扣**未实现**" + 新增 P9b（退款退会分家）/P9c（签到与退款资格）/P9d（URL 破缓存）；⑦其余：docs/01 域数、docs/07 任务数/字段名/WM 状态、docs/08 新增 TD-8、docs/09 C9/C10 翻 DONE 且剩余清单 8→5、docs/15 令牌名对照表+字体栈+"未实施"改已落地、docs/18 补 2 端点+破缓存纪律、docs/19 条号/书本数/命令输出、docs/20 校验项与基线数字、README 域数与步数与 PRD 链接。**顺带修的真 bug**：退款页/订单页订单类型 `custom` 裸英文（收编到 `utils/labels.js` 唯一映射源）。 | **DONE**（文档与代码对齐；同日随"整体 ok"提交） | 证据：`check_docs_code_alignment`（23 文档/162 接口/40 配置键/38 表/1568 符号）✓、`check_miniapp_style` R1–R11 全零 ✓、`check_miniapp_bindings` ✓、`check_rbac_consistency` ✓、`ruff` ✓；两路审计各自主动纠正了我给的口径错误（任务数 16→实测 15、检查器 R1-R7→实测 R1-R11），已按实测落笔 |
| 门店扫码签到（二维枪·连扫）（2026-09-15，**ACCEPTED**——用户"整体 ok"并授权提交；**真机需复看**：亮度实际变亮/扫码枪连扫手感） | 用户决定：门店配**二维影像扫码枪**，签到支持**连扫**（自动聚焦 → 回车即提交 → 防重复 → 成功提示带**孩子姓名+活动名+时间** → 失败四类不静默 → **成功与失败都回焦** → 最近签到列表 → 成功/失败不同提示音）；家长侧入场券出示**二维码（主）+ 条形码 Code128（备）**。**后端**：`ActivityService.signin()` 补 `child_name`/`activity_title`/`ticket_code`（契约快照无变化，已重导确认）。**管理端**：新增 `admin-web/src/components/ScanCheckin.tsx` 落在**报名抽屉顶部**（工具栏裸输入框已删）——提交读 DOM 不读 state（枪打字极快）、`onPressEnter` 与枪结尾回车同路、`busyRef` 防连发回车、**finally 回焦**（失败也回焦）、「● 等待扫码…」就绪标识、四类失败分级、最近 20 条对账列表、AudioContext 双音静默降级，另加**越场券告警**（签了 B 场的票要看得出来）。**小程序**：`libs/qrcode.js`（vendor `qrcode-generator@2.0.4`，MIT，逐字复制）+ `libs/code128.js`（Code128 Set B，图案表取自 jsbarcode MIT）+ `utils/ticket-code.js`（画到 canvas 2d；颜色写在 JS 因 canvas 不解析 CSS 变量，且扫码对比度优先于配色），🎟️ 占位换成真码，绘制失败折叠画布露券码文本兜底。**演示数据**：免费活动预置 5 个已报名孩子（S8 连扫 5 张免造数）。**施工中发现两个真 bug 并修掉**：①`el.value=""` 清不掉（AntD 内部 React 值会写回 → 下一枪拼接成废码，连扫必废）改原生 setter + 冒泡 input；②对账时间用 UTC 差 8 小时改本地。 | **ACCEPTED**（2026-09-15 用户"整体 ok"并授权提交；未提交时的证据见下） | 证据：码可解码自检（`drawQr` 像素→`qrcode-reader` 3/3 命中；`drawBarcode`→macOS Vision `DECODED`；与 jsbarcode CODE128B **逐位比对 6 组 0 差异**）；小程序截图再解码（二维码/条形码均得 `TK0000200002FBE8`）；浏览器实操 S2–S8（连扫 5 张零鼠标点击、重扫不改签到时间、四类失败均回焦）；`pytest tests/unit/test_wm9_activity.py` 7 passed；`check_miniapp_style` R1–R11 全零；`docs-code` ✓；交接卡 **v39** |
| 门店扫码签到·追加（管理端只读详情 + 小程序券码置顶/亮度）（2026-09-15，**ACCEPTED**——用户"整体 ok"并授权提交） | 用户当轮实测反馈三项：**①管理端点活动名称看只读详情**（领导视角）——「活动」列改可点链接 → 只读抽屉：封面大图 + 状态/类型/会员限制标签 + 开始时间/报名截止/地点/费用/名额已占/创建时间 + **已缴费待参加 / 已签到 / 待收款**三计数 + 活动介绍全文，右上角给「报名名单」「编辑」动作；后端 `AdminActivityService.get_detail` 仅补 `created_at`。**②小程序券码区搬到最上**（到店点开即扫，活动信息下移，标题改「我的入场券」）。**③展示券码时屏幕亮度拉满 + 保持常亮**，先存原值、onHide/onUnload 还原（拿不到原值就不调亮），页面示明"离开自动恢复"。**关键技术发现**：用户报的"二维码遮挡按钮"真因是 **`canvas type="2d"` 原生层首帧错位**（布局稳定前画完 → 画面停在旧坐标且不自愈，实测二维码画到 hero 上），修复=**展示层不用 canvas**（画布移出可视区只当渲染后端 + `canvasToTempFilePath` 导出 `<image>` 展示；"布局稳定后补画"方案实测**下次冷启又错**，已废弃），配套坑：出图画布须与展示图**同比例**否则条形码被拉伸发虚；**只挪位置修不好它**。同轮还修：`checked_in` 分支缺失导致按钮显示"状态更新中"（枚举族第四例）、我改 JSX 时删掉报名名单抽屉 `open` prop（tsc/lint/单测全绿，浏览器实操才抓到）、抽屉间跳转自动聚焦被焦点回填顶掉（改 3 次重试 + 只在非输入框时抢焦点）。 | **ACCEPTED**（2026-09-15 用户"整体 ok"并授权提交） | 证据：管理端浏览器实操（详情齐全、跳名单且扫码框自动聚焦、再扫一张券成功）；小程序截图（码在最上/活动信息在下/按钮不被遮挡、`已签到 ✓ 欢迎参加`、`brightnessBoosted=true`）＋**导出物 PNG 直接解码**（二维码 720×720 → 券码；条形码 1065×204 → macOS Vision 解出同一券码）＋连续 4 次冷启/离开再回来稳定出图；错误库新增 **E-23~E-26**；`check_miniapp_style` / `bindings` 复跑通过 |
| 小程序视觉自查·第四轮（网格列数 / 成绩单入口 / 吸顶遮挡）（2026-09-15） | 用户 3 条 + 4 张截图。**①我的页「留白那么大」**——`.func-item { width: calc(25% - 12rpx) }` + `gap: 16rpx` 让 4 格 + 3 隙**正好等于容器宽 686rpx**，浮点误差吃掉一列 → 每行 3 个、卡片被撑高（改 `calc(25% - 16rpx)`，复截回到 4 列）。**※ 本族第三例**（messages 分类条 −9rpx→−10rpx、shelf 页签溢出→flex:1、member −12rpx→−16rpx），规则：多列网格宽度 calc 必须留 ≥ 间隙总数的余量且不能取等号。**②已通过的书点「看成绩单」两种错法**（有的弹「需先听完音频（95%）」、有的进测验页）——**我只改了文案没改跳转**：图标仍绑 `onQuiz()`，它先判本地进度、通过后跳测验页；改为 `quizStatus === 'passed'` 直接走 `onQuizCard()`（复截：进 quiz-result，80 分/答对 4/5）。小修：成绩单页「+0 积分」→ 无积分显示「—」。**③现场扫码签到：只评估未实施**（等用户拍板）——后端 `POST /api/admin/activity-signin` 与 `ticket_code` 状态机、管理端「输入入场券码签到」输入框、小程序「我的报名」文本券码**都已存在**，只差小程序把券码画成二维码/条形码；三条路径（本地画码 / 门店扫码 / 小程序码）与安全口径见交接卡 v37 §一点五 | **ACCEPTED**（2026-09-15 用户"整体 ok"；未提交时状态见右列证据） | 证据：三个 check 单独复跑通过（bindings / style / docs-code）+ 复截 member-r4 / quiz-result-from-detail；错误库新增 **E-18**（改文案没改跳转）/ **E-19**（calc 正好等于容器宽·本族第三例）；交接卡升级 **v37** |
| 小程序视觉自查·第三轮（退款/退会拆分 + 判定源归一 + 横版活动图）（2026-09-15） | 用户 9 条反馈（6 张截图）。**①已通过的书详情显示「未开始读」**——同一判定两个事实源：列表按词账枚举、详情 PASSED 看 `get_quiz.unlocked`（只认 `ReadingProgress.finished`）→ 后端把 `passed_before` 上移并 `unlocked = 已完播 or passed_before`（通过的前提就是听完）+ 前端「本书已通过测验 ✅」与底部「看成绩单」文案联动 **②退款页滑不到底**（200rpx 未含 safe-area）**③退会展开后按钮压按钮**——`.submit-wrap` 是页面级固定底栏样式却用在两处卡片内（上轮遗留）→ 回文档流 + 按钮满宽 **④我的页「关于」看不到内容**——卡写在 `</scroll-view>` 之外（占位插错位置）→ 移进容器、占位后置 **⑤多页文字左右贴边框**——全局卡片组规则漏 padding（上轮）＋本轮退款订单行 `.x-3fde25`（`padding:16rpx 0`）与活动详情 `.info-card` 补内边距 **⑥⑦退款/退会彻底拆分（用户裁定）**——退款页删净退会区块（wxml 31 行+js 5 处）；**新建独立退会页** `pages/order-pkg/withdrawal/`（只提申请、不预先罗列费用）；member 入口改跳新页；新端点 `GET /api/miniapp/withdrawals/{id}/settlement` 在**审核通过后**把自动排查出的可退费用明细给家长（含费用规则/订单号/金额/状态+合计，带越权防护）；`my_list` 补 `status_text`/`can_cancel` **⑧按押金页口径统一处理贴边** **⑨活动横幅上半部分被切（连续两轮）**——根因是**按比例推算的"安全带"不可靠**（实测 devtools 的 `aspectFill` 不是居中裁切）→ 改为**按横条比例出图**：新增 `gen_activity_banner()`（900×320 专用横版图，元素离上下缘≥28px），不再有裁切环节 **＋产品级修复**：封面 URL 带 `?v=<文件名token>`（5 处生成点）、`regen_covers.py` 改「写新文件名+删旧」——原来管理端换封面后所有小程序用户都会继续看旧图 | **ACCEPTED**（2026-09-15 用户"其他的都差不多了"→收尾后"整体 ok"） | 证据：全量门禁 **PASS**（`/tmp/gate_r3.log`：ruff ✓ / **464 passed**（+3 新测试）/ BDD 8 features·30 scenarios·103 steps / 架构关 PASS / alembic 无漂移 / tsc ✓ / T27 契约快照一致（新端点已重导）/ 交付完整性 ✓）+ 现场自动恢复；新增单测 4（退会结算可见性/越权防护/无进度已通过判定 + 上轮 3 条退款扫描）；API 直查 book 1/31/35 = `passed/unlocked=True`；复截 withdrawal / refund-apply / activity-list / activity-detail / passed-book-35 ✓；错误库新增 **§七十二**（E-13 判定源散落·本族第三例 / E-14 一个页面两件事·交互歧义 / E-15 比例裁切不可推算 / E-16 媒体 URL 缓存 / **E-17 吸顶 `top` 写成导航栏高度 → 滚动时下移 112rpx 压住首卡**：`activity-list` 与 `achievement` 的 sticky `top` 改 0；`order-history` 因自绘 sticky 导航栏故 `top:96rpx` 是对的、勿误改）；交接卡升级 **v36**；遗留：封面孤儿文件 4922 个 / uploads 151MB（清理属破坏性操作，待用户点头） |
| 小程序视觉自查·第二轮（用户 8 条实测反馈）（2026-09-15 深夜） | 用户 6 张截图 + 2 条需求，逐条落地：**①「PASSED 但最佳成绩 1 星 0 分」**——同一事实写在两张表（`status=passed` 看词账、`best_percent` 看测验记录），seed 榜单演示段直插词账未插测验 → 加 `_ensure_ledger_attempts` **seed 收尾对账自愈** + 前端 `quizHasScore` 守卫（无成绩显示「成绩待同步」，不编 0 分）**②退款页滑不到底**（`padding-bottom:200rpx` 未含 safe-area，而固定栏实际 ~236rpx）**③退会展开后按钮压按钮**——`.submit-wrap` 是 `position:fixed` 的页面级底栏样式却被用在两处卡片内部（**我上轮遗留的错用**）→ 两页回文档流 + 提交按钮补 `width:100%` **④我的页「关于」看不到内容**——该卡写在 `</scroll-view>` 之外（**我上轮把占位插在了 scroll-view 内**）→ 卡移进去、占位放其后 **⑤多页文字贴边框**——我上轮新增的全局卡片组规则**漏了 padding**（且"复原"本身是误判，HEAD 里这些类不存在）→ 只给 `.section-card` 补 `padding:32rpx`（不整组补，避免 activity-detail 双重内边距）**⑥退款/退会边界**（用户裁定）→ 退会结算由「会员费+押金」改为扫**全部费用类已支付订单**（`first_activity/observation/formal/activity_fee`）+ 押金，并排除已有在办/已退退款单的项防重复退**⑦书架新增「已通过」页签**——新端点 `GET /api/miniapp/growth/passed-books`（`PassedBooksService`）+ 4 页签等分自适应，每本显示封面/🏆/词数/最佳成绩**⑧再修一轮** | **ACCEPTED**（同批同口径） | 证据：全量门禁 **PASS**（`/tmp/gate_r2b.log`：ruff ✓ / **461 passed**（+3 新测试）/ BDD 8 features·30 scenarios·103 steps / **架构关 PASS**（`growth/service.py` 加方法后 840 行超 800 → 按惯例拆出 `passed_books_service.py`）/ alembic 无漂移 / tsc ✓ / T27 契约快照一致（新端点已重导 +50 行）/ 交付完整性 ✓）+ 现场自动恢复；专项复截 6 张（refund-apply-bottom / benefit-transfer-bottom / member-bottom / report / shelf-passed / book-detail-noscore）；新增单测 3：`test_withdrawal_sweeps_all_refundable_orders`（含"押金不得被退两遍"）、`test_withdrawal_skips_order_with_existing_refund`、`test_passed_books_lists_passed_with_best_score`；错误库新增 **§七十一**（E-09 固定定位复用 / E-10 半成品复原漏 padding（**同族第二次**）/ E-11 同事实两表 / E-12 押金双表示漏算——**后者是既有测试逮到的**）；交接卡升级 **v35** |
| 小程序视觉全量自查·第一轮修复（2026-09-15 深夜） | 用户委派「用多模态视觉**主动找并主动修**」（间距/布局/按钮尺寸/配色可读性/绘本风统一/文字贴边/遮挡覆盖/毛坯房 + UX 最佳操作，生词本为样板）。本轮 **11 项真修复**：①**生词本改造**（用户点名样板）——`vocabulary.wxss` 整套统计/闪卡样式**早已写好但 wxml 一个没用**（设计做完、模板被掏空＝毛坯房根因）；重写 WXML 接上统计三卡（收录/累计查词/来源书目）+ 生词卡（大字强调色单词/音标/释义/来源书分行/「查 N 次」徽章）+ 点词弹闪卡 + 删除按钮弱化 ②生词本**新增查词次数**（`lookup_count` 列 + migration `e5c1a7d3b9f4` + 重复查自增 + `list_words` join 词典出音标/释义/翻译）③**封面体系重做**：`gen_cover` 由「纯抽象几何色块」改为真绘本场景（天空/太阳/云/远近山丘/草地/树/小屋/正脸小动物）+ 书名放进 **aspectFill 安全带**、取消必被裁的作者条与顶部标签；新增 `scripts/regen_covers.py` 幂等重刷 37 张 ④图书馆首屏 5 本无封面（落 `📕` emoji）——真因 `seed_wm11_demo._ensure_demo_books` 建书不设 cover_path 且补齐跑在它之前；改为建书即出封面（**已验证**：清空后只跑 seed 即自愈）⑤activity-detail **卡中卡**（上轮我自己追加的重复定义块造的）删除 ⑥hero 45% 均匀遮罩→纵向渐变 ⑦activity-list 费用文字压气球→左下角实底价签 ⑧messages 8 个分类横排溢出被切成半个字→4×2 网格 ⑨权益转让裸枚举 `formal/none`→抽 `utils/labels.js` 中文化 ⑩shelf 书名两行 + 续听/详情按钮收窄 ⑪books 书名两行（不再断在单词中间）。**撤回上轮 2 条误判**（"标题组规则被删"、"member 会员信息大空框"——放大后均为假缺陷）。**新线索**：全项目扫出 23 页 / 608 个"死设计"类（定义了却没被 wxml 用过），成块的即毛坯房来源（reader 91: 整套 `audio-*`；achievement 52: `badge-progress-*`；observation-report 46: `cc-*`；quiz-result: `confetti-*`），清单见错误库 §七十 E-20260915-08 补充表 | 待用户目视验收（11 项；未提交） | 证据：全量门禁 **PASS**（`/tmp/gate_final8.log`：ruff ✓ / **458 passed** / BDD 8 features·30 scenarios·103 steps / alembic 无漂移 / tsc ✓ / T27 契约快照一致 / 交付完整性 ✓）+ 现场自动恢复（health ok、admin-web 200、生词本 8 条数据在）+ 逐页复截（`/tmp/shots2/`，含闪卡页）+ 封面按 1:1 / 0.82 / 2.81 三种真实裁切逐张核对；错误库新增 §七十「低分辨率误判族」；交接卡升级 **v34** |
| 全维度审查 + 文档零差异 + 5 项缺口清偿（2026-09-12/13） | 用户两轮专项指令（①全量全维度前后端 API 深度审查 ②文档↔代码零差异）后逐项批准清偿：**⑤** seed 出题顺序（补齐循环跑在建书之前 → 书 31-35 永远 0 题）→ **③** R-313「入会跟进任务」从零实现（新待办场景 `admin.member_follow_up` + 放行即写 + 显示态随孩子会员态自愈 + 计数进 admin_total + Dashboard 项，pytest 全链覆盖）→ **①** 权限码三方对账（`config.update`/`audit.view` 显式登记为仅超管 + 新增 `scripts/check_rbac_consistency.py` 入 gate 第[5]步，宪法 §五.2 承诺项落地）→ **④** admin-web 7 处硬编码金额改读配置（新增 `hooks/useConfigs.ts`，9 个读取点）→ **②** 家长端退会入口（api 包装零调用 → 退款页退会区块 + 我的页入口 + 撤销）。审查侧另立两道门禁：`check_docs_code_alignment.py`（文档引用对账，V1 路径/接口/配置键/表名/符号 + V2 数量断言） | MANUAL_TEST（等用户真机复验界面部分） | 证据：全量门禁 `gate-runs/2026-09-13/gate-fixes-wm410-batch.log`（457 passed / 83% / BDD 103 / 9 步全绿）；审查实测 640 次接口调用 0 个 5xx、0 越权、孤儿外键全 0；检查器 exit 0（含产物缺席演练）；`test_wm5_circulation` 8 passed；tsc 0 错 | **未 push**（等用户指令）|
| WM4-10 批量验收前置盘点 + 修复（2026-09-12） | 用户重启「WM4-10 批量手动验收」→ dev 逐模块体检演示数据：查出 **1 处真缺陷**（seed 词账与成长态漂移：演示孩 words_total=0 vs 流水 78220 → 读 state 的里程碑补发空转，WM7-9 会失败）+ **3 处手册/数据错位**（WM5/WM6 步骤账号 13800000001 不存在、WM6-14「时长 10 分钟」期望过期、WM10-1 订单类型措辞与实际预置不符）。修复：`seed_wm11_demo.py` 收尾新增 `_resync_all_growth_states`（按词账流水全量重算，禁用中途单次同步——禁手拍数字）；`docs/04` 新增「演示家长/孩子账号」表 + 数据纪律告示，WM4/5/6/7/10 步骤按 seed 实测数字改准（演示孩 可借 29 / 押金 1200 / 打卡 3 天 / 词账 78220 / 逾期预置） | MANUAL_TEST（等用户按手册验收） | 证据：`ruff check` ✓ + `ruff format --check` ✓ + `test_fix19_seed_points_related_id` 1 passed + seed 实跑输出「成长汇总收尾重算：5 个孩子」+ 复核成长态/流水不一致数 **0**（修前 4）。**未跑全量 gate**：改动仅 seed 脚本（B 级），CI 在 push 时跑全量 | **未提交**（DoD3：用户验收前禁 commit/push）；**2026-09-12 专家收编**：审查通过（DB 实证 0 漂移/手册校准抽查合格）后随本批提交；同批 §十「WM4-10 批量验收总清单」落盘（专家指令+subagent 执行：进度跟踪表/自包含前置块[5 账号 / **11 个演示孩子**（演示家长 2 + WM3 异常态 6 + WM13 演示 3）/券码自查命令/已知边界]/全步骤账号映射执行视图，L588-833）——全量 gate 按前置盘点批 B 级口径留到验收收口批统一跑（fix34 收口行先例）；**收编 commit：`88561a4`**（seed 修复 + error_list E-03 + 本行）**+ `1abd9fd`**（docs/04 §十 + 手册校准 14 处）；2 刀本地未 push 【2026-09-17 注：本行"演示孩 可借 29 / 词账 78220"是**当时**读数；现行口径见 docs/04 §演示账号表——**可借 28 / 词账 5 笔共 102,320 / 积分 1,063 / 零头池 20 词**】 |
| WM15-A 阅读圈社交化升级（FEAT-085，2026-09-11） | 九件：①朋友圈式信息流（头像+英文名+相对时间 / 成就文字**原生渲染** / 缩略配图 / 淡灰分隔线 / 点赞弹跳动效）②卡片视觉升级为**绘本卡哇伊**（Pillow 程序化：柔光渐变/云朵星星/大眼腮红吉祥物/纸纹，独立 judge 4 轮返工至 11/11 PASS + 用户目视过样图门）③系统内置头像 **24 枚**（12 动物×2 配色，白名单 AVATAR_IDS，**三端同源**：小程序本地包/管理端 public/后端 assets（入库））④孩子名片页（头像+英文名+等级+词数/读本/打卡+勋章墙+TA 的帖子；隐私红线无中文名/家长名/手机号/生日；**退会孩可见=历史荣誉域**，与榜单 active_only 分域）⑤名片分享海报（专用端点 query token）⑥点赞深链（liker_child_id 名义快照→通知「Tommy 赞了你的成就」→点进名片页；老端降级）⑦点赞头像墙（批查禁 N+1）⑧阅读圈 tab 红点（= 消息中心 circle.liked 未读，计数同源）⑨管理端建档/编辑头像选择器 | MANUAL_TEST 进行中（用户按 docs/04 §8 实测，已反馈 4 项 → fix33 修复批 GATE_PASS 待复验，见上一行）→ **ACCEPTED（2026-09-11 用户目视通过含 fix33 修复；遗留观感反馈"割裂感"挂 WM15-B 视觉优化）** | 2026-09-11 全量 gate PASS（gate-runs/2026-09-11/gate-fix32e.log（终轮，与提交树一致），**443 passed / 83.32% 覆盖率 / BDD 103 steps 不降**，基线 430→443 只增[WM15 新增 13 例]；9 步全绿含架构关 PASS＋[8] 契约快照一致＋[9] 交付完整性 PASS）；三项核验：书目 35/家长 4/孩子 12/活动 2 + 快照 5 + 旧帖缩略图回填 6 张；活体：晒卡落双规格文件 + thumb/image 双端点 200、带名义点赞→头像墙+「小朋友008 赞了 小朋友003 的成就」ref=child/8、不带 body 幂等 200、名片页 200（勋章墙+帖子）、海报 200/714KB、退会孩 is_history=true、旧帖 thumb 回落 200、unread_by_scene={activity_enroll:4, activity_remind:1, circle_liked:3} | 先决：专家 17 问裁决（A1 深链从零建/A3 专用端点/B1 两列两清/B2 assets 入库/B3 一次性回填/C1 分场景未读/C3 名义快照/C4 可选 child_id）；两处迁移（thumb_path、liker_child_id）；**施工期自纠 4 处如实**（第 4 处最值得记）：**seed 回归**——render_card 改双规格返回 dict 后只改了 service.create_post，seed._ensure_post 仍把 dict 当字符串塞 image_path → seed 整体崩溃、门禁自动恢复后现场数据为空；**pytest 与门禁都不跑 seed，是三项核验抓到的**（教训 65 同族：改共享函数返回类型必须枚举全部消费点）；另修**海报文件堆积**（原按 uuid 每次请求落新文件 ~700KB → 改为按孩子覆盖写）；其余 3 处：①门禁[6]因改列注释未同步迁移而 FAIL（回归原注释）②门禁[4] god file （art.py 832→拆分 art_mascot.py）③update_avatar 误挂 ParentService（移到 ChildService）；**字体实测定案**：中文 Hiragino Sans GB **W6**（丸ゴ W4 日文缺 20 汉字已排除）、数字 .SF NS Rounded；超采样画布必须同步放大字号（sticker_text 用 font_variant(size*SS)——首版字太小的真根因） |
| fix31 收口对齐批（2026-09-11） | 全量文档对齐 + push + 双 tag：①交接卡升级 **v25 区块置顶**（本轮全史五批 + 状态快照 430/83.15%/BDD103 + 核验基线 35/4/12/2+帖 3+快照 5 + 任务 15 + 配置键 40 + 挂账全景 + 教训 67）【2026-09-17 注：此处"核验基线 35/4/12/2"是**门禁残留脏读数**（behave 夹具未清场），正确基线是 **35/3/11/2** —— 见 E-20260916-41 与 `scripts/check_demo_baseline.py`】②docs/01 阶段表补 WM14 A/B 行 ③docs/02 架构图 + ADR-002 补 reading_circle（8→9 域）④docs/08 风险册新增 R12 微信社交类目审核受阻（降级预案=荣誉墙模式）⑤docs/04 §七 A/B 双期状态行翻「用户已验收通过」⑥docs/09 T46 结论行陈旧修正（C-13 实战已 PASS，原结论仍写 PENDING 自相矛盾）⑦D8 交叉引用清查 | DONE | 纯文档批零 gate（分级纪律 E-20260901-04：markdown 改动 git diff 逐文件自查）；push 前确认无并发 gate/pytest（教训 66） | 文件 6 个（+85/−4）+ LEDGER 本行；push 11 刀一次性全推（部分推送禁令）+ tag acceptance-wm14a→e59d0cc（A 期验收终点）/ acceptance-wm14b→8a6fd5d（B 期翻牌刀）；D8 清查：旧 tab 口径 0 命中 / 「孩子 10」仅历史行（LEDGER 插修 15 行、v24 考古区块）放行 / 任务数 13 无残留 |
| 验收修复批 1（WM4-10 批量验收反馈，2026-09-09/10） | 三件：①验收补数（seed_wm11_demo 扩展 _ensure_wm4_10_acceptance_data——小红/活动报名三态[enroll→confirm→signin 真链]/过期孩在借/四榜对比数据/生词本 2 词/退款演示孩押金，全真实链路三表一致）②**E-20260909-02 音频 child_id 断链修复**（§六十二断链族新变种：插修 15 后端加 child_id 必填漏改 reader.js 消费端=全量哑火；另修 id-only 进入补详情后不重建音频实例的隐藏断点——主入口自 F-L19 起即断）③**E-20260909-03 补缴漏待结清真金漏洞**（超扣场景补缴单只算 标准额−可用、支付凭空清零 unpaid——扣 2000 只收 1200；**用户裁决「扣多少补多少」**：订单金额=标准额−可用+待结清，支付拆分入账[先恢复余额再真结清]、金额对不上拒付、流水 reason 标注含结清额） | ACCEPTED（2026-09-09 用户实测两项均过：音频出声+补缴金额正确） | 2026-09-10 C 级全量 gate PASS（gate-runs/2026-09-10/gate-114151.log，**410 passed / 82.93% 持平基线 / BDD 103 steps 不降**；红测试重写 test_wm4_deposit.test_deduct_insufficient_creates_unpaid——原断言即漏洞行为反例，三场景锁定[2000→2000/1500 含结清 300/68 回归]）；**插曲如实**：首轮 gate FAIL 系两次 gate 并发跑同一 MySQL（打断期间第二轮抢跑）→Duplicate admin 环境冲突非代码，单进程重跑 PASS（教训 66 同族）；docs/19 标准收尾：自动恢复 ✓+三项核验（35/4/12 孩[小红+退会孩新增]/活动报名 3/309 词/进程 11:57 新启/health ok/admin 200） | 本批单刀 `4271cc6`（8 文件 +511/−18）；改动清单：miniapp/pages/reading-pkg/reader/reader.js（child_id 补拼+补详情重建实例+destroy 防双上报）/backend/domain/billing/service.py（create_supplement_order 公式+on_deposit_order_paid 拆分入账）/tests/unit/test_wm4_deposit.py（红测试重写）/features/deposit.feature（超扣补缴场景+修正过时"待结清不拦截借书"文本——与 B-12 矛盾）/scripts/seed_wm11_demo.py（验收补数段）/docs/03 FEAT-027 口径登记/error_list E-20260909-02/03 入库；**验收复现证据**：API 三连（用户现场待结清 300→补缴单 1500 自动含结清/干净 2000→2000/68→68）+音频 curl 模拟 200（会员+过期在借担保语义） | 用户 WM4-10 批量验收继续中（本批为其中反馈修复）；待办：WM4-6 重测（大额赔偿>可用余额才触发待结清，赔 50 在余额内=部分扣除不拦属设计） |
| 插修 17 | 活动封面三处收尾（RA 编辑残留/RB 详情 hero 封面/RC 轮播留白——纯前端三小卡；任务包：外部专家意见/20260903-外部顶级专家审查/30-插修17-活动封面三处收尾-20260908.md 随刀入库） | ACCEPTED（用户目视通过 2026-09-09+专家严审收官） | 2026-09-08 C 级全量 gate PASS（gate-runs/2026-09-08/gate-fix27.log，**410 passed / 82.95% 持平基线**[纯前端无后端改动——基线不降合规]；BDD 103 不降）；**插曲如实**：首轮 gate FAIL 系 OrbStack docker daemon 挂（MySQL 连不上——环境非代码），`open -a OrbStack`+容器重启后 PASS；**docs/19 标准收尾**：自动恢复 ✓+三项核验（35/4/10/活动 2/进程 16:31 新启/health ok/admin 200） | **hash/来源如实声明**：`e1cac6f`（fix27-ra-rb-rc）系**会话外提交**（同一账号 16:01:29，非 dev 本轮施工）——dev 实读核对三处改动与 30 号任务包口径一致：RA 发布按钮 onClick reset 三行（editTarget 置 null+form.resetFields——编辑残留收口）/RB detail hero 接封面（media.fullUrl 拼 token——**断链第三处收口**，封面链上传→预览→轮播→列表→详情五处闭环）/RC 轮播 margin 去横向（与首页卡片对齐）；tsc 0+node --check 过+dev 复核通过 | **目视清单（3 项）**：①编辑过再点发布=空白创建弹窗 ②详情页 hero 显示真封面 ③轮播与首页组件左右对齐；**教训（专家收官入教训库）**：媒体资源消费点清单化——每个新增媒体字段枚举全部消费端逐一接 token，防第四处断链 |
| 插修 16 | 封面 token 断链+播放入口前置拦截 3 卡（任务包：外部专家意见/20260903-外部顶级专家审查/29-插修16-封面token与播放入口-20260908.md 随刀入库；专家活体诊断——双端点鉴权正常但消费端没传 token） | ACCEPTED（用户目视通过 2026-09-09+专家严审收官） | 2026-09-08 C 级全量 gate PASS（gate-runs/2026-09-08/gate-fix26.log，**410 passed / 82.95%**，基线 409→410 只增[R3 红测试 1 例]；BDD 103 不降；一轮收敛[9] 29 号包入库）；**docs/19 标准收尾**：自动恢复 ✓+三项核验（35/4/10/活动 2/进程 15:34 新启/health ok/admin 200） | 逐卡 hash：R1（管理端预览 token：activityCoverUrl 用 getToken 同源拼 ?token=，上传刷新 &t= 共存；纯前端）`b76d616`/R2（miniapp 轮播+卡片 cover_url 走 media.fullUrl 拼 token——书封面正解同款漏抄补齐；start_at 顺带格式化；sweep：detail 无封面消费清白）[与 R3 同刀 `05b37ae`]/R3（**播放入口前置拦截**：guards 判定抽 check_audio 同源[AUDIO 分支与预检端点共用禁漂移]；GET /books/{id}/audio-permission 五态对齐矩阵[契约快照显形]；book-detail onPlay 预检 allowed=false 三态文案分流[unpaid'了解入会'/withdrawn'重新入会'——R-301 口径/expired'续费到店']+**占位跳 member 页**[TODO 锚点：**FEAT-083 会员购买动线** 登记REGISTERED 排期依赖 WM12 支付，上线后一行替换]；reader onError 403 与加载失败区分文案；id-only 进入补详情拉取+detail→reader id 传参收口[F-L19 遗留]）`05b37ae` | **目视清单（3 项）**：①管理端上传封面→预览立即显示 ②小程序轮播真图（非裂图）③学籍孩 B 点开始听→**不进播放页**弹'收听音频需要入会'引导 |
| 插修 15 | 第四批目视返工 4 卡（R5 零代码；任务包：外部专家意见/20260903-外部顶级专家审查/28-插修15-第四批目视返工-20260908.md 随刀入库；浏览器自动化实锤白屏根因） | ACCEPTED（用户目视通过 2026-09-09+专家严审收官） | 2026-09-08 C 级全量 gate PASS（gate-runs/2026-09-08/gate-fix25.log，**409 passed / 82.92%**，基线 407→409 只增[R2 红测试 2 例]；BDD 103 不降；二轮收敛 ruff I001+[9] 28 号包入库）；**docs/19 标准收尾**：自动恢复 ✓+三项核验（35/4/**孩子 10**[押金孩 seed 态]/活动 2/进程 13:45 新启/health ok/admin 200） | 逐卡 hash：R1 `759119c`（**白屏修复**：openEdit new Date→dayjs 两处[antd5 DatePicker 需 dayjs——tsc 不查运行时类型，浏览器实测唯一防线]；dayjs 显式依赖[transitive 不保]；**ErrorBoundary 根部兜底**[E-20260831-04 白屏欠账补上——渲染异常显兜底 UI+刷新，console.error 不吞堆栈]）/R2 `0100126`（**C-13 提前实施**：guards AUDIO 矩阵[未缴费禁/在册允/过期仅手头在借允/退会禁——403 ForbiddenError]；audio 端点加 child_id[契约变更快照显形；可选参数防 FastAPI 校验抢跑 401 语义]+child_of_parent+守卫；report_progress 同守卫+ReadingService 散落判定删除收口；红测试 audio 五断言+收口回归[wm6/d1 三处旧 422 断言→403 语义更新留痕]）/R3+R4 seed 刀（**T41 演示数据**：逾期书 1[OVERDUE+due_at-3d+copy borrowed]+押金孩[WM3 家长下 formal+_ensure_demo_deposit 复用]——**顺序教训**：t41_data 依赖两段建序置后[首跑押金孩 None 抓到]；**活动封面**：gen_cover 同款 Pillow 幂等生成→carousel 2 条实测） | **目视清单（4 项）**：①编辑弹窗正常弹出预填保存（不再白屏——R1）②小程序首页轮播双封面自动播放（R4）③已退会孩点播放→403 拦截不播（R2）④押金孩/逾期书孩走退款执行→六项复核拦截弹窗（R3）；R5 编辑=新增同 Modal 零改动（R1 修复后自然达成） |
| 第四批 p0batch4 | 活动域根治+拍板项落地+WM12 前置 8 卡（T40-T47；任务包：外部专家意见/20260903-外部顶级专家审查/26-第四批-活动域根治与WM12前置-20260907.md 随刀入库；苏格拉底拷问 7 问专家批复执行：Q1 两层断言/Q3 _outstanding_obligations 加法结构/Q4 押金拦/Q5 冒号前缀+manual_override/Q6 独立拍板文档/Q7 名额下限/Q8 白名单+activity_type 禁改） | ACCEPTED（用户目视通过 2026-09-09+专家严审收官） | 2026-09-07 C 级全量 gate PASS（gate-runs/2026-09-07/gate-p0batch4.log，**407 passed / 82.88%**，基线 399→407 只增；**BDD 67→103 steps**[T40 8 场景 36 steps 入正式集，264 skipped=draft 场景数不变——口径 LEDGER 留痕]）；**五轮收敛如实**：F-L9b 撞 BDD staff 目录断言（结论修正：activity.manage 恢复保留）+架构关三违（HTTPException/ORM 进 Router——AdminActivityService+quiz_batch 拆分清偿）+god file 2 处（activity/growth 拆子类/独立模块）+wm10 反向依赖（超管字面量）+ruff；docs/19 标准收尾：自动恢复 ✓+三项核验（35/4/9/**活动 2**——**T47-2 核验首战命中真缺口**：seed 原无活动造数，基线 1 系手动造，T40 清库后永久丢失→_ensure_demo_activity 幂等补齐） | 逐卡 hash：T40 `2ba9cc9`（**结构性根治**：activity_lifecycle.feature 8 场景——报名三路径×状态机全组合+资金不变量三对账；场景 8 两层断言[裸输出扫描+映射完备性对拍 models 六态]；自增手机号防 409/10 位案）/T41 `652318f`（**H-1 机器化**：_outstanding_obligations 共享[退会 7 项同源，加法结构]；execute 前六项复核——会员资格类+押金触发[Q4 担保语义]，活动/自定义豁免；不过 422 列未清项**不翻状态**[M2 语义]；「人工放行:」冒号前缀+超管+审计 manual_override；红测试 5 例）/T42 `a8573fb`（**M-2 对照表待拍板**：R-303 14 语义项→12 实现项全对齐，真缺失 0+额外加固 2 项[同家长/无预约]请拍板；docstring 虚标修正；docs/09/PRD 刷新挂拍板后）/T43 `5f4416f`（**U2 书架角标**：status-batch 3 次 IN 查询禁 N+1[拆 quiz_batch.py]；shelf 🏆 金标）+契约显形/T44 `ad4531a`（F-L9 staff.manage 幽灵码→audit.view+activity.manage 死码处置[结论修正如上]；F-L16 验证即完成；F-L19 三调用点 id 传参+白屏防护）/T45 `ea94d8a`(contract)+`abb4c1a`(后端)+`ba7c51c`(miniapp)+`7973246`(admin)（**FEAT-082 全链**：cover_path 列已有零迁移；详情统计/PUT 编辑[Q7 名额下限 Q8 白名单 activity_type 禁改 extra=forbid]/封面 R-316 转 JPG/cover-media token 双通道/miniapp carousel+卡片封面+管理端编辑 Modal）/T46 `8eaf72b`（WM12 前置核对表入 docs/09 欠账事实源：**4 PASS/3 PENDING——C-13 实锤 audio/progress 无 R-313 守卫直链可绕**[修法方向已列]+部署清单缺+wxpay 甲方阻塞 T3）/T47 `9e7bbf0`+`fdb247e`（CI v5/核验加活动计数/**seed 缺口补齐**/R9 评估留痕不做）；gate-fix 两刀 `5fb3a89`+`790f6a8` | **目视重点**：T41 六项拦截弹窗+「人工放行:」放行动线｜T43 书架 🏆 角标｜T45 封面全链[上传→管理端列表→**首页轮播**→活动卡片→详情]+编辑[activity_type 灰+名额下限 422]｜T44 书详情页正常打开（id 传参回归）｜T40 不需目视（BDD 即规格）；**M-2 对照表 2 项待拍板**（docs/任务包目录 27 号文档） |
| 插修 14 | 活动域三轮收口 4 卡（R7 补刀+目视 3 项；任务包：外部专家意见/20260903-外部顶级专家审查/25-插修14-活动域三轮收口-20260907.md + 24-R7 补刀包，随刀入库） | ACCEPTED（2026-09-07 用户目视实测通过+专家严审判 A·push 收口） | 2026-09-07 C 级全量 gate PASS（gate-runs/2026-09-07/gate-fix24.log，**398 passed / 82.77%**，基线 393→398 只增——R7/R8/R10 红测试 5 例+fix21 免费单测试改写；二轮收敛：ruff format+fix21 前提推翻+[9] 任务包 untracked 如实入库）；BDD 67 steps 不降；**docs/19 标准收尾**：自动恢复 ✓+三项核验（35/4/9 WM13 演示链预期/进程 gate 后新启/health ok/admin 200） | 逐卡 hash：R7+R8 `296c9d2`（**R7 补刀**：enroll() T6 发送段抽 _notify_enroll_manual 私有方法——家长报名+管理端造单双链调用[此前造单绕过发送点=通知 0 条，专家活体实锤]；免费活动管理端造单 422 防呆['免费无需创建订单，请走家长端报名']。**R8（#1·资金风险·本批最重）**：T2 造单段补 ACTIVE_STATUSES dup 检查[抄家长端 enroll 同款]——已报名孩再造单 422 拦截双倍收费；refunded 终态不拦[S4 口径回归锁定 200]；实施声明：R8 dup 检查与 R7 同刀先行实施，红测试后补锁定[4 例全绿]）/R9 `53bbbed`（**A4 口径第三犯收口**：筛选/表单 options 分离——TYPE_FILTER_OPTIONS 头插「全部」+STATUS_FILTER_OPTIONS，Select 去 allowClear 改显式空串[切筛选回第 1 页]；**sweep 实锤漏点 DepositManage 押金状态同修**；MemberManage×2/Reservations 合规 ✓/BookManage+BookDetail allowClear=表单用途合理 ✓；机械判定[verify_architecture 加项]：TSX 筛选 Select AST 解析复杂度高——记 docs/09 待办简报声明不硬造假检查）/R10 `ce73e58`（**计数同源第 7 案·用户裁定推翻 T6 旧裁定**：活动报名待确认统一进 admin_total——运营视角一个数=全部要干的活['审核五类'历史口径废弃]；tab 数字[list_inbox pending_count 全场景]与徽标严格同源；activity_enroll_pending 细分值保留供活动徽标；**跳转目标选择声明**：'处理'=确认收款→/members?tab=orders[confirm-payment 唯一入口，活动中心报名名单区无收款动作]；ADMIN_REF_ROUTE+refText'活动报名'+REF_META 同补；红测试 += 位置首实施在计算前读 0——自测捕获修正如实留痕）/fix21 测试改写 `a5432ab`（R7 防呆推翻 fix21 免费单 confirm 前提——改写为新契约 422 锁定[非绕过，注释留痕]） | 补刀 R10b（目视反馈：'去处理'到订单 tab 无高亮）：订单行响应补 enrollment_id 高亮锚点[contract-change `3a1f4d6`：OrderResponse+list_orders 批量映射零 N+1+快照显形+schema.d.ts 重新生成]→MemberManage 订单 tab readUrl highlight+hlArmed 数据到达起算 3s[R5 教训]+rowClassName 匹配 wm13-highlight-row（跳转链闭环：通知→订单 tab→行高亮）`6a4f675`；gate fix24 三轮 PASS **399 passed / 82.79%**；**目视清单（4 项）：**①已报名孩管理端再造同活动单→422 拦截（R8）②类型下拉显式「全部」（R9）③报名待确认通知出现在管理待办[家长端报名+管理端造单双链各验一次]+**tab 数字和侧边栏徽标都含它**（R7+R10）④该通知"去处理"按钮→跳会员管理订单 tab（R10） |
| 插修 13 | 活动域二轮返工 6 卡（任务包：外部专家意见/20260903-外部顶级专家审查/23-插修13-活动域二轮返工-20260907.md，随刀入库；诊断：活动域 WM9 欠账一次性暴露——4 项非上批修坏） | ACCEPTED（2026-09-07 用户目视实测通过+专家严审判 A·push 收口） | 2026-09-07 C 级全量 gate PASS（gate-runs/2026-09-07/gate-fix23.log，**393 passed / 82.74%**，基线 386→393 只增——T1/T2/T6 红测试 7 例；四轮收敛如实：F841 修一半/E731/def 换行忘 format——**教训 59 强化：测试文件就位即 ruff check --fix+format 双跑，commit 前全量再验**）；BDD 67 steps 不降；[8] 契约快照显形（T3 contract-change 单独 commit）；[9] PASS；**docs/19 标准收尾**：自动恢复 ✓+三项核验（35/4/9 WM13 演示链预期/进程 gate 后新启/health ok/admin 200） | 逐卡 hash：T1 `ffde72c`（计数同源第 6 案：家长未读胶囊补数据源——todo-counts 补 parent_unread 全量 COUNT[权限分支外=导航感知]，胶囊兜底全局数+loadParent 本地覆盖双源同值）/T2 `56c4ff9`（**用户裁定推翻插修 11 R2 修法**：管理端活动单=报名代客创建——TYPE_ACTIVITY 名额校验前置[满员 422]+order flush 后同步 enrollment[PENDING_PAYMENT+ticket+占名额]，与家长端殊途同归；confirm 联动自动全量恢复；**实况修正**：手动 cancel 链"已有"联动系任务包误记——实况仅 timeout 链有，补齐；红测试 4 例）/T3 `67da085`（contract 快照 `8e12eef`）（活动列表三件套：keyword 标题 ilike+activity_type+status 组合 AND，B7 先例；契约快照先行显形；前端搜索+双 Select）/T4 `43dce9d`（**枚举错配老毛病**：detail wx:else 兜底裸输出英文→refund_pending/cancelled wx:elif 补齐+防御文案；sweep：refund-apply else 裸输出→statusText 全枚举映射，reservation/deposit 无裸输出清白）/T5 `b1a9ae0`（付费/免费按钮互斥：付费只留"取消报名并申请退款"[用户语义原话]，免费只留"取消报名"，一屏一主操作）/T6 `804e025`（付费报名进管理待办：新场景 admin.activity_enroll_manual[ref_type=activity_enrollment 独立防 resolver 混流]+resolver _decide_enroll/_enroll_states+todo-counts activity_enroll_pending **单独口径不进 admin_total**[admin_total=审核五类]+confirm 联动 mark_handled+家长"报名确认+入场券码"通知[实况修正：任务包"若已有复用"→实况无，补发 dedup_key=paid-{id}]+Layout 线下活动徽标；staff member.manage 可见实测）；T6 hotfix（resolver 双 @staticmethod/_decide_activity 装饰器失位——gate 四轮收敛捕获修复） | **目视清单（6 项）**：①admin 视角家长 tab 胶囊真实未读数（T1）②管理端造活动单→小程序见待收款报名→确认收款→报名转正（T2 全链）③列表搜索+类型/状态筛选（T3）④退款待审中文状态（T4）⑤付费活动单按钮"取消报名并申请退款"/免费"取消报名"（T5）⑥付费报名→通知中心管理待办【活动报名待确认】→去处理跳活动页（T6） |
| 插修 12 | 活动域目视返工+计数同源第 5 案 5 卡（任务包：外部专家意见/20260903-外部顶级专家审查/22-插修12-活动域返工与计数同源-20260907.md，随刀入库；与插修 11 合并施工——**11 已先期交付 6 刀+gate fix21 PASS，专家'未开工'口径纠偏**） | ACCEPTED（2026-09-07 用户目视实测通过+专家严审判 A·push 收口） | 2026-09-07 C 级全量 gate PASS（gate-runs/2026-09-07/gate-fix22.log，**386 passed / 82.64%**，基线 385→386 只增——S4 红测试 1 例：refunded 全链→detail my_enrollment=None 实锤+可重报断言；二轮 FAIL=ruff 测试格式如实收敛）；BDD 67 steps 不降；[8] 契约快照一致（S3 仅注释未动 schema）；[9] PASS；**docs/19 标准收尾**：gate 自动恢复 ✓+三项核验（书目 35/家长 4/孩子 9 WM13 演示链预期/进程 gate 后新启/health ok/admin 200） | 逐卡 hash：S1 `e54f2f4`（**计数同源第 5 案语义分工收口**：侧边栏徽标=管理待办+分工表注释锚点两处[Layout+Notifications]；家长 tab 补未读红底白字胶囊——两真实数各归各不互相冒充）/S2 `e54f2f4`（确认 toast 按订单类型分 7 文案；sweep：'会员权益已开通'全前端仅此一处）/S3 `343713a`（活动中心删'退款待审'tab——refunds state/apiListActivityRefunds/apiReviewActivityRefund/onReview/isSuper 分叉全删，活动页全员同构；**后端端点处置实读声明**：退款中心走统一 refund-requests 链→GET /activity-refunds+POST /{id}/review 成死代码→deprecated 注释保留[契约兼容；S4 测试造数仍用，统一链语义由 RefundService.review 委托保证]；[8] 无 diff）/S4 `082acd5`（_my_enrollment_map 仅 ACTIVE_STATUSES 入 map[终态不占入口位，历史'我的活动'仍全量可见；enroll 判定本就正确不动]；前端 activity-detail load 防御对齐[终态置 null]；红测试先红后绿）/S5 `97c447e`（FEAT-082 登记 REGISTERED 排期第四批[docs/03 表行+附录 Gherkin 草案 D8 先例]；纯 docs 零门禁）；**目视清单（11+12 合并）**：R1 家长 tab 可点/R2 活动单确认成功/R3 staff 行展开/R4 联动单查看/R5 高亮 3 秒/R6 徽标双挂/S1 家长胶囊+侧边栏分工/S2 toast'活动费已确认'/S3 活动中心无退款 tab/S4 退款后可再报名/S5 无（登记卡） |
| 工作区清理 + PRD 收敛（2026-09-06，专家亲自施工·用户授权） | 全项目垃圾/上下文污染文件清剿 + PRD 文件夹收敛为单一现行文档 + R 编号对照表并入 PRD | DONE（纯 docs/文件操作，零门禁口径 E-20260831-03；git diff 验证） | **A 档物理删**：.DS_Store×9 / backups/×3（空壳旧备）/ admin-web/dist / .coverage / gate-runs 2026-08-24~31（对应模块全 ACCEPTED，证据已收割）；**B 档归档 docs/legacy-attic/**：docs 过程文档 16 项（10/11/12/13/14×2/16/17 号报告+老任务书+20260831 任务包×3+交接 20260823+规划提案+UX 清单+专家交接书 20260901 已被取代版）+ 错误记忆库旧 4 版（0812/0828/0829/0830，0831 为活文档）+ 外部专家 20260831 修复包全目录 + 风险分析报告 20260829；**C 档 PRD**：删 4（详版 V0.2×2/确认终版 V1.0/docx 副本），修正稿归档 attic；**核心交付：PRD 附录 A 规则编号对照表（R-300~R-323 共 24 条，三对照=修正稿原文→PRD 正文→代码实况全部亲验）**——9 条 🔁 已修订（R-307 等级不回算/R-308 B-15 改造/R-309 T16 台账统一/R-311 T5 死锁出口/R-313 E-9+B-11+B-12 额度与押金拦截/R-319 超额实现/R-322 枚举对照/R-323 frozen_amount 不做/R-303 M-2 复核中）+ 2 条 ⏳（R-310 H-1 机器化/R-320 WM12 三时点）；活引用修正 2 处（AGENTS.md 交接 20260823 路径/docs/01 规划提案路径）；历史行旧路径不回写（日志性质，attic 可循） | 待用户过目后 commit；PRD 附录 A.3 维护规则：后续 R 规则变更必须同步刷表（禁止只改代码不刷表）；**已 commit 3e06ede（2026-09-06），攒批 push——随 WM13 复测全绿后与 tag 双打同推（用户裁定）** |

## 状态机

PENDING → IN_DEV → GATE_PASS（自动门禁 exit 0，贴 gate-runs 证据）→ MANUAL_TEST（用户按手册测）→ ACCEPTED / REWORK → GATE_PASS（循环）
| 文档对齐批（2026-09-09） | 两轮 push 后全量文档同步（用户抓的停更缺口）——PRD 附录 A 4 行修正（R-303 M-2 完成/R-310 六项复核机器化 ✅/R-313 音频门禁/R-316 活动封面）；docs/02 横切防护层+状态机执法段（8-22 三周停更清偿）；docs/04 WM9 增补 12 步（FEAT-082+音频门禁动线）；docs/01 阶段表 2 行；docs/08 技术债清偿台账 TD-1~7；docs/18 端点族+媒体消费点纪律；教训 E-20260909-01 入库（防复发：任务包模板加文档影响面必填段） | DONE | 专家亲修（判断密集型，9-06 先例）；纯 docs 零 gate（分级纪律）；防复发=五件套制从此生效 | 下一批任务包起交付简报四件套升五件套（第五件=文档对齐核对表） |
| 小程序审查批接手返工（2026-09-14/15，专家接手） | 前轮模型批量令牌化/上收后用户报"页面面目全非、点击没反应"，接手逐类排查并返工 | **ACCEPTED**（2026-09-15 收口：后续视觉三轮全部落地、用户"整体 ok"并授权提交；原 IN_DEV 时的未提交状态已成为历史） | **本批另闭环三项**：①**主包体积** 2731KB→**1426KB**（`scripts/optimize_miniapp_icons.py` 降采样 icons，文件名不变零代码改动；`cli preview` 成功出码）②**22 页/44 张分段滚动截图 + 逐段遮挡核查**（未发现遮挡/覆盖；固定底栏与正文分离；四页末段空白经查为内容短非缺失）③**活动封面烧字被 aspectFill 裁出碎片** → `gen_cover(with_text=False)` 修复并复截确认。`check_miniapp_style.py` 规则 8→**11**（新增 R9 花括号结构 / R10 组件变量兜底 / **R11 非法选择器**；R1 放行 `var(--x, 字面)` 兜底；豁免白名单 6 条）→ **exit 0 全零**；全量 gate 见 `gate-runs/2026-09-14/`；**30/30 页逐页渲染校验 + 截图取证通过** | **六类回归全部落地**：①组件作用域缺变量（`custom-tab-bar` 令牌化致背景透明/贴纸框消失/红点失色 → 改带字面兜底，A/B 截图验证）②不透明→半透明冲淡（411 替换对中 11 处，还原 7 处）③10 个 wxss 结构性畸形（补尾括号后遗症，状态机精确删除）④元素级并集对比查出 46 条声明丢失（`.info-card` 仅剩 `position:relative`、`.h-section` 全仓无定义、`.info-row` 丢 flex）→ 对照 HEAD 逐条核回，**不复原**有意改动（负 margin 归零）⑤组合规则被误删致 `.info-card` 边框消失 → 随④核回⑥`.info-row` 同义类重名撞车（全局卡片风 vs 页面表格行风）→ 退款页改挂 `.x-3fde25`；**🔴 真凶（用户"点击没反应/卡死"）**：全局令牌子串替换把类名 `gold`/`silver` 也换掉 → `.podium-card.var(--gold)` 非法选择器 → **member-pkg 分包 WXSS 编译失败 → 该分包 7 页白屏**（重编译后错误 2→0，全部恢复；R11 已机械化拦截）；**新增教训 E-20260914-06…11**（含接手方自身两次"假 0"：检测器未自证 → 固化为"上线前必须用构造违例自证 + 打印基线计数"） |
| 后台媒体预览统一（2026-09-15，用户裁定） | 用户报「图书详情封面预览非常清新干净，全后台有图片或凭证预览的地方统一采用这个效果；收款凭证那种**带窗口的预览**很不好用」 | **MANUAL_TEST**（等用户目视验收；门禁 PASS） | **全量枚举 7 处媒体预览点**，改前分两派：干净派（antd `Image` 内建 preview）只有 BookDetail 封面 + GrowthManage 报告图；带窗口派（`<Modal><img/></Modal>`，640px 弹窗 + `maxHeight:70vh` + **不能缩放**）三处——收款凭证回看、凭证/评估报告选图本地预览、阅读圈成就卡片；另有 2 处（活动详情/编辑封面）是裸 `<img>` **根本没有预览入口**。**修复**：新增 `admin-web/src/components/PreviewImage.tsx` 作为唯一实现——`PreviewImage`（缩略图 + 点开全屏，视觉基准逐字取自图书详情封面：圆角 6 / 1px `--paint-border` / `object-fit:cover` / 遮罩「预览」/ 虚线空态）+ `useImageViewer()`（命令式打开，Blob 回收挂关闭回调）；7 处全改走它，**删掉 3 个带窗口预览 Modal**。**两个技术坑**：①受控宿主 `style={{display:'none'}}` 不会连预览一起藏（rc-image 的 style 落 `<img>`，Preview 走 Portal）②`preview={{mask:null}}` **不是**「用默认遮罩」而是**不要遮罩**（antd 默认值在前、调用方 preview 展开在后）→ 删掉 `maskText` 参数，组件内固定 `mask:"预览"`。教训入库 E-20260915-28/29（§七十六）；`docs/15` 增 §十三「媒体预览统一规范」V1.3 | 证据：浏览器逐点目视 5 处（图书封面 / 收款凭证 600×900 / 成就卡片 / 活动详情抽屉内的封面 / 周报长图），点开后 `.ant-image-preview-root=1` 且 **`.ant-modal-wrap=0`（结构性证明无窗口壳）**，凭证连点两次放大仍清晰；`pnpm exec tsc --noEmit` exit 0；**gate 全量 PASS**（`gate-runs/2026-09-15/gate-222930.log`：464 passed / 覆盖率 84% / BDD 8 features·30 scenarios·103 steps 全过 / 架构关·契约·docs-code·miniapp 风格·alembic·tsc·openapi·交付完整性 9 步全绿，exit 0）。**浏览器验收所造的测试数据已还原**：删除验收用订单（`orders.id=10`）+ 2 个凭证文件，订单 4（WM3-DEMO-1789469913）还原 `pending_manual_confirm` 且清空 `pay_method/paid_at/voucher_path`，过期孩（child 4）还原 `member_expire=2026-09-14 / member_start=NULL`、小红（child 10）还原 `member_status=none`；gate 后按 docs/19 又跑了一次清场（`test_p0_t8_rate_limit` + restart），三项核验 = **书目 35 / 家长 3 / 孩子 11 / 活动 2（干净基线）** + 后端 22:46:08 新起 health ok + admin-web 200（confirm-payment 的会员联动副作用；seed 的 `ensure_child` 不覆盖已存在行，必须手工还原） |
| 报告图绘本风重做（2026-09-15，用户报障） | 用户：「生成的周报和月报，跟本项目的样式风格格格不入」 | **MANUAL_TEST**（等用户目视验收；门禁 PASS） | **根因（E-20260915-30，E-28 同族）**：同一个「要出图」的业务有两套画法——阅读圈卡片/头像/勋章走 `reading_circle/art.py`（超采样画布+马卡龙渐变+白描边贴纸字+圆角气泡+纸纹），而 WM8 报告图在 growth 域 **又手搓了一遍矩形+系统字体**（深蓝横幅 `#2c4a6e` + Hiragino/STHeiti + 细线白卡 + 金字），与绘本令牌零交集；art.py 注释里「四类资产必须同源同风格，各画各的必然割裂」这条纪律早写死了，报告图没被算进「资产」才漏网（同坑第二次）。**修复**：新增 `paint_report()` 让报告图**全部改走 art 引擎**——周报=薰衣草紫 `PALETTES["weekly_report"]` / 月报=薄荷绿 `PALETTES["books_count"]`；版式=标题横幅→孩子·周期→主数字卡（大词数 + **孩子自己的头像动物**做吉祥物探头）→2×2 统计卡→鼓励语→日期条→馆标；数字 `font_round`、中文 `font_cn`、量词 `sticker_pair` 与数字整体居中；`paper_grain` 收尾。尺寸仍 750×1100（两端展示布局零改动）；删掉已死的 `FONT_CANDIDATES`/`_font`。零阅读周期鼓励语改「这个周期还没有阅读记录，今晚挑一本开始吧～」。**施工中自查抓到 E-20260915-31**：标题横幅按「看着差不多」写 450px，实测字宽 454.1px → 白字两端糊在渐变底上；改为先用 `textlength` 量、再定 (100,54,650,146)，并把云朵挪到横幅两侧探头位。 | 证据：`tests/unit/test_wm8_features.py::test_report_image_drawn_on_picture_book_palette`（**像素锁色盘归属**：左上角背景 = `art.PALETTES[...]["top"]`，容差 30 抵纸纹；**已自证**：喂旧版深蓝横幅会红、喂新版会绿）；周报/月报/零阅读月报三张样图逐张目视（云朵探头、吉祥物=孩子头像动物、圆体数字、鼓励语分支）；浏览器实操 成长与测验→成长档案→生成周报图片 见新画风落地；`pytest tests/unit/test_wm8_features.py -k report` 2 passed；**gate 全量 PASS**（`gate-runs/2026-09-16/gate-121628.log`：465 passed（基线 464→465，新增本例）/ 覆盖率 84% / BDD 103 steps / 架构关·契约·docs-code·miniapp 风格·alembic·tsc·openapi·交付完整性 9 步全绿，exit 0）；门禁后清场 + restart，三项核验 = **书目 35 / 家长 3 / 孩子 11 / 活动 2（干净基线）** + 后端 12:33:18 新起 health ok + admin-web 200。 |
| 阅读链三页修复 + 悬空类名门禁 + 未接线模块清理（2026-09-16，用户"按你的推荐来，开工"） | 用户点名的三页视觉欠账 + v40 §七 的未接线模块清单（用户裁：SMS 与 mock 留着） | **MANUAL_TEST**（等用户目视验收） | **① 先纠正我自己的审计错误**：交接卡记的「读者页 佚名 / 0:00」**不存在**——automator 复核为正常（作者 Bill Martin Jr / 时长 0:05）；真因是我拼 URL 时用了 `child_id=1`（演示孩其实是 2）→ 音频 422「孩子不存在」→ onError 弹窗 + duration 恒 0，我把**自己拼错参数的产物**记成了产品 bug（错误库 E-20260916-33）。**② 真修 4 处（全部截图前后对比）**：reader 速度档 `speed-chip-active`→`active`（选中态一直不亮）；reader 完播弹窗 `num`→`val`（数字掉默认样式）；quiz-result 补齐 `score-ring`/`score-inner`/`--pct`（wxss 早写好三层环形、wxml 只渲染数值层 → 环形退化成实心米灰盘子 = 用户说的「发闷」）；quiz 已通过态仍挂「提交测验」+「1/5·剩余机会」→ 底栏与进度行补 `!== 'passed'`。**③ 机械门禁 R12**（`check_miniapp_style`）：WXML 类名必须在**本页作用域**有定义（WXSS 按页隔离），只判静态可判类名（`badge-{{...}}` 按拼接判定，修掉 3 个假阳性）；存量 22 个类名/35 处引用进 `R12_BASELINE`（只许减不许增）；**三重自证**：内置注入样本 + 实树注入悬空类 FAIL + 还原 PASS。**④ 未接线清理**：删 3 个假死模块（question_validation / pdf_service / consent_texts）；`escape_like` **实测 12 处**搜索全接线（原估 3 处）+ 架构关 `check_like_escape` + `test_search_wildcards_are_escaped`（**均已自证**：注入旧写法会红、临时关掉转义也会红）；SMS 两家/mock_routes/consent.js 加「未接线」标注。文档：`docs/08` TD-9/TD-10/TD-11、错误库 §七十八（E-32/33/34）、交接卡 §3.6 与 §四/§七 口径纠正 | 证据：automator 逐页截图（reader 选中态修复前后 / quiz-result 灰盘→80% 绿环 / quiz 已通过页底栏消失）；`pytest tests/unit/test_wm3_member.py -q` 13 passed（含新通配符测试）；`check_miniapp_style` R1–R12 全零 （R12 新违规 0 / 基线 22）；`verify_architecture` PASS（LIKE 全转义）；**gate 全量 PASS**（`gate-runs/2026-09-16/gate-154255.log`：466 passed（基线 464→466，新增本例两例）/ 覆盖率 84% / BDD 103 steps / 9 步全绿 exit 0，第 [4] 步已打印「LIKE 全转义」、第 [5] 步 R1–R12 全零）；门禁后清场 + restart，三项核验 = **书目 35 / 家长 3 / 孩子 11 / 活动 2（干净基线）** + 后端 16:00:42 新起 health ok + admin-web 200。 |
| 头像不一致修复：小程序孩子列表刷新（2026-09-16，用户报障「点赞的头像跟我的页面的头像不匹配」） | 同一份孩子资料两处显示不同：点赞墙走服务端实时、我的页走**登录那一刻的本地快照** | **MANUAL_TEST**（等用户目视；门禁 PASS） | **复现（真实入口）**：后台把演示孩头像 cat_sun→penguin_mint 后，阅读圈/点赞墙显示企鹅，「我的」页仍显示猫，只有重新登录才纠正。**真因**：`session.getChildren()` 只读本地缓存且**全项目没有任何刷新入口**；顺带暴露第二个缺陷——登录载荷**缺 member_expire/member_start**，而 `_expireLine()` 依赖它 → **正式会员的「到期时间」行从来没显示过**（E-20260916-35）。**修复**：① 后端抽 `identity.auth.children_payload()` 作**唯一序列化出口**（登录与刷新共用，补齐到期字段）+ 新增 `GET /api/miniapp/children`；② 小程序 `session.refreshChildren()`（写回缓存 + 校正失效的 currentChildId，**6 秒超时 + 失败清零节流**），「我的」页 onShow 先 await 刷新再渲染。**施工中踩到并修掉两个微信运行时坑**：⑥ `getApp()` 在 `App.onShow` 里返回 undefined（E-36，刷新改挂页面 onShow + request.js 加显式守卫把时机错误翻译成人话）；⑦ **去重锁无超时**——实测 `api.myChildren()` 进入后 promise 永不 settle，`_inflight` 锁被永久占住导致刷新静默失效（E-37，改 `Promise.race` 超时兜底）。**测试**：`tests/unit/test_miniapp_children_refresh.py` 3 passed（同源字段集合对拍 / **后台改头像后刷新端点必须看到新值**（端点若返回快照必红：测试孩子初始无头像）/ 越权只回本人孩子）。 | 证据：automator 真实链路截图（改头像前「我的」=猫 → 改后修复前仍=猫（复现）→ 修复后=企鹅，且「到期时间 会员到期 2027-08-17」行首次出现）；阅读圈 post1 作者头像同为 penguin_mint（两端一致）；`pytest tests/unit/test_miniapp_children_refresh.py -q` 3 passed。文档：`docs/18` 增补端点族（**载荷单出口纪律**）、错误库 §七十九（E-35/36/37）、docs/04 增第 10b 步。**gate 全量 PASS**（`gate-runs/2026-09-16/gate-170551.log`：469 passed（基线 466→469，新增 3 例）/ 覆盖率 84% / BDD 103 steps / 9 步全绿 exit 0）。⚠️ 首轮 gate 曾红在 **[8] 契约快照**（`+ 端点新增 /api/miniapp/children`）——T27 设计正是如此：改端点必须更新快照显形。已按 docs/18 §五 补跑 `export_openapi.py` + `pnpm gen:api`（**顺带发现 `admin-web/src/api/schema.d.ts` 自 09-12 起就没再生成过**，漏了 passed-books / withdrawals settlement / unlike 的 child_id 参数，一并补齐）。快照更新应作 **contract-change: 单独 commit**。门禁后清场 + restart，三项核验 = **书目 35 / 家长 3 / 孩子 11 / 活动 2（干净基线）** + 后端 17:21:17 新起 health ok + admin-web 200；重 seed 后再验一次小程序「我的」页（本地缓存已彻底失效）→ 页面正确同步为服务端值（cat_sun + 到期行）。 |
| 视觉二批：悬空类名清零 + emoji 换自家图标（2026-09-16，用户"全都做，开工"） | R12 基线 22 个悬空类名 + 22 页约 40 处 emoji 当图标 | **MANUAL_TEST**（等用户目视；门禁 PASS） | **① R12 悬空类名 22 个全部清零**（R12 基线表清空、规则改为"新增即红"）：类名写岔 2（`plan-feats`/`section-content`）、设计缺失 12（`page-bg`×4 从未定义、`hero-left` 真布局、`badge-member`、`section-label`、`hero-cover-empty`、`order-icon-text`、`qr-bright-hint`、`is-unread` 去冗余、`check-item.active`、`record-arrow.cancel`）、换共享件 3（observation-report / vocabulary / share 手搓空态 → `components/empty-state`；messages 加载失败行改 `.list-error`）、去冗余类 2（`pay-button-container`）。**② emoji 当图标 → 自建图标体系**：新增 `scripts/gen_ui_icons.py`（art 引擎绘制，与头像/勋章同语言）产出 **37 枚 96×96 PNG** 到 `miniapp/icons/ui/`；22 页图标槽位逐个换 `<image>`；`empty-state` 组件 `icon`(emoji) → `icon-name`(自家名)，14 个调用点统一。**③ 机械门禁 R13**（双向）：图标槽位出现 emoji = FAIL + 引用的图标资产不存在 = FAIL（含 `TYPO_GLYPHS` 白名单：✓✕★☆▶ 属设计系统字形不算 emoji）；**R13 上线即回本**——扫出我自己漏掉的首页"快捷入口"8 个 emoji。**④ 施工自伤被目视抓到（E-20260916-40）**：books 页换占位图时**漏搬 `wx:else`** → 每张书卡都多一个幽灵占位图，同时 `width/height` 覆盖了原 340rpx 容器定义；已修（补 `wx:else` + 占位改"容器+图标"两层）并 grep 全量复查其余替换点。 | 证据：`check_miniapp_style` R1–R13 全零（R12 新违规 0/基线 0；R13 emoji 0/资产缺失 0）；check_miniapp_bindings ✓；`docs-code` ✓；架构关 PASS（含 LIKE 全转义）；ruff 全绿；automator 逐页目视：我的（10 宫格图标）、首页（通知行 3 + 快捷入口 8 + 继续听）、图书馆（搜索/音频/占位修复前后对比）、书架、活动列表；**gate 全量 PASS**（`gate-runs/2026-09-16/gate-220037.log`：469 passed / 覆盖率 84% / BDD 103 steps / 9 步全绿 exit 0，其中 [5] 打印 `R13 图标槽位 emoji: 0 / 图标资产缺失: 0` 与 `R12 新违规 0 / 基线 0`，[8] 契约快照一致）；门禁后清场 + restart，三项核验 = **书目 35 / 家长 3 / 孩子 11 / 活动 2（干净基线）** + 后端 22:20:11 新起 health ok + admin-web 200。 |
| 交接 v41（2026-09-16，第八轮收尾） | 用户「做一个完整的交接，@handoff 我要压缩一下上下文，务必完美交接」 | **DONE（交接卡）** | 新增 **v41 区块**（`docs/项目交接-20260824.md` 置顶，八节自包含：用户常驻视觉目标 + 当前状态 + 本轮 5 刀明细（含 commit 号）+ 下一轮建议 + 防复发红线（本轮新增 6 条，编号 11~16）+ 环境命令（含 automator 真登录三段式与 `gen_ui_icons.py`）+ 7 项未决 + 「压缩后读哪个文档」）；**v40 已归档**（标题加「已归档，最新见 v41」）。**诚实修正两处笔误**：把"62 项 Characteristic"改为 Feature；把战绩统计从"4 刀用户报障/1 刀自查"改为**"3 刀用户发起（预览裁定/报告图/头像）+ 2 刀我自查（阅读链三页/类名与 emoji 二批）"**——原写法美化了自查占比。⚠️ **交接卡首条即写明：本批 56 文件未提交**，接手第一动作是复核+提交+push+盯 CI。文档引用自检：`check_docs_code_alignment` ✓（23 文档 / 163 接口 / 40 配置键 / 38 表）。 |
| **视觉二批入库 + 一次自我误判的纠正**（2026-09-16，用户「把这 57 个文件提交 + push + 盯 CI」） | 视觉二批 + 交接卡全部未提交（`git status` 57 行 / 展开 93 文件） | **DONE（已提交 `a7e46f6`）** | **① 提交前重跑全量门禁复核**（只 `docs/LEDGER.md` 在上一轮门禁之后被改过，仍整套重跑，不做"看起来没动代码"的推断）：`gate-runs/2026-09-16/gate-224505.log` **9 步全绿 exit 0**（469 passed / 覆盖率 84% / BDD 8 features·30 scenarios·103 steps；[4] 架构关含 LIKE 全转义；[5] `R13 图标槽位 emoji: 0 / 图标资产缺失: 0`、`R12 新违规 0 / 基线 0`；[8] 契约快照一致；[9] 交付完整性 ✓）→ 提交 `a7e46f6`（**93 文件 = 55 改 + 38 增**，含 37 枚图标资产与 `scripts/gen_ui_icons.py`）→ push → 盯 CI。**② 现场核验时我先把读数搞错了一次（错误库 E-20260916-41，如实记）**：跑完门禁不按清场流程直接 COUNT，读到 **家长 4 / 孩子 12**，据此一度判定"文档里的 3/11 是过期数字"并改了文档。**按 `docs/19` 实测证明是我错、文档对**：多出的 `学籍家长B`/`学籍孩B` 是 **behave 残留**（`features/wm3_member_edit.feature`；本批 gate 日志 23:01:18–23:01:21 有 `action: parent.create, detail: {'name': '学籍家长B', 'phone': '13811120002'}` 原文），种子不清它；按配方 `pytest tests/unit/test_p0_t8_rate_limit.py` + `dev.sh restart` 清场后计数**回到 35 / 3 / 11 / 2**。**③ 把清场固化成工具**：新增 `scripts/check_demo_baseline.py`（清场 → restart → 计数 → 与基线比对，一条命令 `python -m scripts.check_demo_baseline`）；**双向自证**：注入 1 条脏家长 → `家长: 实测 4 / 期望 3` **FAIL exit 1**，删除后 → **PASS exit 0**，默认清场路径实测 **EXIT=0**；`docs/19` §一 第 4 步改为指向本脚本（并补记残留来自 pytest **与 behave** 两边）。**④ 文档更正**：交接卡 §二/§六 改回 **3/11 并写明"脏读数 vs 清场读数"**、§四 第 2 项标记已闭合、§五 新增红线 17（判数字对错前必须先清场）。ruff 全绿（`scripts/check_demo_baseline.py`）。**⑤ CI**：`35113928738` **success（8m47s）**（push `198f902` 触发；GitHub 一次 push 只跑 head commit，两条 commit 共用这条绿）；push 后本地三项核验复核 = 基线 **35 / 3 / 11 / 2** + 后端 23:12:03 新起 health ok + admin-web 200。 |
| **图片体积纪律（上传必压 + 生成图控体积 + 存量回压）**（2026-09-17，用户裁定「磁盘有限…jpeg / 要回压存量 / 1600 够 / 证据目录删掉」） | 图片无体积纪律：上传只转格式不缩尺寸、观察报告**原图直存**、生成图 PNG 装纸纹噪点、报告图每次查看落新文件 | **ACCEPTED（用户 2026-09-17 口头目视通过「就当我目视通过了，感觉问题不大了」；已提交 `8920b64`，CI `35187510750` success 8m40s）**。备注：用户未逐条走 `docs/04` §十一 的 5+4 步，属**口头放行**，故清单保留在手册里供日后回归 | **① 上传侧（用户点名第一件事）**：新增 `file_storage.normalize_image()` 作**唯一出口**——按 header 拒超像素图 → EXIF 摆正（`exif_transpose`，此前全项目无此调用，手机竖拍会存歪）→ 长边超限才 LANCZOS 缩放 → JPEG(optimize+progressive) → 超标则逐档降质(-5/-10/-15)再缩边(×0.8,≤3 轮)；四处接线：封面(≤1080)/活动封面(≤1200)/凭证与观察报告(**doc 类 ≤1600，要看小字**)，其中观察报告由 `open(...,wb)` 原图直存改为走管线；路由层按 `file.size` **解码前**拦 >8MB（巨图不进内存）。**② 生成侧**：`Canvas.finish(path, quality=)` 分 PNG(要 alpha 的资产)/JPEG 两态，卡片大图+缩略图、周报月报、海报全部收口到它（海报原是自己一份 save，顺手合一，不留第二套画法）；缩略图规格 `v3→v4`（版本进文件名 ⇒ URL 变 ⇒ 端上缓存必刷）；**5 处**媒体端点的 `media_type` 由硬编码改按扩展名派生（4 处 image/png + 观察报告 1 处 image/jpeg，存量可能是 PNG）（`file_utils.image_media_type`）。**③ 施工中发现并修掉一个真实泄漏**：报告图**不落库**、端点每次请求都出图，旧命名用 随机 uuid 命名 ⇒ 家长每看一次就多一个文件；改按 `sha256(report_data)[:10]` 幂等命名 + 清理同(孩子,类型)历史文件（同内容零新增，内容变才换名）。**④ 数值全配置化**：新增 7 键（`image_upload_max_mb`=8 / `image_upload_max_output_kb`=600 / `image_jpeg_quality`=85 / `image_generated_jpeg_quality`=85 / `image_cover_max_edge`=1080 / `image_activity_cover_max_edge`=1200 / `image_doc_max_edge`=1600）。**⑤ 存量回压**：新增 `scripts/optimize_uploads.py`（默认 dry-run；引用集**复用** `cleanup_uploads.collect_referenced` 不另写一份）——阅读圈旧 PNG 重渲换名、报告图删存量（按需重出）、上传图**原地重编码**（同名 ⇒ 端上不会「变图」），原字节进 `--trash` 备份。**⑥ 磁盘**：`cleanup_uploads.py --trash` 清 860 个孤儿 91.1MB（**引用文件 49/49 零缺失**复核）→ uploads **132MB → 36MB**；再按用户选 (a) 删除三个证据目录（`miniapp-audit-20260913`/`wm15-samples`/`fix34-samples`，31MB）→ **uploads 5.3MB**（**口径警告**：5.3MB 是「删证据目录后那一刻」的读数；**双 seed 重种现场后回升到 ~9MB 属预期**——种子会重新生成 35 本书封、卡片、凭证等，不是回压失效）。**⑦ 测试**：`tests/unit/test_image_size_policy.py` **8 例**（上传限幅/凭证 doc 口径/观察报告压缩/EXIF 摆正/超大拦截/卡片与缩略图 JPEG≤250KB/报告幂等/存量 PNG 仍按 png 提供），**三处自证**：关掉全部缩边 → `assert 3000 <= 1080` 红、关掉 EXIF → 竖版断言红、关掉两层体积校验 → 「图片文件无法解析」替代「体积超限」红；既有断言同步：`test_wm15_circle` content-type png→jpeg、`test_wm8_features` PNG 魔数→JPEG SOI + ≤250KB。**⑧ 文档**：`docs/15` §十六（图片体积规范，V1.4→V1.5）、`docs/08` TD-13、`docs/04` 证据目录引用改注「已删+重出命令」、`cleanup_uploads` 保护名单注释同步。 | 证据：`pytest tests/unit/test_image_size_policy.py` **8 passed**；受影响域回归 `test_wm15_circle/test_fix34_circle/test_fix33_circle/test_wm3_voucher/test_wm8_features/test_p0b2_t25_observation_image_authz` **52 passed**；种子实链验证：restart 后新帖落 `card_*.jpg`/`thumb_v4_*.jpg`（22–27KB，旧 `thumb_v3_*.png` 302KB）；回压实测：报告图 2.72MB→0、上传图 37 个 1.43MB→1.17MB；孤儿清理后 DB 引用 49/49 在盘。⚠️ 存档：被删的审计截图目录**无生成脚本、不可复现**，用户已知情并选择删除。 |
| **封面预览又小又糊（Safari）**（2026-09-17，用户报障「图书编辑页封面预览非常模糊」） | 管理端 antd 预览大图 `.ant-image-preview-img{max-height:70%}` 是百分比高度，在 Safari 下父级高度按内容算 → 链条退化，784px 高窗口里预览只剩 212px（Chromium 是 549px）；书名缩到几像素 → 用户感知为「模糊」 | **ACCEPTED（用户 2026-09-17 复验：「ok 了，没问题了」；已提交 `53d0bd9`，CI 同一 run success）** | ① 先排除数据侧（全 35 本封面同规格/同量化表、端点无 resize、锐度同档、浏览器渲染与源图差 3%）；② 真因见错误库 **E-20260917-42**；③ 修复两步：**(a) 尺寸链** `.ant-image-preview-img` 改视口单位（70%→82vh，366×549→429×643）；**(b) 真凶是竞态**——预览大图无显式 width/height，布局取决于解码出的 intrinsic size，Safari 在「挂载时未解码」会按错尺寸栅格化且不回刷（Chromium 会重栅格化，故我侧一直正常），故「每次刷新换一本」无规律；修法：`PreviewImage.tsx` 加 `useNaturalSize()` 预加载取真实尺寸并作为 `preview.width/height` 交给预览（命令式 viewer 同补）。**连带坑**：补属性后 max-height 会把 box 压矮，而 antd 默认 `object-fit: fill` 会把图压扁 → CSS 须同时 `object-fit: contain`。最终实测 attrs 600x900 / box 600x643 / contain / 图比例正确；**(c) 连带副作用**：尺寸补全后 antd 的 zoom 入场动画（从点击点放大到居中）变可见 → 图从左侧缩略图滑到中间，用户当场报「左边闪一下再闪到中间，好难受」；修法 `preview.transitionName = 空串`（rc-motion 见假值不启用动画，遮罩淡入另走 maskTransitionName 保留）；逐帧采样验证 0→408ms 只有 1 个状态（x/y/w/h 全程不变）。**(d) 动画重做**：用户否掉 (c) 的"直接显示"（"以前很丝滑，像从小图放大到大图"）——逐帧采样证明 antd 自带动画前面有约 60ms 预滚帧（先画最终态再跳回起点）；改为关掉 antd zoom、自己写 `pv-fly-in` CSS 动画（点击瞬间把缩略图位移/起始缩放写进 `--pv-dx/dy/scale`，img 从第一帧即起始态），实测首帧 101×108@缩略图位置 → 平滑放大到 600×643 居中的 600×643、无预滚帧；**(e) 滚轮只缩放**：`.ant-image-preview-wrap{overflow:hidden}` 关掉 rc-dialog 带来的可滚动通道（Safari 把滚轮默认行为落到它身上才出现滚动条+图被滚走；rc-image 的滚轮处理器是被动监听、preventDefault 无效）；实测滚轮后 1.5× 缩放生效、window.scrollY=0、wrap.scrollTop=0。`tsc --noEmit` exit 0。 |
| 交接 v42（2026-09-17，第九/十轮收尾 + 文档体检） | 用户「做一个完整的交接…**检查好各文档的说法，禁止再犯压缩后新接手理解的冲突**」 | **DONE（交接卡）** | 新增 **v42 区块**（九节自包含：用户常驻视觉目标 + 诚实战绩（5 项里 4 项用户发起，且第 ③ 层修法被用户打回）+ 当前状态证据表 + 第九/十轮明细（含 commit 号）+ 下一轮建议 + 防复发红线 19~23 + 环境命令（含 check_demo_baseline / optimize_uploads）+ 8 项未决 + 「压缩后读哪个文档」+ **§九 文档读法**）；**v41 已归档**（标题与引导语都改成归档口径，防止新手照旧区块开工）。**§九 是本轮新增的防冲突机制**：① 事实源优先级；② 状态词含义（ACCEPTED / DONE / MANUAL_TEST 的区别）；③ **本轮体检修掉的 5 处冲突**（归档区反向结论「3/11 不再成立」就地标注推翻 / uploads 5.3MB vs 9.4MB 口径 / 测试数 7→8 / 端点处数 4→5 / 错误库 E-42 归位并新开 §八十一）；④ 两条读法提醒（LEDGER 是追加式台账、看最上面几行；归档区一律按历史读，优先相信能现场复算的）。 |
| 第十一轮·视觉四修 + 演示数据按规则重做（2026-09-17，**未提交**） | 用户四条报障/裁定：①「星星还不是很明显…能不能换个背景」②「等级与勋章的，矛盾的可能要解决，**没达到的就是没达到**」③「护照页头像没同步」＋「头像最好也同步外框」④「切换孩子的卡最下面那条边缘线被微微遮挡、下面没留白」；另「成绩单的积分都是横线，**这个按规则增加积分啊**」、「对齐全部文档，务必文档之间没有任何冲突」 | **未提交（用户未走表验收；DoD3 禁 commit/push）**；分级验证口径=miniapp/scripts/docs 改动，**未跑全量 gate** | 已跑全绿：`ruff check backend/ tests/ features/ scripts/`（All checks passed）· `ruff format --check .`（365 files）· `check_miniapp_style` R1–R13 全零 EXIT=0 · `check_miniapp_bindings` EXIT=0 · `check_docs_code_alignment` ✓（23 文档/163 接口/47 配置键/38 表）· 干净基线 **35/3/11/2**（`check_demo_baseline`）。**全量 gate 仍以上一轮 `gate-runs/2026-09-17/gate-115422.log`（477 passed/84%/9 步）为准**——本轮改动尚未过全量 | ①**星级三段式返工**：`a7e46f6` 换 emoji ⭐→`star.png` 时星面用 sun(#FFC94D)，与 PASSED 金卡渐变底**实测只差 3 个色阶**（246,202,100 vs 243,198,103）→ 填充隐形只剩墨线=用户「空心」；压深 #F59E0B 用户「太暗」；提亮 #FBBF24 用户「没有变亮」（amber→amber 不可感知）→ 定档**白药丸垫层 `rgba(surface,.85)`**（同卡 PASSED 印章的白底垫层手法）+ 星面**亮黄 #FFD84D** + 半径 36→40（44rpx 框内占比 71%→79%），`star-off` 同半径；金卡配色语义未动（docs/03 FEAT-081 仍成立）。②**阅读护照**：头像原为写死 emoji（📚/🧒）→ 改用 passport 接口本就返回的 `avatar` + 接入共享件 `avatar-ring`（**第 6 个消费端**，docs/15 已同步）；卡片 `margin: 24rpx`（原左右顶屏）→ 变窄把标题挤成两行 → 内边距 48→32rpx、头部 gap 32→24rpx 让回宽度；**84rpx 是算出来的**：框溢出 `size×0.359` ≤ 卡片左内边距 32rpx，否则被 `overflow:hidden` 切。③**「我的」切换孩子卡**：`overflow-x:auto` 会把 `overflow-y` 强制算 auto，容器内容盒＝chip 高 → **chip 4rpx 硬阴影被整条裁掉**（.active 还叠加 translate）→ 滚动行底部 12rpx（同族 `.related-scroll` 早有 8rpx）；`.section-card.no-padding` 底部 24→36rpx（上方留白 56rpx/底部仅 24rpx）。④**演示数据按规则重做**：里程碑勋章原先**手插** `MilestoneAward(100000)`（演示孩只 78,220 词 → 等级页自相矛盾；该 award 撑着 docs/04 #30/#31/#34/#35/#36+§688 **6 条验收动线**故不能删）→ 改让演示孩**真读满 10 万**：补 2 本真实链路（Matilda 12,100+Diary 12,000，三表齐）→ **102,320 词** → 勋章走真实判定链补发（`check_milestones_now`，同管理端「里程碑核对补发」）；积分原先**手拍 5/3/2=10 全挂 Brown Bear**、其余已通过书无积分 → 成绩单按 `related_id=book_id` 求和得 0 显示「—」→ 改**按 `on_quiz_passed` 规则重建**（词数折算 `words_per_point=100` 零头池滚动 + 满分 10/首过 5 互斥取高，常量全从 ConfigService 读）→ 演示孩 **积分 10→1,063 / 零头池 20**，5 本成绩单分别为 +11/+785/+131/+130/+6。⑤**文档全库对齐**（用户「务必没有任何冲突」）：审计 37 条（9 真冲突/21 历史标注/7 互相矛盾），修 `CLAUDE.md`（lint 真实范围=backend/tests/features/scripts、**alembic/versions 不在范围**；477 passed；R1–R13）· `README.md`（WM1–WM15）· `docs/09`（FEAT 范围时点说明）· `docs/15`（六端消费 + R1-R13 + ★☆ 现状）· `docs/20`（477）· `docs/04`（表头日期 09-17、六个头像框位置、验收记录 +10 加历史标注）· `docs/LEDGER`（两处历史标注）· `error_list`（三处历史标注）· **交接卡 v43 置顶 + v42 归档**（v42 的「工作区干净/scripts 未动」已按历史读）。**过程如实报**：验证里程碑真实判定链时用「事务内试验+回滚」，因 `check_milestones_now` 内部自己 `commit()` 导致删除被提交、帖子 `ref_id` 悬空 → 删孤儿帖 + 重跑 seed 由自愈逻辑重建（现 ref 全可解析/馆长赞通知 1 条未读指向新帖）。**提交与 CI**：四刀 `7b9e6a1`（视觉四修）· `7fc3860`（演示数据按规则重做）· `99daf73`（文档全库对齐 37 处）· `9d3da4b`（阅读圈补数改收尾段 + 19 号种子测试对齐）；**首轮 CI `35237484469` FAIL**（我推送前只跑 ruff + 前端/文档门禁、漏跑引用 seed 的 `test_fix19_seed_points_related_id`），修复后 `35240752962` **success 8m19s**（477 passed / BDD 103 steps / 9 步全绿）。**另一处自己踩的坑**：补数原先放在 `_ensure_demo_circle`（早于词账/榜单快照）→ 增量脏库全过、**清库从零跑只剩 6 帖/积分 0/无勋章**；改到收尾段 `_ensure_demo_circle_extra` 后用 `check_demo_baseline` 清库重建验证：**10 帖（7 卡型）/ 积分 1,063 / 勋章 [100000] / 基线 35-3-11-2**。新增红线 27（seed 顺序 + 必须清库验证）/ 28（改脚本必须跑引用它的测试）。 |
| 第十二轮·专家 18 问全面审查 → **fix44 全量施工完工**（2026-09-20，**已推送 + CI 绿，专家审查通过**） | 专家审 v28→v43（35 刀）：18 问 + 三处前提更正；挖出 N1 真缺陷（`like()` 无自赞守卫）、N2/N3/N4 三类债、Q10（T27 不管 `schema.d.ts`）等。用户 relay 任务包 R1–R10 + 四项拍板 | **完工（等用户验收；push 等指令）**：第 0 刀 `5e6c905` 已入库，R1–R10 全部落地，**C 级全量 gate PASS**（`gate-runs/2026-09-20/gate-153942.log`：**481 passed**（基线 477 + 新增 4）/ 覆盖率 84% / BDD 8f·30s·103steps / 9 步 exit 0） | **R1** 自赞守卫：`like()` 加 `post.parent_id == parent.id → 422`（同家长跨孩一并拦，与通知排除同口径）+ 小程序自家帖点赞钮置灰（未赞态）+ 存量自赞仍可取消；测试 `test_fix44_circle.py`（真链路 HTTP，含"别家孩子照常能赞"反向断言）；**live 复核**：演示孩赞自家帖 → 422「不能给自己的帖子点赞」且 like_count 不变。**R2** Q10 治根：新增 `admin-web/scripts/gen-api.mjs`（`pnpm gen:api` / `--check`），契约源统一为快照（不再直连 :8002，CI 可跑）+ 接入 gate[8]；**首跑即红**（存量 diff 被专家说中）——查明是**路径块次序**差异（快照 sort_keys vs 路由注册序），**行集合完全一致**（证明语义零变化）→ 由快照重生 `schema.d.ts`（2550 行位移，tsc 绿）+ `docs/18 §五` 错误口径更正（原文"漏 gen:api → gate[8] diff 红"当时是错的）。**R3** Q8 三陷阱：`-samples/` 前缀匹配不到 `wm15-samples/` → 改**首段后缀匹配**；`cover/activity/` 与书封分流 → `scope_of()` **最长前缀**（activity_cover 1200 不再被 cover 1080 覆盖）；回归锁 `test_fix44_media_scope.py`（3 例）。**R4** 机化三条：新增 `scripts/check_media_discipline.py`（M1 落盘单出口 / M2 破缓存单出口 / M3 清理脚本默认 dry-run）接 gate[5]，含 S1 注入自证；**实树注入三条全命中、还原全绿**（M1 还揪出我自己 `_is_path_like` 漏判字面量路径的 bug）；顺带实修 `regen_covers.py`——原先 opt-in `--dry-run`（**默认就写盘+删旧封面+commit**）→ 改默认 dry-run + `--apply`。**R5** 报告图实测（此前三项全无数据）：冷 634ms/HTTP 765ms、命中复用 7.4/16ms、750×1100/101KB、页脚 23px 笔画中位 2px、300dpi=A5 6.35×9.31cm（页脚 1.95mm/6pt，低于胶印 0.1mm 安全线）→ 结论"屏幕清晰、打印不在范围，要打印须重绘 2x"，入 `docs/15 §16.7`。**R6** N2：四个口头放行功能的需求来源留痕（交付刀 `5775f1b`）+ 更正"零 FEAT 登记"的旧说法（承载行已在 09-15 补登）；N4：`refundable_types` 白名单三处同源（代码常量 + 模式手册 P9b 表 + FEAT-028 行）。**R7** BDD `@draft` 12 场景（扫码连扫/防重/亮度 · 生词本统计 · 自赞守卫三态），基线保持 8-30-103。**R8** 补打 `acceptance-scan-checkin` / `-2`（指向 `5775f1b`）+ CLAUDE.md 新增 **DoD 3b「验收通过必打 tag」**（升 v1.5.1）。**R9** 交接卡滚动窗口：4048 → **516 行**，23 版归档 `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`（带版本索引）。**R10** emoji 豁免口径 → `docs/15 §15.5`（文案语气可留、图标位禁；三处灰区如实登记）。**施工中拦下的新缺陷**：全量 gate 首跑 4 连红——活动域测试写死 `start_at="2026-09-20T15:00:00"`（作者当时的"明天"）当日 15:19 到期 → 422「开始时间必须在未来」＝**测试自己的时间炸弹**；四文件改相对时间（`_future_iso(days=7)`），并机化为 `check_fake_assertions` **规则 B**（[当前年,2049] 的绝对时间字面量 = FAIL；≥2050 远期哨兵放行），错误库新增 **§八十二**。**四项拍板落痕**：积分 1,063 定稿 / 预览只缩放不滚动定稿 / 走表 74 步（分批 17-41-16）/ 扫码真机复看挂 docs/20。 | **已 push 并由专家实证审查通过**（`03fd714..958de21` 共 6 刀 + `acceptance-scan-checkin` / `-2` 两 tag）；**CI `35498851116` success 10m53s**——481 passed / BDD 8f·30s·103steps / 9 步 exit 0，且**两个新检查器在 CI 里真实生效**（`媒体纪律三条规则全零`、`前端类型与契约快照逐字节一致`）；专家十项全过（含亲跑检查器与 `check_demo_baseline`、reflog 对拍重排前后树）。**下一动作在用户手上：74 步走表**（分批 WM7+WM8 17 步先）。新增红线 32（测试禁写死近期未来时间）/ 33（契约产物同源：快照 → 前端类型，次序不可颠倒）。 |
| 第十三轮·活动模块重构（签到码搬家 / 图文详情 / 往期回顾）（2026-09-20，**已推送**） | 客户三条新需求：①签到码不该出现在活动详情页，应在「已报名」处按需出示 ②详情页要能放「像公众号一样」的图文吸引报名 ③已结束活动进「往期活动回顾」。用户五项拍板：结构化图块 / 独立入场券页 / 列表双入口 / 图文与照片图集同源 / 照片只放不含可辨识孩子的图 | **全批交付完成（A/B/C）**——本行正文按施工先后记录，"A 批已完成、B/C 未开工"是本行写作时点的快照，**最终状态见本行末「状态」列**（用户两轮目视均通过 + 全推 + CI 触发） | 施工任务包 `docs/任务包-20260920-活动模块重构（签到码搬家+图文详情+往期回顾）.md`（现状核查 + 两个存量盲区 + 五条红线）。**A 批（后端零改动）**：新增 `miniapp/pages/activity-pkg/ticket/ticket`（二维码 + 条形码 + 券码 + 状态 + 亮度常亮，四态兜底）；详情页删「我的入场券」区块 → 改报名状态条 + 「出示签到码」按钮；`activity-list`「我的报名」每项加「出示签到码」（仅 enrolled）与「查看活动」；列表不再默认露券码（已签到改显签到时间）。**两个存量盲区顺手收口**：待收款态不再发放入场券（原先有券即出码）、出示动线不再与「了解与报名」混在一页。文档同步：`PRD §9.2.2` 口径修订（保留 09-15 历史口径）、`docs/03` FEAT-059、`docs/04` S1/S3、`features/activity.feature` +3 场景。**施工中自伤并已机化**：wxml 注释写成 `*/` → 整页编译失败且报错行指向别处（模拟器停旧帧）→ 新增门禁 **R14 WXML 结构**（`check_miniapp_style`，docstring 同步、实树注入自证 4 项命中）+ 错误库 **§八十三**。**用户目视反馈（当日即修）**：① 详情页文案不再指路到没有入口的页面名；② 入场券页加**在场轮询 + 签到成功动效 + 自动返回活动详情**（原先只有 onShow 刷新 → 家长举着码站着看不到反应）。**B 批图文详情（已完成）**：`activities.detail_blocks`（JSON，块类型仅 paragraph/image，≤60 块、配图 ≤30 张）+ 迁移 `f2b8d4a6c1e3`；新增 `PUT /activities/{id}/detail-blocks` 与 `POST /.../detail-images` 两个端点——**图文是纯展示字段，活动开始后/结束后仍可编辑**（活动前写招募图文、活动后补回顾），而时间/名额/费用继续受旧守卫约束（守卫刻意分开）；配图独立目录 `uploads/activity_detail/` + 口径 `activity_detail`（长边 1200），**已同步清理脚本的引用集与保护名单**（否则会被当孤儿删——上个月同类事故的形状）；配图端点只接受 basename + 文件名前缀=活动 id（路径穿越与越权同时封死）；管理端编辑弹窗内新增图文编辑器（段落/图片增删排序、独立保存按钮、照片合规提示）；小程序详情页按序渲染 + 点图全屏预览。**C 批往期回顾（已完成）**：`GET /api/miniapp/activities/past`——**按「开始已过 24h」取数而非 status=finished**（有人报名未签到的活动过期后不会被自动结束，只有按时间取数才不漏——顺手收口黑洞盲区）；小程序活动页第三个 tab「往期回顾」（封面/时间/参与人数），详情页切只读回顾形态（顶部往期标识、不显示名额、无报名入口）；管理端状态筛选补 finished。**god-file 拆分**：`service.py` 866 行触顶 → 拆出 `detail_blocks.py`（序列化单一来源）与 `past_service.py`（往期取数），架构关恢复 PASS。契约快照 + `schema.d.ts` 按序重导。**演示素材进种子**（否则清库重建即失）：`_ensure_activity_detail_blocks`（两场演示活动的图文，配图由绘本引擎现画，不含任何孩子形象）+ `_ensure_past_activities`（两场往期活动 + 签到/取消报名记录）。**测试**：`tests/unit/test_wm9b_activity_detail_blocks.py` **8 例**（写入/顺序/校验/开始后仍可编辑/取消后锁定/移出图清理/穿越拒绝/清理脚本对账/管理端预览端点——原先写 7 例是漏数，2026-09-20 校正）。**干净基线口径变更**：往期活动进种子后**活动数 2 → 4**（2 场可报名 + 2 场往期回顾演示），`check_demo_baseline` 常量 + `docs/19` + `docs/04` 核对步骤同步；**历史行不回改**（台账追加式：历史行是当时快照，改了就毁证据）。**状态**：用户两轮目视均通过（A 批「功能没问题」→ B/C 批「大问题没有」→ 两处报障当日修复后「没问题了」）；**5 刀 + 2 枚验收 tag 已一次全推**（`958de21..e084e95`：`37e1d91` 台账回写 / `f76fd17` A 批 / `b43656e` B/C 批 / `3a868d2` 配图链路修复 / `e084e95` 滚动位置修复；tag `acceptance-activity-ticket`·`acceptance-activity-rich-detail`）；CI `35516394455` 已触发。**两处报障修复已入错误库**：配图全裂（漏 media.fullUrl + 管理端预览误用小程序端点）/ 看图预览关闭后跳回页面顶部（回页刷新置 loading → 整页卸载重挂丢滚动位置，§八十四）。 | 证据：`check_miniapp_style` PASS（R1/R12/R13/R14 全 0）· `check_miniapp_bindings` ✓ · `check_docs_code_alignment` ✓ · 两个 Node 桩测试全绿（详情页三态 + 入场券页四态 + 列表按钮与跳转参数）· 开发者工具 `Errors: 0`。**未跑全量 gate**（A 批仅小程序 + 文档，无后端改动）；B/C 批动后端时按 C 级全量 gate。 |
| 第十四轮·文档全量同步（PRD / docs 全套 / BDD / LEDGER）（2026-09-20，**未提交**） | 用户「全量更新相关的文档，包括 prd、feature bdd 等等」——把活动模块三项需求与四个口径变更（活动数 2→4 / 签到码位置 / "已结束"=开始过 24h / 图文可编辑范围）铺到全部相关文档；**D 级纯文档刀**（不跑门禁、不单独推、不等 CI——见 `CLAUDE.md §八` D 级与交接卡红线 37） | **DONE（仅文档，代码零改动）** | **PRD**：新增 §9.1.1 活动图文详情（含 60 块/30 图/1000 字/60 字上限、可编辑范围、配图合规）与 §9.2.3 往期活动回顾（24h 口径 + 为何不按 status + 隐私口径），§9.2.2 修订补**第二个入口**（活动详情页状态条「出示签到码」——用户反馈后应有），§9.1 交叉引用。**docs/01**：阶段表新增本轮交付行、P10 内容列补图文/往期。**docs/02**：状态机表补「活动生命周期」（无转移矩阵、不依赖 finished）+ 图文守卫分离口径 + uploads 子目录清单。**docs/03**：复核一致（FEAT-057/059/088 前批已同步）。**docs/04**：消 **S11 重号**（报名名单行 S11→S15）+ S9 归位到 S8 之后 + S13 写明参与人数口径 + §10.3 新增「增补表 3」纳入 S11–S15 + §10.2③ 活动③④明细。**docs/07**：后端施工图新增 **B13/B14/B15**（展示字段守卫分离 / 时间口径常量同源 / 新增媒体目录三处同接）+ 演示数据规范补基线常量纪律（章节标题九模式→十五模式）。**docs/08**：新增 **TD-14~TD-17**（黑洞活动 / 待收款出码 / 活动图文目录孤儿风险 / 展示字段守卫放宽留痕）。**docs/09**：C9 改「独立入场券页」口径、C10 交叉引用、C11 澄清（C9≠孩子码）、C20 行号 53-54→81 + 往期不做 R-313 注明、WM11 补课清单标注"活动自动结束边界仍缺"、**新增 §十一** 本轮全量留痕（含两条未决：参与人数口径/回顾上线通知）。**docs/15**：wxss 计数 `43→39`（实测）、PreviewImage `7→8 处`、新增 **§15.7 R14**（WXML 结构）并消 15.5 重号、§16.2 配置键共用说明、§16.6 补陷阱 ④（清理保护名单）⑤（optimize 回压**不覆盖** activity_detail，如实登记）。**docs/18**：补齐漏掉的第五个端点（admin `detail-image`）+ 两条 token 渠道纪律（`media.fullUrl(url,true)` 第二参 / admin `authorize_media` 与"误用 miniapp 端点 401"）+ past 的 `limit/offset` 例外说明。**docs/19**：种子清单与基线构成补"活动 4 场"、标题「三个防呆基建」→四个（表实为 4 行）。**docs/20**：恢复演练对拍四表→五表（加 activities）、gate 基线 477/84%→489/84.46%。**README**：门禁第 5/8 步描述补全（假绿断言/媒体纪律/gen:api --check）、87→88 项 Feature。**docs/LEDGER**：本行 + 第十三轮行状态列与测试数（7→8）校正。**校验**：`check_docs_code_alignment` 通过（未跑全量门禁——D 级纪律）。 |
| 第十六轮·借阅台扫码闭环 A–D 四批（2026-09-21，**未提交，等用户目视**） | 用户「docs/任务包-20260921-借阅操作台扫码闭环（会员码+连扫+借还记录）.md **开工**」+ 五项拍板（会员码后端不可枚举 / 书码沿用 ISBN / 逾期不拦截全端统一且额度只扣一本 / 不做预约分配 / 要归还操作人+Excel 导出） | **A–D 全部落地**（等用户按 `docs/04` 增补表 4 的 S16–S23 目视） | **A 批·会员码**：`children.member_code`（M+8 位随机+校验位、字母表剔除易混字符、唯一索引 `uq_child_member_code`）——生成/校验单一来源 `identity/member_code.py`，**列默认值兜底**（seed/测试/任何直插路径都绕不过）；迁移 `b4d8e2f6a1c5` 含**存量回填**（11 个演示孩子实测全部有效且不重复）；小程序载荷增字段；`GET /circulation/children/by-code/{code}/card`（声明在 `/{child_id}` 之前，422/404 分开）。**B 批·小程序出示页**：首页 `hero-badges` 加「会员码」入口 + 新页 `pages/member-pkg/member-code/`（二维码 + Code128 + 大字号文本码 + 亮度拉满常亮，复用 ticket 那套出图/亮度函数）；`check_miniapp_style` 全零。**C 批·扫码统一判定**：`POST /circulation/scan`——判定顺序即口径（会员码 → 本孩子在借→还书 → 自己的预约→核销借出 → 在馆副本→借出 → 否则按原因拦截）；**不改 borrow 对 reserved 的守卫**，扫码端点分流到既有 checkout（红线 R2）；管理端抽出通用件 `ScanInput`（把已验收的扫码八条机制提取，**ScanCheckin 一并改用它**，不留第二份实现，红线 R5）+ 操作台焦点链（进页面落①会员码框 → 识别成功自动交棒②图书框 → 成败都回焦 → 提示音 → 本次操作列表）；实测附带修掉一个误导文案：形似会员码但校验位错 → 422「会员码不合法」，不再落进 ISBN 分支报"未入库"。**D 批·借还记录**：`borrow_records.returned_by`（迁移 `c7e1f5a9b3d2`，**不回填**历史——没记就显示"未记录"，不编造）+ `GET /circulation/records`（分页 + 关键词/状态/时间段，含借出/归还操作人、副本码、逾期天数）+ `/records/export`（**按筛选**导出，超出 10000 行在表内标注截断）+ 操作台第二 tab；`service.py` 793 行贴 800 上限 → 查询单独拆 `records_service.py`。**测试**：新增 `test_borrow_member_code.py`(4) / `test_borrow_scan.py`(7) / `test_borrow_records.py`(3)；**验证**：相关域 pytest 全绿 + `ruff check`/`format --check` 通过 + admin-web `tsc --noEmit` exit 0 + 契约快照与 `schema.d.ts` 按序重导 + 现场 `check_demo_baseline` PASS（35-3-11-4）；**实测**：扫演示孩会员码 → 出卡片（在借 2 / 可借 27）；**端到端**：扫在馆书 ISBN → `borrow`（应还 2026-10-21）→ 再扫同一码 → `return`（正常归架）→ 借还记录出现该行（副本码 / 借出人 / 归还人 / 归还状态齐全）→ 按筛选导出 xlsx 200（5,457B，Microsoft Excel 2007+）。
**门禁（C 级标准，两次跑）**：第一次第 [4] 步红＝**业务域反向依赖 admin**（`records_service` import 了 `AdminUser`）→ 改读审计表 `AuditLog` 后 PASS；第二次第 [1][2][3][4][6][7][8][9] 步全绿（**506 passed / BDD 8f·30s·103steps / 架构关 PASS / 契约快照与前端类型逐字节一致 / 交付完整性 PASS**），第 [5] 步红＝错误库两处**文档引用悬空**（扫码签到里那个取本地时分秒的辅助函数因抽通用件改名、审计模型被我写成"文件点类名"）→ 修完并加 docs-code-ignore 豁免后**单独复跑 `check_docs_code_alignment` exit 0**；此后代码零改动。**未做第三次全量重跑**（如实记录）。
**用户目视反馈 → 当日修复（6 条）**：① 小程序会员码入口太小太暗 → 改成与右侧「连续打卡」同形制的**亮黄圆形按钮**；② 扫完会员码焦点没跳到书码框 → 根因是 `ScanInput` 提交后在 `finally` 里自己回焦、把父组件交棒的焦点抢回；加 `refocusAfterSubmit` 开关（会员码框关掉，失败时父组件主动要回来）；③ 演示孩可借 27 而非 28 → 根因是 seed 造了**同一孩子既在借又预约同一本书**、且预约锁的副本已被改成 borrowed 的鬼状态（`_ensure_demo_fav_reservation` 按词数取书、`_ensure_demo_t41_data` 按 id 取书，两段独立取书撞同一本）——演示孩只留一条已借出预约历史（不占额度），在用预约挪给观察期孩并**排到 WM3 家族建完之后**；另加三条 seed 取值约束（不取孩子已在借的书、不取有在用预约的书、副本必须 available）；④ 在借表只显示日期看不出是哪本书 → `BorrowRecordResponse` 补 `book_title`/`copy_code`（`child_card()` 批量取，禁逐条），前端补两列 + 时间统一成 `MM-DD HH:MM`；⑤ 预约成功借阅后不释放 → `borrow()` 里**统一关闭该孩子对同一本书的在用预约**（转 checked_out）并把该预约锁着的**另一册**放回在馆（防鬼锁），修复用户实测的「核销报副本状态异常、预约永远核销不掉」；⑥ 自查：全库扫描「既借又约」「预约锁非 reserved 副本」两类鬼状态 = 0。
**最终门禁 PASS（退出码 0）**：`gate-runs/2026-09-21/gate-131140.log`——**507 passed / 84% 覆盖率 / BDD 8 features·30 scenarios·103 steps / 架构关 PASS / 契约与反假绿全 PASS（假绿 0·时间炸弹 0·文档对齐 ✓·RBAC ✓·风格 R1–R14 全零·媒体纪律 M1–M3 全零）/ 迁移一致 / tsc / 契约快照与前端类型逐字节一致 / 交付完整性 PASS**，PASS 后自动 `dev.sh restart` 恢复现场；再跑 `check_demo_baseline` 清场重建 **35/3/11/4 PASS**，实测演示孩 `在借 2（其中逾期 1）/ 可借 28` ✓。
**用户目视第二轮反馈 → 当日修复（5 条）**：① 操作台顶部留白太多 → 压掉标题/Tabs/说明三处默认间距 + Tabs 改 small；② 界面眼晕 → 两个扫码框**配色区分**（①会员码=主色橙系、②图书=次色绿系，新增 `.scan-block-*` 类）且"未聚焦"标签从橙色告警降级为灰标签；③ 小程序会员码页加**底部大按钮「关闭会员码」**（真机左上角返回太难按）；④ **未入会竟然显示可借 30 本** → 新增 `backend/domain/circulation/borrow_gate.py`（`member_gate`/`deposit_gate`/`first_block`）作为**借书资格单一来源**：借书守卫与卡面 `borrow_block`/`borrow_block_hard` 共用同一判定，卡面显示「当前不可借：<原因>」+ 硬拦截/可放行说明（新增 `test_borrow_gate_card.py` 4 例，含"卡面说能借就真能借"一致性锁）；⑤ 借还记录 tab 切过去不刷新 → 传 `active` 给面板、激活即重拉。
**第二轮修复后的最终门禁**：`gate-runs/2026-09-21/gate-140222.log`——**511 passed / BDD 8f·30s·103steps / 架构关 PASS / 契约与反假绿全 PASS / 迁移一致 / tsc / 契约快照与前端类型一致 / 交付完整性 PASS，退出码 0**；PASS 后自动恢复现场，再跑 `check_demo_baseline` 清场重建 35/3/11/4 PASS。终检实测：演示孩 `在借 2 / 可借 28 / 可借 ✓`；小红（未入会）`可借 30 但不可借：未入会临时借书开关未开启（硬拦截）`；借还记录含书名与副本码。
**第三轮目视反馈（2 条）**：① 留白"还是没改" → 真凶是 AntD `Typography.Title` 默认 `margin-top: 1.2em`（上一轮我只改了 marginBottom）——全站标题统一 `margin: 0`，并抽出 `PageTitle` 组件；② 标题要"换字体+底色框"：第一版深底霓虹绿被用户否（"颜色不太搭"）→ 定稿**暖色绘本版**（纸感底+深墨描边+硬阴影+主色橙左条+等宽字距），已铺到**全部 9 个有页面大标题的页面**（实测无残留 level-4 标题；另有 7 页本来就没有大标题——顶部直接是搜索/按钮行，或阅读圈用卡片标题，已如实登记未擅自新增）（带按钮的表头行 `baseline→center`）；浏览器实测：借阅操作台/会员管理/图书管理/审计日志/押金与赔偿 标题框间距均 **24px**（会员管理参照 27px）、字号 20px、无残留 level-4 标题；**用户当场裁定"行吧，都改成这样吧"**（本轮唯一一次由用户明确拍板的样式方向）。
**已推送（2026-09-21）**：三刀 `90a8cb8`（文档：全库同步 + 文档先行入宪）· `b520c48`（后端：扫码闭环 A–D + 资格单一来源 + 额度口径修复，511 passed 门禁 PASS）· `008a3c1`（前端：会员码页 + 焦点链 + 记录 tab + 标题字体统一）；`origin/main` = `008a3c1`。

**第四轮目视反馈（2 条，同日）**：① 图书详情页标题被套进标题框后"太大了，也很丑" → 判定**书名是内容不是页面名**，回退为普通标题，并把使用边界写进 `PageTitle` 注释与 `docs/15`（**标题框只给 2–6 字页面名用**）；② 「原来的字体看英文很难受，歪歪扭扭的」→ 新增 `--font-mono` 令牌（等宽栈），书名改用等宽（实测 `ui-monospace` 20px，浏览器截图确认可读），`PageTitle` 的字体栈也改用同一令牌；同时在 `docs/15 §17.1` 立了**字体分工表**（中文标题走 display / 英文与数码走 mono / 正文走 body），并登记两处**待用户定**的品牌字（侧边栏与登录页 DmkWords、仪表盘大数字）。
**2026-09-21 收尾三件事**：① **用户裁定借阅台模块验收通过** → 打验收 tag（见 git tag）；② 仪表盘统计大数字改等宽（`--font-display` → `--font-mono`，3 处 24px，实测 `ui-monospace`），侧边栏/登录页品牌字 DmkWords 按用户裁定**不动**；③ 出**店外大屏可视化任务包**（`docs/任务包-20260921-店外大屏可视化（中央仪表盘·电视轮播）.md`）：判断"现仪表盘是内部运营视角、不能直接投店外"→ **用户当场打断裁定："就用仪表盘页面，不要再新增页！那些数据没关系的，投屏不影响的"** → 方案改为**融合进现有仪表盘**；**第一版已实现三张图**（馆藏构成甜甜圈 / 今日流量双柱 / 健康度三环，全部用既有 dashboard 字段、零新依赖手写 SVG）；下一批候选（封面墙/趋势图/里程环/星光榜/成长塔/活动预告/上新）列在任务包 §四、需新增聚合端点故等用户挑；零新依赖（手写 SVG + paint 令牌）、聚合端点 `GET /admin/screen/summary`、四条红线（**隐私：店外不出现孩子真实姓名与可辨识照片**）、S1–S4 分批 + **5 条待用户拍板**（隐私口径/轮播时长/金额数据/二维码形态/屏幕方向）。 |
**施工中两次自伤（已入错误库）**：① god file——扫码逻辑塞进 `service.py` 后 849 行超 800 上限，拆出 `scan_service.py`（与 `records_service.py` 同族手法）；② **我自己制造的一次假红**：门禁跑到 pytest 时我又并发跑了一个 pytest（同库互踩，教训 66 复发）→ 25 例假失败、白费一轮门禁——**门禁在跑时不许再碰库/再跑测试**，已入 §九十二。 |
| 第十五轮·**宪法「文档先行」+ 借阅额度口径修复 + 借阅操作台任务包**（2026-09-21，**未提交**） | 用户：①"我以后必须要**文档先行，再动代码**！这是强制性要求，必须写进最高宪法"；②借阅操作台改造五项拍板（会员码后端不可枚举 / 书码沿用 ISBN / 逾期不拦截全端统一且额度只扣一本 / 不做预约分配 / 要归还操作人+Excel 导出） | **DONE（等用户验收；未提交）** | **① 宪法升 v1.5.2**：`CLAUDE.md §六` 新增**前置铁律「文档先行」**（含可机械检查的判据：本批 diff 有代码改动必须伴随文档改动）+ `AGENTS.md` 协作纪律同步。**② 逾期口径修订**（用户报障"扣 2 本"→ 复核为真且是**双向误差**）：卡面 `child_card()` 旧式 `- len(overdue) - len(active)`（active 已含逾期 → 每本逾期多扣 1；又没减预约 → 有预约时多报；两者在"1 逾期+1 预约"时正好抵消，故长期潜伏）→ 统一为与 `borrow()` 同源的 `上限 − 在借含逾期 − 预约`；同时按"全端统一不拦截"**摘除小程序预约端的逾期硬拦**（`reading/service.py` 原"有逾期未还图书，请先归还"）；口径同步 PRD §5.4 现行口径 + §5.6 问答 + §十二 速查表 + `docs/03` FEAT-034/036 + `docs/01` P5 + `docs/04` WM5 步 1/3/4/7（含 §10.3 复表）+ `features/borrowing.feature`。**③ 任务包** `docs/任务包-20260921-借阅操作台扫码闭环（会员码+连扫+借还记录）.md`：需求原文、五项拍板、现状核查（**三条与用户预期不符的事实**：自己预约的副本走 borrow 一律 409 / 别人预约只是被静默跳过 / 还书端点只认 copy_id 故"扫 ISBN 自动判借还"当前做不到）、现状与目标流程图、五条红线、A–D 分批、预登记验收步骤 S16–S22。**④ 测试**：新增 `tests/unit/test_borrow_quota_overdue.py`（3 例：逾期只扣 1 且卡面数字可兑现 / 预约占 1 / 逾期不拦预约但逾期书仍不可续借）；`test_wm5_circulation.py` 逾期用例期望 28→29 校正。**⑤ 验证**：相关域 pytest **51 passed**（wm5 + 新回归锁 + 押金守卫 + 预约额度 + 并发 + 任务 + 门禁族）+ `ruff check` 全过 + `ruff format --check` 通过 + admin-web `tsc --noEmit` exit 0；`dev.sh restart` 后三项核验（health ok / 5173 200 / 基线 `check_demo_baseline` PASS 35-3-11-4）；**实测复核**：演示孩卡面 `在借 2 / 其中逾期 1 / 可借 27 / 押金已缴 1200` 与 `docs/04` 第 1 步期望逐项一致。**⑥ 错误库新增 §八十六**（同一指标算两遍 + 误差抵消型 bug 的防复发）。 |

**仪表盘六图版（2026-09-21 用户反馈「这布局你不觉得丑吗？才 3 个图表，至少得 6 个吧，否则太单调了」）**：① 布局改**三列等宽网格**（六卡两行等高对齐）；② 图从 3 张扩到 **6 张**——新增「近 14 天借还趋势」（面积折线）、「热门书 TOP5」（横向条形）、「小读者构成」（按会员状态堆叠条）；③ 新增聚合端点 `GET /api/admin/dashboard/charts?days=&top=`（趋势**连续日期且缺日补 0**、热门书降序带书名、会员构成按状态聚合；`charts_service.py` 单独成文件——`admin/service.py` 已 775 行贴 800 行上限）；④ 图形区**自轮询 60 秒**（投屏场景），拉取失败保留上一帧、不弹错误框；⑤ 自查修掉两处界面假象与一处裁切（0 值画弧留色点 / 柱图最小高度留色块 / 健康度第三环被卡片右缘切掉）；⑥ 施工自伤一处：加完端点没重启后端 → 三张新图显示"暂无数据"，重启后正常（教训：**改后端必须 restart 才能自测**）。**测试**：`tests/unit/test_dashboard_charts.py`（2 例：趋势序列连续性与热门书排序、days/top 越界收敛）。 |
