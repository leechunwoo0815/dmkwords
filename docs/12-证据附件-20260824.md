# DmkWords 开发证据附件 — 2026-08-24（供专家复查）

> ⚠️ **历史归档**：本文件为带日期的时间快照，仅作过程留痕；当前状态一律以 `docs/项目交接-20260824.md` + `docs/LEDGER.md` 为准。

> 配套：`docs/11-开发工作简报-20260824.md`。本文件 = 原始输出索引 + 关键片段全文。
> 注：`gate-runs/` 目录按项目约定进入 `.gitignore`（本地留档），本文记录索引与不可再生的关键原始输出。

---

## 1. 证据文件清单

**目录 `gate-runs/2026-08-24/`（本地，git 外）：**

| 文件 | 内容 | 大小 |
|---|---|---|
| `gate-full-p1.log` | 最终全量门禁完整原始输出（ruff/单测 131/BDD/架构/反假绿/迁移/覆盖率 76.96%），exit 0 | 78 KB / 1787 行 |
| `mdl-hang-faulthandler-BEFORE-FIX.log` | **修复前** MDL 挂起时的 faulthandler 线程栈（原始 pytest 输出） | 7.6 KB / 68 行 |
| `regression-26.log` | P0 四测试文件回归原始输出：26 passed | 0.6 KB |
| `p1-3.log` | P1 三测试原始输出（-v）：3 passed | 1.3 KB |
| `tsc.log` | `pnpm exec tsc --noEmit` 输出，exit 0 | 0.3 KB |
| `mysql-state.log` | 取证后 MySQL 基线快照（innodb_trx 0 / threads 1 / metadata_locks 1） | 0.2 KB |

## 2. MDL 挂起证据（修复前）— faulthandler 全文

> 复现命令：`.venv/bin/python -m pytest tests/unit/test_wm10_state_machines.py -o faulthandler_timeout=25 -v`
> 完整产物：`gate-runs/2026-08-24/mdl-hang-faulthandler-BEFORE-FIX.log`

```
============================= test session starts ==============================
platform darwin -- Python 3.13.2, pytest-9.1.1, pluggy-1.6.0 -- /Users/litianyu/cc-projects/dmkwords/.venv/bin/python
plugins: cov-7.1.0, asyncio-1.4.0, anyio-4.14.2
collecting ... collected 5 items

tests/unit/test_wm10_state_machines.py::test_member_refund_links_withdrawal_r309 PASSED [ 20%]
tests/unit/test_wm10_state_machines.py::test_refund_execute_fail_then_retry FAILED [ 40%]
tests/unit/test_wm10_state_machines.py::test_parent_cancel_refund_and_withdrawal Timeout (0:00:25)!
Thread 0x00000001f4c62180 (most recent call first):
  File "/opt/homebrew/Cellar/python@3.13/3.13.2/Frameworks/Python.framework/Versions/3.13/lib/python3.13/ssl.py", line 1138 in read
  File "/opt/homebrew/Cellar/python@3.13/3.13.2/Frameworks/Python.framework/Versions/3.13/lib/python3.13/ssl.py", line 1304 in recv_into
  File ".../socket.py", line 719 in readinto
  File ".../pymysql/connections.py", line 810 in _read_bytes
  File ".../pymysql/connections.py", line 772 in _read_packet
  File ".../pymysql/connections.py", line 1245 in read
  File ".../pymysql/connections.py", line 847 in _read_query_result
  File ".../pymysql/connections.py", line 582 in query
  File ".../pymysql/cursors.py", line 330 in _query
  File ".../pymysql/cursors.py", line 159 in execute
  File ".../sqlalchemy/engine/default.py", line 952 in do_execute
  File ".../sqlalchemy/engine/base.py", line 1969 in _exec_single_context
  File ".../sqlalchemy/engine/base.py", line 1848 in _execute_context
  File ".../sqlalchemy/engine/base.py", line 1643 in _execute_clauseelement
  File ".../sqlalchemy/sql/elements.py", line 526 in _execute_on_connection
  File ".../sqlalchemy/engine/base.py", line 1421 in execute
  File "/Users/litianyu/cc-projects/dmkwords/tests/unit/conftest.py", line 55 in clean_db
  File ".../_pytest/fixtures.py", line 998 in call_fixture_func
  ...（上层 pytest 运行栈，略去重复帧，全文见原始文件）
  File "/Users/litianyu/cc-projects/dmkwords/.venv/lib/python3.13/site-packages/pytest/__main__.py", line 9 in <module>
```

关键点：卡点唯一执行帧 = `tests/unit/conftest.py:55`（`clean_db` fixture 内 `TRUNCATE TABLE`，DDL 等待独占 MDL）；客户端栈停在 pymysql `_read_bytes`（收包等待）。

## 3. 用例 2 失败 traceback（修复前）

> 转录自本会话复现输出（修复前单跑 `-k "fail_then_retry" --tb=short`），原始屏显未留存文件（如实标注）。

```
tests/unit/test_wm10_state_machines.py:156: in test_refund_execute_fail_then_retry
    assert w.status == "pending_settle"  # 失败回待结算
E   AssertionError: assert 'applying' == 'pending_settle'
E     - pending_settle
E     + applying
```

## 4. 全量门禁（最终）关键段

> 完整原始输出：`gate-runs/2026-08-24/gate-full-p1.log`（exit code 0）

```
===== [1] lint (ruff) =====
All checks passed!
===== [2] 单测（真实 MySQL 同构环境）=====  ... 131 passed
===== [3] BDD (behave) =====  5 features passed, 0 failed, 28 skipped / 12 scenarios, 0 failed / 28 steps
===== [4] 架构关 =====
架构关 PASS：四件套齐全 / Router 零违规 / 行数达标 / 无 sqlite / import 白名单通过
===== [5] 契约与反假绿 =====  OK: all assert True/False have explanations
===== [6] 数据库迁移一致性 =====  No new upgrade operations detected.
===== [7] 覆盖率 =====
Required test coverage of 25% reached. Total coverage: 76.96%
131 passed, 1 warning in 198.81s (0:03:18)
===== 全量门禁 PASS（退出码 0） =====
```

（修复前另有一次"全量 FAIL"运行留档：原因是刚改完 C8 模板后未跑 ruff format——`✗ 门禁失败: ruff format --check .`；format 后立即通过。属流程疏漏已在简报 §5 日期注记，非代码缺陷。）

## 5. 分组测试与前端

```
# 26/26（P0 四文件）实际输出行：
tests/unit/test_wm10_state_machines.py ... 5 passed
tests/unit/test_wm10_transfer_refund.py ... 7 passed
tests/unit/test_r313_guards.py ... 4 passed
tests/unit/test_d1_member_gates.py ... 10 passed
# 合计行：26 passed, 1 warning in 30.15s（见 regression-26.log）

# P1 三文件 -v（见 p1-3.log）：
test_import_template_download PASSED  [ 33%]
test_staff_crud_permissions PASSED    [ 66%]
test_child_update_ar_only_up PASSED   [100%]
3 passed, 1 warning in 5.73s

# tsc（见 tsc.log）：无输出行；末尾 "tsc exit=0"
```

## 6. MySQL 基线快照（取证后）

```
innodb_trx count: 0
Threads_connected: 1
metadata_locks: 1
```

（快照查询命令与容器内 socket 权限细节见简报 §2.4；`performance_schema.metadata_locks` 可用性已验证——后续若需复核挂起瞬间锁持有者，可直接以该表采集。）

---

*附件结束。所有"请见文件"者均可：`ls gate-runs/2026-08-24/` 后按名查看；`docs/12` 与 `docs/11` 同步提交，随仓库可追溯。*
