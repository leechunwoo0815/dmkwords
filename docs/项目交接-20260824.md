# DmkWords 项目交接 - v46 最新交接书（2026-09-20 第十四轮：活动模块三项客户需求 A/B/C 全批交付 + 已推送 CI 绿；**工作区干净**）

> 🔴 **压缩后第一件事：读本区块全部七节，自包含（含 §七 一致性体检）。**
> **本轮产物 = 施工任务包** `docs/任务包-20260920-活动模块重构（签到码搬家+图文详情+往期回顾）.md`：
> 三项客户需求、五项用户拍板、现状核查（两个存量盲区）、五条本批红线、逐批交付证据——**明细都在那里**，
> 本区块只记"现在是什么状态、下一步做什么、哪里容易理解错"。
> **保留窗口**：本文件只留 **v46 / v45 / v44** 三版；**v43 及更早 25 版已并入
> `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`**（文件名沿革见该文件头部说明，2026-09-20 两次滚动瘦身）。
> **专家侧恢复点**：`docs/专家交接书-20260920-在任专家会话交接.md`（专家角色/裁定/fix44 原文/四项拍板）。
> **继承不重复**：v42 §一 长期目标 / §五 红线 1~23 / §六 环境命令 / §九 文档读法 + v43 §五 红线 24~28 +
> v44 §六 红线 29~33 + v45 §五 红线 34~35 **全部有效**。

---

## 一、当前状态（一句话 + 证据）

**客户三项活动模块需求（签到码搬家 / 图文详情 / 往期回顾）A/B/C 全批交付，用户两轮目视均通过；
7 刀 + 2 枚验收 tag 已全部推送，**两轮 CI 均 success**（最新 489 passed）。**

| 项 | 事实 |
|---|---|
| HEAD / 远端 | **本地 = `origin/main`、零未推**（当前 HEAD 用 `git log --oneline -1` 取——**按 v28 教训不写死 HEAD hash**：写进文档的那一刀本身会改 HEAD，写死必然滞后）。本轮共 7 刀：`37e1d91` 台账回写 → `f76fd17` A 批 → `b43656e` B/C 批 → `3a868d2` 配图链路修复 → `e084e95` 滚动位置修复 → `203ea71` 本交接（v46）+ `174b49c` CI 结论回写 |
| 验收 tag | `acceptance-activity-ticket`（→`f76fd17`）· `acceptance-activity-rich-detail`（→`e084e95`）——**DoD 3b 立规后首批** |
| 最近 CI | **`35516922900`（`203ea71` 本交接刀）success 8m49s**：489 passed / BDD 8f·30s·103steps / 9 步 exit 0，且**新增检查器在 CI 同样生效**（媒体纪律三条全零 / 前端类型与契约快照逐字节一致）；前一轮 `35516394455`（`e084e95`）亦 success（15m59s，含本轮小程序修复） |
| 最近全量 gate | `gate-runs/2026-09-20/gate-195525.log`：**489 passed（481 基线 + 本批 8 例）/ 覆盖率 84% / BDD 8f·30s·103steps / 9 步 exit 0**（另有 `gate-190438.log` 488 passed 为 B/C 批首跑） |
| 现场 | 后端 :8002 + :8443 200 · 管理端 :5173 200 · **干净基线 35 / 3 / 11 / 活动 4**（活动数本轮 2→4，见 §三 口径变更） · 阅读圈 10 帖 · 演示孩 5 本 / 102,320 词 / 1,063 分 |
| 工作区 | **干净**（已跟踪文件零改动；根目录有一个工具自动生成的未跟踪 `.zcodeignore`（内容为 .venv/__pycache__ 等忽略项），**不入库**、不影响门禁第 [9] 步——它只查 docs/外部专家意见/error_list 目录） |
| 本轮新增迁移 | `f2b8d4a6c1e3`（`activities.detail_blocks`）——已是 alembic head |

---

## 二、本轮（第十三/十四轮）做了什么 —— **别重复做**

1. **需求→任务包**：用户转述客户三条需求 + 邀请"刨根问底"→ 两路只读侦察 + 自查四条关键事实 →
   回收用户五项拍板（结构化图块 / 独立入场券页 / 列表双入口 / 图文与照片同源 / 照片只放不含可辨识孩子的图）。
   **拍板结论只写在任务包 §二**，不要重问用户。
2. **A 批｜签到码搬家**（后端零改动）：二维码移出活动详情页 → 独立「我的入场券」页，由「我的报名」列表的
   「出示签到码」按钮进入；详情页改报名状态条；**待收款态不再发放入场券**；入场券页**在场轮询 + 签到成功动效 +
   自动返回活动详情**。顺带收口两个存量盲区（待收款出码 / 黑洞活动见 §四 C 批）。
3. **B 批｜图文详情**：`activities.detail_blocks`（JSON，段落/图片块）+ `PUT /activities/{id}/detail-blocks` +
   `POST /activities/{id}/detail-images` + 独立媒体目录 `activity_detail/`（**已同步清理脚本引用集与保护名单**）+
   管理端编辑器 + 小程序渲染（点图全屏预览）。**守卫刻意分开**：图文是纯展示字段，活动开始后/结束后仍可编辑；
   时间/名额/费用仍受"仅 PUBLISHED 且未开始"约束。
4. **C 批｜往期回顾**：`GET /api/miniapp/activities/past` 按**「开始已过 24h」**取数（不按 `status=finished`，
   见 §四）+ 活动页第三 tab + 只读回顾形态 + 管理端 finished 筛选。
5. **两次用户目视报障当日修复**（都是真缺陷，已入错误库）：
   ① **配图全不显示**（§八十四前一条：小程序漏 `media.fullUrl` 相对路径 + 管理端预览误用小程序端点导致 401）；
   ② **看图预览关闭后跳回页面顶部**（§八十四：`onShow` 刷新置 `loading` → 整页卸载重挂丢滚动位置）。
6. **god-file 拆分**：`service.py` 866 行触顶 → 拆出 `detail_blocks.py`（图文解析单一来源）与
   `past_service.py`（往期取数）；架构关恢复 PASS。
7. **演示素材进种子**：两场演示活动的图文 + **两场往期活动**（含签到/取消报名），配图由项目绘本引擎现画
   （版权干净、天然无孩子形象、与全站视觉同源）。

---

## 三、口径变更（**新接手的理解冲突高发区，务必看**）

| 项 | 旧口径 | **现行口径** | 同步位置 |
|---|---|---|---|
| 纯净基线·活动数 | 活动 **2** | 活动 **4**（2 场可报名 + 2 场往期回顾演示） | `scripts/check_demo_baseline.py` 常量 · `docs/19` · `docs/04` 核对步骤 |
| 签到码位置 | 详情页最上方（2026-09-15 用户要求置顶） | **独立「我的入场券」页**（详情页只留状态条） | `PRD §9.2.2`（保留 09-15 历史口径）· `docs/03` FEAT-059 · `docs/04` S1/S3 |
| 活动"已结束"判定 | `status=finished` | **"开始已过 24h"**（`service.PAST_AFTER_HOURS`，与 `activity_auto_finish` **同源常量**）；往期列表按此取数 | `backend/domain/activity/service.py` · 任务包 §四 C 批 |
| 活动可编辑范围 | 仅 PUBLISHED 且未开始（全部字段） | **时间/名额/费用**同前；**图文字段**在活动开始后/结束后**仍可编辑**（独立端点） | `admin_service.update_detail_blocks` · `docs/03` FEAT-057 · `docs/04` S14 |

> **台账写法**：历史行是**当时快照**，本轮的口径变更**只改现行口径的文档**（`docs/19`/`docs/04` 核对步骤等），
> **历史行一律不回改**（改了就是毁证据）。本轮曾用批量替换误改 16 处历史行，已精确回滚——见 v46 §七 体检记录。

---

## 四、两个存量盲区（本轮顺手收口，别再当"发现"报）

1. **黑洞活动**：`activity_auto_finish` 只在"开始超 1 天**且无活跃报名**"时置 finished → 有人报名未签到的活动
   过期后永远停在 published，而 `list_upcoming` 又按时间过滤掉它 → **此前任何列表都不显示**。
   现由往期列表（按时间取数）覆盖 ✓。
2. **待收款也能出码**：原实现只要 `my_enrollment` 存在就显示二维码（报名成功弹窗自己却写"确认收款后生效"）。
   现仅 `enrolled` / `checked_in` 可出示 ✓。

---

## 五、下一轮（压缩后）该做什么

1. **盯 CI**：`35516394455`（`e084e95`）——预期 489 passed / BDD 103 / 9 步绿；红了先看是不是 seed/环境类
   （本轮已修的两处口径：活动数 4、契约快照）。
2. **74 步走表**（用户开测后）：按 `docs/04 §十` 分批（WM7+WM8 17 步先）；本轮新增 **S11–S14**
   （图文编辑 / 图文渲染 / 往期回顾 / 结束后可编辑）可一并走。
3. **待定（用户未拍）**：往期回顾是否显示"参与人数"聚合（现显示已签到数，无则退回报名数）、
   活动结束后是否推送"回顾已上线"通知（客户未提，**先不做**）。
4. **长期目标未变**（用户 2026-09-15 原话，见 v42 §一）：主动视觉巡检、主动找观感问题，别等用户报。
   **本轮已登记的视觉缺口**：`docs/15 §15.5` 三处"emoji 实质当图标用"灰区（error-view 图标映射 /
   首页章节标题前缀 / 会员页头像兜底）。
5. **口径待同步项**（低优先，见 §七 体检）：`docs/02`/`docs/01` 未逐字复核本轮活动改动（无冲突，仅未提及）。

---

## 六、必读命令与环境（照抄）

```bash
# 现场
bash scripts/dev.sh restart            # 起/重启（含双 seed）；stop 停
python -m scripts.check_demo_baseline  # 清场 + 重启 + 计数 + 比对基线（35/3/11/4）
# 门禁
bash scripts/gate.sh full              # 9 步；PASS 自动 restart + 双 seed
# 契约（改接口必须按序）
python scripts/export_openapi.py       # ① 先更新快照（单独 commit，contract-change: 前缀）
cd admin-web && pnpm gen:api           # ② 再由快照生成前端类型（gate[8] 会拦漏做）
# 本轮新增/相关的检查
python scripts/check_miniapp_style.py  # R1–R14（R14 = WXML 注释/标签结构）
python scripts/check_media_discipline.py   # M1 落盘单出口 / M2 破缓存单出口 / M3 清理脚本默认 dry-run
python -m scripts.verify_architecture      # 含单文件 ≤800 行（本轮因它拆了 service.py）
```

**演示数据关键点**：活动 1/2 = 可报名（含图文详情）· 活动 3/4 = 往期回顾（含图文与参与记录）；
演示孩 id 每轮清库会变（`/api/miniapp/children` 查），票码每次 seed 会变（禁写死）。

---

## 七、本轮文档一致性体检（用户明令"禁止压缩后理解冲突"）

**已核对并确认一致**：`CLAUDE.md`（门禁 9 步表含 R14 与 gen:api --check、DoD 3b 打 tag 条款）·
`docs/03`（FEAT-057 增补图文、**新增 FEAT-088 往期回顾**、总数 87→88）· `docs/04`（S1/S3 口径变更新增 S11–S14、
核对步骤活动数 4）· `docs/18`（4 个新端点）· `docs/15`（活动配图口径入表 + §15.5 emoji 豁免）·
`docs/19`（基线 4）· `PRD §9.2.2`（口径修订 + 历史口径保留）· `features/activity.feature`（**现 30 场景**，全 @draft、不进 gate 计数）·
`LEDGER`（第十三轮行含 B/C 与口径变更，历史行不回改）· 错误库（§八十三 WXML 注释 / §八十四 回页刷新丢滚动）·
任务包（A/B/C 交付证据）。

**体检方法**：`check_docs_code_alignment`（文档引用必须落到真实文件/接口/配置键）+ 对"活动数 / 签到码位置 /
结束判定 / 可编辑范围"四个口径族逐文件 grep 比对 + `git diff` 人工复核（含回滚误改的历史行）。

**遗留（如实标注，无冲突）**：`docs/01`/`docs/02` 未提及本轮活动改动（其阶段表与架构图不涉及本次三个能力，
不改不影响正确性）；`docs/api/openapi.json` 已按序重导 ✓。

---

# DmkWords 项目交接 - v45 交接书（2026-09-20 第十三轮：客户活动模块新需求 → 任务包 + A 批交付）—— **已归档**（读上方 v46）

> 🔴 **压缩后第一件事：读本区块全部六节，自包含。** 本轮的关键产物是**施工任务包**——
> `docs/任务包-20260920-活动模块重构（签到码搬家+图文详情+往期回顾）.md`：三项客户需求、五项用户拍板、
> 现状核查（含两个存量盲区）、五条本批红线、A 批交付证据，**B/C 批明细只写在那里**，本区块只记状态与命令。
> **保留窗口**：本文件现有 v45/v44/v43/v42 四版（v42 是下一个该归档的，归档区在
> `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`）。专家侧恢复点仍是 `docs/专家交接书-20260920-在任专家会话交接.md`。
> **继承不重复**：v42 §一 长期目标 / §五 红线 1~23 / §六 环境命令 / §九 文档读法 + v43 §五 红线 24~28 + v44 §六 红线 29~33 **全部有效**。

---

## 一、当前状态（一句话 + 证据）

**fix44 已由专家收口 push 并 CI 绿；随后接手客户三项活动模块新需求，出任务包 + 完成 A 批（签到码搬家），A 批代码在盘未提交、等用户目视。**

| 项 | 事实 |
|---|---|
| HEAD / 远端 | `origin/main` = `958de21`（专家已推 6 刀 + 2 tag）；**本地另有 1 刀未推** `37e1d91`（fix44 的 CI 结论回写），**外加本轮 A 批未提交改动** |
| 最近 CI | **`35498851116`（`958de21`）success 10m53s**：481 passed / BDD 8f·30s·103steps / 9 步 exit 0；两个新检查器在 CI 真实生效（媒体纪律三条 / 前端类型与契约快照逐字节一致） |
| 现场 | 后端 :8002 + :8443 200 · 管理端 :5173 200 · 基线 35/3/11/4 · 阅读圈 10 帖 · 演示孩 5 本 / 102,320 词 / 1,063 分 |
| 本轮 A 批改动 | 新增 `miniapp/pages/activity-pkg/ticket/ticket`（+ app.json 分包注册）；`activity-detail`（wxml/js/wxss）删入场券区块改状态条；`activity-list`（wxml/js/wxss）「我的报名」加双入口；文档 5 处同步；门禁新增 **R14** |
| 本轮验证 | `check_miniapp_style` PASS（R1/R12/R13/**R14** 全 0）· `check_miniapp_bindings` ✓ · `check_docs_code_alignment` ✓ · 两个 Node 桩测试全绿 · 开发者工具 `Errors: 0`；**模拟器截图未取到**（macOS 通知中心浮层占全屏像素） |

---

## 二、本轮（第十三轮）做了什么 —— **别重复做**

1. **读者反馈驱动的方法**：用户给了三项客户需求 + 邀请"刨根问底" → 先派两路只读侦察（小程序活动页现状 / 后端与管理端活动域），
   自己复核了 4 条关键事实，**挖出两个存量盲区**（"黑洞活动"：有人报名未签到的过期活动既不显示也不会变 finished；"待收款也能出示二维码"），
   再以 4 个决策点回收用户拍板（结构化图块 / 独立入场券页 / 列表双入口 / 图文与照片同源 + 照片合规口径）。
2. **A 批交付**（后端零改动，因为券码本就在 `my_enrollments` 里）：签到码从详情页搬到独立页，由「我的报名」按钮按需出示；
   详情页改状态条；列表不再默认露券码。**顺带收口两个盲区**（待收款不发券；出示动线独立于"了解与报名"）。**用户目视后当日即修两点**：① 详情页文案不指路到没有入口的页面名；② 入场券页加在场轮询（3s）+ 签到成功动效 + 自动返回活动详情（原先只有 onShow 刷新，家长举码站着看不到反应）。
3. **施工中自伤并已机化**：我在 wxml 里写 JS 风格注释 `*/` → **整页编译失败**，且报错行指向别处（`.wxml:79 expect end-tag view`），
   模拟器一直停在旧帧；靠读**开发者工具自己的日志** + 写忠实解析器定位。已加门禁 **R14 WXML 结构**（docstring/CLAUDE.md/gate.sh 同步口径 R1–R14）
   + 错误库 **§八十三**（含"轻量扫描器会撒谎"的教训）。
4. **B/C 批已实现（第十三轮下半场）**：B = 活动图文详情（新列 + 迁移 + 两个端点 + 媒体新出口 + 管理端编辑器 +
   小程序渲染 + 7 例测试）；C = 往期回顾（按「开始已过 24h」取数的列表端点 + 第三个 tab + 只读回顾形态 + 管理端 finished 筛选）；
   **演示素材已进 seed**（图文样稿 + 两场往期活动，配图由绘本引擎现画、天然无孩子形象）。
   施工中被门禁拦下并已修：① `service.py` 866 行超 800（god file）→ 拆 `detail_blocks.py` + `past_service.py`；
   ② 契约快照漏新端点 → 按序重导；③ 一次**两个 pytest 撞同库**（MySQL 1213 死锁，教训 66 复发）——原因是 pkill 只杀了
   gate 脚本、pytest 子进程还在跑，**杀进程要连子进程一起确认**（`pgrep -f pytest` 必须为 0 再开下一轮）。
5. **环境坑（记下来省下一次时间）**：macOS 通知中心的浮层会持有全屏像素（`notificationcenterui`，bounds 全屏），
   期间**所有点击都被它接走**（报错会直接写明像素所有者 pid/window）；`killall NotificationCenter` 后会重弹。
   本轮因此没能取到小程序截图，改为：a11y 面板读数（`Errors: 0`）+ Node 桩测试 + 检查器三证。

---

## 三、下一轮（压缩后）该做什么

1. **A 批目视**（用户 2 步）：开发者工具「编译」→ ①首页轮播点进活动详情，确认**不再有二维码**、只显示报名状态条与「出示签到码」；
   ②活动页 →「我的报名」→ 点「出示签到码」→ 独立页出二维码/条形码。
2. **B 批｜图文详情**（任务包 §四）：`Activity` 增结构化图文块字段（`paragraph`/`image`）→ alembic →
   `python scripts/export_openapi.py` **再** `cd admin-web && pnpm gen:api`（顺序不可颠倒，gate[8] 会拦）→
   管理端编辑区（增删/排序/上传）→ 小程序渲染 + `wx.previewImage` → **先出 1 场样稿过目再批量**。
   ⚠️ 新增图片落盘必须**同步 `cleanup_uploads.py` 引用集**，否则会被当孤儿删（红线 R1）。
3. ~~C 批｜往期回顾~~ **已完成**（见 §二 第 4 条）：B/C 两批代码与演示数据齐备，**等用户目视**（活动详情页往下滑看图文；
   活动页第三个 tab「往期回顾」点进往期活动）。目视通过后提交（push 等指令）。
4. **74 步走表**（用户开测后）：按 `docs/04 §十` 分批（WM7+WM8 17 步先）；敞口：§十 四榜预期未清库实测。

---

## 四、未决 / 待用户（别自己拍板）

- **A 批目视是否通过**（不通过就按反馈改；通过后我按 `fixNN` 前缀提交，push 等指令）。
- **B 批图片体积口径**（任务包给的建议值：长边 1200 / 输出 ≤400KB / 每活动 ≤30 张）——用户可否决。
- 往期回顾是否要给家长看"参与人数"聚合数字、以及"活动结束后通知引导看回顾"（客户未提，**先不做**）。
- `37e1d91`（CI 结论回写）与 A 批一起 push，还是分两次（默认：等用户说"可以提交"后一次全推）。

---

## 五、本轮新增防复发红线（**34~35**，接 v44 的 33 条）

- **34（WXML 注释与结构）**：wxml 注释**只能** `-->` 收尾；注释里不要出现 `*/`。写错不会报"注释错"，
  而是**吞掉半个模板**、报错行指向别处（实测差 60 行）。已机化 → 门禁 **R14**。
  同理：**轻量"按行"扫描器会撒谎**（它报"闭合 ✓"而编译器报错），验证模板类文件要用尽量同构的解析。
- **35（GUI 自动化点击全部落空时）**：先查**当前像素所有者是谁**（报错信息会写明 pid/bounds）——
  macOS 通知中心浮层会持有全屏像素并吃掉所有点击；此时改用 **a11y 元素点击**（不依赖像素）或等浮层消失，
  别继续怀疑自己的坐标。

---

# DmkWords 项目交接 - v44 交接书（2026-09-20 第十二轮：专家 18 问审查 + fix44 施工）—— **已归档**（读上方 v45）

> 🔴 **压缩后第一件事：读本区块全部六节，自包含。**
> **本文件只保留最近 3 版**（v44 正文 / v43 / v42）；**v41 及更早 23 版已移入
> `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`**（2026-09-20 fix44 R9 滚动窗口瘦身：原 4048 行 →
> 主文件约 500 行；归档区自带版本索引，只在追溯历史决策时读）。
> **专家侧另有独立恢复点**：`docs/专家交接书-20260920-在任专家会话交接.md`（专家角色/全会话史/18 问裁定/
> **fix44 任务包全文**/四项拍板/恢复链）。**两者互补不重复**：fix44 逐项明细只写在专家书 §五，本区块只记
> **施工侧状态 + 命令 + 纪律**。dev 接手：先本区块，再按 §三 去专家书取 fix44 明细。
> **继承不重复**：v42 §一 长期目标 / §五 红线 1~23 / §六 环境命令 / §九 文档读法 + v43 §五 红线 24~28 **全部有效**。
> **总口径**：本文件里的 v43/v42 区块是当时快照、不再追改；其中数字与规则名按当时口径读，
> 现行值以本区块 §一 + `docs/04` + 可现场复算的脚本（`check_demo_baseline` / `check_docs_code_alignment` / `gate-runs/` 最新日志）为准。

---

## 一、当前状态（一句话 + 证据）

**fix44（专家 18 问 → R1–R10）已全部施工完毕 → 专家实证审查通过 → **已 push 并 CI 绿**（`958de21`）；等用户走 74 步表验收。**

| 项 | 事实 |
|---|---|
| HEAD / 远端 | **本地 = `origin/main` = `958de21`（已同步，零未推）**；本次全推 6 刀：`b3f5da8` 专家书 → `5e6c905` 第 0 刀 → `84d7a81` R1 → `faf30d9` R2 → `50aec2c` R3/R4 → `958de21` R5~R10 |
| 工作区 | **干净**（fix44 全批已入库；本条保留落盘清单备查）：后端 1 · 小程序 3 · admin-web 3（含 `admin-web/scripts/gen-api.mjs` 新增）· 脚本 6（含 `scripts/check_media_discipline.py` 新增）· 测试 3（2 新增）· features 3 · 文档 7 · 宪法与 CI 2 |
| 本轮全量 gate | **PASS** `gate-runs/2026-09-20/gate-153942.log`：**481 passed**（基线 477 + 新增 4）/ 覆盖率 **84%** / BDD **8f·30s·103steps** / 9 步 exit 0；`gate-151926.log` 是首跑（4 连红＝测试时间炸弹，已修，见 §2.4） |
| 最近 CI | **`35498851116`（`958de21`）success 10m53s**：`481 passed` / BDD `8f·30s·103steps` / 9 步 exit 0；**两个新检查器在 CI 里真实生效**——`媒体纪律三条规则全零`、`前端类型与契约快照逐字节一致`（本地上一次是 `35449614097`） |
| 现场（gate 后已核验） | 后端 :8002 + :8443 `/health` 200 · 管理端 :5173 200 · 后端进程 15:58:56 新起 · 干净基线 **书 35 / 家长 3 / 孩子 11 / 活动 4 / 阅读圈帖 10** |
| 演示口径（定稿未变） | 演示孩 **5 本 / 102,320 词 / 积分 1,063 / 零头池 20 / 勋章[100000]**（live 复核通过） |
| 新增 tag（本地） | `acceptance-scan-checkin` / `acceptance-scan-checkin-2`（指向 `5775f1b`，R8 补打；**push 时一并推**) |

---

## 二、本轮（第十二轮）做了什么 —— **别重复做**

### 2.1 专家 18 问全部答复（审查对象 = v28→v43 共 35 刀）
四个方向：A 流程与状态（Q1–Q4）/ B 数据与口径（Q5–Q7）/ C 技术风险（Q8–Q13）/ D 收尾一致性（Q14–Q18）。
**三处更正了专家的前提**（专家已书面接受）：① Q11「五层修复系环境误判产物」不成立——那次证据是**同视口下
Safari 212px vs Chromium 549px** 的布局差（同引擎比较，RDM 整页缩放会同时作用两边、不可能产生该差异），
RDM 是 **09-18 另一条报障（阅读圈）**；② Q5「§十 还是旧数字」已被 09-17 轮覆盖；③ Q9「原版清理工具删过
实际文件」——实跑在 `78d7ab5`(08-24)，而 voucher(09-01)/活动封面(09-07)/circle 表(09-10)/证据目录(09-13)
**当时都不存在** → 损害不成立、无未上报 P0。

### 2.2 本轮已做的小修（4 文件，= fix44 第 0 刀）
- `scripts/gate.sh:74` 注释 `R1–R7` → **R1–R13**（陈旧口径，专家批准顺手修）
- `scripts/check_miniapp_style.py` docstring 写"十二条规则"却只列到 R12 → **补 R13**，并写明"改规则必改本清单"
- 交接卡 §五 标题"24~26" → **"24~28 共 5 条"**（专家 Q3 指出的内部不一致）
- 验证：`ruff` ✓ / `ruff format --check` ✓ / `check_miniapp_style` EXIT=0 / `check_docs_code_alignment` ✓

### 2.3 审查挖出的新缺陷与缺口（fix44 的成因，详见专家书 §五）
- **N1（真缺陷）**：`like()` **无自赞守卫**（`reading_circle/service.py:324-360` 只查"帖子存在 + 一孩一赞"）
  → 孩子可给自己点赞；而产品语义是"不自赞"（fix33 只做了通知排除 `post.parent_id != parent.id`）。
  **且 seed 从不造点赞**（改前 `grep CircleLike scripts/seed_wm11_demo.py` = 0）→ 现存自赞行是**应用真实点击**产物。
- **N2（流程债）**：09-12 后三个新功能零 FEAT 登记（生词本统计闪卡 / 书架已通过 / 扫码签到），
  `grep 5775f1b docs/` **零命中** = 口头需求直接施工。
- **N3（规格债）**：`features/` 38 文件 / 258 场景停在 09-12，**8 天零增长**；新功能全靠单测 + 走表。
- **N4（留痕债）**：`refundable_types` 资金白名单**只在代码注释**（`wm10_withdrawal_service.py:367-382`），
  PRD / 模式手册 / Feature 清单**零记载**（dev 已核）。
- **门禁职责空白**：T27/`export_openapi.py --check` **不管 `schema.d.ts`**（全文不含），
  而 `docs/18 §五` 却写"漏 `pnpm gen:api` → gate[8] diff 红"——**这句是错的**（新增红线 30）。
- 其余：cleanup 三陷阱（`"-samples/"` 前缀匹配不到 `wm15-samples/`；活动封面落 `cover/activity/` 被 `cover/` 覆盖、
  保命全靠 DB 引用；回压用 1080 而非 1200）· 报告图 Pillow 小字**三项全无数据**（清晰度/耗时/打印）·
  acceptance tag **09-12 后零新 tag 且纪律未成文** · §十 四榜预期**未清库实测**。

### 2.4 本轮施工中拦下的新缺陷（不在专家任务包内，是跑门禁时自己暴露的）

**测试时间炸弹**：首次全量 gate 跑出 `4 failed, 477 passed`，四条全在活动域，错误信息都是
`422 {"detail":"开始时间必须在未来"}`——测试 payload 把 `start_at` 写死成 `"2026-09-20T15:00:00"`
（作者当时的"明天"），**当日 15:19 到期**，测试自己过期了（上一轮 09-17 还是绿的，纯属日期未到）。
另有两个隐患：`test_p0_fix20_a` 的 `...T15:00:00.000Z` 靠 UTC(+8) 时区差续命、当晚 23:00 同样到期；
`test_wm1_dashboard` / `test_r313_guards` 用 `2099-01-01` 属"远期哨兵"，安全。
**修法**：四文件全部改**相对未来**（`_future_iso(days=7)` = `now + timedelta`），语义不变且永不过期；
**已机化**：`check_fake_assertions` 新增**规则 B**（键名 `start_at`/`end_at`/`enroll_deadline`/`publish_at`/`deadline`/`expire_at`
上的绝对时间字面量落在 [当前年, 2049] → FAIL；≥2050 放行），注入自证红/还原绿；
错误库新增 **§八十二**。**教训**：测试里的绝对时间就是定时炸弹，且炸在无关的人身上——要么相对时间、要么远期哨兵。

---

## 三、fix44 执行结果（R1–R10 全部落地，**逐项明细以专家书 §五 为准**）

| 项 | 结果 | 关键证据 |
|---|---|---|
| 第 0 刀 | ✅ `5e6c905`（4 文件：gate.sh 注释 R1–R13 / 检查器 docstring 补 R13+计数 / 交接卡 v44 / LEDGER 第十二轮行） | 顺手把 docstring「十二条规则」也改成十三条（红线 31：自述与实现同步） |
| R1 自赞守卫 | ✅ 后端 `like()` 加 422 + 小程序自家帖置灰 + 存量自赞可取消 | `test_fix44_circle.py` 真链路（本人/同家长跨孩都 422、零副作用、别家孩子 200）；**live**：422「不能给自己的帖子点赞」且 like_count 不变 |
| R2 Q10 治根 | ✅ `admin-web/scripts/gen-api.mjs`（`pnpm gen:api` / `--check`）接 **gate[8]** + `docs/18 §五` 口径更正 | **首跑即红**（专家预判正确）→ 查明仅**路径块次序**差异（行集合完全一致=语义零变化）→ 由快照重生 `schema.d.ts` + tsc 绿；契约源统一为快照（不再直连 :8002，CI 可跑） |
| R4 机化三条 | ✅ `scripts/check_media_discipline.py`（M1/M2/M3 + S1 注入自证）接 **gate[5]**；实修 `regen_covers.py`（原默认即破坏性） | **实树注入三条全命中、还原全绿**；M1 还揪出我 `_is_path_like` 漏判字面量路径 |
| R3 Q8 三陷阱 | ✅ `-samples/` 改首段后缀匹配；`cover/activity/` 走 `scope_of()` 最长前缀（1200 不再被 1080 覆盖） | 回归锁 `test_fix44_media_scope.py`（3 例）+ 两脚本 dry-run 实跑 |
| R5 报告图验证 | ✅ 数据入 `docs/15 §16.7` | 冷 634ms/HTTP 765ms、命中复用 7.4/16ms、750×1100/101KB、页脚 23px 笔画中位 2px；300dpi=A5 6.35×9.31cm（1.95mm/6pt）→ 屏幕清晰、打印不在范围 |
| R6 N2+N4 | ✅ 需求来源留痕（交付刀 `5775f1b`）+ 白名单三处同源 | 更正专家 N2 前提：三个功能的承载行**已在 09-15 补登**，缺的是"口头放行→登记"的可追溯链 |
| R7 N3 BDD | ✅ 12 条 `@draft` 场景（扫码连扫/防重/亮度 · 生词本统计 · 自赞守卫三态） | behave 仍 8 features / 30 scenarios / **103 steps**（draft 不计，基线不降） |
| R8 补打 tag | ✅ 两枚 `acceptance-scan-checkin` / `-2` → `5775f1b` + CLAUDE.md **DoD 3b**（宪法升 v1.5.1） | 立规：**tag 与 push 必须同批**（tag 没 push 等于没打） |
| R9 滚动窗口 | ✅ 4048 行 → **516 行**；23 版归档到 `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`（带索引） | 主文件头部说明 + 归档区读法 |
| R10 emoji 豁免 | ✅ `docs/15 §15.5`（文案语气可留 / 图标位禁；三处灰区如实登记） | 专家 Q12 裁定：不加检查器 |
| 增补 | ✅ 四项拍板落痕（LEDGER + 本卡 §四）；`docs/20` 扫码真机复看待上线前 | — |

**纪律遵守**：`fix44` 前缀 · R1/R2/R3/R4 触后端与门禁 → 已跑 **C 级全量 gate**（481/84%/BDD103）· gate 绿后本地 commit · **push 由专家收口执行**（用户已授权，一次全推 6 刀 + 2 tag）· 未 amend。
**专家实证审查**：R1–R10 十项全过（含亲跑 `check_media_discipline` / `check_demo_baseline`、reflog 对拍重排前后树完整性）；唯一小瑕疵 = `docs/18` 重排期间措辞精修 22 行（纯 prose，已复跑 `check_docs_code_alignment` 绿，不阻断）。

---

## 四、四项拍板（2026-09-20 终局，**不可重问**）

1. **走表模式确认**：按 `docs/04 §十` 走表；分批 **WM7+WM8（17 步）先 → WM5/WM6/WM6-F（41 步）→ WM10+WM9（16 步）**；
   先测 WM7/8 的理由：新口径动线全在这两块。
2. **积分 1,063 定稿**：`words_per_point=100` **不动**。
3. **预览只缩放不滚动定稿**（Q11⑤ 取舍闭环，**不改代码**）。
4. **扫码签到真机复看**（亮度 + 连扫手感）→ **挂 `docs/20` 上线前检查清单**，上线后再看。

> ⚠️ **步数勘误（dev 已核，口径以本行为准）**：专家书 §四/§六 写的"**74+5=79 步**"把 **WM9 的 5 步重复计入**。
> 按 `docs/04 §十` 剩余步数逐行相加实为 **74 步**：WM7 10 + WM8 7 = 17 ／ WM5 13 + WM6 14 + WM6-F 14 = 41 ／
> WM10 11 + WM9 5 = 16，三批合计 **17+41+16 = 74** ✓。**分批方案与拍板决定均不受影响**，只是对外口径请用 **74**。
> （专家书 `docs/专家交接书-20260920-在任专家会话交接.md` 是专家资产，dev 未改动其内容，仅在此标注勘误。）

---

## 五、下一轮（压缩后）该做什么

1. ~~push~~ **已完成**（专家收口：`03fd714..958de21` 共 6 刀 + `acceptance-scan-checkin` / `-2` 两 tag；CI `35498851116` success）。**下一步不在 git 侧，在用户手上**：走表验收。
2. **交用户验收 fix44**：可目视的点 = ①小程序自家帖点赞钮置灰与否 ②管理端活动页/报名抽屉无回归
   ③gate 日志（`gate-runs/2026-09-20/gate-153942.log`）。
3. **74 步走表**（用户开测后）：按 `docs/04 §十` 逐模块跟踪打勾、测完模块翻 LEDGER；
   **敞口**：§十 的四榜预期（观察期孩 480/180、押金孩 260）**未清库实测**，走表时按表核。
4. **长期目标未变**（用户 2026-09-15 原话，见 v42 §一）：主动视觉巡检、主动找观感问题，别等用户报；
   已扫过 vs 未扫过的页见 v43 §三 第 1 条。**本轮新登记的视觉缺口**：`docs/15 §15.5` 三处"emoji 当图标"灰区
   （error-view 图标映射 / index 章节标题前缀 / member 头像兜底）——建议下一轮视觉批一并收口。

---

## 六、本轮新增防复发红线（**29~33 共 5 条**，接 v43 的 28 条）

- **29（破坏性批量操作）**：**必须留原始清单（manifest）+ 可回滚备份**。Q8 教训：`optimize_uploads` 回压后
  原始清单与 `--trash` 均不在，只剩文件 mtime 能证明"未动"——**mtime 不能逐字节对账**。
- **30（门禁职责边界）**：新契约产物（如 `admin-web/src/api/schema.d.ts`）**要么接进 gate，要么在文档里明说
  "不在门禁范围"**；禁止文档声称"漏了会红"而实际不红（`docs/18 §五` 就是反例，gate[8] 根本不管它）。
- **31（检查器自述与实现同步）**：改检查器的规则**必须同步它的 docstring 规则清单**（R13 漏列、`gate.sh` 注释
  写 R1–R7 都是实锤）；同理"以 main() 实际打印为准"这句话不能当免罪符。

- **32（测试禁写死"近期未来"时间）**：测试里的绝对时间字面量**要么是过去、要么是远期哨兵（≥2050）**；
  写死的"明天"必然会到期，且炸在跑门禁的无关的人身上（本轮 4 连红实例）。相对时间用 `now + timedelta`。
  **已机化**：`check_fake_assertions` 规则 B。
- **33（契约产物同源，次序不可颠倒）**：`admin-web/src/api/schema.d.ts` 由**快照**生成
  （`docs/api/openapi.json`），不再直连 :8002；链路 = 代码 →（gate[8]）→ 快照 →（gate[8]）→ 前端类型。
  **生成顺序颠倒会让前端类型照着旧快照生成**（改了接口先 `export_openapi.py` 再 `pnpm gen:api`）。
  另：快照是 `sort_keys` 序、在线服务是路由注册序 → 两者生成的 TS 只差**块次序**，比对要按行集合看语义。

---

---

# 归档索引（2026-09-20 fix44 R9：滚动窗口瘦身）

本文件只保留**最近 3 版**（v44 / v43 / v42）。更早的 **23 版**在：

📁 **`docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`**（3588 行，自带版本索引）

| 归档区版本 | 日期 | 一句话 |
|---|---|---|
| v41 | 09-16 | 第八轮：视觉四刀 + 视觉二批（R12 类名门禁 / emoji→自绘图标 R13 / 未接线清理） |
| v40 | 09-15 | 第七轮：全库文档基线对齐 + 仓库清理（87 项 FEAT 状态重算） |
| v39 / v38 / v37 / v35 / v34 / v33 | 09-15 | 门店扫码签到（规划→施工）与视觉全量自查第一轮 |
| v32 / v31 / v30 | 09-13~14 | 小程序全页面审查与统一风格基准（R1–R7 门禁成型） |
| v29 / v28 / v27 / v26 / v25 / v24 | 09-09~09-12 | 阅读圈 A/B 双期闭环 + WM15 社交化 + WM4-10 验收起步 |
| v22 / v21 / v20 | 09-03~09-06 | P0 三批交付 + 插修批次 + gate 9 步制确立 |

> **读法**：归档区一律按"历史"读——其中数字与结论可能已被后续轮次推翻；
> 追溯用，不作为现状依据。现状以本文件 v44 区块 + `docs/LEDGER.md` + 可现场复算的脚本为准。
