# DmkWords 项目交接 - 2026-08-24 深夜 v2

> 恢复口令：读本文件，继续。

## 0. 一句话状态

DmkWords 少儿英语分级阅读项目 WM1 已验收、WM2 已修并持续精化中。当前 git 头待提交，B 级验证通过（26 passed）。F1-F4 已完成待推送。

## 1. 当前状态

- 仓库：github.com/leechunwoo0815/dmkwords (main)
- 宪法：CLAUDE.md；任务源：docs/09-补充任务清单.md
- 最新 gate：144 passed / 77.95% / exit 0
- dev：MySQL healthy / 后端 8002 / 前端 5173 运行中

## 2. 最近完成（C30-C36）

- C30：适读年级下拉选择（3-15 岁 6 阶段）
- C31-C34：封面预览、列表封面/题数/编辑删除、搜索支持编号、ISBN 可编辑
- C35：删除书目联动清理 uploads 媒体文件
- C36：批量导入模板「年级」改「适读阶段」并加 Excel 下拉
- 测试纪律：pytest/gate 前必须 stop dev.sh；conftest 加 lock_wait_timeout=10
- uploads 清理：删除 416 个孤儿文件，释放 34.89 MB，新增 scripts/cleanup_uploads.py
- A/B/C 验证分级纪律（用户裁定）

## 3. 在途任务（F1-F4）

| # | 任务 | 状态 |
|---|---|---|
| F1 | 批量导入 grade 白名单校验 | ✅ DONE |
| F2 | 图书管理页面批量删除 | ✅ DONE |
| F3 | 新增筛选 tab：未传封面/未传音频/未录入 5 道测验 | ✅ DONE |
| F4 | 其他人性化交互优化 | ✅ DONE |

### 实现提示

**F1**：在 backend/domain/catalog/import_service.py 加入 GRADE_OPTIONS 白名单校验；测试 test_p1_crud.py 加用例。

**F2**：后端已有 DELETE /api/admin/books/{id}；加 POST /api/admin/books/batch-delete 接收 list[int]；前端 BookManage 加 rowSelection + 批量删除按钮。

**F3**：BookListQuery / list_books 增加 no_cover、no_audio、quiz_incomplete 参数；前端 Tabs 增加对应 tab。

**F4**：可考虑图标按钮 + Tooltip、封面缩略图、搜索 placeholder 细化、导入结果保留等。

## 4. 关键文件

- 批量导入：backend/domain/catalog/import_service.py
- 常量：backend/domain/catalog/constants.py + admin-web/src/constants/grade.ts
- 书目删除：backend/domain/catalog/service.py BookService.delete_book
- 列表筛选：backend/domain/catalog/service.py list_books + schemas.py
- 图书管理页：admin-web/src/pages/BookManage.tsx
- uploads 清理：scripts/cleanup_uploads.py

## 5. 环境与纪律

- 管理端：http://localhost:5173，admin/dmkwords123
- 跑 pytest/gate 前必须 `bash scripts/dev.sh stop`
- A/B/C 验证分级：A 纯前端 tsc；B backend/tests 相关域 pytest + ruff；C 全量 gate + CI

## 6. 已知脏数据

books.grade 已有 '9-10岁（小学中年级）' 到 '9-26岁（小学中年级）' 等脏数据，因 import_books 未校验。F1 修复后可防止新增，旧数据待用户决定是否清理。

---

交接完毕。
