# DmkWords 项目交接 — 2026-08-24（完整版）

> **恢复口令：读本文件，继续。**
> 本文件 = 当前完整状态（截至 2026-08-24 深夜），接任者读完全部 §0-§10 即可零成本恢复。
> 上一份交接：`docs/项目交接-20260823.md`（§9 内容已过时，历史参考）；跨会话记忆规范不变。

---

## 0. 一句话状态

DmkWords（少儿英语分级阅读，FastAPI + MySQL + React admin-web + 微信小程序）**WM1-WM10 全部代码 + P0/P1/P2 已知缺陷全部修复**，本地门禁与远端 CI 双绿，**用户正在手动验收（WM1 已 ACCEPTED，WM2 缺陷已修复待重测）**。

## 1. 项目身份（同 20260823）

- 仓库 `github.com/leechunwoo0815/dmkwords`（SSH/main），**每次模块交付后必须推送**
- 宪法 `CLAUDE.md`（最高法律）；需求基线 `PRD/业务需求文档-定稿V1.1.md` + R-300 系列
- 台账 `docs/LEDGER.md`；欠账事实源 `docs/09-补充任务清单.md`（C/D/P 编号体系）
- 专家复审报告 `docs/10`（已全 DONE）、`docs/11/12/13/14`（工作简报/证据/审查结论/二轮简报）

## 2. 当前状态总表（2026-08-24 23:00 实测）

| 项 | 状态 |
|---|---|
| git 基线 | `main` 头 = `260f577`；**工作区干净**（仅 .workbuddy/ 忽略）；本地=origin/main |
| CI | 最近 run **32709580371 绿**（8m49s，138 pytest / 77.83% / tsc step8）——D3 重写后 CI 首次稳定绿 |
| 门禁 | `bash scripts/gate.sh` exit 0（138 passed / 77.83%）；证据 `gate-runs/2026-08-24/` |
| dev 环境 | MySQL healthy / 后端 8002 (pid 68248) / 前端 5173 (pid 68255)，**运行中** |
| 验收 | WM1 **ACCEPTED**（用户 11 步全过）；**WM2 缺陷（C24-C29）已修已推（`04fd2af`），等用户重测**；WM3-WM10 未开始 |
| 未决 | 无未提交修改；P2×8（C2/C3/C5/C6/C9/C10/C11/C18）与 WM11 统筹；澄清项（LOCAL 编号）用户拍板不改 |

## 3. 本会话全部工作（2026-08-24 早→深夜，按闭环批次）

1. **MDL 挂起根治**（接手第一任务）：
   - 根因双层：①`RefundService.review(approve)` 未推进联动退会 applying→refunding（execute-fail 分支失效）；`_advance_withdrawal` 白名单缺 pending_settle ②测试 `_db()` 断言失败泄漏 session 持有 MDL（优雅版=用 `with get_session() as db`）
   - 修复：`wm10_service.py` 两处 + `test_wm10_state_machines.py` 7 处 with 化；**后续全项目 16 处同源泄漏扫尾**（wm6/7/8、audit_fixes、r313、transfer_refund 全部 with 化，grep 零残留）
   - 拆分：`TransferService` → `transfer_service.py`（架构关 1154 行 god file）
2. **docs/10 P0 批次**收尾（R-313 守卫、退款 7 态/退会 6 态、联动退会、转让 12 步、C20/WM10-07/WM3-03/WM4-01）+ RefundCenter.tsx 适配（执行退款按钮/中文状态/`applying` 过滤 bug）
3. **P1 五项**（C1 小程序周月报页 / C7 音频试听 / C8 模板下载 / C12 员工管理 / C19 AR 维护）
4. **P1 测试锚点复核**（裁定2：无无效断言；4 项自定防护待追认——已全部追认）
5. **C21-C23**（流程整改验证包）：仪表盘 8 格经营数据（RED→GREEN）/ 小程序会员到期日（验收表先行）/ 小程序押金页（ledger 补端点 + 四件套）
6. **D3 CI 重写**：ci.yml 推倒重写（MySQL 8 service + uv + gate.sh 单 job + alembic 前置 + pnpm setup）；**首绿 run 暴露真实缺陷**：迁移 `362eb9327df9` 空壳（编译后 drop uq_active_borrow），导致"还书后无法再借同副本"；已补迁移（downgrade 对称）
7. **WM2 验收缺陷 C24-C29**：C25 媒体端点（cover/audio + media_auth.py，Router 零 try/except）、C24 AR 清空→NULL、C26 0 秒 MP3 拒收（7 处哑 MP3 夹具→合法帧 7s）、C27 题目表单重构、C28 题目编辑、C29 sort_order 服务端 max+1

## 4. 环境与账号

```bash
bash scripts/dev.sh start|stop|restart|status   # 前端5173 / 后端8002 / MySQL=OrbStack dmkwords-mysql:3306
```
- 管理端 `http://localhost:5173`：admin/dmkwords123（超管）、staff01/dmkwords123（运营）
- 小程序：开发者工具导入 `miniapp/`；登录=家长手机号+验证码 **1234**；**勾"不校验合法域名"**（C1 报告图片）
- Python: `uv`（.venv 就绪；装包 `uv pip install`）；Node: `pnpm`；DB 凭据在 `.env`（不要打印）
- **测试会清库**：pytest 每用例 TRUNCATE——验收期间用户造的数据会被清；**跑任何 pytest/gate 前先问用户**（铁律）

## 5. 协作模型与流程纪律（用户裁定后固化）

**分工**：用户手动验收（唯一验收人）→ 报 bug 带编号（C/D/P 续号）→ 我修（RED→GREEN）→ 专家 GLM 只审不改（业务/架构/测试三维）→ 用户重测闭环。

**流程裁定（2026-08-24 固化）**：
1. **测试先行**：新功能先写红测试；后补的必须锚点复核（找不到需求锚点的断言=无效，重写；自定防护要标注待追认）
2. **提交粒度**：任务包内 commit 自由；**push 前统一问用户一次**；docs/简报类可直接推；清库/删数据/改迁移文件无例外先问
3. **选型**：两种以上合理实现→简报列【方案】（推荐/默认/待追认）；涉资金/权限/不可逆数据必须等明示
4. **操作严谨性**：改完立刻跑受影响测试；脚本改文件后 grep 验证落盘
5. **诚实**：未验证说"未验证"；失败如实报；转录证据要标注来源；不弱化断言/不删测绕过

**关键红线**：不跑全量 pytest 清用户数据；gate/CI 绿才算完成；`verify_architecture`（Router 零 try/except/HTTPException、单文件≤800 行、四件套）不可绕过。

## 6. 血泪教训（本会话新增，照抄防复发）

1. **本地库手工 DDL 掩盖迁移链缺陷**（W5 缺索引被本地掩盖半月，CI 首绿才暴露）→ 迁移补丁必须在干净库验证；"autogenerate 无 diff"要查 SHOW INDEX/alembic history
2. **夹具与校验规则冲突会连锁**：C26 加"0 秒拒收"时，7 处哑 MP3 夹具（`\xff\xfb\x90\x64`+2000B）全部失效——改规则前先 grep 受检夹具
3. **契约变更要清调用方**：删 schema 字段（sort_order）后 3 处测试/前端残留 422——改 schema 必 `rg` 全仓
4. **测试能抓真 bug**：C25 测试写"坏 token 应 401"抓出 500（jwt 异常未捕获）——自定防护断言的价值
5. **架构关样例**：Router 里 try/except + 抛 HTTPException = 违例（有独立 `media_auth.py` 先例可循）

（20260823 交接 §6 的 1-8 条继续有效：僵尸进程假绿/脚本部分写入/ruff format 锚点/bash 花括号/WXML 禁 Math/alembic 非事务 DDL/工作区陷阱/测试夹具系列）

## 7. 下一步（接任者按序）

1. **陪用户重测 WM2**（docs/04 §WM2 11 步；重点：步5 音频播放器 C25/C7、步7 题目增删改 C27/C28/C29、步8 模板、AR 待配置筛选 C24）→ bug 续 C30+ → 修完回写 docs/09 §五
2. **WM3→WM10 依次验收**（手册 docs/04；每个模块：用户测→修→专家审→重测→LEDGER ACCEPTED）
3. **验收后 WM11 通知任务看板**（FEAT-067/068 + D1 第3层 + C13 自动转换 + 数据看板 + Excel 导出）
4. **P2×8**（C2 分享海报/C3 AR 曲线/C5 词典 330 万词/C6 schema 兜底检查/C9 券码二维码/C10 活动封面/C11 孩子二维码/C18 审计导出）与 WM11 统筹；**C19 已在 P1 完成（AR 只升不降），C3 曲线需要 AR 历史表——新决策点**
5. WM12 微信支付（阻塞：甲方资质 T3）；T3 微信资质 T4 需求签字
6. P3 观察项：CI actions 被强制 Node 24（告警），随下次 CI 触碰升级

## 8. 代码速查（改哪块找哪）

| 域 | 文件 | 内容 |
|---|---|---|
| media | `backend/domain/catalog/media_auth.py` | 管理端媒体鉴权（Bearer/query token）|
| 维权 | `backend/domain/identity/guards.py` | R-313 统一守卫（require_member_action）|
| WM10 | `wm10_service.py`（Refund/Withdrawal）+ `transfer_service.py`(Transfer) | 退款7态/退会6态/转让12步 |
| 仪表盘 | `backend/domain/admin/service.py` DashboardService + `Dashboard.tsx` | C21 经营格子 |
| 文件 | `backend/common/file_storage.py` | 封面 JPG/MP3 时长解析（C26 先解析后写盘）|
| 题目 | `catalog/service.py` QuizQuestionService（create max+1 / update）| C27-29 |
| CI | `.github/workflows/ci.yml` | MySQL+uv+gate 单 job |
| 测试 | `tests/unit/test_c24_c29.py`、`test_p1_crud.py`、`test_wm1_dashboard.py` 等 | 各批次证据 |

## 9. 证据索引（本地 gate-runs/2026-08-24/）

- `gate-full-c21.log` / `gate-full-sweep.log` / `gate-full-p1-final.log` / `gate-full-c24c29.log`：各轮全量门禁
- `mdl-hang-faulthandler-BEFORE-FIX.log`：MDL 复现栈（历史证据）
- 远端 CI：run 32696010700（D3 首绿）、32709580371（C24-C29）

## 10. 账户/数据注意

- 手动验收数据会随 pytest 被清（用户已知已同意过一次；**此后每轮跑测试前再问**）
- 小程序真机图片需合法域名；开发工具勾不校验
- 已知未改：LOCAL-000002 内部编号格式（用户拍板设计如此）；C22 观察期文案"会员到期(观察期30天)"——专家建议验收后改"观察期评估日"（P3 文案反馈，不阻塞）

---

*交接完毕。恢复口令已更新于本文件头部。祝新会话顺利。*
