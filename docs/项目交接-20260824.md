# DmkWords 项目交接 - v49 最新交接书（2026-09-23 第二十四~二十六轮：**走表验收通过并打 tag** / G1·G3·G4·G5·G7 落地 / **R16 新门禁 + G9 死类名 725→72** / **G6 文案 emoji 扩到管理端与导出**；工作区仅 5 个纯文档未提交，其余全部已推 CI 绿）

> 🔴 **换会话/压缩后第一件事：读本区块全部九节，自包含。** 然后读 `CLAUDE.md`（宪法）+ `docs/LEDGER.md`（台账最新几行）+ 错误库 **§一百零二 / §一百零三**（本会话新增的两族教训）。
> **与 v48 最大的不同**：① 工作区**基本干净**（只剩 5 个纯文档改动，含本交接卡与台账，见 §一）；② 借阅台/报告页/生词本三张验收表**已由用户亲手走完并通过**，已打三个验收 tag；③ 门禁体系新增两条规则（R16 / R13d），检查器从 6 个变 7 个。
> **本轮产物索引（都在仓里，别重复造）**：① 规范：`docs/15` **§15.8 R16（类名两端对账）** + **§21.3 文案 emoji 扩到管理端与导出**；② 门禁：`scripts/check_miniapp_style.py` 加 **R16a/R16b** + `scripts/check_copy_emoji.py`（**新文件，R13d**）+ `scripts/gate.sh` 第 [9] 步改**全仓 untracked 判红**；③ 台账：`docs/LEDGER.md` 第二十四~二十六轮；④ 错误库 §一百零二 / §一百零三。
> **继承不重复**：v48 §一~§九（含 §九「高频翻车点速查」= 宪法没写但一定会踩的 38 条，**仍是最有价值的检索表**）、v47/v46 区块已归档；v42 §一 长期目标 / §五 红线 1~23 / §六 环境命令 / §九 文档读法 + v43 §五 24~28 + v44 §六 29~33 + v45 §五 34~35 + v46 §八 36~37 + v47 §八 38~40 + **v48 §八 41~48** 全部有效。
> **保留窗口**：本文件只留 **v49 / v48 / v47**；**v46 已并入** `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`（该文件头部有沿革说明）。旧区块里的"保留窗口"都是当时快照，别照它判断。

---

## 一、当前状态（一句话 + 证据）

**走表验收已通过并留 tag；本会话把 8 项登记欠账（G1~G7/G9）全部做完，门禁新增两条规则，工作区只剩 5 个纯文档改动（含本卡自身）。**

| 项 | 事实 |
|---|---|
| HEAD / 远端 | **本地 = `origin/main` = `112c4c1`、零未推**（本轮 12 刀全推：`9ec8cfb` v48 订正 → `fb50db6` 走表+G1/G3/G4/G5/G7 → `245f610`/`37eec1d`/`37abc01` R16+G9 → `be63e51`/`3952bff`/`112c4c1` G6） |
| CI | 最近三轮 **全 success**：`35835058171`(9m17s) / `35839950146`(9m10s) / `35841436785`(11m14s)——均 513 passed + BDD 8f·30s·103steps + 9 步 exit 0，**且新检查器 R16/R13d 在 CI 里实测生效**（72=72 / 两处 emoji 0） |
| 验收 tag（本轮新增） | **`acceptance-borrow-console-2`**（借阅台 S16–S23）· **`acceptance-observation-report`**（报告页 S24–S28）· **`acceptance-vocabulary`**（生词本 S29–S33）——均指向 `fb50db6`，message 含覆盖范围 + 用户原话「全都通过了」 |
| 门禁清单 | 第 [5] 步现为 **7 个检查器**：`check_fake_assertions` / `check_miniapp_bindings` / `check_docs_code_alignment` / `check_rbac_consistency` / `check_miniapp_style`（R1–**R16**）/ `check_media_discipline`（M1–M3）/ **`check_copy_emoji`（R13d，新）** |
| 现场 | 后端 :8002 + 管理端 :5173 均 200；**干净基线 35 / 3 / 11 / 4 + 演示孩口径 10 项全中**（`check_demo_baseline` PASS）；小程序模拟器已重登（演示家长 → 演示孩） |
| 未提交 | **5 个纯文档**：`docs/19`（新增"演示库重建后必须重登小程序"等两条坑）+ `docs/LEDGER.md`（第二十四~二十六轮 + 交接 v49 行）+ `error_list`（§一百零三）+ **本交接卡 v49 与归档区**（v46 转入）；另有工具生成的 `.zcodeignore`（不入库）。按 **D 级**纪律攒批，随下次代码刀一起推 |
| 迁移 / 测试 | 本轮**零迁移**；pytest 基线 **513 passed**（未增删用例） |

---

## 二、本轮（第二十四~二十六轮）做了什么 —— **别重复做**

1. **接手会话的拷问与订正（`9ec8cfb`）**：新模型用「@grill-me」方式拷问前任，查出并订正 6 处不实/漏项——未提交数（52→实取）、第 23 轮"每轮都跑全量"不成立（补相称验证证据文件）、**媒体孤儿机制被证伪**（不是历史灰尘而是结构性水龙头）、seed 对未跟踪脚本的硬依赖（gate 第 [9] 步看不见 `scripts/**`）、演示数字漂移（书 30 改名把词账 102,320→109,320 没人拦）、"真机"用词过松。全部登记为 G1~G8。
2. **走表 4/5/6（S16–S33，18 步）全过** → 用户亲手走一遍后回「**全都通过了**」→ 四刀 + 三 tag 同批推送（DoD 3b）。走表用 devtools 模拟器 + 管理端真浏览器，状态类断言全部带机器证据（`activeElement` 焦点链 / 报告图 `boundingClientRect` 402×563pt / 导出 xlsx 逐行解析）。**三处如实标注不可机器验证**：原生手势（双指/翻页/长按）、原生操作表与确认弹窗（不进模拟器截图）、提示音。
3. **G1/G3/G4/G5/G7 落地（`fb50db6` 前身三刀）**：① gate 第 [9] 步改**全仓 untracked 判红**（白名单仅 `.zcodeignore`）——原只扫三目录，漏掉 seed 的 import 依赖；② `check_demo_baseline` 增**演示孩 10 项断言**（词账/积分/零头池/勋章/在借/逾期/可借/生词本/押金，与 docs/04 同源）；③ 勋章 `awarded_at` **回拨 45 天**（护照"达成日期"不再是今天）；④ **订单图标载体**（押金单 wallet + 自定义单 receipt 各一条 paid——查证四类"无载体"订单在生产都能被创建，所以"收死分支"是错的）；⑤ **生成图内容寻址**（`file_storage.content_tag` 单一来源；清库+seed 每轮孤儿从 **+76/+40 降到 +2/+5**）。
4. **R16 类名两端对账 + G9 死类名清理（`245f610`/`37eec1d`）**：R12 只查「模板用了没样式」，反方向（**样式写了没人用/名字对不上**）是盲区（订单图标 bug 就藏在这）。新规则：**R16a** 死类名（按文件计数基线，只减不增）+ **R16b** JS 提供的类名必须在本页 wxss 有定义（零容忍，当前 0 违规）。首扫 **725 条**死类名 → 两批清到 **72**（删选择器 ~500 个、净删 3,937 行 wxss），残留全是"与活类共处同一组合选择器"的（保守不删）。
5. **G6 文案 emoji 扩到管理端与导出（`be63e51`/`3952bff`）**：新增独立检查器 `scripts/check_copy_emoji.py`（**R13d**）：① `admin-web/src/**/*.{ts,tsx}` 去注释零 emoji；② `backend/**/*.py` 按 **AST 取字符串字面量**零 emoji、**docstring 跳过**。清理 6 处（`ScanCheckin` ⚠→「注意」+ 去冗余 ✅、`ErrorBoundary` 🎨→`WarningOutlined`、`CircleManage`「🌟 已赞」→「已赞」、导出 xlsx 单元格 ⚠→「注意：」）。
6. **两次施工自伤（都没进仓库，已入错误库 §一百零二）**：① R16a 的作用域口径与 R12 分叉 → 误删 13 处定义，**被 R12 当场拦下**（改镜像口径后重做）；② 内联脚本切片出错、`old` 成空串 → `str.replace('', new)` 把检查器文件插成 **84MB/163 万行**（原文未丢，按副本间距反解恢复）。
7. **一次演示现场事故（错误库 §一百零三）**：用户截图报「演示家长 · 正在陪伴：**导出孩甲**」——成因是"跑完 pytest 只 `restart` 没清场"（测试残留 2 家长 2 孩子）+ "清场重建后小程序没重登"（旧 token 的 `parent_id=1` 在 id 重排后指到别的家长，而"我的"页每次都会从服务端拉孩子列表、家长资料却是登录缓存）→ 拼出张冠李戴。已复原（3/11）+ 重登；纪律已写进 `docs/19 §三`。**不是产品缺陷**（生产 id 不重排）。

---

## 三、口径与规范变更（**压缩后理解冲突高发区**）

| # | 变更 | 落地处 |
|---|---|---|
| 1 | **类名必须整名提供，禁止把前缀留在模板里拼**（✅ `class="badge-{{cond?'paid':'x'}}"`；❌ `class="order-icon-{{item.icon}}"` + JS 只给 `type1`） | `docs/15 §15.8`（R16） |
| 2 | **文案 emoji 口径 = 用户可见文案清零（含导出文件）**，机械门禁扩到管理端与后端字符串字面量 | `docs/15 §21.3`（R13d）+ `CLAUDE.md §八` 第 5 步清单 |
| 3 | **生成图文件名 = 内容指纹**（`sha256(字节)[:12]`，单一来源 `file_storage.content_tag`）：内容变才换名 ⇒ 缓存必刷；内容不变 ⇒ 同名复用不重绘 | `docs/15 §16.3 第 2 条` 改写 + `file_storage` / `card_render` / `seed_demo_library` |
| 4 | **演示数据新增三载体**：S21「全锁书」（Nate the Great 两册各被别的孩子预约）、订单图标（押金单 + 自定义单）、勋章达成日期回拨 45 天 | `scripts/seed_wm11_demo.py` + `docs/04` 增补表 4 S21 行 |
| 5 | **验收基线断言 = 两组**（四表计数 35/3/11/4 + 演示孩 10 项）：改演示数据必须同步改 `docs/04` 与该脚本，否则命令红 | `scripts/check_demo_baseline.py` + `docs/19 §一/§二` |
| 6 | **跑过任何 pytest（除点名的那支清场测试）后必须走清场复原**；`restart`/seed 只保证"演示数据在"、不保证"测试残留不在" | `docs/19 §三` + 错误库 §一百零三 |
| 7 | **清场重建后必须重登小程序**（演示库 id 会重排，旧 token 会指到别的家长） | `docs/19 §三` + 错误库 §一百零三 |
| 8 | gate 第 [9] 步 = **全仓 untracked 判红**（白名单 `.zcodeignore`） | `scripts/gate.sh` |
| 9 | `docs/04` 三处数字订正：生词本 **8 词**（原写 2 词）/ 演示孩 **可借 28**（原写 27）/ WM4 押金步注明"已预置 paid 押金单，别重复建单" | `docs/04` |

---

## 四、遗留与盲区（如实）

1. **媒体体检第一步（用户已选①，尚未开做）**：每天自动"点数"孤儿图（**只看不删**）+ 数字进管理端任务看板；运营数字异常时由技术侧执行清理。用户在上一轮明确选了这条路线，但"现在就做"尚未拍板 → **开工前先确认**。
2. **媒体体检第二步（待用户拍）**：给超管做「媒体体检」页（孤儿图 N 张/X MB + 「移到回收站」按钮，二次确认 + 30 天可还原）；普通专员只读。**删除是破坏性操作，别擅自给运营开权限。**
3. **媒体孤儿存量未清**：内容寻址后每轮只 +2/+5（活动封面画了相对日期、榜单卡带周次，属内容真变），但历史孤儿仍在（约 123MB；`cleanup_uploads.py` 默认 dry-run，`--trash` 才真删）。未清理是**用户未拍**，不是忘了。
4. **G9 残留 72 条死类名**（全在组合选择器里与活类共处）+ R16a 基线 72：清它们要拆选择器，收益低风险高，建议挂着。
5. **家长资料缓存陈旧**：服务端没有 `GET /api/miniapp/parent`，页面上的家长名/电话来自登录缓存 → "别人改了我的资料"这类场景会短暂不一致（生产影响轻微）。加固方案（新增 GET 端点 + 页面进入时刷新）**评估为可选**，未做。
6. **未扫过的页面清单**（沿用 v48 §四.5）：押金、退款申请、权益转让、退会、我的预约、活动票券、活动详情、阅读圈名片/分享、quiz/quiz-result、周报月报、会员码、登录页。
7. **大屏下一批（用户已裁定"融合进仪表盘、不新增页"）**：候选见 `docs/任务包-20260921-店外大屏可视化（中央仪表盘·电视轮播）.md §四`。
8. **`gate-runs/` 全库未入库**（`.gitignore:15`）：所有门禁/验证日志只在本机，LEDGER/交接卡引用的 `gate-runs/...` 路径在别人 clone 出来时不存在（政策问题，非本轮引入；CI 才是版本化证据）。

---

## 五、下一轮该做什么（按优先级）

1. **先跟用户确认"媒体体检第一步要不要现在做"**（§四.1）——它便宜、零风险、把"数字悄悄变大"的风险消灭掉。
2. **继续视觉巡检**（用户长期目标：主动找观感问题，别等报）：从 §四.6 未扫页面里挑，沿用两条硬纪律——**改完必须 `simulator_refresh` + 重开页面 + 截图**；**状态类断言必须有机器证据**（computedStyle/像素/接口原始值）。
3. **R16 的延伸**：若哪天要清零那 72 条残留，先改"组合选择器拆分"再动基线；R16b 的覆盖面可以扩到"模板里拼接但 JS 不给整名"的形态（目前靠规范约束）。
4. **G6 第二步**（超管媒体体检页）等用户拍。
5. **大屏下一批**（§四.7）。

---

## 六、必读命令与环境（照抄）

```bash
# 现场
bash scripts/dev.sh restart            # 起/重启 后端:8002 + 前端:5173 + 双 seed；**改后端代码必须 restart 才能自测**
bash scripts/dev.sh stop               # 跑 pytest/gate 前必须（同库互踩会假红）
bash scripts/gate.sh full              # 全量 9 步（C 级）；PASS 后自动 dev.sh restart
.venv/bin/python -m scripts.check_demo_baseline          # 清场 + restart + 双 seed + **两组断言**（四表 + 演示孩 10 项）
.venv/bin/python -m scripts.check_demo_baseline --no-clean  # 只计数（**调试用**；跑过 pytest 后别用它当复原）

# 七个检查器（gate 第 [5] 步同族，可单独跑）
for m in check_fake_assertions check_miniapp_bindings check_docs_code_alignment \
         check_rbac_consistency check_miniapp_style check_media_discipline check_copy_emoji; do
  .venv/bin/python -m scripts.$m; done
.venv/bin/ruff check backend/ tests/ features/ scripts/ && .venv/bin/ruff format --check .

# 小程序视觉自查（唯一通道；首次需用户点 IDE 的「MCP 客户端授权」）
.venv/bin/python scripts/wechat_ide_mcp.py check_wechatide_status
.venv/bin/python scripts/wechat_ide_mcp.py simulator_open_page '{"page":"pages/index/index"}'
.venv/bin/python scripts/wechat_ide_mcp.py simulator_screenshot '{"path":"/tmp/a.jpg","wait":3}'
.venv/bin/python scripts/wechat_ide_mcp.py simulator_refresh        # 改 wxml/wxss/js 后必刷（否则看旧帧）
#   固定五步：改 → refresh → **重新 open_page**（refresh 会回首页）→ 等 3~5s → 截图
#   切登录态：evaluate 里 wx.request 打 /api/miniapp/login {phone, code:'1234'} → 写 storage(token/parent/children/currentChildId)
#   **清场重建后必须重登**（否则旧 token 的 parent_id 会指到别的家长，见错误库 §一百零三）
```

**账号与演示数据**：管理端 `admin / dmkwords123`（超管）、`staff01 / dmkwords123`（运营专员）；小程序 **13800008888 / 1234**（演示家长：演示孩 formal + 小红 none）、**13800007777 / 1234**（WM3 异常态）、**13800006666 / 1234**（退款/转让）。
**演示数据要点**：书目 35 / 家长 3 / 孩子 11 / 活动 4；演示孩 词账 **109,320** / 积分 **1,133** / 零头池 20 / 勋章 [100000]（达成日期 = 45 天前）/ 在借 2（逾期 1）/ 可借 **28** / 生词本 **8 词** / 押金已缴 ¥1,200；**会员码每次 seed 会重生成**（别写死，页面或 `Child.member_code` 现取）。

---

## 七、文档一致性体检（本轮）

- **新增**：`docs/15` §15.8（R16）/ §21.3（R13d）；`docs/04` 增补表 4 的 S21 载体说明；`docs/09 §十二` 的 2026-09-23 两节（G2 落地 / G9 两批清完 / G6 落地）；错误库 **§一百零二 / §一百零三**；`check_copy_emoji.py` 的 docstring 规则清单。
- **改写**：`docs/15 §16.3 第 2 条`（内容寻址）；`docs/19`（基线两组断言 + 新增两条坑）；`CLAUDE.md §八` 第 5 步清单（R16 / R13d）；`docs/LEDGER.md` 第二十四~二十六轮；`docs/04` 三处数字；`check_miniapp_style.py` docstring 规则清单（R16）。
- **体检方式**：`check_docs_code_alignment`（27 文档 / 173 接口 / 47 配置键 / 38 表 / 符号 1790）✓ + 每个改动跑相称级门禁（B′/B″/C 按改动面选）+ 三次 CI 全绿。
- **遗留（如实）**：§四.8 的 `gate-runs/` 未入库；`docs/19`、`docs/LEDGER.md`、`error_list` 与本交接卡（含归档区）的本次改动仍在工作区（攒批）。

---

## 八、本轮新增防复发红线（**49~53**，接 v48 的 41~48）

49. **跑过 pytest 必须走清场复原**（不许用 `--no-clean` 图省事）：`restart` 与 seed 只保证"演示数据在"，不保证"测试残留不在"——错误库 §一百零三 就是这么发生到用户眼前的。
50. **清场重建后必须重登小程序**：演示库 id 会重排，旧 token 的 `parent_id` 会指到别的家长 → 页面"张冠李戴"（家长是缓存、孩子是实时）。**这是演示环境特有，生产不受影响**，产品代码无需改。
51. **同一口径只能有一份来源**：R16a 与 R12 各写一份"哪些文件共享一份 wxss"的作用域 → 分叉 → 误删 13 处（被 R12 当场拦下）。新规则必须**显式复用**既有作用域/配对函数，并在注释里点名"镜像 X"。
52. **改代码文件优先用编辑工具；真要写脚本：先断言、最后写盘**（写后读回比对）。事故：切片取到空串 → `str.replace('', new)` 把文件插成 84MB/163 万行（错误库 §一百零二）。
53. **新增检查器必须写清"扫什么范围"并独立成文件**（不塞进名字只覆盖一半范围的老检查器里）；**S1 注入自证必须同时含"该报的报"与"不该报的不报"两类样本**（R13d 的反例：注释里的 emoji 不算、docstring 不算）。

---

## 九、接手第一小时建议动作（照做即可）

1. `cd` 到项目 → `git status --short`（**先看还剩什么没提交**；本轮应只有 5 个纯文档 + `.zcodeignore`）→ `git log --oneline -5`；
2. 读本区块 **§一 → §二 → §三**，再扫 **v48 §九「高频翻车点速查」**（那份 38 条表仍是检索利器）；
3. `bash scripts/dev.sh restart` 起现场 → `.venv/bin/python -m scripts.check_demo_baseline`（应得 **35/3/11/4 + 演示孩 10 项** 双组 PASS）；
4. 抽查一处小程序视图（`docs/15 §二十` 通道；**若刚清过场，先重登**）确认自查通道可用；
5. 跟用户确认**"媒体体检第一步要不要现在做"**（§五 第 1 条）——这是当前唯一"用户已选路线、待开工"的事项。

---

## 九·补（2026-09-23 **同日追加：第二十七轮·媒体体检落地**）—— **这一节比 §一 的"工作区状态"更新**

> 用户原话：「开做吧，还是做一个删除的按钮吧，没关系的，前几天测试的时候可以每天都试试，**如果不会删掉正在使用的图片就 ok**」。
> 交付物、口径、命令全在 `docs/15 §二十二` + `docs/04 §十二`（S34–S39 验收表）+ `docs/03 F15/FEAT-086` + LEDGER 第二十七轮。

**现状（本节的数字比 §一 新）**：HEAD 仍是 `112c4c1`（**本轮未提交、未推送**——按纪律等用户说提交）；
工作区 = 6 个新文件（`backend/common/media_paths.py`、`backend/domain/admin/media_models.py`、`backend/domain/admin/media_service.py`、`alembic/versions/b3d5f7a9c1e2_wm16_media_health.py`、`admin-web/src/components/MediaHealthPanel.tsx`、`tests/unit/test_media_health.py`，**已 `git add`**——gate 第 [9] 步要求零 untracked）+ 一批修改（registry/router/schemas/seed_configs/catalog router/conftest/两个旧测试断言 + docs 03/04/07/09/15/19/LEDGER/本卡/openapi.json/schema.d.ts）。

**给下一个会话的三条要点**：
1. **别重复造**：`media_census` 任务、五道防线、`REFERENCE_SPECS` 都已落地；要加新的媒体表列**必须**同步 `REFERENCE_SPECS`（`tests/unit/test_media_health.py` 的覆盖守卫会红）。
2. **新增口径**：图片清理 = "引用集即时复算 + 保护/白名单双收窄 + 最小年龄 + 只移不删 + 到期复检再删"；`uploads/.trash/` 不下发、不盘点、不在任何清理白名单里。任务钟点走新加的 `TaskSpec.cron_expr`（interval 会给"每天早上"漂移）。
3. **本轮踩到的坑（已写进 docs/19 §三）**：gate 退出码 0 并打印「现场已自动恢复 ✓」之后，基线仍是 **4 家长 / 12 孩子**——门禁自动恢复只 `restart`，**不清它自己跑的 pytest 残留**；必须再跑一次 `check_demo_baseline`（不带 `--no-clean`）并**重登小程序**。
4. **未做**：**没有点真实「清空回收站」**（那会永久删掉回收站里的 3454 张 / 139MB——属用户的验收动作 S40），也**未提交未推送**；回收站里另有 2 个是我做端到端验证时造的临时文件（`cover/zz-purge-test/`）。
5. **门禁**：最终代码跑过全量门禁 `gate-runs/2026-09-23/gate-media-health-5.log` = **533 passed / BDD 8f·103steps / 9 步 exit 0**（过程四次红/复跑：首轮 ruff 未格式化 + 两处旧计数断言 + 未 `git add`；第二轮全绿后修了弹窗里一处字面 `**` 文案并重跑；第三轮为「清空回收站 + 布局」；第四轮为「记账对账」）。
6. **同日追加（第二十八轮，用户「通过」后的两条）**：① **清空回收站**（= 提前到期，复检闸门照跑，被引用的当场救回）；② **看板首屏收敛**（媒体体检卡片压成一行摘要，明细收进折叠区——用户原话「要翻好久才能翻到」）；③ **自查发现的真缺陷已修**：清场重建会抹掉回收站记账 → 新增 `reconcile_trash()` 自愈（详见 LEDGER 第二十八轮 + `docs/15 §22.2 第 7 道` + `docs/19 §三` 新坑行）。
7. **待用户**：S40（亲手清空一次回收站）与 S41（首屏一行摘要）复验；通过后按 **DoD 3b** 打验收 tag（`acceptance-media-health`）**与交付刀同批推**（现在未提交，别让 tag 落在不存在的 commit 上）。

---

# DmkWords 项目交接 - v48 交接书【**已归档**：读上方 v49；⚠️ 区块内「工作区 50+ 项未提交」是当时快照，本会话已全部提交推送】（2026-09-21 第十七~二十三轮：评估报告页重排 / 生词本成就页 / 成就页两态与达成三件套 / 首页切换孩子下线+串台修复 / 全站 emoji 清零 / WXML 恒真 bug / 订单·阅读圈微修 / 目录改名；**⚠️ 工作区 50+ 项未提交（以 `git status --short` 实取为准），等用户目视**）

> 🔴 **换会话/压缩后第一件事：读本区块全部九节，自包含。** **§九 是「高频翻车点速查」——宪法没写、但接手的人一定会踩的那些坑（含模型自己的施工习惯病），务必看。**
> **与 v47 最大的不同**：v47 说"已全部推送、现场干净"；**本版工作区不干净**——第十七~二十三轮全部未提交（见 §一 状态表、§四 遗留、§五 第一步）。
> **本轮产物索引（都在仓里，别重复造）**：
> ① 规范：`docs/15` **§十九 报告类图片展示规范** / **§二十 小程序视觉自查通道（devtools CLI 用法与四条纪律）** / **§二十一 成就类页面呈现规范**（含 §21.1 徽章两态与达成三件套、§21.2 文案 emoji 一律清出）；
> ② 门禁：`scripts/check_miniapp_style.py` 新增 **R13c（文案 emoji）**、**R15（WXML 表达式禁方法调用）**；**R16（动态类名/死类名）、R17（资产语义）已登记未实现**（`docs/09 §十二`）；
> ③ 台账：`docs/LEDGER.md` 第十七~二十三轮逐行；`error_list/错误记忆库-全项目-20260831.md` **§九十四~§一百**（本轮新增）；
> ④ 验收：`docs/04 §10.3` **增补表 4（S16–S23 借阅台）/ 5（S24–S28 评估报告）/ 6（S29–S33 生词本）**。
> **继承不重复**：v42 §一 长期目标 / §五 红线 1~23 / §六 环境命令 / §九 文档读法 + v43 §五 24~28 + v44 §六 29~33 + v45 §五 34~35 + v46 §八 36~37 + v47 §八 38~40 **全部有效**（v47/v46 区块仍在本文件下方）。
> **保留窗口**：本文件只留 **v48 / v47 / v46** 三版；**v45 已并入 `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`**（该文件头部有沿革说明）。
> **专家侧恢复点**：`docs/专家交接书-20260920-在任专家会话交接.md`。

---

## 一、当前状态（一句话 + 证据）

**小程序端"视觉 + 没生效型缺陷"已完成六轮排查（第十七~二十三轮）**：评估报告页重排、生词本成就页、成就页两态与达成三件套、切换孩子数据串台修复、全站 emoji 清零（R13c）、WXML 恒真 bug 修复（R15）、订单/阅读圈微修、目录改名。**全部未提交，等用户目视**。

| 项 | 事实 |
|---|---|
| 远端 / HEAD | `origin/main` = **`0578cbf`**（第十七轮之前最后一次 push）；**本轮（第十七~二十三）全部在工作区** |
| 未提交规模 | `git status --short` = **52 项**（以命令实取为准；含 `scripts/gen_demo_progress_report.py` 等新增、未跟踪的 `.zcodeignore`） |
| 门禁（**第十七~二十二轮各一次全量**；**第二十三轮按 §八 只跑相称验证、无全量**） | 17 轮 `gate-runs/2026-09-21/gate-170920.log` · 18 轮 `gate-181827.log` · 19 轮 `gate-184549-step5-docs-token.log`（**首轮第 [5] 步红：我文档里写了 `名字()` 形式被当悬空引用 → 只改文档后整组复跑绿、代码零改动**）· 20 轮 `gate-190745.log`（**首轮第 [1] 步红：改检查器忘跑 `ruff format` → 停轮格式化后重跑**）· 21 轮 `gate-195349.log` · 22 轮 `gate-202305.log` —— 均 **513 passed / BDD 8f·30s·103steps / 9 步 exit 0**；**23 轮 `gate-205513.log` 是我习惯性开的全量、被用户当场叫停（第 [2] 步跑一半）**，该轮真正的证据在 **`gate-runs/2026-09-21/verify-b23-scripts-ranked.log`**（ruff 两条 + 引用 seed 的 2 个测试 12 passed + 基线 + 现场 DB 核对 + health） |
| 现场 | 后端 :8002 + 管理端 :5173 均在跑；干净基线 **35 / 3 / 11 / 4**；演示孩 在借 2（逾期 1）/ 可借 28 |
| 测试 | pytest 基线 **513 passed**（本轮未增删用例；演示报告由 seed 造） |
| 迁移 | 本轮**零新增迁移**（`milestone_awards` 复用已有 `MilestoneAward.awarded_at`） |
| 工作区 | **不干净**（见上）——**新会话第一件事是跟用户确认"验收 + 提交/推送"时机** |

---

## 二、第十七~二十三轮做了什么（**别重复做**）

**第十七轮 · 评估报告页重排**（`pages/member-pkg/observation-report/*`、`pages/member-pkg/report/*`、`scripts/gen_demo_progress_report.py`、`scripts/seed_wm11_demo.py`）
- 用户报「评估报告和评语展示太丑了……现在看也看不清」→ **竖版整页展示**（`mode="widthFix"` 满卡宽，实测 402×563pt/页）+ **放大出口改原生 `wx.previewImage`**（双指放大/翻页/长按保存；删掉自造 overlay）+ 老师评语卡（排在图之前）+ 最新一期展开 / 往期折叠 + 页码角标；周报月报页补同款出口。
- **seed 造数**（验收路径必须有载体）：`scripts/gen_demo_progress_report.py` 按外教报告骨架出图（走 `reading_circle/art.py`）；`_ensure_demo_observation_reports` 造 3 期（观察期孩 2 期 / 待评估孩 1 期 / 押金孩 0 期=空态），第 1 期回拨 28 天。

**第十八轮 · 生词本改造成就页**（`pages/reading-pkg/vocabulary/*`）
- 成就头卡（大数字「N 词」+ 分档鼓励语）+ 四指标（累计查词 / 来源书目 / **本周新增** / 查过多次，均前端现算）+ 三层信息行（词 40rpx 等宽主色 → 音标 + 中文释义 → 出处书签小片 + 日期）+ 「查 N 次」贴片 + **行内删除取消**（改长按操作表 + 闪卡内移除，页尾一行提示）+ 闪卡升级（左对齐释义）。
- **用户中途裁定「把封面去掉，图片多了会卡顿」** → 列表不放封面图（后端为此加的 `cover_url` **已回退**，不留无人消费字段）。

**第十九轮 · 首页切换孩子入口下线 + 切换串台修复**（`pages/index/index.*`、`utils/session.js`、PRD §2）
- 首页 `.child-chips` 整组删 → **切换只在「我的」页**（PRD §2 口径已改）。
- 修「切了孩子，积分和词数还是上一个孩子的」：根因三连＝**未入会 422 + 静默 catch 保留旧值 + `getCurrentChild` 严格比较（dataset id 类型不定）**；修法＝`_childScopedReset` 换孩子先清场、失败落"未知"（渲染「—」）、未入会显示原因「入会后可查看」、两侧统一转字符串的宽松比较、写入口径归一为数字。

**第二十轮 · 成就页两态 + 全站 emoji 清零 + 新门禁 R13c**（`profile-card/*`、`achievement/*`、8 个页面 + `components/error-view/*`）
- 护照里程碑换**自家六色奖牌资产**（`miniapp/icons/badges/milestone_m1.png` 起的六枚）+ 已达成/未达成两态；等级改 `level_template.png` 盾牌 + 字母叠加；统计数字改 `--font-mono`（手写体数字顶到边）。
- **R13c**：`miniapp/**/*.{wxml,js}` **全量文案禁 emoji**（跳过注释、`TYPO_GLYPHS` 白名单放行）+ S1 自证 + 接入退出码 —— 此前 R13a 只扫图标槽位字面量，**全库 31 处 UI emoji 而检查器报 0**；一次性清零（首页/阅读圈/书籍详情/听书页/书架/购买页/我的/共享组件 error-view），并修"emoji 换位图要给宽高"（`error-icon` 原来只有 `font-size`）。

**第二十一轮 · WXML 恒真 bug + 达成三件套 + 新门禁 R15**（`profile-card/*`、`achievement/*`、`backend/domain/growth/service.py`）
- 用户问「阅读护照，10 万词成就怎么表现出来？」→ 挖出 **WXML `{{ }}` 不支持方法调用**：`milestones_awarded.indexOf(item) !== -1` 静默得 `undefined` → **恒真** → 六枚里程碑**一直全部显示「已达成」**（老版是全奖杯图标）。
- 修：判定搬进页面 JS + **达成三件套**（状态词 / **达成日期** / 进度与目标「里程碑 1/6 枚已解锁 · 下一枚 50万词还差 39.8万词」）；后端 `summary` 新增 `milestone_awards[]`（node + awarded_at，**零迁移**）。
- **R15**：WXML `{{ }}` 里出现方法调用即 FAIL（按 `{{ }}` 逐个扫、表达式内部跳过字符串字面量）。

**第二十二轮 · 书架/我的/图书馆/订单/阅读圈/活动 扫描**（6 页 8 态）
- 实修两处"没生效"：① 订单类型图标 JS 给 `type1`、WXSS 却是 `.order-icon-type1` → **底色从上线起从未生效**、且是"字块"（活/观/押）→ 类名写全 + 换类型图标资产（会员费 card / 活动费 ticket / 押金 wallet / 其他 receipt）；② 阅读圈社区横幅 `globe.png` 挂 40rpx 在橙底上糊成灰点 → `book.png` + 48rpx（遗留 `banner-emoji` 类名改 `banner-icon`）。
- 登记检查器缺口 **R16（动态类名/死类名）/R17（资产语义）** → `docs/09 §十二`。

**第二十三轮 · 目录改名（收尾）**
- 用户裁定：`books.id=30`（ISBN 9780439064873）改成 **《Harry Potter and the Chamber of Secrets》**（原写第一部书名，与 id35 重名 → 收藏/图书馆并列两条同名书）。
- 词数 15500 → **85000**（真实 ≈85,141，按演示库取整口径）、AR 5.2 → **6.7**（真实值）；**封面已定向重绘**（封面图印着书名，不重绘会"列表新名、封面旧名"）；`scripts/seed_demo_library.py` 同步；现场 DB 已更新（`id30`）。

---

## 三、口径与规范变更（**压缩后理解冲突高发区**）

| # | 变更 | 落地处 |
|---|---|---|
| 1 | **切换孩子只在「我的」页**（首页顶部那排已删） | `PRD §2` · `docs/04 §五 / §10.3 步 3` |
| 2 | **报告类图片**（竖版整页文档）：**整页展示**（禁方图当主展示）+ **放大出口必须走原生 `wx.previewImage`**（禁自造 overlay） | `docs/15 §十九` |
| 3 | **成就类页面**：行内信息三层（主体最大/属性正文/出处 muted）；**行内不放删除按钮**；**列表不放封面图**（用户裁定防卡顿） | `docs/15 §二十一` |
| 4 | **达成三件套**：状态词 + **达成日期** + **进度与目标**；**判定必须在 JS 里做**（WXML 表达式不支持方法调用） | `docs/15 §21.1` · R15 |
| 5 | **文案 emoji 一律清出**：图标走 `/icons/ui/*.png`；排版字形（✓ ✕ ★ ☆ ▶ 等）白名单 | `docs/15 §21.2` · R13c |
| 6 | **取不到数据 = 未知**：显示「—」+ 原因；**禁止静默保留上一个主体的值**（未入会不显示 0、不残留别人数字） | `docs/04 步 3` · 错误库 §九十七 |
| 7 | **`summary` 载荷新增 `milestone_awards[]`**（`{node, awarded_at}`）；`milestones_awarded` 语义不变 | `backend/domain/growth/service.py` |
| 8 | **演示目录改名**：ISBN 9780439064873 = 《Chamber of Secrets》（85,000 词 / AR 6.7） | `scripts/seed_demo_library.py` + 现场 DB |
| 9 | **小程序视觉自查通道** = 微信开发者工具 CLI（`scripts/wechat_ide_mcp.py` 薄封装） | `docs/15 §二十` |

---

## 四、遗留与盲区（如实）

1. **52 项未提交**（以 `git status --short` 实取为准）。按纪律不主动 commit/push；用户目视通过后再分刀（建议：报告页 / 生词本 / 成就页 / 首页切换 / 门禁与检查器 / 目录改名 / 文档，或简化为"前端一刀 + 检查器一刀 + 文档一刀"）。
2. **等用户目视的验收点**：`docs/04` 增补表 4/5/6；此外护照与勋章两态、首页切换下线、订单图标、阅读圈横幅均在真机看过但**未经用户验收**。
3. **媒体孤儿 3,052 个 / 123.3 MB**（`cleanup_uploads.py` dry-run 实测：`circle/` 1020、`cover/` 2025、`reports/` 7；DB 引用仅 79 个）——**未清理**（破坏性操作，等用户拍板；工具默认 dry-run，`--trash DIR` 才真删）。成因倾向：测试与多次重绘产生的历史文件未回收。
4. **检查器缺口 R16 / R17**（`docs/09 §十二`）——本轮订单图标那处正是 R16 该拦住的。
5. **未扫过的页面**（下一批候选）：押金、退款申请、权益转让、退会、我的预约、活动票券、活动详情、阅读圈名片/分享、quiz / quiz-result、周报月报页、会员码页、登录页。
6. **上一会话仍保留**（第十七~二十三轮的施工会话）——细节/语义有疑问可直接追问它。

---

### 四补、接手会话拷问后的六条订正与新增（2026-09-21 深夜，**必读**）

| # | 事项 | 事实（已实测核对） | 处置 |
|---|---|---|---|
| **1** | **未跟踪脚本是双 seed 的硬依赖（最高危）** | `scripts/seed_wm11_demo.py:625` **import** `scripts.gen_demo_progress_report`（`render_report`），而该文件是 **untracked**。gate 第 [9] 步只扫 `外部专家意见/ docs/ error_list/` 三个目录，**看不见 `scripts/**` 的未跟踪文件** → 若按"三刀"提交而漏掉它，任何人 clone 后 `dev.sh restart` 会在 seed 抛 `ImportError` | ① 提交时必须带上它（见 §五 分刀清单）；② **下一批把 gate 第 [9] 步的扫描面扩到 `scripts/**`**（登记 `docs/09 §十二`） |
| **2** | **媒体孤儿是"活的水龙头"，不是历史灰尘** | 实测机制：**pytest/behave 清库 → 下次 seed 重建 35 本（书是新插入的）→ `store_cover` 用 `secrets.token_hex(6)` 出新文件名**（`seed_demo_library.py:695/813`），旧文件无人回收；每次"清库+seed"循环 ≈ **+76 cover +40 circle**。今天 cover 新增 **1,167** 个；单 ISBN 变体 **57~58** 个（`uploads/cover` 共 2,062 个 / 84MB） | §四.3 原写"成因倾向：测试与多次重绘未回收"是**推测措辞，已证伪为结构性单调增长** → 方向改为**改机制**（候选：落盘前删同 ISBN 旧图 / 内容寻址文件名 / 检查器加"同 ISBN 只许留被引用那张"）；**"什么时候扫地"不是拍板点** |
| **3** | **订单图标有两枚资产从无载体** | 映射 `wallet`（押金）/`receipt`（其他）两条分支在演示数据里**从未渲染**——实测 `orders` 表只有 `observation_fee / formal_fee / activity_fee` 三类 | 下一批二选一：① seed 补一条押金类订单载体；② 按实际枚举收掉这两条死分支（登记 `docs/09 §十二`） |
| **4** | **里程碑"达成日期"每天都是今天** | `backend/domain/growth/service.py:128` 每次重建都 `MilestoneAward(awarded_at=now)`（实测 `2026-09-21 21:02:06`）→ 护照上"达成日期"永远显示演示当天，与"历史"语义不符 | 下一批照第 1 期报告的做法**回拨固定天数**（如 45 天前）——"达成日期"是给人看的历史 |
| **5** | **"真机"这个词我用得太松** | §四.2 写"均在真机看过"，而**本批全部是 devtools 模拟器**（`docs/15 §二十` 通道），**没有一台真实设备**。必须真机才能定论的：亮度常亮/会员码对比度、扫码枪连扫手感与输入法、音频播放链路（onCanplay/onEnded）、canvas 原生层首帧、真实安全区/刘海、真机字体回退 | 措辞已改；**待真机复看清单**见本节末 |
| **6** | **S16–S23（借阅台）没有走表** | 单测覆盖了判定与筛选/导出逻辑（`test_borrow_scan.py` 8 例含"别人预约锁定拦截"、`test_borrow_records.py` 3 例含导出跟随筛选），LEDGER 第十六轮记过一次端到端（扫 ISBN → borrow → 再扫 → return → 记录出现 → 导出 xlsx 200 / 5,457B）；但**手册表上的 UI 动线（S16–S19）与失败/空态分支（S21–S23 的手工版）从未逐条走** | 记成**明确欠账**（不是"已闭环"）：下一批**先走表**（详见 §五） |

**待真机复看清单（模拟器验不了的）**：会员码页亮度/对比度 · 扫码枪连扫（借阅操作台 / 活动签到）· 音频播放与完播判定 · 阅读圈卡片/报告图在真机的首帧与字体 · 各页真实安全区（刘海/底部横条）。


---

## 五、下一轮该做什么（按优先级；**2026-09-21 深夜按接手会话拷问重排**）

**裁决：先提交（分刀）→ 再走表 → 最后才做 R16。** 理由：现在 50+ 项未提交 + 三张验收表未走，任何新改动都会让"哪一刀引入了什么"更难追；而 R16 的规格本身还没定标（见下）。

### 第一步：提交（文件级分刀；**每刀都要带上第 4 刀那个未跟踪脚本**）

| 刀 | 内容 | 关键文件 |
|---|---|---|
| ① 后端 + 契约 | `summary` 增 `milestone_awards`；无迁移 | `backend/domain/growth/service.py` |
| ② 小程序（本批视觉/修复主体） | 报告页/生词本/成就页两态/首页切换/订单图标/阅读圈横幅 + session 宽松比较 | `miniapp/pages/member-pkg/observation-report/*`、`pages/member-pkg/report/*`、`pages/reading-pkg/vocabulary/*`、`pages/member-pkg/profile-card/*`、`pages/member-pkg/achievement/*`、`pages/index/index.*`、`pages/order-pkg/order-history/*`、`pages/circle/circle.*`、`pages/shelf/shelf.*`、`pages/member/member.*`、`pages/books/books.js`、`pages/reading-pkg/book-detail/*`、`pages/reading-pkg/reader/reader.wxml`、`pages/member-pkg/purchase/purchase.wxml`、`components/error-view/*`、`utils/session.js` |
| ③ 检查器 + 生成器脚本 | R13c/R15 两条新规则 + 报告图生成器 + seed 改动 | `scripts/check_miniapp_style.py`、**`scripts/gen_demo_progress_report.py`（untracked，必须 add）**、`scripts/seed_wm11_demo.py`、`scripts/seed_demo_library.py` |
| ④ 文档 | 交接 v48 / LEDGER / 错误库 §九十四~§一百零一 / docs 15·04·03·09 / PRD / CLAUDE.md §八 | — |

> `git status --porcelain` 实测 **51 M + 2 ??**（`scripts/gen_demo_progress_report.py` 与 `.zcodeignore`）；`.zcodeignore` 不入库。

### 第二步：走表（**先走表、后开发**）

1. `docs/04 §10.3` **增补表 4（S16–S23 借阅台）**：UI 动线 S16–S19 + 失败/空态 S21–S23 逐条真机过；已有单测的下方标注"逻辑已单测覆盖"，手工仅补 UI 层。
2. 增补表 5（S24–S28 报告页）/ 表 6（S29–S33 生词本）：模拟器已逐态截图，仍请用户过一遍。
3. 走表发现的缺陷：**当场记 LEDGER，别当场改**（避免又变成未提交的第 54 项）。

### 第三步：R16 之前先定标（按接手会话实测）

- 现象：按"类名在本页 wxml+js 完全不出现"的**朴素规格**跑，粗略命中 **684~698 条**（我独立实现得 698；top：`reader.wxss` 99 · `achievement.wxss` 65 · `refund-apply.wxss` 63 · `book-detail.wxss` 60）。
- 其中很大一部分是**故意的**（每页都有的"绘本风精修"通用块 `.card/.btn/.tag/...`、伪类变体、跨页工具类）→ 直接冻结基线＝把 700 条技术债钉死。
- **先做规范再做门禁**：① 在 `docs/15` 立"**类名必须整名出现**：模板里禁止用前缀拼接生成类名（如 `order-icon-{{x}}`）"；② R16 只扫**页面自有类**（排除通用块白名单与注释块）；③ 先**只报告不进退出码**跑一批，人工抽 30 条估误报率，误报率 <10% 再冻结基线进退出码。

### 其余（并行推进）

- **验收基线断言**：`check_demo_baseline.py` 现在只断言 35/3/11/4，**演示孩词数/积分/勋章这些"手册里写死的数字"不在断言范围**（本次书 30 改名导致 +7,000 词/+70 分漂移，就是靠人眼发现的）→ 加断言（与 `docs/04` 表同源），并把"改演示数据"变成**必然要同步改两处**。
- **媒体机制修复**（§四补.2）：不是"扫地"，是落盘策略 + 检查器。
- **演示数据小修**：里程碑 `awarded_at` 回拨固定天数（§四补.4）；订单图标 `wallet/receipt` 的载体缺失（§四补.3）。
- **管理端/导出 emoji 清理批**（§四补 emoji 口径边界）——先定"是否给 admin-web 加同款检查器"。
- 大屏下一批（用户已裁定"融合进仪表盘、不新增页"）。

---

## 六、必读命令与环境（照抄）

```bash
# 现场
bash scripts/dev.sh restart          # 起/重启 后端:8002 + 前端:5173 + 双 seed；**改后端代码后必须 restart 才能自测**
bash scripts/dev.sh stop             # 跑 pytest/gate 前必须（同库互踩会假红）
bash scripts/gate.sh full            # 全量 9 步（C 级）；PASS 后自动 dev.sh restart
.venv/bin/python -m scripts.check_demo_baseline     # 干净基线 35 / 3 / 11 / 4

# 机制检查（可单独跑；gate 第 [5] 步同族）
for m in check_miniapp_style check_miniapp_bindings check_media_discipline \
         check_docs_code_alignment check_fake_assertions check_rbac_consistency; do
  .venv/bin/python -m scripts.$m; done
.venv/bin/ruff check backend/ tests/ features/ scripts/ && .venv/bin/ruff format --check .

# 小程序视觉自查（唯一通道；首次需用户点 IDE 的「MCP 客户端授权」弹窗——模型点不了）
.venv/bin/python scripts/wechat_ide_mcp.py check_wechatide_status
.venv/bin/python scripts/wechat_ide_mcp.py simulator_open_page '{"page":"pages/index/index"}'
.venv/bin/python scripts/wechat_ide_mcp.py simulator_screenshot '{"path":"/tmp/a.jpg","wait":3}'
.venv/bin/python scripts/wechat_ide_mcp.py simulator_refresh        # 改 wxml/wxss/js 后必刷（否则看旧帧）
.venv/bin/python scripts/wechat_ide_mcp.py automation_evaluate '{"fnSource":"function(){return 1}"}'
#   切登录态：evaluate 里 wx.request 打 /api/miniapp/login {phone, code:'1234'} → 写 storage(token/parent/children/currentChildId)
#   量状态：wx.createSelectorQuery().selectAll('.x').fields({computedStyle:['backgroundColor','borderTopStyle']})
#   注意：simulator_refresh 后必须重新 simulator_open_page（会回到首页）
```

演示账号：管理端 `admin / dmkwords123`（超管）、`staff01 / dmkwords123`；
小程序 **13800008888 / 1234**（演示家长：演示孩 formal + 小红 none）、**13800007777 / 1234**（WM3 异常态：观察期/待评估/过期/临期/押金/退会孩）、**13800006666 / 1234**（退款/转让演示）。

---

## 七、文档一致性体检（本轮）

- **新增**：`docs/15` §十九 / §二十 / §二十一（21.1 三件套、21.2 emoji）；`docs/04` 增补表 5 / 6；`docs/09` §十二（R16/R17 登记）；错误库 §九十四~§一百。
- **改写**：`PRD §2`（切换入口）、`PRD §3.3`（报告展示口径）、`docs/03` FEAT-052 / 054 / 066、`docs/04 §五 / §10.3 步 3`。
- 检查器清单现为 **R1–R15**（R13 分 a/b/c）；`check_docs_code_alignment` 全绿。**写文档别用 `名字()` 空括号形式**（会被当"引用了不存在的函数"→ 门禁第 [5] 步红）。
- `docs/LEDGER.md` 第十七~二十三轮逐行；本版交接卡 v48（v45 已归档）。

---

## 八、本轮新增防复发红线（**41~48**，接 v47 的 38~40）

41. **状态类断言必须有机器证据**（computedStyle / 像素 / 属性），不许只凭眼看截图——本轮"六格全亮"我看走眼过一次（错误库 §九十九）。
42. **WXML `{{ }}` 只做取值与三元**；任何计算/方法调用都放 JS（R15 已机化）。
43. **异步取数失败必须落到"未知"或"不可见 + 原因"**，禁止静默保留上一个主体的值（§九十七）。
44. **文案与图标不走平台 emoji**；图标槽位一律 `/icons/ui` 资产**并给宽高**（emoji 靠字号撑大小，换位图不给宽高会渲染 0×0）。
45. **报告类图片整页展示 + 放大出口走 `wx.previewImage`**（禁自造 overlay）。
46. **改任何 py（含 `scripts/` 检查器）→ `ruff check` 与 `ruff format --check` 两条都跑**（本轮首轮门禁就栽在这，白跑 15 分钟）。
47. **演示数据改名/改数要连带核封面与快照**（封面图印着书名；阅读圈卡片是 JSON 快照）。
48. **门禁按改动面选级别，别习惯性开全量**（用户 2026-09-21 质问「该文档为什么跑门禁？」）：`CLAUDE.md §八` 本轮补了 **B′（仅 miniapp → 三个检查器 + 截图）** 与 **B″（仅 scripts → ruff + 引用它的测试）** 两行；**数据性改动（如 seed 里改演示书名）与纯文档都不进全量门禁**。改前先 `git status --short` 判面，再选动作。

---

## 九、高频翻车点速查（**宪法没写、但接手的人一定会踩**；按类分组，每条＝症状 → 规矩 → 出处）

> 写法说明：本节只收"CLAUDE.md 没覆盖、而错误库里反复出现/我在本轮亲手踩过"的条目。**每条都能在 `error_list/错误记忆库-全项目-20260831.md` 找到完整经过**（节号已注）。
> 阅读顺序建议：先看 A（施工习惯，模型自己最常犯）→ B（渲染类）→ C（数据/业务类）→ D（跟用户打交道）。
> **定位说明（回应接手会话的拷问 E2）**：本节是**故障字典**，不是"规则清单"——宪法 §〇.2 要求"做不到机械判定的规则要么删要么改写"，约束的是**规则**（"必须做 X"）；本节给的是**症状 → 去哪查**，判据是"能否被检索、能否缩短定位时间"，允许靠自觉。
> 但**能机化的必须标出来并确实机化**：A2（判改动面 → §八 分级 + 无机器强制）、A3（ruff format，本就是门禁第 [1] 步 ✓）、A6（`check_docs_code_alignment` ✓ 已机化）、B1–B4/B6（R12/R13/R15 ✓ 已机化，**R16 待落地**）、B8（M2 ✓）、C9（check_fake_assertions ✓）。**A1/A5/A7 目前无机化手段**（A7 尤其不可能），它们的价值在于"开工前读一遍，遇到症状能认出"。

### A. 施工习惯类（**模型自己最常犯，占本轮翻车的大头**）

| # | 症状 | 规矩（硬） | 出处 |
|---|---|---|---|
| A1 | 看一眼截图就宣布"改好了"——本轮我把"六格全亮"看成"已达成亮卡 ✓" | **凡是"两种状态/有无角标/选中态/0 值/数字结论"的断言，必须至少一条机器证据**：`wx.createSelectorQuery().selectAll(...).fields({computedStyle:[...]})`、像素采样、或接口原始返回。截图只用于**布局/观感**判断 | §九十九 · 红线 41 |
| A2 | 把"全量门禁"当默认动作：本轮为"seed 改一个书名 + 交接文档"开了 15 分钟全量，被用户当场质问 | 先 `git status --short` **判改动面**再选验证级别（`CLAUDE.md §八`：A/B/B′/B″/C/D）。**数据性改动与纯文档不进全量门禁** | 红线 48 · `CLAUDE.md §八` |
| A3 | 改 `scripts/` 只跑 `ruff check` 就开门禁 → 第 [1] 步 `ruff format --check` 红、白跑一轮 | **改任何 py，check 与 format 两条都跑**（约 3 秒） | §八/B″ · 红线 46 |
| A4 | 门禁在跑 pytest 时我又并发跑了一个 pytest（同库互踩）→ 25 例假失败、白跑一轮门禁 | 门禁/测试**同时只允许一个消费者**；要单跑先 `pgrep -f pytest` 确认清零 | §九十二 |
| A5 | 用内联 `python - <<'PY'` 改文件：引号嵌套/断言一崩就**静默没落盘**，我却以为改好了（本轮踩 5 次以上） | ① 复杂文本**写到 /tmp 文件再读入替换**，别在 Python 字符串里塞 `""`；② 替换后**必须 `grep`/读回验证**；③ 断言写成"命中数 == 1" | §一百零一（另见 AGENTS.md"脚本改文件后必须 grep 验证"） |
| A6 | 文档里把小程序方法写成 `名字()` 空括号 → `check_docs_code_alignment` 当"引用了不存在的函数" → 门禁第 [5] 步红（本轮踩两次） | 文档提**小程序/外部符号**时写不带括号的名字；**只有后端真实符号才允许带调用括号** | §一百零一 |
| A7 | 改了 `wxml/wxss` 不 refresh，或 refresh 后没重新 `simulator_open_page` → **看的是旧帧**（本轮探针实验"像素与改前完全一致"就是这个假象） | 自查固定流程：**改 → `simulator_refresh` → 重新 `simulator_open_page` → 等 3~5s → 截图**；`refresh` 会回首页，必须重开目标页 | docs/15 §二十 · §一百零一 |
| A8 | 改了后端不 restart → 新字段拿不到，页面显示"暂无数据"/旧值，误判为前端 bug | **改后端代码必须 `bash scripts/dev.sh restart` 才能自测** | 交接卡 v47 红线 39 |
| A9 | 遇阻就去造平行实现（本轮造过 WXML→HTML 近似渲染装置，还卡死） | **先找现成通道**（本项目小程序自查唯一通道＝devtools CLI，`docs/15 §二十`）；用户明令"不要绕弯路"；造第二套实现要用户点头 | §一百零一 |
| A10 | 顺手新增页/顺手重构/顺手改样式 | 用户对"不要新增页"有明确态度（大屏那轮打断过我）；改动只做被要求的 | 交接卡 v47 §二 |

### B. 渲染 / 小程序类

| # | 症状 | 规矩 | 出处 |
|---|---|---|---|
| B1 | "我写了样式"但不生效——`text-align:left` 在 `align-items:center` 的 flex 容器里是空话 | flex 里做文本对齐/换行/省略号，先确认子项**是否被收缩**；要撑满就 `align-self: stretch` 或 `width:100%`。**样式类改动的完成判据是截图** | §九十六 |
| B2 | emoji 换位图后**渲染成 0×0**（emoji 靠 `font-size` 撑大小，位图要宽高） | 图标槽位统一 `/icons/ui/*.png` **并显式给宽高** | §九十八 · 红线 44 |
| B3 | 类名对不上：JS 给 `'type1'`，WXSS 写 `.order-icon-type1` → **底色从上线起从未生效、且无报错** | 动态类名写全名；（此族缺机械门禁 → **R16 待补**） | §一百 · docs/09 §十二 |
| B4 | WXML `{{ }}` 里调方法（`indexOf`）→ **静默 undefined → 判定恒真**（护照六枚里程碑全显示"已达成"） | **WXML 只做取值与三元**，计算放 JS（R15 已机化） | §九十九 · 红线 42 |
| B5 | WXML 注释写成 `*/` → 整页编译失败，**报错行指向别处** | 注释必须 `-->` 收尾（R14 已机化）；页面白屏先怀疑注释 | §八十三 |
| B6 | 模板写了类名、wxss 没定义 → 选中态/角标永远不亮 | R12 已机化；**别信"设计做完了"**，以 WXML 实际类名为准 | §七十八 |
| B7 | 图标资产在浅底清楚、在橙底糊成一团（globe.png 挂 40rpx 横幅＝灰点） | 换资产要**连带看背景与语义**；换完真机看一眼 | §一百 |
| B8 | 媒体原地覆盖 → `<image>` 按 URL 缓存，**永远吃旧图** | 破缓存单出口：`?v=` 只能出自 `backend/common/file_utils.py`（M2）；重绘一律**换文件名** | §六十二 · §七十二 |
| B9 | 低分辨率/缩放截图导致"看不清/颜色不对"的误判 | 自查截图用**原始分辨率**（不 optimize）+ 需要时裁剪放大看；避免以缩略图下结论 | §七十 |
| B10 | 回页刷新把整页卸载 → 滚动位置被打回顶部（用户实测过） | 列表页刷新要保位置；`Tabs` 切换的面板要 `active` 触发重拉（否则数据是旧的） | §八十四 · docs/04 借阅台反馈 |
| B11 | 同一交互两套实现（两处写扫码框/两处写预览壳） | 抽公共件（`ScanInput` / `PreviewImage` 先例）；发现第二套就地合并 | §七十六 |

### C. 数据 / 后端 / 业务类

| # | 症状 | 规矩 | 出处 |
|---|---|---|---|
| C1 | 同一指标两处公式（卡面"可借"与借书守卫各写一套）→ 数字对不上 | **单一事实源**：展示"能不能"必须调用执行端同一个判定函数（`borrow_gate.py` 先例） | §八十六 · §九十三 |
| C2 | 切主体后旧主体的数字留在屏幕上（静默 catch + 不清场 + 严格比较） | **换主体先清场**；取不到就落"未知"（渲染「—」）+ 原因；**禁止静默保留**；跨端 id 一律宽松比较 | §九十七 · 红线 43 |
| C3 | 状态机"出口"漏收口（借出成功后预约不释放、副本被鬼锁） | **每个状态的结束条件必须在所有路径上收口**；验收时专门找"出口" | §九十一 |
| C4 | 额度 ≠ 资格（未入会孩子卡片写"可借 30 本"，实际借不出） | 界面撒谎比后端报错更糟；**不能借要说清原因**（硬拦截 / 可放行分开表达） | §九十三 |
| C5 | 验收路径没有载体（"家长能看到报告"但库里一张报告图都没有） | **新验收点必须 seed 造数**（真实链路造 + 幂等 + 顺序排在建序之后） | T47-2 教训 · §九十 |
| C6 | seed 两段独立取数撞同一资源 → 造出"既在借又预约"的鬼状态 | seed 跨段取数要**互相排除**（不取已在借的书/有活预约的书/副本必须 available） | §九十 |
| C7 | 改演示数据只改一处（改名不重绘封面 → 列表新名、封面旧名） | 改名/改数要**连带核封面、卡片快照、下游引用**（`grep` 一遍） | 红线 47 |
| C8 | 清理脚本引用集不全 → 删掉仍在用的文件（uploads 血案） | 清理类脚本**默认 dry-run + 正向 apply**；引用集必须覆盖所有消费列（含软删行） | §七十五 · M3 |
| C9 | 测试里写死"明天"到期日 → 时间炸弹（换天必红） | 测试只用相对时间或远期哨兵（check_fake_assertions 已机化） | §八十二 |
| C10 | 业务域 import admin 模型 → 架构关当场拦下 | 只允许**共用 common 层**（如 `common/system_models.AuditLog`）；`miniapp_router` 也在 A-1 门禁范围内 | §八十七 |
| C11 | 出图字体按"想要的样式"选 → 中文直出豆腐块 | 字体按**内容**选（`art.font_round` 无中文字形）；坐标按实测宽度排，不目测写死 | §九十五 · §七十七 |

### D. 与用户协作类（**用户极其在意，违反代价最高**）

| # | 症状 | 规矩 | 出处 |
|---|---|---|---|
| D1 | 报"已修复"但没验证 / 弱化失败 | **诚实第一**：失败如实报原文；未验证就明说"未验证"；禁止弱化断言/删测绕过 | AGENTS.md 铁律 |
| D2 | 用户报障我凭猜改 | **先复现 + 取证再改**：本轮"切换孩子不刷新"是用 API 探针拿到 `422 入会后可查看` 才定位到"静默 catch"的 | §一百零一 · §九十七 |
| D3 | 用户说"看不清/太丑"我只做形容词式修改 | **先把根因量化**（814px 宽报告缩到 100pt＝3pt 字号 ≡ 不可读 → 整页展示后 10.3pt），再动手 | §九十四 |
| D4 | 破坏性动作（删文件/清媒体/改数据）先斩后奏 | **先说明影响、等确认**；清理默认 dry-run；能回滚的先做成可回滚（`--trash`） | AGENTS.md 铁律 · §七十五 |
| D5 | 自创口径（"期次"、未入会显示 0、"差不多了"） | 口径要么来自文档、要么**当场向用户拍板**；展示口径变更**要写进 docs/15 与 docs/04**（文档先行是宪法 §六 前置铁律） | 宪法 §六 · docs/15 §二十一 |
| D6 | 交了功能但没给验收路径 | 交付＝**后端 + 前端 + 门禁/相称验证 + 手册步骤（docs/04）+ LEDGER 回写**；用户手动验收通过才算交付 | 宪法 §六 DoD |

### 接手第一小时建议动作（照做即可）

1. `cd` 到项目 → `git status --short`（**先看有多少未提交**）→ `git log --oneline -3`；
2. 读本区块 **§一 → §二 → §三**（状态/做过什么/口径变更），再扫一眼 **§九 本节**；
3. `bash scripts/dev.sh restart` 起现场 → `.venv/bin/python -m scripts.check_demo_baseline`（应得 35/3/11/4）；
4. 抽查两处真机视图（`docs/15 §二十` 的 devtools 命令）确认自查通道可用（**首次要用户点 IDE 的「MCP 客户端授权」**）；
5. 跟用户确认**"先验收提交，还是先继续下一批"**（§五 第 1 条）。

---

# DmkWords 项目交接 - v47 交接书（2026-09-21 第十五~十七轮：借阅台扫码闭环 + 两项口径修复 + 视觉统一 + 仪表盘六图）—— **已归档**（读上方 v48；⚠️ v47 之后第十七~二十三轮**未提交**，别照 v47 的「现场干净」判断工作区）

> 🔴 **压缩后第一件事：读本区块全部八节，自包含（含 §三 口径变更表、§七 一致性体检、§八 新增红线）。**
> **本轮产物 = 两份施工图纸**：
> ① `docs/任务包-20260921-借阅操作台扫码闭环（会员码+连扫+借还记录）.md`（需求原文/五项拍板/现状核查/**四轮目视反馈记录**/S16–S23）；
> ② `docs/任务包-20260921-店外大屏可视化（中央仪表盘·电视轮播）.md`（**已被用户当场改口径：不新增页、融合进仪表盘**；含六图已实现与下一批候选）。
> 明细都在任务包里，本区块只记"现在是什么状态、下一步做什么、哪里容易理解错"。
> **保留窗口（v48 起）**：本文件只留 **v48 / v47 / v46** 三版；**v45 及更早已并入
> `docs/legacy-attic/项目交接-归档-v41及更早-20260920.md`**（该文件头部有文件名沿革说明）。
> **专家侧恢复点**：`docs/专家交接书-20260920-在任专家会话交接.md`。
> **继承不重复**：v42 §一 长期目标 / §五 红线 1~23 / §六 环境命令 / §九 文档读法 + v43 §五 24~28 +
> v44 §六 29~33 + v45 §五 34~35 + v46 §八 36~37 **全部有效**。

> 🆕 **2026-09-21 压缩前追加（第十七轮，最新批次）**：小程序**评估报告页重排**——用户报障「评估报告和评语展示太丑了……
> 现在看也看不清，页面也很简陋」，并给了线下外教进度报告样例（竖版 1:1.40）。已落地：**竖版整页展示**（满卡宽
> `widthFix`，实测 402×563pt/页）+ **放大出口改原生 `wx.previewImage`**（双指放大 / 左右翻页 / 长按保存，删掉自造
> overlay）+ **老师评语卡**（排在报告图之前）+ **最新一期展开 / 往期折叠**；seed 新增 `scripts/gen_demo_progress_report.py`
> 与 3 期演示报告（观察期孩 2 期 / 待评估孩 1 期 / 押金孩 0 期＝空态载体）；周报月报页补同款放大出口。
> 规范落 `docs/15` **§十九 报告类图片展示规范** + **§二十 小程序视觉自查通道**（devtools CLI 真实用法；旧
> `wechat_ide_mcp.py` 已按新 CLI 重写，别再当它坏着）。
> **状态：未提交、等用户目视**（验收 = `docs/04 §10.3 增补表 5` S24–S28；四个验收态已在真机模拟器逐态截图自查）。**门禁已过**：`gate-runs/2026-09-21/gate-170920.log`（513 passed / 84.87% / 9 步 exit 0），门禁后三项核验 后端 200 · 前端 200 · 基线 35/3/11/4 PASS。
> 🆕 **同日追加（第十八轮）**：小程序**生词本改造成就页**（用户裁定「分不清哪个是我查过的词 / 颜色和书名一样 / 没有分行 / 字体不大 / 要有统计次数 / 点词弹闪卡」）：成就头卡 + 四指标（含**本周新增**）+ 三层信息行（词 40rpx 等宽主色 → 音标释义 → 出处书签小片 + 日期）+ **行内删除取消**（长按操作表 / 闪卡内移除）+ 闪卡升级。**列表不放封面图**（用户裁定：图片多了会卡顿；后端为此加的 `cover_url` 已回退）。规范：`docs/15 §二十一 成就类页面呈现规范`；验收：`docs/04 §10.3 增补表 6（S29–S33）`。**未提交、等目视**。
> 🆕 **同日追加（第十九轮）**：① **首页切换孩子的入口已删**（用户裁定：只在「我的」页切换；PRD §2 已改口径）；② 修**切换后数据串台**：首页「累计词数 / 积分」曾挂着上一个孩子的数字——根因是 未入会 422 + **静默 catch 保留旧值** + `getCurrentChild` 严格比较（dataset id 类型不定）；现在换孩子**先清场再加载**、取不到就显示「—」+ 原因，跨端 id 比较改宽松（`miniapp/utils/session.js` + `pages/index/index.js`）。错误库 **§九十七**；门禁 `gate-runs/2026-09-21/gate-184549-step5-docs-token.log`（第 5 步文档 token 红 → 只改文档后整组复跑全绿，代码零改动）。**未提交、等目视**。
> 🆕 **同日追加（第二十轮）**：成就页同族扫（**阅读护照 / 等级勋章**）——里程碑换自家六色奖牌资产 + **已达成/未达成两态**、等级改盾牌资产、统计数字走等宽；并**新立 R13c 门禁**（`miniapp/**/*.{wxml,js}` 全量文案禁止 emoji，注释与排版字形除外）后**全站 31 处 UI emoji 清零**（含共享组件 error-view 的 😔📡📭🔒）。规范：`docs/15 §21.1/§21.2`；错误库 §九十八。**未提交、等目视**；下一批拟做 **书架/收藏夹** 与 **我的页宫格** 的同族扫。
> 🆕 **同日追加（第二十一轮，用户一问挖出真 bug）**：用户问「阅读护照，10 万词成就怎么表现出来？」→ 发现
> **WXML `{{ }}` 不支持方法调用**，`milestones_awarded.indexOf(item) !== -1` **恒真** → 六枚里程碑一直全显示「已达成」
> （老版全奖杯图标、我这轮改成六格全亮卡）。已修：判定搬进页面 JS + **达成三件套**（状态词 / 达成日期（后端 summary 新增
> `milestone_awards`，零迁移）/ 「里程碑 1/6 已解锁 + 下一枚 50万词·还差 39.8万词」）+ 新门禁 **R15**（WXML 表达式禁方法调用）。
> **教训**：上一轮我只看截图就写「已达成亮卡 ✓」进台账，其实是六格全亮——**状态类断言必须有机器证据（computedStyle/像素）**，
> 见错误库 §九十九。**未提交、等目视**。

> **下文 §一 表格里"现场干净/工作区干净"是本轮追加之前拍的快照——以 `git status` 与 `docs/LEDGER.md` 第十七轮行为准。**

---

## 一、当前状态（一句话 + 证据）

**借阅台扫码闭环（会员码 / 扫码自动判借还 / 借还记录）已交付并**经用户明示验收通过**；两项口径 bug 修复；
管理端视觉统一（标题框/留白/字体）；仪表盘图形区扩到 6 图。全部已推，现场干净。**

| 项 | 事实 |
|---|---|
| HEAD / 远端 | **本地 = `origin/main`、零未推**（当前 HEAD 用 `git log --oneline -1` 取——**按红线 36 不写死 hash**）。本轮 8 刀：`90a8cb8` 文档全库同步与文档先行入宪 → `b520c48` 后端（扫码闭环 A–D + 资格单一来源 + 额度口径修复）→ `008a3c1` 前端（会员码页/焦点链/记录 tab/标题字体）→ `234d739` 推送状态回写 → `30bbf9f` 仪表盘数字改等宽 + 大屏任务包 → `3a166c2` 仪表盘图形区首版 → `bb65a79` 图形区扩到 6 图 + 聚合端点 |
| 验收 tag | **`acceptance-borrow-console`**（→`008a3c1`）· **`acceptance-admin-visual`**（→`30bbf9f`）——本轮新增，已与 push 同批 |
| 最近 CI | `bb65a79` 的 CI 在推完后刚触发（`gh run list -1` 自取）；**本文件之后的纯文档刀按 D 级不追 CI** |
| 最近全量 gate | `gate-runs/2026-09-21/gate-151722.log`：**513 passed / 84% / BDD 8f·30s·103steps / 9 步 exit 0**（前一轮 `gate-140222.log` 511 passed；`gate-131140.log` 507 passed） |
| 现场 | 后端 :8002 + 管理端 :5173 均 200 · **干净基线 35 / 3 / 11 / 活动 4**（`check_demo_baseline` PASS）· 演示孩 `在借 2（其中逾期 1）/ 可借 28` |
| 迁移 | 本轮新增两个：`b4d8e2f6a1c5`（`children.member_code`，含存量回填）· `c7e1f5a9b3d2`（`borrow_records.returned_by`，**不回填历史**） |
| 工作区 | 拍摄时为干净（除工具生成的未跟踪 `.zcodeignore`，不入库）——⚠️ **2026-09-21 第十七轮追加后已不干净**，以 `git status` 为准（见上方 🆕 注） |

---

## 二、本轮做了什么 —— **别重复做**

1. **文档先行入宪（用户强制令）**：`CLAUDE.md` 升 **v1.5.2** —— §六 新增前置铁律「文档先行」（含可机械检查的判据：本批 diff 有代码改动必须伴随文档改动）；
   §二 借阅并发红线修订；`AGENTS.md` 同步（含 D 级：纯文档不跑门禁、不单独推、不等 CI）。
2. **借阅台扫码闭环 A–D 批**（任务包）：A 会员码（后端不可枚举码 `M`+8 位随机+校验位、列默认值兜底、迁移回填）→
   B 小程序出示页（二维码/条形码/文本码 + 亮度常亮 + 底部关闭按钮）→
   C 扫码统一判定 `POST /circulation/scan`（**判定顺序即口径**：会员码 → 本孩子在借→还书 → 自己的预约→核销借出 → 在馆→借出 → 否则按原因拦）
   + 管理端 `ScanInput` 通用件（**ScanCheckin 一并改用**，不留第二份实现）+ 焦点链 →
   D 借还记录（`returned_by` 加列 + `GET /circulation/records` + 按筛选导出 xlsx + 操作台第二 tab）。
3. **两项口径修复**：① 「逾期扣 2 本」→ 卡面与服务端统一 `上限 − 在借含逾期 − 预约`，并摘除小程序预约端逾期硬拦（全端统一不拦截）；
   ② 「未入会显示可借 30 本」→ 抽出 `borrow_gate.py` 作**借书资格单一来源**（借书守卫与卡面 `borrow_block` 同判定）。
4. **管理端视觉统一**：`PageTitle` 组件（暖色绘本版标题框，铺 9 页，**只给页面名用**）+ 留白统一（Title `margin:0`，实测 24–27px）
   + `--font-mono` 令牌（英文/数码改等宽）+ 扫码框双色区分。
5. **仪表盘图形区**：6 图（馆藏甜甜圈 / 今日流量双柱 / 健康度三环 / 近 14 天借还趋势 / 热门书 TOP5 / 小读者构成）
   + 三列等宽网格 + 新聚合端点 `/api/admin/dashboard/charts`（自轮询 60s，投屏场景）。
6. **四轮用户目视反馈共 13 条全部当日修复**（明细在任务包 §八；含焦点交棒、seed 鬼状态、预约不释放、在借表无书名等真 bug）。
7. **跨域纪律实战**：借还记录要显示操作人姓名，首版 `import AdminUser` 被架构关当场拦下（宪法 §三 铁律）→ 改读横切面审计表 `AuditLog`。

---

## 三、口径变更（**新接手的理解冲突高发区，务必看**）

| 项 | 旧口径 | **现行口径** | 同步位置 |
|---|---|---|---|
| 逾期处理 | 每逾期 1 本上限减 1 + 小程序预约/续借硬拦 | **不拦截、不额外扣减**；逾期书只占它自己 1 个在借名额 | `PRD §5.4`+§5.6+速查表 · `CLAUDE.md §二` · `docs/03` FEAT-034/036 · `docs/04` WM5 · `features/borrowing.feature` |
| 可借额度公式 | 卡面 `上限 − 逾期 − 在借`（在借已含逾期 → 每本逾期扣两次） | **`上限 − 在借总数(含逾期) − 预约占用`**（卡面与服务端同源） | `backend/domain/circulation/service.py` · 错误库 §八十六 |
| 借书资格展示 | 卡面只显示额度（未入会也写"可借 30"） | 卡面新增 `borrow_block`/`borrow_block_hard`（与 `borrow_gate.py` 同判定；硬拦截 vs 可放行分开） | `borrow_gate.py` · `docs/15`? 见 §七 · 错误库 §九十三 |
| 页面标题 | 各页各写内联样式，多数带 AntD 默认 `margin-top 1.2em` | `PageTitle` 组件（**只给 2–6 字页面名**）；内容类标题（如书名）用普通 Title | `docs/15 §17` |
| 字体 | 标题一律绘本字（ZCOOL KuaiLe） | **中文标题 display / 英文与数码 mono / 正文 body** | `docs/15 §17.1` · `--font-mono` 令牌 |
| 仪表盘图形区 | 只有数字卡片 | **6 图 + 三列等宽网格**；0 值不画弧/不画柱身（数字诚实） | `docs/15 §十八` · `docs/18` 新端点 |

> **台账写法**：历史行是**当时快照**，本轮口径变更**只改现行口径的文档**；历史行一律不回改（改了就是毁证据）。

---

## 四、遗留与盲区（如实标注）

1. **未做（等用户挑）**：大屏下一批候选——**热门书升级成封面墙**、30 天趋势 + 实时 ticker「刚刚 XX 借走了《》」、
   阅读里程环、小读者星光榜、成长塔（A–Z 分布）、活动预告、馆藏上新；以及「**一键全屏大屏模式**」（隐藏侧边栏与顶栏）。
2. **`docs/04` 增补表 4（S16–S23）尚未逐条目视**：本轮是随功能边开发边看，用户还没按表走一遍（走表属下一轮）。
3. **小程序会员码页只有静态门禁 + 我的浏览器截图（管理端）**：小程序端**无真机/模拟器取证通道**，视觉必须用户目视。
4. **七页本来没有页面大标题**（线下活动/成长与测验/预约管理/退款中心/通知中心/任务看板 顶部直接是搜索行；阅读圈用卡片标题）——
   未擅自新增标题（属新增设计），等用户定。
5. **`docs/09` 里两条未决**（往期"参与人数"聚合口径、活动结束"回顾已上线"通知）仍未拍板。

---

## 五、下一轮（压缩后）该做什么

1. **先看 `docs/04 §十 增补表 4（S16–S23）`**：借阅台扫码闭环的完整验收路径（焦点链 / 扫第二次＝还书 / 别人预约锁定 / 记录与导出）。
2. **大屏下一批**（用户已表态要"很酷炫"、要投店外电视）：建议先做 **热门书封面墙 + 实时 ticker**（视觉冲击最大，任务包 §四有数据来源与形态）；
   「全屏大屏模式」可一并做（一键隐藏导航/顶栏）。
3. **74 步走表**（`docs/04 §十`，WM7+WM8 17 步先）——用户开测后分批走。
4. **长期目标未变**（用户 2026-09-15 原话）：主动视觉巡检、主动找观感问题，别等用户报；**"数字诚实"是新增的观感红线**（0 值不许画成"有一点"）。
5. **两处待用户定**：仪表盘是否要深色底（室外强光对比度）、趋势图时间窗与刷新频率。

---

## 六、必读命令与环境（照抄）

```bash
# 现场
bash scripts/dev.sh restart            # 起/重启（含双 seed）；stop 停
python -m scripts.check_demo_baseline  # 清场 + 重启 + 计数 + 比对基线（35/3/11/4）
# 门禁
bash scripts/gate.sh full              # 9 步；PASS 自动 restart + 双 seed
# 契约（改接口必须按序）
python scripts/export_openapi.py       # ① 先更新快照（contract-change: 前缀）
cd admin-web && pnpm gen:api           # ② 再由快照生成前端类型（gate[8] 会拦漏做）
# 本轮相关的检查
python scripts/check_miniapp_style.py      # R1–R14
python scripts/check_media_discipline.py   # M1/M2/M3
python scripts/check_docs_code_alignment.py
```

> ⚠️ **两个施工纪律（本轮踩过）**：① **改后端必须 `dev.sh restart` 才能自测**——前端热更新会骗人（我加完端点没重启，三张新图一直"暂无数据"）；
> ② **门禁/测试期间同一个库只允许一个消费方**——门禁跑 pytest 时我又并发跑 pytest，互踩出 25 例假失败（错误库 §九十二）。

---

## 七、本轮文档一致性体检（用户明令"禁止压缩后理解冲突"）

**已核对并确认一致**：`CLAUDE.md`（v1.5.2：§六 文档先行、§二 借阅红线）· `AGENTS.md`（文档先行 + D 级门禁）·
`PRD`（§5.4 现行口径 + 历史口径保留、§9.1.1/§9.2.2/§9.2.3）· `docs/01`（阶段表补借阅台交付行）·
`docs/02`（登记 borrow_gate/scan_service/records_service/member_code 四个新模块）· `docs/03`（FEAT-031/032 增补 + **新增 FEAT-089** + 总数 88→89）·
`docs/04`（WM5 步 1/3/4/7 数字统一 + §10.3 增补表 3/4：S11–S15 与 S16–S23）· `docs/07`（新增 B16/B17/F11 三条施工模式）·
`docs/08`（TD-14~17）· `docs/09`（C11 翻牌 DONE + §十一 本轮留痕）· `docs/15`（§17 标题组件 + §17.1 字体分工 + §十八 图形区）·
`docs/18`（`/circulation/scan`、`/circulation/records`、`/records/export`、`/children/by-code`、`/dashboard/charts` 五个新端点）·
`docs/19`（基线 4）· `docs/20`（对拍五表 + gate 数字）· `README`（89 项 + 门禁步描述）· `LEDGER`（第十四~十六轮 + 六图版）·
错误库（§八十六~§九十三）· 两份任务包（含四轮反馈记录）。

**体检方法**：`check_docs_code_alignment`（引用必须落到真实文件/接口/配置键）+ 对"逾期口径 / 额度公式 / 资格展示 / 字体分工 / 标题用法 / 图形区"六个口径族逐文件 grep 比对 +
浏览器实测核对（标题间距 24–27px、书本详情书名等宽、仪表盘 6 图有数据）。

**遗留（如实标注，无冲突）**：`docs/01`/`docs/02` 未逐字展开本轮前端视觉细节（它们只到"阶段/模块"粒度，不改不影响正确性）；七页无大标题的页面待用户定（见 §四.4）。

---

## 八、本轮新增防复发红线（**38~40**，接 v46 的 37 条）

- **38（文档先行——宪法 §六 前置铁律，2026-09-21 甲方强制令）**：功能/缺陷/口径变更**先改文档再动代码**；
  判据 = 本批 `git diff --name-only` 出现代码文件就必须同时出现文档改动（纯样式/文案微调除外且须在简报声明）；
  口径类变更必须先改 PRD 并注明修订日期，历史口径按"增量增补"保留。
- **39（改后端必须 restart 才能自测）**：前端 Vite 热更新会骗人——新增/修改后端端点后不 `dev.sh restart`，
  前端只会静默走 catch（本轮三张新图显示"暂无数据"，我误以为聚合写错，实际是旧进程）。
- **40（数字诚实：0 值不许画成"有一点"）**：圆头线帽在弧长为 0 时仍留色点、柱图"最小高度"会留色块、
  进度条给 1% 兜底——这些在仪表盘/大屏上都会被读成"有数据"。规则：**0 就是 0**（不画弧、不画柱身），
  并且"能不能"的展示结论必须调用执行端同一个判定函数（与 v46 红线 38 的单一事实源同族）。


---
