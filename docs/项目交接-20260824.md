# DmkWords 项目交接 - v12（2026-08-29，WM11 全部落库 + CI 绿 + tag 重打至最新）

> 恢复口令：读本文件 → CLAUDE.md → docs/LEDGER.md。**WM11 已全部落库并推送远程：
> feat `1051840`（65 项）→ docs `c2ef25a`（7 项）→ fix(ci) `0a281cf`（CI 绿 run 33264078325），
> tag `acceptance-baseline-wm3-wm11` 锚定最新。工作区干净，直接开新批次。**

## 0. 一句话状态

WM11 通知任务看板**验收主体全部通过**（管理端七步 + miniapp 消息中心 + 自动调度取证闭环），
验收期缺陷 C37-C52 全部修复，**代码+文档已全部 commit+push，CI 绿，tag 已锚定**；
**唯一遗留：miniapp 查词 UI 复测未通过**（接口层已修好，
用户重登后复测；若 UI 仍无显示用 `scripts/wechat_ide_mcp.py` 读 reader 页 data 定位）。
**下一主任务：小程序美化**（用户原话"丑到爆，排版布局一塌糊涂，交互完全没有"）。

## 1. 关键身份

- 仓库 github.com/leechunwoo0815/dmkwords (main)；HEAD `0a281cf`（fix(ci)，CI 绿 run 33264078325）；
  WM11 三刀：feat `1051840` + docs `c2ef25a` + fix(ci) `0a281cf`；tag `acceptance-baseline-wm3-wm11` 在此
- 宪法 v1.2（用户验收前禁止 commit/push）；台账 docs/LEDGER.md（WM11=GATE_PASS，验收主体已过）
- dev 运行中 :8002/:5173；**SCHEDULER_ENABLED=true（调度已开，自动调度取证闭环）**
- 测试账号：admin/staff01 dmkwords123；miniapp 演示家长 13800008888/1234（演示孩"Demo" id=1）
- **演示数据一键恢复**：`python -m scripts.seed_wm11_demo`（家长/孩子/押金 paid/在借/6 本上架书带 TTS 音频/19 条四态通知/任务记录，全幂等）

## 2. 本轮（WM11 验收期）完成的修复——已随 feat `1051840` 落库

| 编号 | 内容 | 文件 |
|---|---|---|
| F1-F4 | 未读 Tab 行消失+计数服务端口径/详情删 #ID/手动触发审计 task.manual_run/运行记录中文 | Notifications.tsx、TaskBoard.tsx、admin/service.py、tasks/registry.py、admin/router.py |
| F5 | SCHEDULER_ENABLED 配置开关（验收关调度防抢跑；**现已改回 true**） | config.py、main.py、.env |
| C41/C42 | Tab 计数口径 all_count + 「已读」Tab | admin/service.py、router.py、Notifications.tsx、admin.ts |
| C43/C44 | 场景筛选「全部场景」首项；已读/未读按钮橙/灰配色 | Notifications.tsx |
| C45 | miniapp 消息中心入口提为家长级（原被 currentChild 条件劫持）+ seed 演示孩 | member.wxml、seed_wm11_demo.py |
| C46 | miniapp 单条展开即标已读（原只有全部已读） | messages.js |
| **C47** | **quiz.wxml `'ABCD'[index]` WXML 不支持字符串下标 → reading-pkg 分包编译失败 → 整分包导航挂死**。改数组字面量。血泪：这类错工具 GUI Console 才报，pytest 全绿 | quiz.wxml |
| C48 | 侧边栏选中橙底白字/hover 黄胶囊/不压竖线 | paint.css |
| C49 | 图书馆/详情/书架封面区（cover_url 有则真图无则 📕）+ 序列化补 cover_url | books/shelf/book-detail wxml+wxss、reading/service.py |
| C50 | 查词 500：删除后再查撞唯一索引（软删行挡 INSERT）→ 复活语义 + 回归测试 | reading/service.py、test_wm8_features.py |
| C52 | 书架进详情 422（book_id/id 字段名不一致）+ reader 三处 book.id 依赖统一 bookId | book-detail.js、reader.js |
| C5 | **ECDICT 全量词库 3,402,873 条入库**（librio ecdict.db 已删，源文件 data/ecdict.db 已 gitignore）| scripts/import_ecdict.py |
| C48+ | seed 全家桶扩容：19 条四态通知/演示孩押金 paid/在借/6 本上架书（TTS 语音 say+lame 生成 7-11s）| seed_wm11_demo.py |
| 基建 | **pytest 不再清词库**（conftest 排除 dictionary_words + 404 用例词换 zzqqxxpp）| tests/unit/conftest.py |

## 3. 遗留问题（按优先级）

1. **查词 UI 复测**（用户今天最后一测仍"点查没反应"）：接口层已三重验证（curl hello 200+复活语义 ✓、
   MCP evaluate onDictSearch 曾通）。**但注意用户模拟器当时 token 是 pytest 清库前的旧 JWT（parent_id=1
   已不存在）→ 必须退出重登**。重登后仍无显示 → 用 `python scripts/wechat_ide_mcp.py` evaluate 读
   reader 页 `p.data.dictResult/dictError/showDict` 定位渲染层。
2. **播放音频**：6 本演示书是 TTS 占位音频（7-11s），用户会自己传真实音频，播放不再测。
3. **小程序美化**（新会话主任务）：规范 docs/15 的 miniapp 部分从未实施；用户对排版/布局/交互全面不满。
   注意 reader/reservation 等页有 iOS `new Date("yyyy-MM-dd HH:mm:ss")` 兼容警告（console 可见）。
4. **看板七字段抽查**未做（用户侧）。commit+push+tag **已完成**（feat/docs/fix(ci) 三刀 +
   tag `acceptance-baseline-wm3-wm11` 重打至最新 `0a281cf`，含 CI 修复）。
   覆盖率偿债提案仍挂（79.82% → 卡 77 + billing/circulation 分域，一页即可）。
5. WM12 前补课（docs/09 L148）：逾期标记/活动结束/99元边界专项；borrow-history 页残留。

## 4. 工具链（重大成果，必读）

- **官方通道**：`python scripts/wechat_ide_mcp.py <工具> '<JSON>'`——`wechatide mcp`（Nightly 2.02 唯一
  可靠通道，PATH 自带）。工具集：automation_navigate/runtime_info/evaluate/page_action/element_action、
  compile_wxml/wxss、get_simulator_console（grep）、get_simulator_network、project_*。
  首次连接 IDE 弹窗点允许。**禁止再用 computer-use 模拟点击操作 devtools**。
- `wechat-automation/helper.mjs` 已失效（Nightly 废弃 cli auto --auto-port 注入）；weixin-devtools-mcp
  (npx) 依赖已存在端口，可作二轨备用。
- **pytest 清库恢复流程**：跑完 pytest → `python -m scripts.seed_wm11_demo` + （如词库被旧版清过）
  `python -m scripts.import_ecdict` + **模拟器退出重登**（parent_id 会变，旧 token 全失效）。
- conftest 已排除 dictionary_words（340 万词不再被清）。

## 5. 环境与纪律（不变项）

- 跑 pytest/gate 前必须 `bash scripts/dev.sh stop`；改完跑分级验证（A:tsc B:域 pytest C:全量 gate）
- 管理端 localhost:5173 admin/dmkwords123；后端 :8002；MySQL OrbStack dmkwords-mysql
- 提交纪律：本地验证 OK + 用户明确"可以提交"才 commit/push
- error_list/错误记忆库-20260829.md：**验收期新坑已补录 E-07~E-12**
  （①WXML 字符串下标→分包编译连坐 ②唯一索引缺 is_deleted→复活语义 ③pytest 清库三层涟漪+恢复三步
  ④.env 目录态残留 ⑤devtools 自动化唯官方 wechatide mcp 可用 ⑥text() 包裹）。

## 6. 下一个会话开工顺序

1. 读本文件 + CLAUDE.md + LEDGER → `git status` 确认工作区干净（HEAD 0a281cf 起）
2. **启动小程序美化**：先出美化方案（对照 docs/15 miniapp 章节 + admin-web 绘本风成品）→
   用户确认后分页改造（建议顺序：首页/图书馆/听书页/我的）；
   顺带修 reader/reservation 的 iOS `new Date("yyyy-MM-dd HH:mm:ss")` 兼容警告
3. 用户重登后陪测查词（scripts/wechat_ide_mcp.py 定位）→ 闭环后 C 编号续
4. 收尾：覆盖率偿债提案（等用户放行）
5. **新纪律**：新增 scripts/ 工具脚本必须跑 `ruff check scripts/` + `ruff format` +
   `python -m scripts.verify_architecture`（CI 曾因 wechat_ide_mcp/import_ecdict 三处红，
   修复见 0a281cf；错误记忆库 E-09/20260829 恢复三步同样适用）

---
交接完毕。
