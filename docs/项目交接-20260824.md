# DmkWords 项目交接 - v9（2026-08-29，WM11 开发完成待验收 · 更换开发模型前）

> 恢复口令：读本文件，继续。
> 上一版本：v8（2026-08-24，WM2 ACCEPTED）。本版新增：WM11 通知任务看板全量开发
> （含四轮外部审查 P0+P1+必修 bug 修复 + UX 返工），**全部代码仍在工作区未提交**。

## 0. 一句话状态

- WM1、WM2 已 ACCEPTED（tag `acceptance-baseline-wm3-wm10` 固定于 ed60377）。
- **WM11 通知任务看板已开发完成**：GATE_PASS（216 passed / 79.51%）+ 四轮外部审查
  （P0 并发隔离 / P1 补测 / 必修 bug unread SQL 下沉 / UX 返工 N1-N5）全部通过，
  **等用户手动验收**（专家已放行，验收动线见 docs/04 WM11 7 步 + 专家 8 步）。
- **关键：WM11 全部 57 项改动在工作区，尚未 commit**（宪法：用户验收 OK 前不提交）。
  HEAD 仍为 `9814463`（WM2 时代 docs）。新模型接手第一步：确认工作区改动完好。
- dev 环境运行中（:8002 / :5173）；演示数据已就绪（`python -m scripts.seed_wm11_demo` 可重建）。

## 1. 项目身份与基线

- 仓库：github.com/leechunwoo0815/dmkwords (main)；宪法 CLAUDE.md；台账 docs/LEDGER.md
- 门禁：`bash scripts/gate.sh full`（C 级）；跑 pytest 前必须 `bash scripts/dev.sh stop`
- 最新门禁证据：`gate-runs/2026-08-29/gate-wm11-ux.log`（216 passed / 79.51% / exit 0）
- 覆盖率 25% 线不动（决议 docs/09 §八：WM3 批次分域实测 → WM11 前偿债窗口已过，
  抬线提案未做——**新模型注意：偿债窗口已过期，若继续必须走提案制**）

## 2. WM11 交付内容（全部在工作区）

### 通知中心（PRD §十）
- 3 张横切表：`notifications`（站内消息+微信状态）、`task_run_logs`、`dead_letters`（D6）
  ——模型在 `backend/common/notification_models.py`，迁移 `f7a2c9e3b5d4_wm11_notification_tables.py`
- `NotificationService`（纯写，INSERT IGNORE 幂等）在 `backend/common/notifications.py`
- 事件→通知订阅器在 `backend/tasks/notify_handlers.py`（10 事件）
- 操作点直发通知：退款/退会/转让/活动/报告/评估上传（含 `wm_notify.py` helper）

### 12 项定时任务（APScheduler 进程内，`backend/tasks/registry.py`）
member_expire_check（D1 第 3 层）/ member_expire_remind / pending_evaluation_weekly /
reservation_expire_check / reservation_expire_remind / book_due_remind / overdue_mark /
order_timeout_cancel / transfer_expire_check / activity_remind / activity_auto_finish /
first_activity_90d_remind

### 看板/导出/前端
- 数据看板 +7 字段（D5）；Excel 导出 3 个（C18 审计/看板/通知）
- admin 新页面：通知中心（Notifications.tsx）+ 任务看板（TaskBoard.tsx）+ Dashboard 扩展
- miniapp 消息页（order-pkg/messages/ 四件套）
- 演示数据脚本 `scripts/seed_wm11_demo.py`（INSERT IGNORE 幂等，四态）

## 3. 血泪教训（本会话新增，错误记忆库 20260829 已详录）

1. **E-01 单列 select 用 scalar_one_or_none() 直接取值，禁止 [0]**（TypeError）
2. **E-02 get_session() 返回实例不是工厂，不要再 call 一次**
3. **E-03 造历史/过期边界数据直改库，业务 API 有域校验时读状态码**
4. **E-04 新建模型三同步：env.py import + 迁移列 comment 一致 + alembic check 零差异**
5. **E-05 common 禁止业务域 import（含函数内），横切订阅器放 tasks/ 编排层**
6. **E-06 flush 撞唯一索引后 rollback() 连坐主事务；savepoint 也救不回** →
   共享事务幂等写用 **MySQL INSERT IGNORE**（审查 P0 实锤）
7. **审查必修 bug**：列表"未读 Tab"过滤必须下沉 SQL（total 正确/页不滤薄），
   不能页内 Python 过滤
8. **UX 基线对齐表自评盖章无效**：每项必须附文件+行号证据，外部审查复核才算数
   （本次"12 项全绿"被推翻，U2/U8/U11/U12 四项虚报实锤）

## 4. 验收状态与专家结论

- 四轮审查全部放行手动验收：P0（INSERT IGNORE）/ P1（补测+注释）/ 必修 bug
  （unread SQL 下沉）/ UX 返工（N1-N5 + Q1-Q6）
- 专家 8 步验收动线（补验上次坑）：未读 Tab 计数翻页 → 分页记忆 → 详情展开/
  查看关联 → URL 四态刷新保持 → 任务看板"从未运行" → miniapp 下拉刷新/详情/
  分类后端过滤/加载更多 → 微信四态 Tag
- **验收 OK 后收尾指令**（专家已预告）：commit/push + WM11 → ACCEPTED +
  tag 更新为含 WM11 的新基线

## 5. 待办（新模型）

1. **先确认工作区 57 项改动完好**（`git status`），决定是否等验收或先本地保留
2. 陪用户手动验收（docs/04 WM11 7 步 + 专家 8 步）
3. 验收 OK → `git add` 预期文件（**勿 stage .env/backups**）→ commit（conventional）→ push →
   LEDGER WM11→ACCEPTED → 打新 tag → docs 收尾（错误记忆库已就位，docs/17 已就位）
4. **WM12 前补课清单**（docs/09 L148）：逾期标记/活动结束/99元 边界专项；
   miniapp 借阅记录页（borrow-history 设计稿残留，借阅通知行动信息现靠 content 文案）
5. 后续主线：WM3-WM10 手动验收（UX 基线差距清单批量补齐）→ WM12 微信支付（T3 阻塞）
6. 覆盖率偿债窗口过期未执行（原 WM11 前应抬线，现需重排期走提案）

## 6. 环境与纪律

```bash
bash scripts/dev.sh start|stop|status
```
- 管理端 localhost:5173 admin/dmkwords123；后端 :8002；MySQL OrbStack dmkwords-mysql
- 跑 pytest/gate 前 `bash scripts/dev.sh stop`（共库 TRUNCATE）
- 数据处置铁律（E-08）：备份验证→处置→证据落盘→才跑测试
- 脚本改文档后必须 grep 验证落盘（E-10）
- 提交纪律：本地测试 OK + 用户明确"可以提交"才 commit/push

## 7. 关键文件索引（WM11）

| 功能 | 文件 |
|---|---|
| 通知模型/服务 | backend/common/notification_models.py / notifications.py |
| 事件订阅器 | backend/tasks/notify_handlers.py |
| 任务注册表 | backend/tasks/registry.py |
| 迁移 | alembic/versions/f7a2c9e3b5d4_wm11_notification_tables.py |
| 审核通知 helper | backend/domain/identity/wm_notify.py |
| 微信通道重建 | backend/integrations/wechat/service.py + subscribe.py |
| 通知中心页 | admin-web/src/pages/Notifications.tsx |
| 任务看板页 | admin-web/src/pages/TaskBoard.tsx |
| miniapp 消息页 | miniapp/pages/order-pkg/messages/ |
| 演示数据 | scripts/seed_wm11_demo.py |
| 交付报告 | docs/17-WM11通知任务看板交付报告-20260829.md |
| 错误记忆库 | error_list/错误记忆库-全项目-20260829.md |

---

交接完毕。新模型开工：读本文件 → CLAUDE.md → LEDGER → 确认工作区改动 → 陪用户验收 WM11。