# DmkWords 项目交接 - 2026-08-25 v5（上下文压缩/模型切换前最终版）

> 恢复口令：读本文件，继续。

## 0. 一句话状态

DmkWords 少儿英语分级阅读项目 WM1 已验收，WM2 持续精化中。**F1-F4 图书管理运营增强 + admin-web 绘本风视觉系统整体改造已完成并推送**。当前 git 头 `accc044`，dev 环境运行中。

## 1. 项目身份

- 仓库：github.com/leechunwoo0815/dmkwords (main)
- 宪法：CLAUDE.md；任务源：docs/09-补充任务清单.md
- 最新 gate：147 passed / 78.11% / exit 0（tsc 0 errors）
- dev：MySQL healthy / 后端 8002 / 前端 5173 运行中

## 2. 最近完成（C30-C36 + F1-F4）

- C30：适读年级下拉选择（3-15 岁 6 阶段）
- C31-C34：封面预览、列表封面/题数/编辑删除、搜索支持编号、ISBN 可编辑
- C35：删除书目联动清理 uploads 媒体文件
- C36：批量导入模板「年级」改「适读阶段」并加 Excel 下拉
- F1：批量导入 grade 白名单校验（防脏数据）
- F2：图书管理页面批量删除功能
- F3：新增筛选 tab：未传封面 / 未传音频 / 未录入 5 道测验
- F4：批量删除按钮按选择出现、导入说明同步为适读阶段
- 测试纪律：pytest/gate 前必须 stop dev.sh；conftest 加 lock_wait_timeout=10
- uploads 清理：删除 416 个孤儿文件，释放 34.89 MB，新增 scripts/cleanup_uploads.py
- admin-web 绘本风视觉系统整体改造：theme-paint.ts + paint.css + 角色 SVG + Sidebar 彩色图标 + 全局 Empty/Loading
- A/B/C 验证分级纪律（用户裁定）

## 3. 关键踩坑记录

1. **CI 首败**：F1-F4 推送后 CI 失败，根因不是 tsc，而是 `ruff format --check .` 发现 2 个 Python 测试文件未格式化。修复后 CI 绿。
2. **tsc 诊断插曲**：为定位 CI 失败临时改 `scripts/gate.sh` 加 tsc 诊断输出，最终已恢复原状。
3. **schema.d.ts 必须提交**：新增 `BatchDeleteRequest` 等 OpenAPI schema 后，必须重新生成 `admin-web/src/api/schema.d.ts` 并提交，否则 CI tsc 失败。
4. **绘本字体 CDN**：ZCOOL KuaiLe 通过 fontsource CDN 加载；如 CDN 不可用，管理端标题字体会回退到系统字体。

## 4. 当前任务状态

| # | 任务 | 状态 |
|---|---|---|
| C30-C36 | WM2 复测增强 + 导入模板 | ✅ DONE |
| F1 | 批量导入 grade 白名单校验 | ✅ DONE |
| F2 | 图书管理页面批量删除 | ✅ DONE |
| F3 | 新增筛选 tab | ✅ DONE |
| F4 | 其他人性化交互优化 | ✅ DONE |
| 绘本风 | admin-web 全局视觉系统改造 | ✅ DONE |

**无在途任务。**

## 5. 环境与纪律

```bash
bash scripts/dev.sh start|stop|restart|status
```

- 管理端：http://localhost:5173，admin/dmkwords123、staff01/dmkwords123
- 跑 pytest/gate 前必须 `bash scripts/dev.sh stop`
- A/B/C 验证分级：
  - A 纯前端：tsc
  - B backend/tests：相关域 pytest + ruff
  - C models/迁移/公共层：全量 gate + CI

## 6. 关键代码位置

| 功能 | 文件 |
|---|---|
| 批量导入校验 | backend/domain/catalog/import_service.py |
| 批量删除 | backend/domain/catalog/service.py batch_delete_books / router.py POST /books/batch-delete |
| 列表筛选 | backend/domain/catalog/service.py list_books / schemas.py BookListQuery |
| 适读阶段常量 | backend/domain/catalog/constants.py + admin-web/src/constants/grade.ts |
| 书目删除 | backend/domain/catalog/service.py BookService.delete_book |
| 图书管理页 | admin-web/src/pages/BookManage.tsx |
| API 封装 | admin-web/src/api/catalog.ts |
| OpenAPI 类型 | admin-web/src/api/schema.d.ts（需后端运行后 `pnpm gen:api`） |
| uploads 清理 | scripts/cleanup_uploads.py |
| 绘本主题 | admin-web/src/theme-paint.ts |
| 绘本全局 CSS | admin-web/src/styles/paint.css |
| 角色空状态 | admin-web/src/components/PaintEmpty.tsx |
| 角色加载 | admin-web/src/components/PaintLoading.tsx |

## 7. 下一步（接任者）

1. 本次绘本风改造仅 admin-web 全局样式 + 角色空状态，未改 miniapp；小程序端需单独实施
2. 陪用户继续 WM2 手动验收；新缺陷续 C37+ / D 编号
3. WM2 ACCEPTED 后依次验收 WM3-WM10
4. 遗留：P2x8、WM11/12、P3 CI Node 24 升级

## 8. 已知脏数据（待用户决定是否清理）

数据库 `books.grade` 仍有历史脏数据（如 `9-10岁（小学中年级）` 到 `9-26岁（小学中年级）`）。F1 已防止新增，旧数据如需清理需用户确认。

---

交接完毕。
