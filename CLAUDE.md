# DmkWords 少儿英语分级阅读系统 — 项目宪法（CLAUDE.md）

> **本文件 = 新项目最高法律，每次 AI 会话开工第一件必读。**
> 版本：v1.3（2026-08-28，新增功能闭环必更文档纪律）
> 来源：业务需求文档定稿 V1.1 + 需求决策链 V1.0.1（R-300 系规则）+ 旧项目血泪教训（错误记忆库 8 条红线 + 模式手册）+ 旧宪法 v0.3。
> 边界声明：本宪法只写"**什么不能做**"与"**必须怎么做**"；业务数值以业务需求文档 V1.1 为准，宪法不锁死数值，但**锁死"数值必须配置化"**。

---

## 〇、AI 开发总纲（8 条铁律，出自旧项目血泪教训）

> 旧项目 4 万行代码、多轮全量审查的结论：**所有事故与漂移，都发生在"有规则、无门禁"的地方。**

1. **约定优于理解**：AI 每轮会话都是"新员工"，靠模板不靠记忆。每层/每域/每文件有固定骨架，照抄不发挥。
2. **规则必须可机械判定**：任何一条架构规则必须配一个门禁脚本；做不到机械判定的规则要么删、要么改写。禁止"靠自觉遵守"。
3. **事实由门禁生成，文档只写"为什么"**：文档不手抄数字（API 数/测试数/表数），写"见门禁输出"。
4. **测试即规格**：需求的 Given/When/Then 直接就是 behave 场景；先写场景（RED）再实现（GREEN）。测试必须走真实链路（HTTP/真实 service + 真实 MySQL），禁 ORM 伪造、禁 mock 被测对象、禁恒真断言、禁空断言。
5. **高风险面固定模板，不给自由发挥**：资金面（状态机矩阵 + 行锁 + 幂等 + Decimal）、权限面（声明式校验）、删除面（清单 + 对账）——三面永远用固定模板（参考 `docs/billing-circulation模式手册.md`，从旧项目 design-reuse 迁入）。
6. **数值全配置化**：所有业务数值进 SystemConfig + 变更审计。AI 无魔数可写；改规则不发版。
7. **单一环境**：MySQL-only。开发机（OrbStack）、CI、生产同构；SQLite 分支一律禁止出现。
8. **会话记忆外置**：开工读三件（本宪法 → 错误记忆库最新版 §三 → `docs/LEDGER.md` 台账第一个未闭环项）；完工落证据（门禁输出文件 → 台账流转 → 新坑入记忆库）。

---

## 一、核心铁律（零容忍）

1. **严禁幻觉**：只能引用当前上下文与已读文件中真实存在的代码/字段/接口；找不到依据回答"无相关记录"，禁止编造。
2. **改代码前先 Read 相关文件**，禁止凭空默写。
3. **任何"已修复/已完成"声明必须附终端 Exit Code 证据**；跑不了明说"未验证"，禁止说"应该没问题"。
4. **最小改动**：一个任务只动相关文件；发现无关问题报告但不顺手修；不做未要求的重构、改名、格式化。
5. **不确定就问**：需求模糊或有两种以上合理实现时，先问，禁止猜、禁止替甲方做决定。**需求未覆盖的边界不得擅自推导，必须标记"待确认"上报。**（V1.0.1 R-300-10）
6. 不执行 `git push --force`、`git reset --hard`、`rm -rf`，除非逐字明确要求；破坏性操作前说明影响并等确认。
7. **验证纪律**：改完必须跑测试/lint 并报告真实结果；修 bug 先复现或定位根因，**禁止靠猜连续试三处以上**；测试失败报告并停止，禁删测绕过、禁弱化断言。
8. **变更纪律**：新依赖先查项目是否已有同类工具，不重复引入；新建文件只放在任务涉及的目录。
9. **沟通纪律**：中文回复，代码/标识符/提交信息英文；直接给结论；方案附取舍。
10. **环境纪律**：macOS/zsh；Node 用 pnpm、Python 用 uv + venv；Docker 开发用 OrbStack；工具装进项目内。
11. **Git 纪律**：trunk-based（main + 特性短分支）；conventional commits；提交前 `git diff` 只 stage 预期文件。
12. **敏感信息**：不读取、不复制、不打印 `.env`、私钥、令牌文件的内容。

---

## 二、业务红线（触碰即事故，均出自需求 V1.0.1 甲方终裁）

| 红线领域 | 绝对禁令 |
| :--- | :--- |
| **资金安全** | 金额一律 `Decimal`/`Numeric(10,2)` 严禁 float；退款/押金/转让任何状态转移必须走状态机（禁止绕过直接改状态）+ `with_for_update` 行锁 + 前置状态校验；支付回调验签 + 金额校验 + 幂等；**退款金额按实付金额 × 剩余天数比例**，服务端计算，前端不得自算 |
| **状态机三分离** | 借阅状态、阅读进度、Quiz 状态**三套独立**，禁止混为一个状态；**还书不是 Quiz 解锁条件**（解锁=音频听完 95%）；"待检验"状态已废除，禁止复活 |
| **音频完播防刷** | 读完判定=累计有效播放覆盖 ≥95%（区间并集，seek 跳过不计）；服务端校验覆盖增速（≤时间差×2.0×1.2 容差）；阅读时长按音频原始时长，每本书只记一次 |
| **Quiz 公平性** | 每本书终身 3 次提交（重借不重置）；未提交退出不占次数；取历史最高分；**任何人不得人工标记通过**（仅超管可重置次数且留痕）；每次提交保存题目快照 |
| **有效词数** | 只有 Quiz ≥80% 通过才计入；同一学生×同一书目终身只计一次（唯一约束）；已入账**永不回收**（丢书/下架/过期/退会都不回收）；入账同事务驱动积分折算+等级进度 |
| **借阅并发** | 借书锁主体行（Child）串行化 + 副本行锁 + SQL 原子库存更新；可借上限 = 30 − 逾期未还数（最低 0）；预约占额度；同书同副本并发唯一 |
| **人工放行边界** | 全局原则"提示不硬拦截+放行留痕"**只适用线下馆员操作**；三类硬拦截例外：资金安全类、数据完整性类、**线上自助操作**（预约/续借/Quiz 硬校验，无人可放行）；放行必须记录操作人/时间/原因/异常快照 |
| **数据删除** | 业务主数据禁止物理删除（R-213 清单）；一律软删除或状态作废；软删查询默认带 `is_deleted=0`；孩子档案删除本期不开放（合规请求走超管软删+线下清理兜底） |
| **学生隐私** | 排行榜展示英文名/昵称+头像，禁止真实姓名全名、手机号；总榜退会孩子标"历史学员"、无详情跳转 |
| **环境** | 生产 `DEBUG=false`、真实 SECRET_KEY、真实支付配置——加载时硬校验，违规启动失败 |
| **本期范围纪律** | 只做业务需求文档 V1.1 界定范围；**第十三章 23 项"本期不做"禁止顺手实现**（含分享社区/班级管理/盘点/离线借还/字幕/短信等）；后置功能只允许状态字段级预留，禁止预建表结构 |

---

## 三、分层架构宪法

```text
Router (参数校验/HTTP状态码/依赖注入，🚫不含 try/except、不抛 HTTPException、不直接操作 ORM)
  └── Service (业务逻辑/事务管理，🚫不直接操作 HTTP、🚫网关调用禁抱在DB事务里[三段式])
        └── Repository (数据访问，继承 BaseRepository，🚫只 flush 不 commit)
              └── Model (ORM 映射，继承 BaseModel，🚫不含业务方法)
  └── EventBus (跨域解耦，同步共享事务单模式，🚫禁独立 session 分支、🚫处理器内禁自行 commit)
  └── ConfigService (统一配置读取 + 变更审计，🚫禁止硬编码业务数值)
```

**分层硬规则**（架构门禁机械执法）：
- Router 出现 `db.query/add/commit`、`try/except`、`raise HTTPException` = 违规；
- Service 事务模板统一；跨域副作用走事件；资金类网关调用走三段式（PENDING→commit→调网关→终态）；
- Repository 自动过滤 `is_deleted=0`；单文件 > 800 行 = 门禁报错；事件发布/订阅双向对账入门禁。

---

## 四、域边界（8 域，2026-08-22 定）

```text
backend/domain/identity/     家长账号、孩子档案、会员生命周期(6态)、同意记录、权益转让    ← F0,F3,F10
backend/domain/catalog/      书目、副本(5态+reserved)、音频、题目、收藏夹               ← F1
backend/domain/circulation/  借、还、续借、预约(72h锁定)、逾期扣减、人工放行留痕         ← F4
backend/domain/billing/      订单(5类7态)、支付(线上+人工确认)、退款、押金(状态机+补缴)、赔偿 ← F2,F3
backend/domain/reading/      播放进度与防刷、打卡、Quiz、有效词数、生词本               ← F5,F6,F8
backend/domain/growth/       A-Z等级、积分、五榜单、里程碑、护照、周报月报图片           ← F7,F8
backend/domain/activity/     活动发布、报名、名额、签到、活动退款                       ← F9
backend/domain/admin/        RBAC、SystemConfig、数据看板、操作日志、通知中心           ← F0,F11
```

**依赖方向硬规则**：`common` 不依赖任何域；业务域禁止反向依赖 `admin`；跨域**写**副作用一律走 EventBus，跨域**读**只允许白名单单向边。

---

## 五、前端宪法

### 微信小程序（家长/孩子共用界面，家长微信登录）
1. **防白屏**：所有 `{{}}` 绑定配 `wx:if` 守卫或默认值；状态分支与 loading 互斥正确；
2. **网络**：一律走 `utils/request.js` 封装（fail 回调+超时+401 统一跳登录并清全部本地身份缓存）；禁止页面内裸 `wx.request`；
3. **样式**：设计令牌 v2「暖纸书房」（design-reuse 迁入）；禁 `oklch()`/`backdrop-filter`；触控区 ≥88rpx（MP-018）；
4. **数值与文案**：价格、规则文案一律后端配置下发，禁止硬编码金额（含兜底文案）；
5. **音频播放**：全局音频管理器 + 页面内查词不跳转不中断播放；进度上报含 session_id 供服务端防刷。

### 管理后台 + 馆员操作台（React 18 + TypeScript + Vite + Ant Design）
1. **XSS**：禁止 `dangerouslySetInnerHTML`（确需富文本走白名单 sanitize）；
2. **RBAC 三方一致**：前端路由守卫 ↔ 后端 `require_perm` ↔ 菜单按钮显隐三处权限码一致（对账脚本入门禁）；权限码从 `/me/permissions` 下发，禁止前端硬编码；
3. **类型门禁**：`tsc --strict` 0 错误；API 类型由 `openapi-typescript` 从 OpenAPI 生成，**禁止手写**；
4. **操作台**：扫码优先、少点击、大按钮；v1 禁离线写操作；
5. **审计**：资金/状态变更必写操作日志（含前后值）；放行/豁免/重置必须留痕。

---

## 六、开发流程铁律（AI 每轮工作流）

**开工（每轮必做）**：读本宪法 → 读错误记忆库最新版 §三 → 读 `docs/LEDGER.md` 取第一个未闭环项 → 修改前 `rg` 全库枚举同类调用点。

**施工（TDD/BDD 强制）**：
1. 先写 behave 场景（Given/When/Then 从需求直译，**每个 R-300 系规则必须有场景覆盖**）→ 确认 RED；
2. 按域四件套模板实现 → GREEN；数值进配置；跨域副作用发事件；权限走声明式；
3. 防复发红线全量适用：边界值 [-1,0,+1] 用例；新字段回答契约三维；改基线数字全库 `rg` 同口径核对；改状态机先画转移矩阵对照需求。

**完工（每项，DoD）**：
1. 门禁命令**全量复制不缩范围**（第八节），贴原始输出，退出码 0；
2. **用户本地验收前禁止 commit/push**：代码改完、本地验证通过后，先交用户测试；用户明确说"OK"/"可以提交了"，再执行 `git add → commit → push`；
3. **功能闭环必更文档**：任意功能/修复开发+测试+推送成功后，**立即全量更新其涉及的所有 md 文档**（LEDGER / 交接卡 / 验收报告 / 视觉规范 / 手动验收手册 / 错误记忆库等），保证文档与代码同步，杜绝滞后错乱；
4. 证据落盘 `gate-runs/` → 台账流转 → 提交；
5. 新坑写入错误记忆库（根因+防复发动作）；
6. **门禁未过禁止标记完成；禁止弱化断言/删测/缩范围绕过。**

---

## 七、知识库索引与裁决顺序

| 文件 | 用途 |
| :--- | :--- |
| `PRD/DmkWords业务需求文档-定稿V1.1` | **业务需求唯一事实源**（甲方签字版） |
| `PRD/需求决策链 V0.1→V1.0.1` | 决策过程与规则全文（R-100~R-323），冲突以 V1.0.1 为准 |
| `docs/billing-circulation模式手册.md` | 资金/借阅 18 个模式 + 反模式清单（旧项目知识提炼） |
| `docs/02-架构蓝图.md` + `docs/ADR/` | 架构与关键决策记录 |
| `docs/07-按图施工手册.md` | 后续开发大模型施工标准（Demo 验证后产出） |
| `error_list/错误记忆库-*.md` | 错误记忆库（开工必读最新版 §三） |
| `docs/LEDGER.md` | 任务台账（唯一进度事实源，流转需证据） |

**冲突裁决顺序**：本宪法 > 业务需求文档 V1.1 > 需求决策链 V1.0.1 > 架构蓝图/ADR > 全局 AGENTS.md。
**宪法修改**：走"提案 + 甲方/架构师确认"，禁止 AI 自行改宪法。

---

## 八、验证与门禁（分级纪律，立即生效）

> **改动验证成本必须与风险成正比，禁止"一行样式跑全量"。** 本节约束第六节「完工门禁命令全量复制」的绝对化表述，以本节分级为准。

### 门禁分级

| 级别 | 改动范围 | 本地提交前必须验证 | 是否可信任 CI 跑全量 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| **A 级** | 仅 `admin-web/**` 纯前端文件 | `cd admin-web && pnpm exec tsc --noEmit` | ✅ 可以 | push 后 CI 会跑全量 gate；本地 commit 前只需 tsc 绿 |
| **B 级** | 仅 `backend/**` 或 `tests/**` 单域 | 相关域 pytest 单文件 + `ruff check backend/ tests/` | ❌ 推荐再跑 `bash scripts/gate.sh full` 但非强制 | 例：`pytest tests/unit/test_wm2_catalog.py -q`（约 20-60s） |
| **C 级** | `models/`、迁移、公共层、跨域调用 | `bash scripts/gate.sh full` | ❌ 必须本地绿 | 高传播面，本地全量 + CI 绿 |

### 不变纪律

- **A/B/C 级如需跑 pytest，必须先 `bash scripts/dev.sh stop`**，因为 pytest 与 dev.sh 共库。
- **禁止以"反正 CI 会跑"为由本地完全不验证**：A 级至少 tsc；B 级至少相关域单文件 + ruff；C 级必须本地全量 gate。
- **完成 = 退出码 0 + 输出贴原文**（第六节 DoD 仍然成立，但"门禁命令"按本节分级选取）。

### 全量门禁命令（C 级或 CI 使用）

```bash
# 1 lint
ruff check backend/ tests/ features/ scripts/
ruff format --check .

# 2 单测与验收（真实 MySQL；开发机 OrbStack = CI = 生产同构）
python -m pytest tests/ -x -q --tb=short
python -m behave features/ --no-capture -q

# 3 架构关（每条规则对应一个检查）
python -m scripts.verify_architecture
#    Router 违规==0；函数内 import<阈值；单文件<=800行；域四件套完整；
#    import 白名单；事件发布/订阅双向对账

# 4 契约与反假绿
python -m scripts.verify_api_contract
python -m scripts.check_model_consistency
python -m scripts.check_fake_assertions     # 扫描 tests+scripts+features

# 5 数据库
alembic upgrade head && alembic check

# 6 覆盖率（整体≥85%，billing/circulation≥90%）
python -m pytest tests/ --cov=backend --cov-fail-under=85

# 7 全量门禁（退出码 0 才算数）
bash scripts/gate.sh full
```

---

## 九、技术栈（已定，禁重新讨论）

| 层 | 选型 |
|---|---|
| 后端 | Python 3.13 + FastAPI + SQLAlchemy 2.0 + Pydantic V2 |
| 数据库 | **MySQL 8.0 (utf8mb4) only**；alembic 管全部 schema（禁 create_all）；开发 OrbStack / CI service / 生产 Compose 同构 |
| 缓存/锁 | **第一版不引入 Redis**（ADR-005：单机进程内锁 + JWT 无状态；预留演进） |
| 定时任务 | 进程内 APScheduler（ADR-008；任务逻辑放域 service，调度器只做注册表） |
| 前端 | 小程序原生（家长微信登录）+ React 18 + TS + Vite + AntD（theme.ts 暖纸书房） |
| 文件存储 | 本地磁盘 `uploads/`（ADR-004；音频上百 GB，预留存储抽象层） |
| 支付 | 微信支付 V3（安卓线上）+ 企微人工确认（苹果）+ 线下收款登记（复用 design-reuse 集成资产） |
| 测试 | pytest + behave（中文 Gherkin）；覆盖率门禁见第八节 |
| 部署 | 单机 Docker Compose（ADR-001） |
